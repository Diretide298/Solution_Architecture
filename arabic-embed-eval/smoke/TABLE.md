# nDCG@10

| Model | MLQA ar | Gulf dialectal |
|---|---|---|
| bge-m3 | — | **not measurable** |

All tasks run in FULL — nothing sampled.

- `bge-m3` / `MLQA ar` failed: `TypeError: get_tasks() got an unexpected keyword argument 'hf_subsets'`
