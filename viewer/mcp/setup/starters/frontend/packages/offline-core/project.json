{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "offline-core",
  "sourceRoot": "packages/offline-core/src",
  "projectType": "library",
  "tags": [
    "type:package",
    "scope:offline-core"
  ],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint {projectRoot}/src --ext .ts,.tsx"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root {projectRoot} --passWithNoTests"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/offline-core/tsconfig.json --noEmit"
      }
    }
  }
}
