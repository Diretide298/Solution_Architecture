node_modules/
__pycache__/
*.pyc
.DS_Store
viewer/node_modules/
viewer/public/build/
backend/target/
backend/node_modules/
arabic-embed-eval/.venv/
arabic-embed-eval/results/raw/

# .NET build output under repos/ — none of it source
repos/**/bin/
repos/**/obj/

# harness screenshots, regenerated by anything in viewer/checks/
shot-*.png
viewer/shot-*.png

# The viewer's account store: real accounts, argon2 hashes and live session
# tokens, plus SQLite's -wal and -shm siblings. This repository is public.
# Also in .git/info/exclude, because a package drop overwrites this file.
viewer/api/*.db
viewer/api/*.db-shm
viewer/api/*.db-wal
