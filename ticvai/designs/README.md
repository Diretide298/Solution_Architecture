# designs/ — exported UI/UX boards

**Empty because the drawn boards live in `wireframes/` beside the generated ones**, distinguished by
`wireframe.status` rather than by path — `check-wireframes.py` verifies the anchor join in both
directions and can only do that if every board is in one place.

84 screens are `designed`, 408 `generated`. Client originals are in `sources/`.

**Three packs are outstanding** — Inventory boards 6-7, Marketing & CRM, Seat Management. See
`docs/active/outstanding-design-packs.md`.
