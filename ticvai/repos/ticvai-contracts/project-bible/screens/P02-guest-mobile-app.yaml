# P02 — Guest Mobile App
# Generated from handoff/page-inventory.md, then corrected against the client
# hierarchy v2.1 and the contracts. Offline-capable: every screen declares an
# offline state.

platform:
  code: P02
  audience: guest
  formFactor: mobileApp
  shortName: Guest App
  name: Guest App — Mobile
  surface: guestFacing
  runtime: reactNative
  offlineCapable: true
  themes:
  - light
  - dark
  directions:
  - ltr
  - rtl
  screenCount: 63
  app: guest-app
  appStatus: scaffolded
  packages:
  - design-tokens
  - ui
  - api-client
  - offline-core
  deployment:
    target: iOS / Android native
    distribution: App Store + Play, tenant developer account
    bundleUpdate: over-the-air for JS; store release for native
    hosting: n/a — installed
    storeReview: true
    deviceOwnership: guest device
    networkAssumption: mobile data, intermittent
    releaseCadence: fortnightly, gated by review
  designReferences:
  - path: sources/designs/TICVAI_White_Label_Guest_App_UI_Reference_1.pdf
    note: White Label Builder — the guest app is what the builder configures.
  operator: guest
  wireframeBoard: wireframes/P02 Guest App.dc.html
  wireframeBoardNote: '**The generated board for the whole platform.** Individual screens keep the client-pack
    anchor where one exists — a screen the client drew should open the client''s frame — and this renders
    every screen on the platform including the ones no pack covers.


    **Declared 24 August.** The file was written from the first generation and nothing pointed at it:
    `check-wireframes` has no unmatched-board concept, so the Python suite passed while the viewer flagged
    an orphan. **A board nobody references is a board nobody reviews.**'
screens:
- id: GST-001
  name: Home – Default
  module: TODO
  purpose: The screen this app sits on. Everything else is entered from here and returns to it.
  wave: 1
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
      - kind: cardList
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-001
  capability: C02
  openQuestions:
  - Inventory cites `GET /catalogue, local` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/home-default
    component: apps/guest-app/src/routes/general/HomeDefaultDashboard.tsx
    status: notStarted
  navigation:
    isEntryPoint: true
    inferred: true
    exitTo:
    - GST-002
    - GST-003
    - GST-004
  apis:
  - operationId: getTenantAppStatus
    contract: white-label
    purpose: App status and recent changes
    trigger: onLoad
  - operationId: getGuestProfile
    contract: marketing-crm
    purpose: Read a guest profile
    trigger: onLoad
  - operationId: listMyEntitlements
    contract: access
    purpose: Every ticket, pass and membership this guest holds
    trigger: onLoad
  - operationId: searchCatalogue
    contract: catalogue
    purpose: Find something by name
    trigger: onLoad
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. Landing. **Declared
    only a health check.** Pranay asked for guest details and tickets; Chitrangi asked for search. All
    three existed and none were wired. **Rewired on the 20 August review.**
  density: comfortable
  entryState:
    params:
    - name: subjectId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: marketing
- id: GST-002
  name: Explore Categories
  module: TODO
  purpose: Find explore categories for this venue.
  wave: 1
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
      - kind: cardList
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-002
  capability: C02
  openQuestions:
  - Inventory cites `GET /catalogue` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/explore-categories
    component: apps/guest-app/src/routes/general/ExploreCategoriesList.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-003
    - GST-004
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  density: comfortable
  requiresModule: ticketing
- id: GST-003
  name: Attractions List
  module: TODO
  purpose: Find the right one quickly, and act on it without opening it.
  wave: 1
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
      - kind: cardList
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-003
  capability: C02
  implementation:
    app: guest-app
    route: /general/attractions-list
    component: apps/guest-app/src/routes/general/AttractionsList.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-004
    fromFlows: true
  notes: 'States derived from the screen pattern on 17 August, not individually considered. Purpose
    derived from the screen name and its operations on 17 August, not from a requirement. **Cross-platform
    navigation removed 24 August**: BO-005. **A till does not navigate to a back office and a guest
    app does not navigate to either** — those are device handovers, and a flow declares them with `crossesDevice`
    rather than a screen pretending there is a link.'
  apis:
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  density: comfortable
  requiresModule: ticketing
- id: GST-004
  name: Attraction Details
  module: TODO
  purpose: See attraction details for this venue.
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-004
  capability: C02
  implementation:
    app: guest-app
    route: /general/attraction-details
    component: apps/guest-app/src/routes/general/AttractionDetailsDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: getProduct
    contract: catalogue
    purpose: Read a product
    trigger: onLoad
  - operationId: getAvailability
    contract: catalogue
    purpose: Live remaining capacity
    trigger: onLoad
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  density: comfortable
  entryState:
    params:
    - name: productId
      from: deepLink
    coldEntry: '**A shared product link after the product retired.** Shows what replaced it where a
      successor exists, and the catalogue where none does.'
  requiresModule: ticketing
- id: GST-005
  name: What's On
  module: TODO
  purpose: Find what's on for this venue.
  wave: 1
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
      - kind: cardList
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-005
  capability: C84
  implementation:
    app: guest-app
    route: /general/whats-on
    component: apps/guest-app/src/routes/general/WhatsOnList.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: listPerformances
    contract: catalogue
    purpose: List performances of an event
    trigger: onLoad
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  density: comfortable
  entryState:
    params:
    - name: eventId
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: ticketing
- id: GST-006
  name: Event / Exhibition Details
  module: TODO
  purpose: See event / exhibition details for this venue.
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-006
  capability: C84
  implementation:
    app: guest-app
    route: /general/event-exhibition-details
    component: apps/guest-app/src/routes/general/EventExhibitionDetailsDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: getPerformance
    contract: catalogue
    purpose: Read a performance
    trigger: onLoad
  - operationId: getProduct
    contract: catalogue
    purpose: Read a product
    trigger: onLoad
  density: comfortable
  entryState:
    params:
    - name: performanceId
      from: deepLink
    - name: productId
      from: deepLink
    coldEntry: '**A link to a performance that has happened.** Offers the next performance of the same
      event. **A shared product link after the product retired.** Shows what replaced it where a successor
      exists, and the catalogue where none does.'
  requiresModule: ticketing
- id: GST-007
  name: Select Date & Time
  module: TODO
  purpose: See select date & time for this venue.
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-007
  capability: C02
  openQuestions:
  - Inventory cites `GET /performances, POST /leases` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/select-date-and-time
    component: apps/guest-app/src/routes/general/SelectDateAndTimeDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: listPerformances
    contract: catalogue
    purpose: List performances of an event
    trigger: onLoad
  - operationId: getAvailability
    contract: catalogue
    purpose: Live remaining capacity
    trigger: onLoad
  density: comfortable
  entryState:
    params:
    - name: eventId
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: ticketing
- id: GST-008
  name: Tickets & Add-ons
  module: TODO
  purpose: Find tickets & add-ons for this venue.
  wave: 1
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
      - kind: cardList
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-008
  capability: C02
  openQuestions:
  - Inventory cites `local` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/tickets-and-add-ons
    component: apps/guest-app/src/routes/general/TicketsAndAddOnsList.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  apis:
  - operationId: transferOrderTickets
    contract: orders
    purpose: Transfer tickets to another guest
    trigger: onAction
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: ticketing
- id: GST-009
  name: Review & Payment
  module: TODO
  purpose: Check what you are buying and pay for it.
  wave: 1
  layout:
    template: form
    regions:
    - name: contentBody
      components:
      - kind: textField
      - kind: primaryButton
  states:
    loading: Order summary, then the payment methods this venue accepts
    emptyFirstRun: —
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: '**Could not reach the gateway. The order is held, not lost** — and a payment whose outcome
      is unknown goes to `inquirePaymentStatus` rather than being retried, because retrying an unknown
      charge is how a guest is charged twice'
    offline: '**Not available.** A payment needs the gateway, and pretending otherwise takes money we
      cannot confirm'
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-009
  capability: C47
  apis:
  - operationId: transferOrderTickets
    contract: orders
    purpose: Transfer tickets to another guest
    trigger: onAction
  - operationId: createPayment
    contract: orders
    purpose: Take a payment against an order
    trigger: onAction
  - operationId: inquirePaymentStatus
    contract: orders
    purpose: Ask the provider what actually happened
    trigger: onAction
  - operationId: checkoutCart
    contract: orders
    purpose: Turn the cart into an order
    trigger: onAction
  - operationId: getCart
    contract: orders
    purpose: The cart, priced and checked, right now
    trigger: onLoad
  implementation:
    app: guest-app
    route: /general/review-and-payment
    component: apps/guest-app/src/routes/general/ReviewAndPaymentForm.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    fromFlows: true
  notes: 'States derived from the screen pattern on 17 August, not individually considered. Purpose
    derived from the screen name and its operations on 17 August, not from a requirement. **`createPayment`
    added 18 August** — the guest app''s payment screen did not declare the operation that takes a payment,
    and F11 routed food payment through the AI concierge instead. **Card only, so tender currency equals
    base currency**; a foreign card is converted by the guest''s own issuer at their rate, which is
    not ours and is not recorded as ours. **Navigation repointed to P15 on 20 August** — the F&B screens
    it linked to moved out of the back office when the client board was adopted. **Cross-platform navigation
    removed 24 August**: BO-020. **A till does not navigate to a back office and a guest app does not
    navigate to either** — those are device handovers, and a flow declares them with `crossesDevice`
    rather than a screen pretending there is a link.'
  density: comfortable
  entryState:
    params:
    - name: cartId
      from: session
    - name: orderId
      from: deepLink
    - name: paymentId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: ticketing
- id: GST-010
  name: Booking Confirmation
  module: TODO
  purpose: Confirm it worked, and give them what they need to prove it.
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-010
  capability: C02
  apis:
  - operationId: transferOrderTickets
    contract: orders
    purpose: Transfer tickets to another guest
    trigger: onAction
  - operationId: getOrder
    contract: orders
    purpose: Read an order
    trigger: onLoad
  implementation:
    app: guest-app
    route: /general/booking-confirmation
    component: apps/guest-app/src/routes/general/BookingConfirmationDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: 'States derived from the screen pattern on 17 August, not individually considered. Purpose
    derived from the screen name and its operations on 17 August, not from a requirement. **Wired 24
    August from review**: getOrder. **The operations existed and this screen could not call them** —
    reviewers reported them as missing APIs, which is what an unreachable operation looks like from
    a wireframe.'
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: ticketing
- id: GST-011
  name: Wallet Overview
  module: TODO
  purpose: The screen this app sits on. Everything else is entered from here and returns to it.
  wave: 2
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
      - kind: cardList
  states:
    loading: Terminal or gateway state, shown plainly
    emptyFirstRun: —
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: '**Declined reads differently from unresolved.** An unresolved payment inquires rather than
      retries, and nothing is issued until it resolves'
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-011
  capability: C37
  openQuestions:
  - Inventory cites `GET /wallet` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/wallet-overview
    component: apps/guest-app/src/routes/general/WalletOverviewDashboard.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: getWallet
    contract: retail
    purpose: Read a guest wallet
    trigger: onLoad
  - operationId: listWalletTransactions
    contract: retail
    purpose: Wallet transaction history
    trigger: onLoad
  density: comfortable
  entryState:
    params:
    - name: subjectId
      from: GST-001
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: retail
- id: GST-012
  name: My Tickets
  module: TODO
  purpose: Find my tickets for this venue.
  wave: 1
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
      - kind: cardList
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-012
  capability: C02
  openQuestions:
  - Inventory cites `GET /tickets` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/my-tickets
    component: apps/guest-app/src/routes/general/MyTicketsList.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  apis:
  - operationId: listMyEntitlements
    contract: access
    purpose: Every ticket, pass and membership this guest holds
    trigger: onLoad
  - operationId: getEntitlement
    contract: access
    purpose: One entitlement, with what remains on it
    trigger: onLoad
  - operationId: transferOrderTickets
    contract: orders
    purpose: Transfer tickets to another guest
    trigger: onAction
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. My Tickets. **Declared
    only `transferOrderTickets` until 20 August** — a guest could give a ticket away and could not read
    one. Raised by Pranay. **Rewired on the 20 August review.**
  density: comfortable
  entryState:
    params:
    - name: entitlementId
      from: deepLink
    - name: orderId
      from: deepLink
    coldEntry: '**A ticket link opened after the event.** Shows the entitlement with its status — expired,
      used, transferred — because *not found* to somebody holding a ticket is the wrong answer. **A
      guest opening an order link weeks later.** Shows the order if it still resolves; if it was refunded
      or the performance passed, says which and offers the order list rather than an error.'
  requiresModule: access
- id: GST-013
  name: Ticket Details
  module: TODO
  purpose: Find ticket details for this venue.
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-013
  capability: C02
  openQuestions:
  - Inventory cites `GET /tickets/{id}` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/ticket-details
    component: apps/guest-app/src/routes/general/TicketDetailsDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  apis:
  - operationId: getEntitlement
    contract: access
    purpose: One entitlement, with what remains on it
    trigger: onLoad
  - operationId: getEntitlementCredential
    contract: access
    purpose: The thing that gets scanned
    trigger: onLoad
  - operationId: getEntitlementHistory
    contract: access
    purpose: Every scan, freeze, share and reissue against it
    trigger: onLoad
  - operationId: transferOrderTickets
    contract: orders
    purpose: Transfer tickets to another guest
    trigger: onAction
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. Ticket Details. **The
    credential is a separate call** — a list of tickets is a convenience and the credential admits somebody.
    **Rewired on the 20 August review.**
  density: comfortable
  entryState:
    params:
    - name: entitlementId
      from: deepLink
    - name: orderId
      from: deepLink
    coldEntry: '**A ticket link opened after the event.** Shows the entitlement with its status — expired,
      used, transferred — because *not found* to somebody holding a ticket is the wrong answer. **A
      guest opening an order link weeks later.** Shows the order if it still resolves; if it was refunded
      or the performance passed, says which and offers the order list rather than an error.'
  requiresModule: access
- id: GST-014
  name: Ticket Transfer
  module: TODO
  purpose: Work with ticket transfer for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-014
  capability: C02
  openQuestions:
  - Inventory cites `POST /tickets/{id}/transfer` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/ticket-transfer
    component: apps/guest-app/src/routes/general/TicketTransferDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: transferOrderTickets
    contract: orders
    purpose: Transfer tickets to another guest
    trigger: onAction
  - operationId: claimTicketTransfer
    contract: orders
    purpose: Claim transferred tickets
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    - name: transferId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: ticketing
- id: GST-015
  name: Memberships
  module: TODO
  purpose: Find memberships for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-015
  capability: C03
  openQuestions:
  - Inventory cites `GET /memberships` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/memberships
    component: apps/guest-app/src/routes/general/MembershipsDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    fromFlows: true
  apis:
  - operationId: getMyMemberships
    contract: catalogue
    purpose: A guest's own memberships, benefits and history
    trigger: onLoad
  - operationId: listDelegations
    contract: identity
    purpose: Who may act for this guest, and for whom they may act
    trigger: onLoad
  - operationId: grantDelegation
    contract: identity
    purpose: Let one guest act for another
    trigger: onAction
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: getGuestLink
    contract: cross-cell
    purpose: Read a guest link
    trigger: onLoad
  notes: 'States derived from the screen pattern on 17 August, not individually considered. Purpose
    derived from the screen name and its operations on 17 August, not from a requirement. Membership.
    **`listDelegations` and `grantDelegation` are the member list and add-member** Pranay asked for
    — CF-132 built them as delegated authority rather than a household table. **Rewired on the 20 August
    review.** **`getGuestLink` restored** — F19 step 1 calls it from here and my rewire dropped it.
    **A membership that works in another country resolves through the guest link**, and the flow checker
    caught what the screen edit did not. **Cross-platform navigation removed 24 August**: ADM-003. **A
    till does not navigate to a back office and a guest app does not navigate to either** — those are
    device handovers, and a flow declares them with `crossesDevice` rather than a screen pretending
    there is a link. **Wired 24 August from review**: getSubscription, listSubscriptionInvoices. **The
    operations existed and this screen could not call them** — reviewers reported them as missing APIs,
    which is what an unreachable operation looks like from a wireframe. **`getSubscription` and `listSubscriptionInvoices`
    removed the same day they were wired.** `check-screens` refused them: **both carry a staff permission
    and this is a guest surface.** The reviewer asked for `GET /memberships/current` — a guest-scoped
    read that does not exist. **That is the correct finding and the wiring was the wrong fix**: it is
    a new operation, not a missing link.'
  density: comfortable
  entryState:
    params:
    - name: guestLinkId
      from: deepLink
    - name: subjectId
      from: GST-001
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: ticketing
- id: GST-016
  name: My Reservations
  module: TODO
  purpose: Find my reservations for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-016
  capability: C02
  implementation:
    app: guest-app
    route: /general/my-reservations
    component: apps/guest-app/src/routes/general/MyReservationsDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: listReservations
    contract: orders
    purpose: List reservations
    trigger: onLoad
  - operationId: getReservation
    contract: orders
    purpose: Read a reservation
    trigger: onLoad
  - operationId: cancelReservation
    contract: orders
    purpose: Cancel a reservation
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: reservationId
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: ticketing
- id: GST-017
  name: Reservation Details
  module: TODO
  purpose: See reservation details for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-017
  capability: C02
  implementation:
    app: guest-app
    route: /general/reservation-details
    component: apps/guest-app/src/routes/general/ReservationDetailsDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: getReservation
    contract: orders
    purpose: Read a reservation
    trigger: onLoad
  - operationId: cancelReservation
    contract: orders
    purpose: Cancel a reservation
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: reservationId
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: ticketing
- id: GST-018
  name: Add to Calendar / Reminders
  module: TODO
  purpose: Find add to calendar / reminders for this venue.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-018
  capability: C02
  openQuestions:
  - Inventory cites `local` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/add-to-calendar-reminders
    component: apps/guest-app/src/routes/general/AddToCalendarRemindersDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. Add to Calendar and
    Reminders. **`issueWalletPass` is the operation this screen is for** — a wallet pass is the calendar
    entry. **Rewired on the 20 August review.**
  apis:
  - operationId: getOrder
    contract: orders
    purpose: Read an order
    trigger: onLoad
  - operationId: listOrders
    contract: orders
    purpose: List orders
    trigger: onLoad
  - operationId: issueWalletPass
    contract: orders
    purpose: Generate an Apple or Google wallet pass
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: ticketing
- id: GST-019
  name: Order History (Wallet)
  module: TODO
  purpose: Find order history (wallet) for this venue.
  wave: 2
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
      - kind: cardList
  states:
    loading: Terminal or gateway state, shown plainly
    emptyFirstRun: —
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    error: '**Declined reads differently from unresolved.** An unresolved payment inquires rather than
      retries, and nothing is issued until it resolves'
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-019
  capability: C21
  apis:
  - operationId: listOrders
    contract: orders
    purpose: List orders
    trigger: onLoad
  - operationId: getOrder
    contract: orders
    purpose: Read an order
    trigger: onLoad
  - operationId: listMyOrders
    contract: orders
    purpose: The orders this guest placed
    trigger: onLoad
  implementation:
    app: guest-app
    route: /general/order-history-wallet
    component: apps/guest-app/src/routes/general/OrderHistoryWalletDashboard.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. Order History. **`transferOrderTickets`
    was the only declared operation**, which is not history. Raised by Pranay. **Rewired on the 20 August
    review.** **`listMyOrders` wired 24 August, raised in review.** The staff-scoped list was on this
    guest screen — **a guest-facing list must be scoped to the caller, not filtered by a subject parameter**,
    or a guest is one parameter away from somebody else’s.
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: ticketing
- id: GST-020
  name: Saved Items / Wishlist
  module: TODO
  purpose: Find the right one quickly, and act on it without opening it.
  wave: 3
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
      - kind: cardList
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-020
  capability: C58
  openQuestions:
  - Inventory cites `GET /wishlist` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/saved-items-wishlist
    component: apps/guest-app/src/routes/general/SavedItemsWishlistList.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  apis:
  - operationId: getWishlist
    contract: marketing-crm
    purpose: Read a guest's saved items
    trigger: onLoad
  - operationId: addToWishlist
    contract: marketing-crm
    purpose: Save an item
    trigger: onAction
  - operationId: removeFromWishlist
    contract: marketing-crm
    purpose: Remove a saved item
    trigger: onAction
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. Wishlist. **Device
    and consent operations removed 20 August** — Pranay asked why they were here, and the answer is
    that seven operations were attached in bulk to three unrelated screens. **Rewired on the 20 August
    review.**
  density: comfortable
  entryState:
    params:
    - name: itemId
      from: deepLink
    - name: subjectId
      from: GST-001
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: marketing
- id: GST-021
  name: Interactive Map
  module: TODO
  purpose: See where things are in relation to each other.
  wave: 2
  layout:
    template: canvas
    regions:
    - name: contentBody
      components:
      - kind: scanTarget
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: '**The cached map and graph.** Live wait times are absent and routing still works — which
      is the right way round, because a guest with no signal in the middle of a park needs directions
      more than they need a queue length.'
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-021
  capability: C39
  openQuestions:
  - Inventory cites `GET /venue-map` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/interactive-map
    component: apps/guest-app/src/routes/general/InteractiveMapCanvas.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: 'States derived from the screen pattern on 17 August, not individually considered. Purpose
    derived from the screen name and its operations on 17 August, not from a requirement. **Wired to
    the venue map 18 August (CF-123).** The whole map arrives in one call and the client filters locally
    — which is also what makes it work with no signal in the middle of a park. **Routing happens on
    the device.** The platform guarantees the graph — connected, versioned, with step-free marked —
    and the client walks it, because a phone with the graph cached routes with no signal and a server
    round-trip per step does not. **The version is how a stale route is caught**: a guest holding a
    route across a path that closed this morning gets told, rather than walking into a barrier.'
  apis:
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  - operationId: getVenueMap
    contract: venue-map
    purpose: A map with its points and paths
    trigger: onLoad
  - operationId: getVenueMapGraph
    contract: venue-map
    purpose: The navigation graph, ready to route over
    trigger: onLoad
  density: comfortable
  entryState:
    params:
    - name: mapId
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: seating
- id: GST-022
  name: Attraction Wait Times
  module: TODO
  purpose: See attraction wait times for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-022
  capability: C82
  openQuestions:
  - Inventory cites `GET /queue/wait-times` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/attraction-wait-times
    component: apps/guest-app/src/routes/general/AttractionWaitTimesDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    - GST-059
    fromFlows: true
  apis:
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. Pulled to Wave 2 (CF-101)
    with the queue platform F21 runs on.
  density: comfortable
  requiresModule: queue
- id: GST-023
  name: Virtual Queue / Join Queue
  module: TODO
  purpose: Find the right one quickly, and act on it without opening it.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-023
  capability: C82
  apis:
  - operationId: joinQueue
    contract: queue
    purpose: from page inventory
    trigger: onLoad
  - operationId: getQueueEntry
    contract: queue
    purpose: Read a queue entry
    trigger: onLoad
  - operationId: leaveQueue
    contract: queue
    purpose: Leave a queue
    trigger: onAction
  implementation:
    app: guest-app
    route: /general/virtual-queue-join-queue
    component: apps/guest-app/src/routes/general/VirtualQueueJoinQueueDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  density: comfortable
  entryState:
    params:
    - name: entryId
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: queue
- id: GST-024
  name: F&B – Browse & Order
  module: TODO
  purpose: Order food to where the guest is sitting, or for collection.
  wave: 2
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
      - kind: cardList
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-024
  capability: C05
  apis:
  - operationId: listDiningOutlets
    contract: fnb
    purpose: Outlets open now, with ordering method
    trigger: onLoad
  - operationId: getGuestMenu
    contract: fnb
    purpose: The menu in force at this moment
    trigger: onLoad
  - operationId: claimLocationSession
    contract: fnb
    purpose: Scan the table, cabana or sunbed code
    trigger: onLoad
  - operationId: createGuestFnbOrder
    contract: fnb
    purpose: Place the order
    trigger: onLoad
  - operationId: getGuestOrderStatus
    contract: fnb
    purpose: Track an order
    trigger: onLoad
  - operationId: listDeliveryLocations
    contract: fnb
    purpose: Where an order can be delivered
    trigger: onLoad
  notes: The inventory cited `GET /menu`, which never existed. Now getGuestMenu. Ordering to a cabana
    or a seat uses the same claimLocationSession as a table (4.6.26). States derived from the screen
    pattern on 17 August, not individually considered.
  implementation:
    app: guest-app
    route: /general/fandb-browse-and-order
    component: apps/guest-app/src/routes/general/FandbBrowseAndOrderList.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    - GST-009
    fromFlows: true
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    - name: outletId
      from: deepLink
    - name: venueId
      from: session
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: fnb
- id: GST-025
  name: F&B – Order Tracking
  module: TODO
  purpose: Track an order to the point it reaches the guest.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-025
  capability: C05
  apis:
  - operationId: getGuestOrderStatus
    contract: fnb
    purpose: Ordered → accepted → in preparation → ready → served, collected or delivered
    trigger: onLoad
  - operationId: getGuestBill
    contract: fnb
    purpose: Everything ordered at this location this sitting
    trigger: onLoad
  - operationId: claimTableSession
    contract: fnb
    purpose: Identify which table a guest is sitting at
    trigger: onAction
  notes: 'Nine states from 4.6.35. Collected and delivered are distinct from served — a counter handover,
    a runner delivery and a server putting a plate down are three different events. States derived from
    the screen pattern on 17 August, not individually considered. **Navigation repointed to P15 on 20
    August** — the F&B screens it linked to moved out of the back office when the client board was adopted.
    **Cross-platform navigation removed 24 August**: BO-021. **A till does not navigate to a back office
    and a guest app does not navigate to either** — those are device handovers, and a flow declares
    them with `crossesDevice` rather than a screen pretending there is a link.'
  implementation:
    app: guest-app
    route: /general/fandb-order-tracking
    component: apps/guest-app/src/routes/general/FandbOrderTrackingDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    fromFlows: true
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    - name: sessionId
      from: session
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: fnb
- id: GST-026
  name: Retail / Merchandise
  module: TODO
  purpose: Find retail / merchandise for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-026
  capability: C08
  openQuestions:
  - Inventory cites `GET /products?kind=retail` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/retail-merchandise
    component: apps/guest-app/src/routes/general/RetailMerchandiseDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    fromFlows: true
  apis:
  - operationId: getGameCard
    contract: games
    purpose: Read a card's balances
    trigger: onLoad
  - operationId: listMerchandise
    contract: retail
    purpose: List merchandise
    trigger: onLoad
  notes: 'States derived from the screen pattern on 17 August, not individually considered. Purpose
    derived from the screen name and its operations on 17 August, not from a requirement. **Cross-platform
    navigation removed 24 August**: BO-048, BO-069. **A till does not navigate to a back office and
    a guest app does not navigate to either** — those are device handovers, and a flow declares them
    with `crossesDevice` rather than a screen pretending there is a link.'
  density: comfortable
  entryState:
    params:
    - name: cardCode
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: games
- id: GST-027
  name: Parking – Reserve & Pay
  module: TODO
  purpose: Take the money, and be unambiguous about whether it worked.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Terminal or gateway state, shown plainly
    emptyFirstRun: —
    error: '**Declined reads differently from unresolved.** An unresolved payment inquires rather than
      retries, and nothing is issued until it resolves'
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-027
  capability: C23
  openQuestions:
  - Inventory cites `POST /parking-reservations` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/parking-reserve-and-pay
    component: apps/guest-app/src/routes/general/ParkingReserveAndPayDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: createParkingEntitlement
    contract: access
    purpose: A guest bought parking
    trigger: onAction
  - operationId: updateParkingEntitlement
    contract: access
    purpose: Change the plate, or revoke
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: entitlementId
      from: deepLink
    coldEntry: '**A ticket link opened after the event.** Shows the entitlement with its status — expired,
      used, transferred — because *not found* to somebody holding a ticket is the wrong answer.'
  requiresModule: access
- id: GST-028
  name: Parking – Reservation Confirmed
  module: TODO
  purpose: Confirm it worked, and give them what they need to prove it.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-028
  capability: C23
  openQuestions:
  - Inventory cites `GET /parking-reservations/{id}` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/parking-reservation-confirmed
    component: apps/guest-app/src/routes/general/ParkingReservationConfirmedDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: createParkingEntitlement
    contract: access
    purpose: A guest bought parking
    trigger: onAction
  - operationId: listParkingFacilities
    contract: access
    purpose: Car parks at a venue, and how each integrates
    trigger: onLoad
  density: comfortable
  requiresModule: access
- id: GST-029
  name: Venue Info & Services
  module: TODO
  purpose: See venue info & services for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-029
  capability: C17
  openQuestions:
  - Inventory cites `GET /venue-info` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/venue-info-and-services
    component: apps/guest-app/src/routes/general/VenueInfoAndServicesDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  apis:
  - operationId: listDiningOutlets
    contract: fnb
    purpose: Where a guest can eat, right now
    trigger: onLoad
  - operationId: listDeliveryLocations
    contract: fnb
    purpose: Where an order can be delivered
    trigger: onLoad
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  density: comfortable
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: fnb
- id: GST-030
  name: In-Venue Notifications
  module: TODO
  purpose: Work with in-venue notifications for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-030
  capability: C61
  openQuestions:
  - Inventory cites `GET /notifications` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/in-venue-notifications
    component: apps/guest-app/src/routes/general/InVenueNotificationsDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    - GST-031
    fromFlows: true
  apis:
  - operationId: claimLocationSession
    contract: fnb
    purpose: From the flow it appears in
    trigger: onLoad
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  density: comfortable
  requiresModule: fnb
- id: GST-031
  name: AI Concierge – Home
  module: TODO
  purpose: The screen this app sits on. Everything else is entered from here and returns to it.
  wave: 2
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
      - kind: cardList
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-031
  capability: AI-02
  openQuestions:
  - Inventory cites `POST /ai/concierge` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/ai-concierge-home
    component: apps/guest-app/src/routes/general/AiConciergeHomeDashboard.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    - GST-032
    fromFlows: true
  apis:
  - operationId: getGuestMenu
    contract: fnb
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: sendAiMessage
    contract: ai
    purpose: Ask
    trigger: onAction
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. **Guest concierge
    confirmed in Phase 1 on 17 August (CF-14), charged per token and bounded by `AiPolicy.guestCapabilityScope`.**
    Still degrades to the manual planner rather than to an error.
  density: comfortable
  entryState:
    params:
    - name: conversationId
      from: deepLink
    - name: outletId
      from: deepLink
    coldEntry: A conversation link an agent opens from a notification. Resolves, or says it was closed
      and by whom.
  requiresModule: fnb
- id: GST-032
  name: AI Concierge – Chat
  module: TODO
  purpose: Add ai concierge – chat for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-032
  capability: AI-02
  openQuestions:
  - Inventory cites `POST /ai/concierge` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/ai-concierge-chat
    component: apps/guest-app/src/routes/general/AiConciergeChatDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    - GST-033
    fromFlows: true
  apis:
  - operationId: createGuestFnbOrder
    contract: fnb
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: getGuestOrderStatus
    contract: fnb
    purpose: Track an order
    trigger: onLoad
  - operationId: getCart
    contract: orders
    purpose: The cart, priced and checked, right now
    trigger: onLoad
  - operationId: addCartLine
    contract: orders
    purpose: Add something
    trigger: onAction
  - operationId: checkoutCart
    contract: orders
    purpose: Turn the cart into an order
    trigger: onAction
  - operationId: sendAiMessage
    contract: ai
    purpose: Ask
    trigger: onAction
  - operationId: sendConversationMessage
    contract: marketing-crm
    purpose: Say something, as a guest or an agent
    trigger: onAction
  - operationId: handoverToAgent
    contract: marketing-crm
    purpose: Pass an assistant conversation to a person
    trigger: onAction
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. **Guest concierge
    confirmed in Phase 1 on 17 August (CF-14), charged per token and bounded by `AiPolicy.guestCapabilityScope`.**
    Still degrades to the manual planner rather than to an error.
  density: comfortable
  entryState:
    params:
    - name: cartId
      from: session
    - name: conversationId
      from: deepLink
    - name: orderId
      from: deepLink
    coldEntry: A conversation link an agent opens from a notification. Resolves, or says it was closed
      and by whom. **A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.
  requiresModule: ticketing
- id: GST-033
  name: AI Concierge – Contextual Help
  module: TODO
  purpose: Answer a question without needing a person.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-033
  capability: AI-02
  openQuestions:
  - Inventory cites `POST /ai/concierge` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/ai-concierge-contextual-help
    component: apps/guest-app/src/routes/general/AiConciergeContextualHelpDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    fromFlows: true
  notes: 'States derived from the screen pattern on 17 August, not individually considered. Purpose
    derived from the screen name and its operations on 17 August, not from a requirement. **Guest concierge
    confirmed in Phase 1 on 17 August (CF-14), charged per token and bounded by `AiPolicy.guestCapabilityScope`.**
    Still degrades to the manual planner rather than to an error. **createPayment removed 18 August**
    — attached by module resemblance, not by what this screen does. A screen that does not handle money
    should not be able to move it (CF-87''s class). **Navigation repointed to P15 on 20 August** — the
    F&B screens it linked to moved out of the back office when the client board was adopted. **Cross-platform
    navigation removed 24 August**: BO-020. **A till does not navigate to a back office and a guest
    app does not navigate to either** — those are device handovers, and a flow declares them with `crossesDevice`
    rather than a screen pretending there is a link.'
  apis:
  - operationId: sendAiMessage
    contract: ai
    purpose: Ask
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: conversationId
      from: deepLink
    coldEntry: A conversation link an agent opens from a notification. Resolves, or says it was closed
      and by whom.
  requiresModule: ai
- id: GST-034
  name: Lost & Found
  module: TODO
  purpose: See lost & found for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-034
  capability: C18
  openQuestions:
  - Inventory cites `POST /lost-found/reports` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/lost-and-found
    component: apps/guest-app/src/routes/general/LostAndFoundDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    fromFlows: true
  apis:
  - operationId: listMyCases
    contract: marketing-crm
    purpose: The cases this guest raised
    trigger: onLoad
  - operationId: raiseMyCase
    contract: marketing-crm
    purpose: Report something — lost property, a complaint, a question
    trigger: onAction
  - operationId: replyToMyCase
    contract: marketing-crm
    purpose: Reply on a case the guest raised
    trigger: onAction
  notes: 'States derived from the screen pattern on 17 August, not individually considered. Purpose
    derived from the screen name and its operations on 17 August, not from a requirement. **Navigation
    repointed to P15 on 20 August** — the F&B screens it linked to moved out of the back office when
    the client board was adopted. **Cross-platform navigation removed 24 August**: BO-021. **A till
    does not navigate to a back office and a guest app does not navigate to either** — those are device
    handovers, and a flow declares them with `crossesDevice` rather than a screen pretending there is
    a link. **Rebuilt 24 August.** This screen declared exactly one operation — `getGuestOrderStatus`
    — **order tracking on a lost-property screen.** A guest reporting a lost bag could not raise anything.
    **Lost and found is a case**, the same object as a complaint and a query, which is what lets one
    queue serve all three. **Guest case operations wired 24 August.** **No case operation was guest-callable**
    — a guest could raise nothing and read nothing, and `check-screens` refused the staff-permissioned
    ones on a guest surface. `listMyCases`, `raiseMyCase` and `replyToMyCase` are scoped to the caller
    rather than filtered by a subject parameter.'
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    - name: caseId
      from: deepLink
      optional: true
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error. A case opened from a notification or the list. **Optional** — the ordinary way in is
      to raise a new one, not to open an old one.'
  requiresModule: fnb
- id: GST-035
  name: Feedback & Ratings
  module: TODO
  purpose: Work with feedback & ratings for this venue.
  wave: 3
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
      - kind: cardList
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-035
  capability: C62
  apis:
  - operationId: submitReview
    contract: marketing-crm
    purpose: from page inventory
    trigger: onLoad
  - operationId: raiseMyCase
    contract: marketing-crm
    purpose: Report something — lost property, a complaint, a question
    trigger: onAction
  implementation:
    app: guest-app
    route: /general/feedback-and-ratings
    component: apps/guest-app/src/routes/general/FeedbackAndRatingsList.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. **Guest case operations
    wired 24 August.** **No case operation was guest-callable** — a guest could raise nothing and read
    nothing, and `check-screens` refused the staff-permissioned ones on a guest surface. `listMyCases`,
    `raiseMyCase` and `replyToMyCase` are scoped to the caller rather than filtered by a subject parameter.
  density: comfortable
  requiresModule: marketing
- id: GST-036
  name: Loyalty & Rewards
  module: TODO
  purpose: Your points, your tier, and what is within reach.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Balance and tier
    emptyFirstRun: Not enrolled — explains what joining gives you rather than showing zero
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load. **Redeeming is blocked** rather than attempted against an unknown balance
    offline: Last known balance with its age. **Points earned since are not shown**, and that is said
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-036
  capability: C64
  openQuestions:
  - Inventory cites `GET /loyalty` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /loyalty-rewards
    component: apps/guest-app/src/routes/LoyaltyRewards.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: Merged with GST-064 on 18 August. **I created GST-064 in the parity sweep without noticing
    this screen existed** — same two operations, same capability, and the fuzzy match against the client
    storyboard is what surfaced it. Board 6 panel 6 shows one Loyalty & Rewards screen, not two.
  apis:
  - operationId: getLoyaltyPosition
    contract: marketing-crm
    purpose: A guest's points, tier and what is within reach
    trigger: onLoad
  - operationId: listLoyaltyProgrammes
    contract: marketing-crm
    purpose: List loyalty programmes
    trigger: onLoad
  density: comfortable
  requiresModule: marketing
- id: GST-037
  name: Offers & Promotions
  module: TODO
  purpose: Find offers & promotions for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-037
  capability: C86
  openQuestions:
  - Inventory cites `GET /offers` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/offers-and-promotions
    component: apps/guest-app/src/routes/general/OffersAndPromotionsDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: listPromotions
    contract: promotions
    purpose: List promotions
    trigger: onLoad
  - operationId: getPromotion
    contract: promotions
    purpose: Read a promotion
    trigger: onLoad
  - operationId: evaluatePromotions
    contract: promotions
    purpose: Evaluate promotions against a cart
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: promotionId
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: ticketing
- id: GST-038
  name: Digital Companion Mode
  module: TODO
  purpose: 'The in-venue home: your tickets, the map, and what is near you.'
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-038
  capability: C105
  apis:
  - operationId: getTenantAppStatus
    contract: white-label
    purpose: App status and recent changes
    trigger: onLoad
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  implementation:
    app: guest-app
    route: /general/digital-companion-mode
    component: apps/guest-app/src/routes/general/DigitalCompanionModeDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: '**Not minuted and not in the matrix.** The only unsourced screen of the eight audited on 17
    August — it reads as a framing for the in-venue capabilities rather than a capability of its own,
    and CF-92 asks whether it is a real screen or a name for a mode.'
  provenance: none — see CF-92
  density: comfortable
  requiresModule: queue
- id: GST-039
  name: Profile
  module: TODO
  purpose: What we hold about a guest, and what they can change.
  wave: 1
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
      - kind: cardList
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-039
  capability: C58
  openQuestions:
  - Inventory cites `GET /guests/me` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/profile
    component: apps/guest-app/src/routes/general/ProfileDashboard.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: recordConsent
    contract: marketing-crm
    purpose: Record a consent decision
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: subjectId
      from: GST-001
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: marketing
- id: GST-040
  name: Help & Support
  module: TODO
  purpose: Answer a question without needing a person.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-040
  capability: C17
  openQuestions:
  - Inventory cites `GET /support` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/help-and-support
    component: apps/guest-app/src/routes/general/HelpAndSupportDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement. **Guest case operations
    wired 24 August.** **No case operation was guest-callable** — a guest could raise nothing and read
    nothing, and `check-screens` refused the staff-permissioned ones on a guest surface. `listMyCases`,
    `raiseMyCase` and `replyToMyCase` are scoped to the caller rather than filtered by a subject parameter.
  apis:
  - operationId: listFaqs
    contract: white-label
    purpose: List FAQs
    trigger: onLoad
  - operationId: listContentPages
    contract: white-label
    purpose: List custom content pages
    trigger: onLoad
  - operationId: listMyCases
    contract: marketing-crm
    purpose: The cases this guest raised
    trigger: onLoad
  - operationId: raiseMyCase
    contract: marketing-crm
    purpose: Report something — lost property, a complaint, a question
    trigger: onAction
  - operationId: replyToMyCase
    contract: marketing-crm
    purpose: Reply on a case the guest raised
    trigger: onAction
  density: comfortable
  requiresModule: marketing
  entryState:
    params:
    - name: caseId
      from: deepLink
      optional: true
    coldEntry: A case opened from a notification or the list. **Optional** — the ordinary way in is
      to raise a new one, not to open an old one.
- id: GST-041
  name: Checkout Entry
  module: TODO
  purpose: Take the money, and be unambiguous about whether it worked.
  wave: 1
  layout:
    template: form
    regions:
    - name: contentBody
      components:
      - kind: textField
      - kind: primaryButton
  states:
    loading: Terminal or gateway state, shown plainly
    emptyFirstRun: —
    error: '**Declined reads differently from unresolved.** An unresolved payment inquires rather than
      retries, and nothing is issued until it resolves'
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-041
  capability: C56
  openQuestions:
  - Inventory cites `—` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/checkout-entry
    component: apps/guest-app/src/routes/general/CheckoutEntryForm.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: getCart
    contract: orders
    purpose: The cart, priced and checked, right now
    trigger: onLoad
  - operationId: checkoutCart
    contract: orders
    purpose: Turn the cart into an order
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: cartId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: ticketing
- id: GST-042
  name: Simple Registration & OTP
  module: TODO
  purpose: Work with simple registration & otp for this venue.
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-042
  capability: C56
  apis:
  - operationId: requestGuestOtp
    contract: identity
    purpose: from page inventory
    trigger: onLoad
  - operationId: completeSsoAuthorization
    contract: identity
    purpose: Exchange an SSO code for a session
    trigger: onAction
  - operationId: enrolMfaMethod
    contract: identity
    purpose: Enrol an MFA method
    trigger: onAction
  - operationId: getCurrentSession
    contract: identity
    purpose: Current session and effective permissions
    trigger: onLoad
  - operationId: getGuestSession
    contract: identity
    purpose: Read the current guest session
    trigger: onLoad
  - operationId: guestLogout
    contract: identity
    purpose: End a guest session
    trigger: onAction
  - operationId: guestSocialLogin
    contract: identity
    purpose: Sign in with Apple or Google
    trigger: onAction
  - operationId: guestUaePassLogin
    contract: identity
    purpose: Sign in with a national identity provider
    trigger: onAction
  - operationId: linkGuestCheckout
    contract: identity
    purpose: Attach a guest checkout to an account
    trigger: onAction
  - operationId: listMfaMethods
    contract: identity
    purpose: Enrolled MFA methods
    trigger: onLoad
  - operationId: listSsoProviders
    contract: identity
    purpose: Identity providers configured for this tenant
    trigger: onLoad
  - operationId: login
    contract: identity
    purpose: Authenticate and open a session
    trigger: onAction
  - operationId: logout
    contract: identity
    purpose: Close the current session
    trigger: onAction
  - operationId: refreshToken
    contract: identity
    purpose: Rotate the access token
    trigger: onAction
  - operationId: registerGuest
    contract: identity
    purpose: Create a guest account
    trigger: onAction
  - operationId: removeMfaMethod
    contract: identity
    purpose: Remove an MFA method
    trigger: onAction
  - operationId: selectRole
    contract: identity
    purpose: Choose a role for a multi-role session
    trigger: onAction
  - operationId: startSsoAuthorization
    contract: identity
    purpose: Begin an SSO flow
    trigger: onLoad
  - operationId: verifyGuestOtp
    contract: identity
    purpose: Verify a one-time code and issue a session
    trigger: onAction
  - operationId: verifyMfaChallenge
    contract: identity
    purpose: Complete a step-up challenge
    trigger: onAction
  - operationId: verifyMfaEnrolment
    contract: identity
    purpose: Complete enrolment
    trigger: onAction
  implementation:
    app: guest-app
    route: /general/simple-registration-and-otp
    component: apps/guest-app/src/routes/general/SimpleRegistrationAndOtpDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  density: comfortable
  entryState:
    params:
    - name: challengeId
      from: deepLink
    - name: methodId
      from: deepLink
    - name: providerId
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: core
- id: GST-043
  name: Arabic / RTL Experience
  module: TODO
  purpose: Arabic / RTL Experience — the screen a person opens when they need to deal with arabic /
    rtl experience.
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-043
  capability: C57
  openQuestions:
  - Inventory cites `—` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/arabic-rtl-experience
    component: apps/guest-app/src/routes/general/ArabicRtlExperienceDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  density: comfortable
  requiresModule: core
- id: GST-044
  name: Multi-Currency & Pricing
  module: TODO
  purpose: See prices in your own currency, in the app, before and during the visit.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Rates and prices, with the base currency shown first
    emptyFirstRun: One currency configured at this venue — the selector is hidden rather than shown
      with a single option
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Rates unavailable. **Prices show in base currency rather than a stale conversion** — a wrong
      rate is worse than no rate, because a guest who budgets against it arrives short
    offline: '**Last known rates, with their age shown.** A rate is a number a guest may act on, and
      an undated one they cannot judge'
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-044
  capability: C107
  openQuestions:
  - Inventory cites `GET /currencies` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/multi-currency-and-pricing
    component: apps/guest-app/src/routes/general/MultiCurrencyAndPricingDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: 'Drawn in guest storyboard board 7 panel 4 — a selector across AED, USD, EUR, GBP and SAR with
    *"payment will be processed in AED"* — and board 8 carries an AED selector in its header. **Confirmed
    for both surfaces on 18 August (CF-111).** The matrix names the website (2.6.33, 2.9.1) and the
    storyboard draws the app; both are client documents and the answer is both, which is what CF-93
    concluded for every other guest capability. **Display only — the sale settles in base currency**
    (CF-37), and that is stated on the screen rather than in a footnote. **`getRegionSettings` deliberately
    not called** — a guest does not need the venue''s scope configuration to pick a currency. `listFxRates`
    is the currency list: a rate exists only for a currency the venue enabled, so the two questions
    have one answer.'
  apis:
  - operationId: listFxRates
    contract: finance
    purpose: The rates in force
    trigger: onLoad
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  density: comfortable
  requiresModule: ticketing
- id: GST-045
  name: Ticket Delivery & Sharing
  module: TODO
  purpose: Find ticket delivery & sharing for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-045
  capability: C02
  openQuestions:
  - Inventory cites `POST /tickets/{id}/share` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/ticket-delivery-and-sharing
    component: apps/guest-app/src/routes/general/TicketDeliveryAndSharingDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  apis:
  - operationId: transferOrderTickets
    contract: orders
    purpose: Transfer tickets to another guest
    trigger: onAction
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: ticketing
- id: GST-046
  name: Branded Queue / Waiting Room
  module: TODO
  purpose: Find branded queue / waiting room for this venue.
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-046
  capability: C98
  openQuestions:
  - Inventory cites `GET /waiting-room` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/branded-queue-waiting-room
    component: apps/guest-app/src/routes/general/BrandedQueueWaitingRoomDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  apis:
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  density: comfortable
  requiresModule: queue
- id: GST-047
  name: Maintenance / Upgrade Page
  module: TODO
  purpose: Say what is wrong in a way that tells the person whether to wait or leave.
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-047
  capability: C57
  apis:
  - operationId: getTenantAppStatus
    contract: white-label
    purpose: from page inventory
    trigger: onLoad
  implementation:
    app: guest-app
    route: /general/maintenance-page
    component: apps/guest-app/src/routes/general/MaintenancePageDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: Merged with GST-060 on 18 August. The client storyboard shows this on two boards — G7 panel
    7 and G8 panel 10 — which is a **board convention for a screen every journey can land on**, not
    two screens. Both had the same single operation and the same purpose.
  density: comfortable
  requiresModule: core
- id: GST-048
  name: Upsell / Cross-Sell
  module: TODO
  purpose: See upsell / cross-sell for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-048
  capability: C88
  openQuestions:
  - Inventory cites `GET /recommendations` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/upsell-cross-sell
    component: apps/guest-app/src/routes/general/UpsellCrossSellDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: addCartLine
    contract: orders
    purpose: Add something
    trigger: onAction
  - operationId: getUpsellSuggestions
    contract: promotions
    purpose: Suggestions for a cart
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: cartId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: ticketing
- id: GST-049
  name: Interactive Seat Selection
  module: TODO
  purpose: See interactive seat selection for this venue.
  wave: 2
  layout:
    template: canvas
    regions:
    - name: contentBody
      components:
      - kind: scanTarget
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-049
  capability: C53
  implementation:
    app: guest-app
    route: /general/interactive-seat-selection
    component: apps/guest-app/src/routes/general/InteractiveSeatSelectionCanvas.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: getSeatAvailability
    contract: seating
    purpose: Seat status for a performance
    trigger: onLoad
  - operationId: createSeatHold
    contract: seating
    purpose: Hold specific seats
    trigger: onAction
  - operationId: recommendSeats
    contract: seating
    purpose: Recommend seats for a party
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: performanceId
      from: deepLink
    coldEntry: '**A link to a performance that has happened.** Offers the next performance of the same
      event.'
  requiresModule: seating
- id: GST-050
  name: Resource Booking – Cabana
  module: TODO
  purpose: Book a cabana or other capacity-based resource.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-050
  capability: C78
  openQuestions:
  - Inventory cites `POST /resource-bookings` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/resource-booking-cabana
    component: apps/guest-app/src/routes/general/ResourceBookingCabanaDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: 'Minuted 10 Aug §4.8: *"resource bookings (e.g., cabana rental) decrement a capacity-based
    inventory on booking"*. **Distinct from GST-058**: this is one resource with a price and a booking
    action; GST-058 is availability across areas. Confirmed against storyboard boards 7.10 and 8.8 on
    18 August.'
  apis:
  - operationId: addCartLine
    contract: orders
    purpose: Add something
    trigger: onAction
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: getAvailability
    contract: catalogue
    purpose: Live remaining capacity
    trigger: onLoad
  provenance: MoM 10 August 2026 §4.8
  density: comfortable
  entryState:
    params:
    - name: cartId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: ticketing
- id: GST-051
  name: Plan Your Adventure – Start
  module: TODO
  purpose: Start a plan for the visit — which attractions, in what order — before arriving.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-051
  capability: AI-35
  openQuestions:
  - Inventory cites `POST /ai/itinerary` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/plan-your-adventure-start
    component: apps/guest-app/src/routes/general/PlanYourAdventureStartDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: Minuted 10 Aug §4.9. Benchmarked against Skidata. Tied to a ticket purchase and saved as a
    personal visit profile.
  apis:
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  provenance: MoM 10 August 2026 §4.9
  density: comfortable
  requiresModule: queue
- id: GST-052
  name: Suggested Itineraries
  module: TODO
  purpose: Curated plans a guest can take rather than build.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-052
  capability: AI-35
  openQuestions:
  - Inventory cites `GET /itineraries/suggested` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/suggested-itineraries
    component: apps/guest-app/src/routes/general/SuggestedItinerariesDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: 'Minuted 10 Aug §4.9. ** wired 24 August.** The board drew four bespoke AI endpoints for itinerary
    planning; **one operation with a kind answers all of them** (ADR-0028), and recording the outcome
    is what lets a model replace the heuristic later. **`recordSuggestionOutcome` removed from the guest
    surface.** `check-screens` refused it and was right: **a guest does not record an outcome — the
    platform observes what they did.** A guest app that self-reports whether it took the advice is a
    training label the guest could forge, and the observation belongs server-side where the plan and
    the visit can be compared.'
  apis:
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: listCatalogueBundles
    contract: catalogue
    purpose: List published bundles
    trigger: onLoad
  - operationId: requestSuggestion
    contract: ai
    purpose: ''
    trigger: onAction
  provenance: MoM 10 August 2026 §4.9
  density: comfortable
  requiresModule: ai
  entryState:
    params:
    - name: suggestionId
      from: deepLink
      optional: true
    coldEntry: A suggestion opened from a saved plan.
- id: GST-053
  name: Build Your Own Itinerary
  module: TODO
  purpose: Choose the attractions and the order yourself.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-053
  capability: C02
  openQuestions:
  - Inventory cites `POST /itineraries` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/build-your-own-itinerary
    component: apps/guest-app/src/routes/general/BuildYourOwnItineraryDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: Minuted 10 Aug §4.9. **Group sharing agreed** — an in-app QR a friend scans to join the itinerary
    (Chinmay raised, Qossai confirmed).
  apis:
  - operationId: addCartLine
    contract: orders
    purpose: Add something
    trigger: onAction
  - operationId: getCart
    contract: orders
    purpose: The cart, priced and checked, right now
    trigger: onLoad
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  provenance: MoM 10 August 2026 §4.9
  density: comfortable
  entryState:
    params:
    - name: cartId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: ticketing
- id: GST-054
  name: AI Optimized Itinerary
  module: TODO
  purpose: Let the assistant order the day around waits and opening times.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: '**Falls back to the manual planner** (GST-053) rather than to an error. A guest who wanted
      a plan should get one, optimised or not'
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-054
  capability: AI-35
  openQuestions:
  - Inventory cites `POST /ai/itinerary/optimise` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/ai-optimized-itinerary
    component: apps/guest-app/src/routes/general/AiOptimizedItineraryDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: 'Minuted 10 Aug §4.9. **The AI half is Wave 2** — the manual planner must work without it (CF-41).
    **Guest concierge confirmed in Phase 1 on 17 August (CF-14), charged per token and bounded by `AiPolicy.guestCapabilityScope`.**
    Still degrades to the manual planner rather than to an error. ** wired 24 August.** The board drew
    four bespoke AI endpoints for itinerary planning; **one operation with a kind answers all of them**
    (ADR-0028), and recording the outcome is what lets a model replace the heuristic later. **`recordSuggestionOutcome`
    removed from the guest surface.** `check-screens` refused it and was right: **a guest does not record
    an outcome — the platform observes what they did.** A guest app that self-reports whether it took
    the advice is a training label the guest could forge, and the observation belongs server-side where
    the plan and the visit can be compared.'
  apis:
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: sendAiMessage
    contract: ai
    purpose: Ask
    trigger: onAction
  - operationId: requestSuggestion
    contract: ai
    purpose: ''
    trigger: onAction
  provenance: MoM 10 August 2026 §4.9
  density: comfortable
  entryState:
    params:
    - name: conversationId
      from: deepLink
    - name: suggestionId
      from: deepLink
      optional: true
    coldEntry: A conversation link an agent opens from a notification. Resolves, or says it was closed
      and by whom. A suggestion opened from a saved plan.
  requiresModule: ai
- id: GST-055
  name: Dynamic QR Ticket
  module: TODO
  purpose: Find dynamic qr ticket for this venue.
  wave: 1
  layout:
    template: canvas
    regions:
    - name: contentBody
      components:
      - kind: scanTarget
  states:
    loading: Availability is live, never cached
    emptyFirstRun: '**Sold out is a real answer.** Offers the next available rather than a dead end'
    error: Availability unavailable. **Selection is blocked** — overselling is worse than waiting
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-055
  capability: C09
  openQuestions:
  - Inventory cites `GET /tickets/{id}/dynamic-qr` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/dynamic-qr-ticket
    component: apps/guest-app/src/routes/general/DynamicQrTicketCanvas.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  apis:
  - operationId: transferOrderTickets
    contract: orders
    purpose: Transfer tickets to another guest
    trigger: onAction
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  density: comfortable
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves;
      if it was refunded or the performance passed, says which and offers the order list rather than
      an error.'
  requiresModule: ticketing
- id: GST-056
  name: Bundle Package
  module: TODO
  purpose: See bundle package for this venue.
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-056
  capability: C87
  implementation:
    app: guest-app
    route: /general/bundle-package
    component: apps/guest-app/src/routes/general/BundlePackageDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: States derived from the screen pattern on 17 August, not individually considered. Purpose derived
    from the screen name and its operations on 17 August, not from a requirement.
  apis:
  - operationId: listCatalogueBundles
    contract: catalogue
    purpose: List published bundles
    trigger: onLoad
  - operationId: getBundle
    contract: promotions
    purpose: Read a bundle with components and allocation
    trigger: onLoad
  - operationId: addCartLine
    contract: orders
    purpose: Add something
    trigger: onAction
  density: comfortable
  entryState:
    params:
    - name: bundleId
      from: deepLink
    - name: cartId
      from: session
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: ticketing
- id: GST-057
  name: Accessibility Information
  module: TODO
  purpose: Step-free routes, facilities and what to expect.
  wave: 2
  layout:
    template: form
    regions:
    - name: contentBody
      components:
      - kind: textField
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-057
  capability: C17
  openQuestions:
  - Inventory cites `GET /accessibility` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/accessibility-information
    component: apps/guest-app/src/routes/general/AccessibilityInformationForm.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: 'Minuted 10 Aug §4.1: custom content pages *"with accessibility-guideline compliance to be
    followed"*. **Tenant-authored content, not a built feature.**'
  apis:
  - operationId: listContentPages
    contract: white-label
    purpose: List custom content pages
    trigger: onLoad
  provenance: MoM 10 August 2026 §4.1
  density: comfortable
  requiresModule: core
- id: GST-058
  name: Resource Availability (Cabana)
  module: TODO
  purpose: See which cabanas are free, by area and date, before choosing one.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-058
  capability: C78
  openQuestions:
  - Inventory cites `GET /resources/availability` — no matching operation. Written before the contracts
    existed.
  implementation:
    app: guest-app
    route: /general/resource-availability-cabana
    component: apps/guest-app/src/routes/general/ResourceAvailabilityCabanaDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
  notes: '**Deliberately separate from GST-050**, and checked on 18 August. Board 8 panel 8 is availability
    across areas with counts remaining; board 7 panel 10 is one cabana with a price and a Book Now.
    **A list and a detail, not a duplicate** — merging them would put a booking action on a browsing
    screen.'
  apis:
  - operationId: getAvailability
    contract: catalogue
    purpose: Live remaining capacity
    trigger: onLoad
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  density: comfortable
  requiresModule: ticketing
- id: GST-059
  name: Plan My Day – In Progress
  module: TODO
  purpose: The plan while you are in the venue, against live waits.
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
  states:
    loading: Content loads
    emptyFirstRun: TODO — what exists here, and what this person does to create the first one.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Could not load
    offline: Cached content with its age. **Nothing that spends money or holds capacity works offline**
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-059
  capability: C02
  openQuestions:
  - Inventory cites `GET /itineraries/{id}` — no matching operation. Written before the contracts existed.
  implementation:
    app: guest-app
    route: /general/plan-my-day-in-progress
    component: apps/guest-app/src/routes/general/PlanMyDayInProgressDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-002
    - GST-003
    fromFlows: true
  notes: 'Minuted 10 Aug §4.9. **The in-visit half of GST-051** — one capability, two moments. ** wired
    24 August.** The board drew four bespoke AI endpoints for itinerary planning; **one operation with
    a kind answers all of them** (ADR-0028), and recording the outcome is what lets a model replace
    the heuristic later. **`recordSuggestionOutcome` removed from the guest surface.** `check-screens`
    refused it and was right: **a guest does not record an outcome — the platform observes what they
    did.** A guest app that self-reports whether it took the advice is a training label the guest could
    forge, and the observation belongs server-side where the plan and the visit can be compared.'
  apis:
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: requestSuggestion
    contract: ai
    purpose: ''
    trigger: onAction
  provenance: MoM 10 August 2026 §4.9
  density: comfortable
  requiresModule: ai
  entryState:
    params:
    - name: suggestionId
      from: deepLink
      optional: true
    coldEntry: A suggestion opened from a saved plan.
- id: GST-061
  name: Menu Item Detail
  module: In-Venue Experience
  purpose: Decide on a dish, choose modifiers, check allergens.
  capability: C05
  wave: 2
  apis:
  - operationId: getGuestMenu
    contract: fnb
    purpose: Item, modifiers and allergens
    trigger: onLoad
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        bindsTo: GuestMenu.sections[].items[]
      - kind: selectField
        label: Modifiers
        notes: Constraints come from the group — minimum, maximum, required
      - kind: banner
        notes: Allergens. Always present; not a field a tenant may omit (4.8.9)
  states:
    loading: Item skeleton
    emptyFirstRun: Not applicable
    error: Item unavailable, with a route back to the menu
    offline: Cached menu shown with a staleness warning. Ordering is refused — availability changes
      by the minute
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-061
  notes: P02-019.1 in the client hierarchy. Absent from my page inventory until 14 August.
  implementation:
    app: guest-app
    route: /in-venue-experience/menu-item-detail
    component: apps/guest-app/src/routes/in-venue-experience/MenuItemDetail.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-062
  density: comfortable
  entryState:
    params:
    - name: outletId
      from: deepLink
    coldEntry: TODO — what this screen shows when somebody arrives with no upstream screen.
  requiresModule: fnb
- id: GST-062
  name: Shop & Drop Collection
  module: In-Venue Experience
  purpose: Collect shopping left earlier, using the ticket rather than a receipt.
  capability: C21
  wave: 3
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: ShopAndDrop
      - kind: banner
        notes: Collect-by time. Usually venue close
  states:
    loading: List skeleton
    emptyFirstRun: Nothing awaiting collection
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Lookup unavailable — the collection point can still find it by receipt
    offline: Cannot look up. Shows the drop reference held locally so a guest can quote it
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-062
  notes: 4.4.7. Claimed by the entitlement the guest is already carrying, not a paper slip.
  implementation:
    app: guest-app
    route: /in-venue-experience/shop-and-drop-collection
    component: apps/guest-app/src/routes/in-venue-experience/ShopAndDropCollectionList.tsx
    status: notStarted
  navigation:
    entryFrom:
    - GST-001
    inferred: true
    exitTo:
    - GST-001
    - GST-061
  apis:
  - operationId: lookupShopAndDrop
    contract: retail
    purpose: Find a guest's dropped goods
    trigger: onLoad
  density: comfortable
  requiresModule: retail
- id: GST-063
  name: Search
  module: Discovery
  purpose: Find something when you do not know what it is called.
  capability: C00
  wave: 1
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: dataTable
  apis:
  - operationId: searchCatalogue
    contract: catalogue
    purpose: Find something by name
    trigger: onLoad
  states:
    loading: Results as you type
    emptyFirstRun: '**Nothing matched. Offers what is on** rather than a dead end'
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    error: Search unavailable. Falls back to browsing categories
    offline: '**Searches the cached catalogue only**, with a note that newer items may exist'
  navigation:
    exitTo:
    - GST-001
    inferred: true
  implementation:
    app: guest-app
    route: /search
    component: apps/guest-app/src/routes/Search.tsx
    status: notStarted
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-063
  notes: Added 17 August for parity with WEB-003. **Not on the wireframe board** — needs drawing. CF-93.
  density: comfortable
  requiresModule: ticketing
- id: GST-065
  name: Newsletter & Preferences
  module: Marketing
  purpose: What we may send you, and how.
  capability: C00
  wave: 3
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: dataTable
  apis:
  - operationId: recordConsent
    contract: marketing-crm
    purpose: Record a consent decision
    trigger: onAction
  - operationId: listConsentPurposes
    contract: marketing-crm
    purpose: Configured consent purposes
    trigger: onLoad
  states:
    loading: Your current consents
    emptyFirstRun: —
    emptyNoResults: TODO — the filter matched nothing. The action widens or clears, never creates.
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: '**Could not save. Consent is not recorded on a failed request** — a subscription the guest
      believes happened and did not is worse than a visible failure'
    offline: '**Not available.** A consent change must reach the server to mean anything'
  navigation:
    exitTo:
    - GST-001
    inferred: true
  implementation:
    app: guest-app
    route: /newsletter-preferences
    component: apps/guest-app/src/routes/NewsletterPreferences.tsx
    status: notStarted
  wireframe:
    status: generated
    board: wireframes/P02 Guest App.dc.html#gst-065
  notes: Added 17 August for parity with WEB-027. **Not on the wireframe board** — needs drawing. CF-93.
  density: comfortable
  entryState:
    params:
    - name: subjectId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: marketing
