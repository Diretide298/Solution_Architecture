{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "design-tokens",
  "sourceRoot": "packages/design-tokens/src",
  "projectType": "library",
  "tags": ["type:package", "scope:design-tokens"],
  "targets": {
    "lint": { "executor": "@nx/eslint:lint" },
    "test": { "executor": "@nx/vite:test" },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p packages/design-tokens/tsconfig.json --noEmit" }
    }
  }
}
