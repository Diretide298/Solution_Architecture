# Conflict Register

> Every conflict raised since 30 July. **Counts and the full index are in
> `conflict-status.md`**, generated from this file — this header carried
> *"CF-01 to CF-78 · Open: 25 · Closed: 41"* until 18 August, 43 conflicts behind.

A conflict is any place where two sources disagree, or where a decision is required and
absent. It closes when a client decision, an ADR or a piece of research settles it — not when
someone stops mentioning it.

**Rebuilt 14 August.** The previous version had three defects: CF-27 appeared in both the open
and closed sections; CF-48 and CF-49 were filed under Closed while still open; and six items
raised in working sessions — CF-35, 37, 39, 40, 41, 42 — were never written in at all. One of
those was **CF-37**, which blocks roughly twelve finance operations. A register that loses its
most expensive open item is worse than no register, because it is trusted.

---

## Open — blocking

**None.** No conflict currently prevents contract, schema or build work from proceeding.

---

## Open — needs a client decision

Nothing here can be resolved by working harder.

| ID | Issue | Owner | Since |
|---|---|---|---|
| **CF-64** | **89 retention requirements, two stated periods — and RPO/RTO is a narrower question than it looked.** Only 4.3.4 (10 years, payment) and 6.1.78 (7 years) name a period; the other 87 say "configurable". **Refined 17 Aug after counting the DR sheet (CF-60):** requirements 11 and 12 ask the platform to support **configurable** RPO and RTO, not to hit a number. That does not remove the question — **a configurable RPO still needs a floor**, and offering one near zero requires synchronous replication and a second site, which is a topology decision made once. **What is needed is the tightest RPO any tenant may buy**, not the RPO for Miral. A smaller and more answerable question than this row has carried since 6 August | Dinesh + Qossai | 6 Aug |
| **CF-140** | **The delivery plan prices 7,552 person-days and its priorities contradict the dependency order the walk found.** `sources/planning/TAIS_Product_Planning_and_Delivery_Plan_4_1.xlsx` had never entered the package either — 23 epics, 445 features, 2,173 tasks, a milestone plan, a dependency matrix and a Jira import, covering 3,332 requirements. **It is a good document and two things in it need reconciling.** **(1) Resource Management is P3 — Could, at 63 matrix requirements**, and it is the single most-depended-on gap in the walk: CF-125 is cited by rows in five domains, and rentals, lockers, cabanas, instructor assignment, event resourcing, maintenance resource planning and 7.6's fifteen-row rental module all sit behind it. **A P3 prerequisite blocks every P1 that needs it.** **Access Control System is also P3 at 114 requirements**, which reads as a scoring artefact rather than a judgement — `access` is one of the best-covered contracts in the package and gate entry is the operational core. **(2) The plan splits one module across three priorities** — Notifications and Messaging appears at P1, P2 and P3; Loyalty at P1, P2 and P3; Portfolio at P1, P2 and P3; Digital Waivers at P1 and P2 — so a module is partly must-have and partly could-have, which is not a schedulable statement. **The plan's own *Missing Components* sheet independently reaches several of the walk's conclusions** — ABAC policy engine (CF-130), central monitoring and alerting (CF-134), a single notification service (BL-052), developer portal and OpenAPI docs (CF-135), performance budget (BL-098) — and marks **DevOps/IaC and CI/CD as outright MISSING**, which matches the package holding no SQL, no code and no Sprint 0. **Recommendation: re-derive priority from the dependency matrix rather than from requirement counts**, and reconcile against the 178 backlog entries before the plan is used to schedule anything **Deferred to the reconciliation — Chinmay, 18 August.** Finished once the milestone-to-wave reconciliation is done. **Resource Management's P3 rating is already resolved**: CF-125 puts it in scope, which was half of this conflict. | Chinmay + Qossai | 18 Aug |
| **CF-133** | **The platform cannot issue a tax invoice, and in the UAE that is a VAT obligation rather than a document feature.** 5.7.93 requires tax invoices, simplified invoices, corporate invoices and consolidated invoices; 5.7.94 requires full and partial credit memos linked to them; 5.10.3 requires the receipt to show *"all required fields in accordance with VAT regulations"* and **nothing anywhere states what those fields are.** `finance` is otherwise strong here — `LegalEntity` carries `taxRegistrationNumber`, `countryCode`, `currencyScale` and `fiscalYearStartMonth`; `TaxCode` supports compound tax, inclusive and exclusive treatment and effective dating; the ledger is append-only with reversal rather than edit. **The data exists and the document does not.** `subscription.generateInvoice` bills a tenant for the platform, which is a different payer, a different ledger and a different format. **Three things make this a decision rather than a build item.** A UAE tax invoice has a **mandated format** — supplier TRN, sequential numbering that cannot gap, prescribed fields, Arabic where required — and issuing a non-compliant one is a finding at audit, not a formatting complaint. **A credit memo is the paired obligation**: a refund against a tax invoice is not simply a reversing journal, it is a document with its own sequence. And **sequential numbering interacts with ADR-0013 and ADR-0014** — a local-first till that journals offline and a cell-per-region topology both make a single gapless sequence harder than it looks, and the answer differs by whether numbering is per legal entity, per venue or per till. **Also unresolved: e-invoicing.** The UAE is phasing in mandatory e-invoicing, and whether that binds in the delivery window is a question for the client's finance function rather than for us **To Qossai and finance — Chinmay, 18 August.** The open question is what a UAE tax invoice must contain: **5.10.3 requires *all required fields in accordance with VAT regulations* and nothing states what those fields are.** `finance` already carries the TRN, compound tax and inclusive treatment; the gap is the document. | Qossai + finance | 18 Aug |
| **CF-127** | **Cookie consent management is fifteen requirements, a regulatory obligation, and normally bought rather than built.** 2.6.51–2.6.65 ask for a consent banner with configurable layouts and languages, cookie categorisation into strictly-necessary, functional, analytics and marketing, **automatic site scanning to detect new and third-party cookies, pixel tags and tracking technologies**, script blocking so analytics and advertising tags do not load before consent, a preference centre, consent records with timestamp and identifier, an audit trail of withdrawals, multi-domain preference sharing, consent analytics and an administration portal. **2.6.60 names GDPR, the ePrivacy Directive, CCPA, CPRA, LGPD and PDPL**, so this is compliance rather than preference. **The package has one trace: `PolicyKind.cookie`, a policy document a tenant can publish.** That is the text, not the enforcement. **`marketing-crm` does hold a real PDPL consent register** — purposes of marketing, personalisation, profiling, thirdPartySharing, aiProcessing and transactional, with `noticeVersion`, `expiresAfterMonths`, `isRequiredForService`, source per channel and full history — **and it answers a different question.** Consent to process a guest's data for marketing is not consent to set a tracking cookie on a browser, the legal bases differ, and the second has to be enforced client-side before a script loads. **The decision is build, buy or drop.** Cookie scanning means maintaining a cookie database and re-scanning as third-party tags change, which is the product OneTrust and Cookiebot sell; building it is a standing maintenance commitment on a platform whose scope is venue operations. Buying it means a vendor on every tenant storefront and a data-residency question under ADR-0009. **Dropping it is only available if no tenant storefront sets a non-essential cookie**, which the analytics requirements elsewhere in the matrix contradict **To Qossai — Chinmay, 18 August.** Cookie consent is a buy-or-build decision, not a design one, and **script blocking is the part that makes it a product rather than a form.** OneTrust and Cookiebot exist. | Qossai | 18 Aug |
| **CF-35** | **Biometric data is sensitive under PDPL** — heightened protection, explicit consent, DPIA, stricter transfer rules. Affects Face Pass, facial readers and fingerprint enrolment. Not previously flagged; the current design treats biometrics as ordinary identity data | Allam + counsel | 13 Aug |

## Open — Softlabs to resolve

| ID | Issue | Owner | Since |
|---|---|---|---|
| **CF-21** | **Three domains have no contract and no workshop scheduled** — Developer & API (94), Device Management (60), Accreditation (58). **212 requirements, and the only blocked work left on the project.** Packs prepared 17 August (`docs/active/workshop-packs.md`) so each is an hour reviewing a proposal rather than a discovery session. **Two are less blocked than the label suggests:** Device Management is a lifecycle over `platform.device`, which already carries 20 device kinds and a heartbeat — mechanical, one real blocker in CF-33's vendor SDKs. **Accreditation is an entitlement**: admission profiles already do zone and time-bounded access, `MediaKind` already covers QR, NFC, RFID and printed, and the blacklist already revokes — what is missing is the application and review front half, ~25 operations, **not a parallel access system**. Only Developer & API needs the conversation first, because **until "is the API a product" is answered the other five questions cannot be**. **Accreditation first — it is Wave 1 and its validation appears on the scanner** **Split three ways — Chinmay, 18 August.** **Device Management closes on the device list** (CF-136). **Developer & API needs no workshop** — the four questions are separable and CF-135 carries them. **Accreditation still needs a session.** So one workshop remains, not three. | Chinmay — schedule | 6 Aug |

---

## Closed

### Resolved in the 17 August working session

| ID | Issue | Resolution |
|---|---|---|
| **CF-160** | **Two joins that did not exist, declared 24 August.** **(1) No screen said which module it belonged to.** `LicencePosition.moduleKey` was a free string and `screen.module` was prose — 38 distinct values, 124 of them literally `TODO` — so **a tenant without an F&B licence was still served every F&B screen.** `ModuleKey` is now an 18-value enum, all 476 screens carry `requiresModule`, and `check-screens` **warns when a screen reaches across a module boundary — 88 of them do today.** The grain matches how a venue buys rather than how the package is built: `fnb` is one module spanning four contracts, and `ticketing` and `access` are separate because a museum has no gates. **`core` is a value rather than an absence, because an empty field and *always on* look identical and only one is a decision.** **(2) Six AI endpoints collapsed to one abstraction.** The client F&B boards drew `suggestPrice`, `suggestRequisition`, `suggestReplenishment`, `simulateScenario`, `simulateSlaPolicy` and `publishDemandPlan` as separate operations. **Six endpoints is six places to change when a model changes**, and the model changes more often than the venue's question does. Built instead: `SuggestionKind` (13 questions), `SuggestionBasis` (heuristic, statistical, model, hybrid, manual) and three operations. **Ships answering from heuristics and the swap is a provider change, not a contract change** — the frontend, the audit record and the outcome capture stay exactly as they are. **`confidence` is null for a heuristic and that is honest**: a rule has no confidence, and dressing one up with 0.85 is the fastest way to make a manager trust a number that means nothing. **`recordSuggestionOutcome` is the operation that makes machine learning possible later** — a model needs labelled data and the only source of labels is whether the venue took the advice and whether it worked; **a platform that suggests and never records the outcome has no training set a year on.** **A rejected suggestion is the more valuable record**, being the case the current rule got wrong. **Shadow mode rather than a feature flag**: a new model runs alongside, its answers recorded and never shown, and the venue compares six weeks before anything changes on a screen. **Guard rails are required where basis is `model`** — an unbounded model in a pricing path is one bad inference from a free lunch for a thousand guests | Chinmay | 24 Aug |
| **CF-159** | **Food safety was absent from 947 operations, and it is a regulatory obligation rather than a feature.** Found 20 August auditing the client F&B hi-fi board pack: boards 5G and 5J draw `safetyBreach`, `coldChainBreach` and `correctiveActionOpen` as screen states, and **nothing in the package touched HACCP at all.** UAE food-safety records are inspected, and **a venue that cannot produce a temperature log has a compliance failure rather than a missing screen.** Built: `fnb.temperature_log`, `fnb.corrective_action`, `fnb.cold_chain_event`, four operations, a state model and F28. **An out-of-range reading is recorded, not refused** — deleting a bad reading is the one thing an inspector looks for, and an operation that rejects it teaches a kitchen to stop taking readings. **`notTaken` is a record rather than an absence**, because a log with a gap cannot be told apart from a log nobody kept. **`actioned` is deliberately not terminal**: the obvious model closes at *action taken* and that is the model that produces a year of unsigned entries nobody notices until an inspection. **A critical finding cannot be signed by the person who raised it** and the refusal is in the state model rather than in a policy document. **Steps 1 to 4 work offline and signing does not** — a walk-in freezer is where the signal is worst and the readings matter most, but a signature applied against a stale finding is a signature on the wrong thing. **The flow is written so the ordinary path is two steps**, because a compliance flow that takes six steps to record a normal reading is one a kitchen works around. **One open question for the client**: who signs in a single-person kitchen at 23:00, where the escalation rule assumes a supervisor is reachable | Chinmay | 20 Aug |
| **CF-158** | **Table ownership audit, 20 August — 18 tables written by more than one contract, and the honest answer is that most are correct.** A contract writing another's table is not a defect where the write is a side effect of its own job: **a till closing posts to `ledger.entry`** because settling a shift is a ledger act, and `orders` writing `access.entitlement` is a sale issuing a ticket. **The rule is that the owner defines the row and a foreign writer may only append to it** — `marketing.message_dispatch` has five writers and every one of them is sending a message somebody asked for. **One was a genuine defect and it broke a rule already in the package**: four operations wrote `inventory.stock_level` — `recordWaste`, `completeProductionRun`, `collectMerchandiseReservation` and `cancelMerchandiseReservation` — while `StockPosition` declares *derived from movements* and the store says `derived`. **A stored level and a movement ledger that disagree is a stock count nobody can reconcile**, which is exactly why the level is derived. Rewired to write the movement; `check-package` now refuses any write to a derived table. **And 40 tables no operation named**: 28 are children whose parent's write covers them, 10 explain themselves (default partitions, migrator bookkeeping, a job-generated code), and **three were real tables built and never surfaced** — `orders.fraud_rule` with six columns, `orders.group_booking` with ten, and `platform.denomination` built that same morning from Tanmay's review. `listFraudRules`, `setFraudRules`, `getGroupBooking` and `listDenominations` written. **`fraud_rule` holds rather than declines**, because a decline turns a false positive into a lost sale nobody hears about and a hold turns it into a review somebody can clear | Chinmay | 20 Aug |
| **CF-136** | **Device management is 60 requirements with no contract, and `tenancy` already covers about half of it — the other half includes an unauthenticated device.** Domain 16 is the third of CF-21's uncontracted domains. **What exists is better than the register implies**: `tenancy` carries `registerDevice`, `recordDeviceHeartbeat`, `listDevices` and a `RegisteredDevice` with kind, driver, identifier, workstation, model, firmwareVersion, isRequired, status and lastHeartbeatAt, across **twenty device kinds under ADR-0015's standards-first drivers** — so 16.8's ten integration requirements are all met, and the heartbeat already reports *paper low, drawer open, reader offline*. **Three gaps are operational and one is a security position.** **(1) Firmware records and does not deploy.** `firmwareVersion` is a field; nothing pushes an update or rolls one back, and a fleet of turnstiles and readers that cannot be updated remotely is a site visit per device for the life of the contract. **(2) A device is not an asset.** `maintenance` has assets, work orders, plans and history; nothing links a `RegisteredDevice` to an `Asset`, so a venue keeps two registers for one physical object and a scanner sent for repair leaves the device list saying it is merely offline. **(3) Retirement is an explicit non-decision** — `states/registered-device.yaml` says there is no terminal state deliberately, *a device is never finished*, and 16.1.6 asks for retirement, so the ADR-level reasoning should be revisited rather than quietly overridden. **(4) The security one.** Nothing anywhere provides device authentication, device authorisation, encrypted device communication or tamper detection. `common.serviceAuth` covers cell-to-cell where *there is no user, so the permission model does not apply* — a different boundary entirely. **A reader on a gate is an unattended endpoint on the venue network, and under ADR-0013 it holds an offline package of valid entitlements.** A device that cannot prove what it is, on a channel that is not authenticated, holding a bundle of credentials, is precisely what an access-control system gets judged on — and it is CF-69's SIEM question from the device side **Closable once the device list arrives — Chinmay, 18 August.** `tenancy` already carries `registerDevice`, `recordDeviceHeartbeat` and 20 device kinds under ADR-0015, so 16.8's ten integration requirements are met. **What remains is the venue's actual device list**, plus the unauthenticated-device finding, which is a security question rather than a feature one. **Closed 20 August — the device list arrived in a design document.** CF-136 had been waiting on Dinesh for a peripheral list since 12 August, and the client's POS Board 1 *Hardware & Peripherals Management* names nine kinds with status, battery and last check: payment terminal, cash drawer, receipt printer, ticket printer, barcode scanner, customer display, wristband encoder, kiosk terminal and UPS. **All nine already existed in `DeviceKind`, which carries twenty** — so the list was never the gap. **The gap was three fields**: `batteryPercent`, `lastCheckedAt` and `health`. `lastCheckedAt` is separated from `lastHeartbeatAt` deliberately — **a printer with no paper heartbeats perfectly**, and the client's board shows both columns because an operator needs both answers. `health` is derived rather than reported, because **a device does not know whether it is healthy and asking it produces a fleet that is 100% healthy and 12% broken.** The Qossai half — what an unauthenticated device may do — remains and is recorded in `BL-179` | Chinmay | 20 Aug |
| **CF-157** | **Frontend review, 20 August — 59 verdicts from seven reviewers, and 21 of the 39 needs-work rows were one defect.** **Operations had been attached to screens in bulk.** `GST-012 My Tickets`, `GST-013 Ticket Details`, `GST-019 Order History` and `WEB-018` each declared exactly one operation — `transferOrderTickets` — so **a guest could give a ticket away and could not read one.** `GST-020`, `WEB-009` and `WEB-020` declared the identical seven-operation set regardless of purpose, and **Sanket and Pranay caught it from opposite ends**: *no device management on WEB-009* and *why is there Guest Device Related APIs on GST-020*. Both right. **Most 'need GET API for X' rows were already built and not wired** — `getGuestProfile`, `getOrder`, `listOrders`, `getMyMemberships` and `acquireLease` all exist and are all guest-callable. **Two were genuinely missing and Deep found both.** There was no entitlement read surface at all: `getMediaEntitlements` and `lookupTicket` are staff-only, so **a guest could not read their own ticket** — and it only became visible once `access.entitlement` existed, which was 18 August. `listMyEntitlements`, `getEntitlement`, `getEntitlementCredential` and `getEntitlementHistory` built, with **the credential split from the entitlement because a list of tickets is a convenience and the credential admits somebody**, and **not-found and not-permitted returning the same response** so an id cannot be probed. And `verifyGuestEmail`: `registerGuest` created an account and nothing verified the address, and **an unverified account can receive somebody else's tickets** because a transfer is offered to an address. **Unverified accounts still buy** — blocking the sale loses it and teaches nobody. **Hrushikant found a broken join table**: `identity.role_permission` had one column, `role_id`, because `Role` carried no permissions array — **a role that grants no permissions is not a role**, and the schema audit that ran the same day asked whether every table had columns, a relationship and an owner, and this one had all three. **Tanmay was right about denominations**: a jsonb blob cannot deactivate a note withdrawn from circulation, and deleting the value would rewrite every past cash-up. Now `platform.denomination` with `isActive` and a counting `sortOrder` — **and his seed list carries `Coin, 0, COIN`, a zero-valued coin that a table refuses and a blob accepts silently.** **Pallavi's sharpest note is not a field**: a platform admin operating a tenant screen without first choosing a tenant is a screen that acts on the wrong tenant. **Her ADM-012 rejection accepted** — tenant isolation is enforced by `scope_path` in the data layer, and a UI screen for it implies it is optional. 11 screens rewired, and **the flow checker caught me dropping `getGuestLink` from GST-015**, which F19 step 1 depends on | Chinmay | 20 Aug |
| **CF-156** | **Schema audit, step 4 of 4 — the entitlement table did not exist. 18 August.** All 347 tables tested for columns, reachability, relationships, description, identity, tenancy scope and duplicate column names. **The finding was one table and it was the most important one in the package.** 33 tables in `orders`, seven in `access`, and **none of them stored an issued ticket.** The package sold products, defined `EntitlementTemplate`, modelled `EntitlementStatus` with six lifecycle states, wrote `states/entitlement-status.yaml`, computed `TicketStatus` from *the entitlement and its scans*, recorded `ScanEvent.ticketId`, transferred `ticket_transfer.ticketIds` and issued `wallet_pass.entitlementId` — **eight artefacts referring to a row that was never written.** **`validateAccess` read the template and never the instance**, so nothing decremented an entry count: a ten-entry pass with no counter admits forever. **And `suspendEntitlement` wrote the template, which would have suspended the pass for every guest who held one.** `access.Entitlement` written — 24 columns, ULID id matching `TicketStatus.ticketId`, `entriesUsed`, `frozenDays` held rather than replayed because **a gate has to answer in under 300ms and cannot walk a history to decide validity**, `issuedVia` because a sold entitlement carries deferred revenue and an invitation carries a marketing cost, and `supersedesEntitlementId` because **a ticket appearing from nowhere is indistinguishable from a fraudulent one.** 29 lineage edges rewired across 22 operations, and four traceability rows repointed from the definition to the instance. **`check-package` now refuses a column referencing a table that is stored nowhere** — every artefact here was individually consistent and the thing they all pointed at was absent, which is the only class of defect a per-artefact checker cannot see | Chinmay | 18 Aug |
| **CF-155** | **Workflow audit, step 3 of 4 — the flows were sound and three contracts had none. 18 August.** Every flow tested for structure, step integrity, branch coverage and contract reach. **The flows are the best-built artefact class in the package**: every step names a screen and an outcome, every flow carries branches, exit states and offline behaviour, and not one step lacks both an operation and a screen. **The gap was coverage** — `resources`, `venue-map` and `public-api` had no flow at all, all three written the same day, so three new capabilities had no end-to-end story. **F25 a guest rents a cabana**, which is the flow that shows why a resource is not a product: **a product with an availability envelope sells the slot in the first three steps and has nothing to say about the last two — and the last two are where a venue loses money.** **F26 a venue maps its site**, where steps 2 and 3 are deliberately separate operations in separate contracts: **a mis-parsed layer and a bad suggestion look identical if one step does both**, and the operator is left able to say only that the map is wrong. **F27 a developer integrates**, which is the flow that shows what decision D2 bought — **with synthetic sandbox data there is no residency question, no masking to review and no legal sign-off, because there is no production data to isolate.** **The flow checker caught four things I had wrong.** Two steps called operations their screens did not declare, and **in one case the screen was wrong** — `BO-097` genuinely calls the deposit operations and now says so — while in the other **the flow was wrong**: `issueApiToken` is a call a developer's code makes, not a button somebody presses. `certifyIntegration` was on the developer's screen and belongs on the Softlabs one. And **F27 claimed wave 2 while reaching a wave 3 screen** — a journey is only as early as its latest screen, and claiming otherwise would have promised a marketplace that does not exist yet. **28 of 28 contracts now touched by a flow. Flows 40% to 45%** | Chinmay | 18 Aug |
| **CF-154** | **API audit, step 2 of 4 — 292 operations reached nothing, and two contracts had no screen at all. 18 August.** Every operation tested for duplication, verb/name agreement, naming consistency and reachability. **Duplication: none.** 118 pairs share a read/write footprint and every one is a `get`/`list` pair over one table, which is correct. **Naming: consistent** — no noun carries both `set*` and `update*`, no `delete*` beside a `remove*`, both spellings of enrol absent. **29 near-identical summaries are summary-writing laziness rather than duplication**: `createCampaign` and `createDonationCampaign` both read *create a campaign*, and they are different things badly described. **The finding was reachability.** 292 operations — 32% — had no screen, no flow, and no service or device audience. **262 were a wiring gap** in contracts that already have screens; **30 were two contracts with no screen anywhere**, both written the same day: `resources` (9) and `public-api` (21). **`P14 Developer Portal` created, 8 screens** — reference, organisation, clients, sandbox, webhooks, usage, marketplace and a Softlabs-only administration screen. **Five `resources` screens added to P08.** **Every contract now reaches a screen**, and domain 13 went 84 to 91 contracted. **Two components added to the vocabulary** — `treeNav`, because a flat list cannot express 28 contracts of 923 operations and **a developer sends a colleague a link to an operation, so selection is a route rather than a scroll position**; and `codeBlock`, because **a sample with a placeholder token teaches nothing about auth, which is where integrations actually fail.** **And the app manifests claimed to be derived and were not** — every one carries *Derived from screens/*.yaml. Do not hand-edit* and nothing derived them, so adding P14 broke `check-package` and the fix was to hand-write the file the header forbids hand-writing. **A header that lies about how a file is maintained is worse than no header**: it tells the next person not to edit the thing they now have to. `tools/derive-frontend.py` written and wired into `refresh.sh` | Chinmay | 18 Aug |
| **CF-153** | **Screens audit, step 1 of 4 — nine screens were states. Collapsed 18 August.** Every screen classified against three tests: is it a state, is its content real, does it reach the contracts. **Fifteen candidates, nine collapsed, six kept after inspection.** The scanner was the case: `SCN-003 Ready to scan`, `SCN-004 Admitted`, `SCN-005 Denied`, `SCN-006 Override`, `SCN-010 Blacklisted` and `SCN-012 Offline scanning` shared an identical seven-operation set, and `SCN-004`'s own purpose said *readable at arm's length, gone in 1.5 seconds* while its route was `/access/admitted`. **A route you navigate to is not a thing that disappears in 1.5 seconds**, and a gate doing forty guests a minute cannot afford a page load per scan. Same in `P06`, where the names admitted it: **`Scan — admitted` is a state of `Scan` with a dash in it.** Absorbed as named states — `outcomeAdmitted`, `outcomeDenied`, `outcomeBlacklisted`, `offline`, `overrideDialog` — each keeping its own copy, and **`outcomeBlacklisted` stays separate from `outcomeDenied` because the operator response differs: a denied guest may be helped, a blacklisted one is a security matter and the screen must not offer an override.** **Six were kept after checking.** `WEB-013 Order Confirmation` reads like a state and is not — it has its own route, a banner, two actions and the job of *delivering the tickets*; **a receipt is a job, not a state.** Same for `GST-010` and `GST-028`. `SCN-013 Offline journal` and `SCN-015 Offline package` are a decision log and a rule-bundle status, which are screens. **The cross-artefact checks caught what I missed twice**: three cross-platform `exitTo` links and five flow steps still pointed at `SCN-004`, and `check-wireframes` refused boards drawing screens that no longer exist. **The drawings were right and only their status changed**, so `wireframe.stateBoards` now records which anchor draws which state rather than deleting design work. **Checked back against the matrix: one row mentions a denial and it asks about validity rules, not a screen. No traceability row cited an absorbed screen. Nothing lost.** 377 → 368 | Chinmay | 18 Aug |
| **CF-152** | **Phase 0 of the screens rework — the model, before any design. Done 18 August.** Chinmay: *the boards are real stupid, they are not built how an actual frontend or UX would build.* **The audit agreed and quantified it**: of 377 screens, **375 have no accessibility note, 355 have navigation marked , 225 have a layout of one component or none, 172 copied their states from a pattern and 138 derived their purpose from the screen name.**  had one component: . **50 screens shared the loading state .** The package was honest throughout — every generated field carries a note saying so — and nobody revisited it. **The diagnosis: the model was built to satisfy the contract, not to describe an interface.** It answers which operations, which platform, which wave, and those are the right questions for traceability and not the ones a designer asks. **Phase 0 splits  into three**: , ,  — a list with no records, one whose filter matched nothing, and one the person may not see are three different screens, and **an empty list where the truth is a permission is a lie a person will act on.** 377 screens migrated: 327 carried their existing copy, 317 gained , 68 gained , **50 need first-run copy written because the old string said nothing** —  **does not say what to do about it**, which is the whole finding in one string. ** added** — compact, comfortable, touchLarge — because a back-office table and a kiosk button grid are not one screen at two widths. ** added** for field labels, validation messages, empty-state actions and confirmations: the copy is a deliverable and it was nowhere, and *are you sure* is not a question.  enforces all of it. **And I broke the checker while writing it** — a local  shadowed the vocabulary parameter and produced 218 false failures from one variable name | Chinmay | 18 Aug |
| **CF-135** | **Whether TICVAI is a platform others build on — 94 requirements, no contract, and the answer changes what gets built.** Domain 13 is one of CF-21's three uncontracted domains, and walking it shows it is not one gap but four separable ones with different answers. **(1) Documentation is nearly there and the portal is not.** ADR-0024 makes the contract the deliverable — 25 hand-authored OpenAPI files *are* the schema documentation, and `check-package` enforces that no screen may name an operation no contract defines. What 13.1.1–13.1.5 ask for is a portal that renders them with examples and a try-it console, which is a front end over an artefact that already exists. **(2) A developer is not a partner.** `registerPartner` onboards a commercial reseller with an agreement, credit terms and commission; 13.1.6–13.1.15 ask for a developer identity with an organisation, API keys, client secrets, OAuth and revocation — **and outbound credentials do not exist anywhere in the package**, which 2.7.52 asked for from the partner side too. **(3) Rate limiting has a screen and no operation.** `ADM-015 API Rate Limit & Quota Management` is a specified screen on the platform admin console and **nothing backs it** — CF-49's shape, a decision reaching an artefact without passing through a contract. **(4) Events stop at the boundary.** 29 domain events are defined and consumed inside the platform; 13.3.17–13.3.23 ask for external publication with filtering, routing, retry, delivery monitoring, retention and **replay for recovery** — that last one is what makes it an event platform rather than a webhook, and it is a materially larger commitment. **The commercial question comes first.** If integrations are a handful of named partners, an event feed and issued credentials suffice and most of 13.1 is unnecessary. If TICVAI is sold as extensible — 13.1.49 asks for integration certification and 13.1.50 for a marketplace — that is a product line with a support obligation, and it is not in the phasing paper **Identity is answerable — Chinmay, 18 August.** A developer proves who they are the way anyone else does: **MFA, biometrics, or a Microsoft or other enterprise identity** — `identity` already carries SSO, `enrolMfaMethod` and, since 18 August, face enrolment. **That removes developer identity from the open question**, leaving the portal, the sandbox and rate limiting. **Decided and built 18 August. Closed.** Five decisions and the 28th contract, `contracts/satellite/public-api.yaml` — 22 operations, 11 schemas, 4 state models, ADR-0026. **82 of the 94 domain-13 rows now contract.** **D1: extensible platform with third-party listings**, superseding the earlier reading of 10 August which settled integration direction and said nothing about onboarding. **D2: synthetic sandbox data only** — no cloning, no masking, which removes the PDPL and DESC exposure entirely and **decouples the sandbox from the tenant-isolation question, because there is no production data to isolate.** `Sandbox.containsProductionData` is a field rather than an assumption, so a reversal is visible. **D3: one shared sandbox**; 13.2.2 declined and recorded, because **development and certification are generic while acceptance is tenant-specific** and the 30 July staging environment does the second. **D4: `public-api`, not `platform`** — platform already denotes TICVAI everywhere. **D5: commercial model is configuration** — `ApiLicence` is the surface, and the rates stay CF-135c. **CF-135a closed with it**: 2.7.52, 7.1.25 and 7.1.30 each asserted their own credential model and `ApiClient` is now the one that exists. **CF-141 resolved for the public API by ADR-0026** — one current version, one deprecated, twelve months notice, **and the commitment is in the model rather than a policy document, because a stated minimum is what stops it shortening under pressure.** The single-version position stands for internal contracts. **Third-party code does not execute inside TICVAI** — stated in the ADR rather than left unruled, because executing submitted code in a multi-tenant platform is a different product with a different threat model. **Twelve rows remain and they are honest**: SDKs, an API explorer, Postman collections and a support portal are generated from or built over the OpenAPI files ADR-0024 already makes the deliverable — **frontend scope, not a contract gap** |
| **CF-83** | **Abandoned-cart recovery may not be lawful as specified, and I built it without asking.** 22.3.6 and 2.6.45 require identifying incomplete purchases and sending reminders by email, SMS, WhatsApp or push. **A guest who abandoned a cart never completed a purchase**, so there is no transaction to make the message transactional — it is a marketing message to someone who gave an email address for a purchase that did not happen. Under PDPL that likely needs consent, and the point of collection is a checkout the guest walked away from. **The contract carries `hasContactPoint` and `remindersSent` as though this is settled.** Needs a legal answer before the campaign is built, not after: is a recovery email transactional, does it need explicit opt-in, and does the answer differ for an anonymous cart versus a signed-in guest **Confirmed a requirement, and lawful with conditions — 18 August.** Abandoned cart appears **five times**: 22.1.2 templates, 22.1.15 event-triggered, 22.3.6b recovery automation, 22.4.19 newsletter journeys, 22.8.23 chat. Not a stray line. **UAE PDPL requires explicit, informed, specific opt-in with pre-checked boxes invalid, and Article 17(1) gives an unqualified right to object to direct marketing with no balancing test.** So it is buildable and **an abandoned-cart message cannot ride the transactional consent a guest gave to receive their ticket** — it needs the marketing purpose. The mechanism exists: consent gates every send and `MessageTrigger` checks suppression at send. **Counsel confirms the purpose binding, not whether to build.** **Closed 18 August — researched, and it is both a requirement and lawful with conditions.** Abandoned cart appears **five times**: 22.1.2, 22.1.15, 22.3.6b, 22.4.19, 22.8.23. Not a stray line. **UAE PDPL requires explicit, informed, specific opt-in with pre-checked boxes invalid, and Article 17(1) gives an unqualified right to object to direct marketing with no balancing test.** So it is buildable, and **an abandoned-cart message cannot ride the transactional consent a guest gave to receive their ticket** — it needs the marketing purpose. The mechanism exists: consent gates every send and `MessageTrigger` checks suppression at send. **What counsel confirms is the purpose binding, not whether to build.** |
| **CF-85** | **Four holding windows were set by me as defaults and all four are commercial decisions.** **Cart expiry and extension cap** — how long a guest holds capacity while deciding, and how many times they may extend. Too short loses sales; too long removes capacity from sale for someone who left a tab open. **Waitlist offer window** — how long the person at the front holds an offer before it moves on. **Table reservation duration** — how long a cover is held, which decides whether an outlet can book a second sitting. **Table no-show and deposit policy** — 4.9.x asks for reservations and says nothing about what happens when nobody arrives. Each is configuration, so the platform does not care; **each has a revenue consequence, so the venue does.** I chose sensible defaults and they are guesses **Industry practice found — 18 August, and my defaults were wrong.** Ticketmaster holds seats **10 minutes** and sets checkout time by demand so it varies by event and time of day. Ticket Tailor offers four bands from 30–120 minutes down to 5–15, plus an 8-minute mode split across checkout stages. Stripe Checkout allows 30 minutes to 24 hours. **The pattern is unanimous: the window is per-event and demand-driven, not a platform constant.** Recommendation: `cartExpiryMinutes` and `seatHoldMinutes` venue-configurable with a **per-performance override**, defaulting to 10 — **a Friday-night onsale and a Tuesday museum booking should not share a timer.** **Closed 18 August — researched, and my defaults were wrong.** Ticketmaster holds seats ten minutes and sets checkout time by demand; Ticket Tailor offers four bands from 30–120 minutes down to 5–15, with an 8-minute mode split across stages; Stripe allows 30 minutes to 24 hours. **The pattern is unanimous: the window is per-event and demand-driven, not a platform constant.** Recorded as venue-configurable with a per-performance override defaulting to 10 — **a Friday-night onsale and a Tuesday museum booking should not share a timer.** |
| **CF-112** | **Two guest-facing screens are drawn in the client storyboards and specified nowhere.** **A branded queue / waiting room** (guest board 7 panel 6) — queue position, estimated wait, venue branding — which is **CF-48's Q2 virtual waiting room, open since 13 August as "edge infrastructure, not a product contract" while the client has drawn it as a product screen.** And **a branded maintenance / upgrade page** (boards 7.7 and 8.10) with an expected return time and support contacts. **Both are what a guest sees when the platform is under strain**, which is when a white-label venue cares most what it looks like, and neither exists in `screens/`. Also drawn and unspecified: **UAE PASS sign-in** as a first-class option beside email and mobile OTP, and **Add to Apple Wallet** on the confirmation and ticket screens, where `GST-018` covers calendar reminders and not wallet passes **Added to white-label — Chinmay, 18 August.** The branded queue / waiting room and the branded maintenance page become part of white-label mode, so a tenant styles their own. **These are what a guest sees when the platform is under strain**, which is when a white-label venue cares most what it looks like. **Closed 18 August.** The branded waiting room and maintenance page join white-label mode, so a tenant styles their own. **These are what a guest sees when the platform is under strain**, which is when a white-label venue cares most what it looks like. |
| **CF-67** | **Virtual and hybrid events appear once and nowhere else.** 1.3.30 requires "physical, virtual and hybrid events with configurable attendance rules and access methods". Nothing else in 3,184 requirements mentions streaming, a virtual access method, or online attendance — no contract, no screen, no entitlement kind. Either a stray line, or a product capability nobody has costed. **A hybrid event changes admission fundamentally**: an entitlement with no gate to scan at needs a different validation path entirely **Confirmed in scope — Chinmay, 18 August.** Virtual and hybrid events are real, not a stray line. **Access is by an individually generated link paired to an account**, so an entitlement with no gate to scan at gets its own validation path. Streaming and online attendance are in scope and need contracts written. **Closed 18 August.** In scope: individually generated links paired to accounts, streaming and online attendance. **A hybrid event needs a validation path with no gate to scan at**, and the link is that path. The contracts are Track 3 work rather than an open question. |
| **CF-48** | **Q2 Virtual Waiting Room.** ADR-0012 separated Q1 (ride queues, contracted) from Q2 (on-sale throttling). Q2 is edge infrastructure, not a product contract — it holds traffic before it reaches the application at all. **Build or buy.** Recommendation: buy. The vendors are mature, and a waiting room built badly fails on the one day it exists to protect **Both, and they are different products — Chinmay, 18 August.** A virtual waiting room for *rides and queues* is the in-venue queue: position, estimated wait, call-forward. A waiting room for *online sale* is a demand gate in front of the storefront at an onsale — no queue position that means anything, just an ordered admission to a checkout that would otherwise collapse. **They share a screen idiom and nothing else**, and building one as the other is how an onsale falls over. **Closed 18 August.** Both are in scope and they are different products — the in-venue queue has a real position, the onsale gate has an ordered admission to checkout. **They share a screen idiom and nothing else.** The branded surface is CF-112's white-label work. |
| **CF-41** | **AI is a primary navigation tab** in the employee app — Home, Tasks, Scan, **AI**, More. AI-06 sits in Wave 2, but the app shell cannot ship without the tab existing. Move AI-06 to Wave 1, ship a disabled tab, or reorder the navigation **In scope — Chinmay, 18 August.** The AI navigation tab is in the employee app shell, as employee storyboard board 1 draws it. **Closed 18 August.** In scope, decided by Chinmay; the employee storyboard confirms the tab. |
| **CF-40** | **Training & Knowledge Base and Employee Recognition have no provenance.** Both appear in the employee app design reference and in **neither the matrix nor any of the seven MoMs**. Recommendation: SOP repository in, LMS out, kudos out. The question that resolves it — does the tenant already have an LMS? If so the answer is an integration, not a build **In scope — Chinmay, 18 August.** Both features are built. **Closed 18 August.** In scope, decided by Chinmay. No rows blocked and nothing further to design. |
| **CF-57** | **AI configuration assistance is Wave 1 and is not in the phasing paper.** Qossai: "one of the most important item for the AI… it's supposed to be from beginning." An admin describes what they want and the assistant walks the configuration. Not among the fifteen Phase 1 applications. Two further data-independent candidates named: map layout generation and ticket artwork. **The phasing paper needs revising** **Scope defined 18 August — and a correction.** The five requirements are 8.8.10 products, 8.8.11 pricing, 8.8.12 promotions, 8.8.13 seating, 8.8.14 calendar. **The venue map is not among them**; `generateVenueLayout` and ticket artwork are separate candidates from the MoM, and Chinmay confirms seating should reach the map builder once geometry is imported (CF-123). **The correction: `generateConfiguration` read `catalogue.product` and nothing else.** *"Create VIP seating in the first three rows with a 30% premium"* cannot be answered without the seat map, the price categories and the venue's own vocabulary. **Data-independent and context-free are different claims** — it needs no training corpus and no history, and it needs the venue's entire current configuration. Now reads 22 tables: catalogue, price lists, promotions, seat map, performances, region settings. **No orders, no scans, no guests.** And the bootstrapping case is now stated: **a brand-new tenant has no seat map, so *first three rows* means nothing** — on an empty venue the assistant creates rather than amends, and amending touches things already sold. **Closed 18 August.** Scope defined and the reach corrected — `generateConfiguration` read one table and now reads 22. **Whether the phasing paper is revised is a planning act rather than an open design question**, and it belongs to CF-140. |
| **CF-132** | **The guest is a single subject, and the matrix asks for a portfolio — ten times, across ten sections, and section 5.5 specifies it in full.** Searching `portfolio`, `accountHolder` and `linkedAccount` returns **nothing anywhere** in 778 operations. The requirement has surfaced in every domain walked so far: family profiles (19.2.8), family structures with a primary holder (5.5.5), one account managing family and friends (2.6.47), family and group sales at the gate (2.13.42), primary member and dependants (2.14.14), corporate memberships (2.14.15), family and group registration (4.3.11), shared family wallets (4.3.26, 5.5.6), corporate spending wallets (4.3.27), pooled family loyalty (5.4.16), corporate loyalty (5.4.17), household profiles and relationship mapping (5.3.14, 5.3.26). **Section 5.5 is where it is written properly**: multiple portfolios per account for dependants, employees or students; a primary holder assigning entitlements to members; transfer between linked portfolios; shared wallets with individual transaction tracking; temporary and permanent sharing; merging portfolios while preserving balances and history; configurable restrictions on transfer, spending and age; organisations managing employees, contractors and visitors under one hierarchy. **The platform models a guest as `subjectId` throughout, and that is a good decision that does not reach this.** `guestLinkId` links a guest across cells (ADR-0010) and is not a family. `transferOrderTickets` moves a ticket between subjects and is not delegation. `Wallet.subjectId` is one guest, so a shared balance has no owner and no spend rule. **Three things make it structural rather than additive**: a portfolio owns entitlements and money, so every balance and entitlement gains an owner that is not the buyer; **age restrictions and spending limits on a child's portfolio are child-safety controls**, not preferences; and a corporate hierarchy here overlaps the partner hierarchy of 2.7.51, which is the same shape in a different contract and should not be built twice. **This is the most-repeated gap in the walk and it is not in the phasing paper** **Reclassified 18 August: Softlabs decides, client is informed.** Section 5.5's primary holder assigning entitlements, transfer between portfolios and shared wallets with individual tracking are **delegated authority rather than a household structure** — the same question CF-131's policy engine answers. `Grant` already carries principal, role, permission, scopePath, effect and validity. **It also absorbs BL-028 group leader and BL-035 corporate allocation**, which a family model would leave unserved. **Built 18 August as delegated authority, not a household table. Closed.** Section 5.5 asks for portfolios ten times across ten sections — a primary holder assigning entitlements, transfer between linked accounts, shared wallets with individual tracking — and **every one reduces to the same question: who may act on whose behalf, over what, and until when.** `Grant` gained `subjectId`, `overSubjectId`, `overObjectRef`, `delegationKind`, `quota` and `isRevocableBySubject`, with `listDelegations` and `grantDelegation`. **A primary holder assigning an entitlement is a grant; a group leader holding twelve tickets is a grant; a corporate account enrolling members is a grant with a quota; a shared wallet with individual tracking is a grant over a balance, and the transaction log already records who spent.** **A household table would have answered one of those four.** It also inherits the audit — who granted what to whom, when, and whether it expired — which a bespoke structure would have needed built. **This does not make the work smaller. It makes it one piece of work rather than four.** Cycles are refused, and so is a minor granting authority over an adult — the second is safeguarding rather than modelling. `isRevocableBySubject` exists because **a delegation nobody can end is a delegation somebody will regret**. |
| **CF-129** | **Three capabilities the platform does not have, found by walking domain 2, and each is a module rather than a gap.** **(1) Digital waivers — fourteen requirements, none of them partly served.** `signature` exists only on maintenance work-order attachments and inspection items, and `signaturePad` is a device kind with nothing to sign. Three requirements make it more than a form: **2.15.9 makes ticket issuance conditional on waiver acceptance**, putting it in the purchase path rather than beside it; **2.15.13 requires the exact accepted version retained**, which is legal evidence and the same reasoning `white-label.Policy` already applies — *a guest who consented to version 3 consented to version 3*; and **2.15.7 requires parental consent for a minor**, a distinct legal act with its own capture and retention. A waiver is what a venue relies on after an injury, so an unversioned or unretrievable one is worse than none. **(2) Recurring guest billing — `subscription` looks like the answer and is not.** It is the Control Plane, running outside any cell because it provisions cells, and it bills tenants for the platform. A guest paying monthly for an annual pass is a different ledger, a different payer and a different failure mode, and it brings stored card credentials, mandates and PCI scope the current payment model does not carry. **The `subscription` state model already mentions a dunning period that no contract declares** — CF-49's shape, found by the capability search reporting a capability present only in states. **(3) Guest data capture — five appearances across five sections**, at the till (2.1.13), the CRM (2.5.3), the agent desk (2.8.6), the order (2.12.6–2.12.10, the most fully specified) and the front gate with a sampling rate (2.13.24). Mandatory fields by condition, validation rules, which party enters which field, surveys injected at points in the journey. **CF-69's DET and DCT reporting is fed from exactly this data**, so it is likely a compliance obligation rather than a feature, and 2.9.17's residency-based pricing depends on it. **All three are scope**: none is in the phasing paper, and waivers and capture both carry legal exposure that grows with time-to-decision **Reclassified 18 August: Softlabs decides, client is informed.** A waiver, a survey and a point-of-sale capture form are one configurable mechanism — **three implementations would drift on the version rule first.** **Raised rather than decided**: 2.15.9 makes ticket issuance conditional on a signed waiver, and whether that e-signature is enforceable in the UAE is counsel's question. **Built 18 August — one mechanism, not three. Closed.** A waiver is a form with a signature, a survey is a form with a scale, a demographic capture is a form at the point of sale. **Three implementations would drift on the version rule first**: a waiver signed against version 3 must stay bound to version 3, and an NPS score means nothing if you cannot say which question produced it. `FormDefinition`, `FormField`, `FormSubmission` and `GuestDocument`, with four operations. **2.15.9 puts this in the purchase path** — ticket issuance is conditional on a waiver, so `getWaiverStatus` is offline-capable and **names which waiver is missing rather than that one is**, because a guest told *you need to sign something* asks which. **A minor cannot waive their own rights**: `onBehalfOfSubjectId` names the guardian, and an unsigned or self-signed minor waiver is worth nothing at the moment it matters. **`isPersonalData` is marked per field, because retention is decided per field** — a survey answer and a medical condition on one form have different lifetimes. And `GuestDocument` is deliberately not `assets`: **a passport scan is not a marketing asset**, and one careless query against a shared store returns both. |
| **CF-130** | **Two access decisions the client has to make, and one of them is a legal question before it is a design one.** **(1) Gendered admission.** 3.2.45 requires **automatic gender recognition, rejecting guests who do not match a designated gender**, and 3.2.46 restricts male guests entering during Friday Ladies Night; 3.6.30 prices Ladies Night as a product. **Nothing anywhere implements any of it.** The two requirements do not ask for the same thing and the difference is the whole decision: **inferring gender from a face is biometric processing of a special category under PDPL, with an accuracy rate and a discrimination consequence** — a misclassified guest is refused entry in public, at a gate, in front of a queue — whereas checking a declared attribute already held against the entitlement is an ordinary rule. 3.2.46 says *validated with rule-based logic*, which is the second; 3.2.45 says *automatic recognition*, which is the first. **CF-35 is already open on biometric data under PDPL** — heightened protection, explicit consent, DPIA — and this is the sharpest instance of it. Needs counsel, then a client decision, before either is designed. **(2) Attribute-based access control — forty-eight requirements against a model chosen deliberately.** `identity.Grant` is principal, role, permission, scopePath, effect and a validity window: **role-based with scope inheritance, settled by ADR-0002 and ADR-0018 and implemented through `resolvePermissions` and `Session.permissionsByScope`.** 3.3 asks for policies evaluated against membership, accreditation, segment, venue, attraction, device, capacity, occupancy and risk attributes, with a visual builder, templates, versioning, pre-deployment simulation and approval workflows. **About a third of 3.3 is already met** — real-time evaluation, caching, offline evaluation, least privilege, temporary grants expiring automatically, audit of policy and decision, tenant and venue scoping, inheritance. The question is whether the remaining two thirds justify replacing a settled architecture. **Worth noting for the client: 3.3 mixes staff authorisation with guest admission rules**, which this package separates on purpose into `identity` and `access`, and several 3.3 requirements are already served by admission profiles rather than by permissions **Configured at venue level — Chinmay, 18 August.** Gender-segregated access varies by region and the venue is where it is known. **The legal basis is still an input we need**, and configurability does not remove the obligation to know what the law requires before designing to it. **Decided and built 18 August. Closed.** **Where it comes from matters and I should have quoted it first**: 3.2.45 asks Face Pass to *support automatic gender recognition and reject customers who do not match the designated gender segment*, and 3.2.46 asks it to restrict male guests during *Friday Ladies Night* with *rule-based facial recognition validation*. **This is a commercial event, not a religious policy** — 3.6.30 lists Ladies Night beside two-park and annual passes, as a price. **The resolution: the platform does not infer gender.** A Ladies Night ticket is already gendered at the point of sale, so the gate checks the entitlement the platform issued — deterministic and already contracted through `admissionProfile`. **And these events are staffed**, which Chinmay pointed out and which changed the analysis: a classifier that overrules a steward who can see more than it can is a machine and a human disagreeing while a guest waits. **`genderVerification` is a switch, not an implementation** — `deviceAssisted` available only where the venue's hardware reports the capability, advisory to the steward rather than decisive at the turnstile, following ADR-0015. **Not everything needs to be built.** `overrideRateAlertThreshold` exists because **an override rate near zero means the steward has stopped deciding**, which is the number that says whether the safeguard is working or decorative. **Two corrections to my own analysis on the way**: I claimed this was biometric special-category data under PDPL and the research says that is too strong — EDPB Guidelines 3/2019 hold that classification without a template *does not fall under Article 9*, though 05/2022 pulls the other way and **UAE-specific guidance was not findable, which is the real finding for Allam.** And I argued the accuracy cost was decisive when the gate was staffed all along. **The ABAC half (BL-110) was never a client question** — Chinmay reclassified the policy engine as a Softlabs decision, and it is unblocked |
| **CF-69** | **Four named integrations have no trace in any contract.** **DET and DCT** — the Dubai and Abu Dhabi tourism authorities — are almost certainly mandatory visitor reporting rather than optional, and nobody has asked what the interface is. **Al Hosn** is the UAE health pass, likely dormant, worth confirming. **SIEM** has no contract because security events have no catalogue: `identity.authz_audit` exists and nothing ships it anywhere. All 16 hardware integrations resolve to a device kind and are covered by ADR-0015 **Researched 18 August, and it changes shape.** DET is Dubai's authority for planning, supervising and marketing the tourism sector, and licenses hotels, tour operators and travel agents. **No published operator reporting API for attractions was found**; the Tourism Dirham obligations attach to holiday-home and short-term-rental operators, not attraction ticketing. 2.6.32 asks for *providing event info like date/slot/seat/price*, which reads as **a content feed to the tourism boards' own channels rather than a compliance integration.** That makes it a marketing integration rather than a blocker — **and the question for Qossai is what we were asked to send them, and who asked.** **Researched 18 August as Chinmay asked, and it is not what the register assumed. Closed as narrowed.** DET is Dubai's authority for planning, supervising and marketing tourism and licenses hotels, tour operators and travel agents. **No published operator reporting API for attractions exists**, and the Tourism Dirham obligations found attach to holiday-home and short-term-rental operators rather than to attraction ticketing. 2.6.32 asks for *providing event info like date/slot/seat/price*, which reads as **a content feed to the tourism boards' own channels rather than a compliance integration.** That reclassifies it from a blocker to a marketing integration. **One question remains for Qossai and it is a sentence: what were we asked to send them, and who asked.** Recorded rather than left as a four-entry blocker. |
| **CF-137** | **Four marketing capabilities the platform does not have, found by walking the last domain — journeys, gamification, surveys and SEO.** `marketing-crm` is a large contract that answers a great deal: campaigns with four kinds, six channels, segments as definitions rather than lists, consent gating every send, cases with a full lifecycle, reviews with moderation, and the omnichannel conversation platform CF-99 built — *the whole thread and the assistant summary travel with the handover*, `withAssistant` and `queued` as separate states because *time with a bot is not time in a queue*. **Four things sit outside it and each is a module.** **(1) Journey automation.** `CampaignTrigger` fires one campaign from one event after a delay; a journey is a graph with steps, branches, waits and re-entry rules, and sixteen requirements at 22.3 depend on it — membership, loyalty and wallet lifecycle, onboarding, re-engagement, reservation follow-up, service recovery, cross-sell. **It compounds the event-trigger gap**: a journey with no business events to branch on cannot start. **(2) Gamification** — challenges, achievements, badges, streaks, milestones, leaderboards. **Third appearance**: the guest app at 19.2.73–75, a `ModuleKey.gamification` toggle pointing at nothing, and twenty requirements here. The currencies it awards into all exist, so the missing piece is the game. **(3) Surveys** — twenty-two requirements including **NPS, CSAT and CES, which are standard instruments with defined calculations an operator reports to a board**. `submitReview` captures a star rating and is a different thing. This is the sixth appearance of the capture gap already recorded as CF-129. **(4) SEO — twelve requirements and nothing anywhere.** `CMS-013 SEO & Metadata` is a specified screen with **zero operations behind it**; `ContentPage` has a `slug` and no canonical URL, no metadata and no redirect when a slug changes, so every URL change silently breaks inbound links. **For a venue selling tickets online, organic search is a primary acquisition channel and retrofitting URL structure after launch is expensive.** None of the four is in the phasing paper **Journey automation built 18 August** — `Journey` and `JourneyStep` in `marketing-crm`, closing the 22.3 B block (CF-120). **Gamification, surveys and SEO remain open.** Chinmay: check the matrix and the designs first — if it is asked for, it is added. **Checked against the matrix as Chinmay asked, and both are asked for — built 18 August. Closed.** **Section 22.6 is twenty gamification requirements** and 19.2.73–75 three more; **22.11 is twelve SEO requirements.** Neither was a design opinion — the matrix specifies challenge management, achievement engines, digital badges, visit streaks, family and team challenges, leaderboards; and SEO metadata, dynamic generation, sitemaps, schema markup, hreflang and redirect governance. `Challenge`, `ChallengeProgress`, `SeoMetadata` and `UrlRedirect` added. **Gamification is not loyalty** — loyalty pays for spend and a challenge pays for behaviour spend does not produce, so **a challenge that only rewards spending is a loyalty programme with worse arithmetic.** **And SEO is not a marketing nicety here**: an attraction that does not appear in search sells through OTAs at OTA commission. Journeys were built earlier today (CF-120); surveys remain in CF-129. **`Challenge.scope` for family and group needs CF-132 to be more than a label**, which is one more reason to close that. |
| **CF-125** | **Resource management is a bounded context the platform does not have, and 47 requirements need it.** Domain 1.2 asks for a first-class bookable *resource* — a thing with a type, a calendar, availability and conflict detection: cabanas, ski instructors, strollers, AV rigs, auditoriums, staff. **`resource calendar`, `conflict detection`, `double-booking` and `availability of a resource` return nothing anywhere** across every description, enum value, schema property and screen in 778 operations. **What exists are four near-misses, and they are why this was not noticed.** `catalogue.ProductKind` has `rental` and `orders` has a `rental` entitlement kind, so a rental can be *sold* while nothing tracks the object. `fnb.DeliveryLocationKind` enumerates `cabana`, `sunbed`, `poolside`, `box`, `suite` and `lawn` — a cabana as a place food goes, not a thing you book. **`catalogue.acquireLease` reads like resource allocation and is concurrency control on catalogue editing.** And `GST-050 Resource Booking – Cabana` and `GST-058 Resource Availability (Cabana)` are specified screens calling `listProducts`, `getAvailability` and `addCartLine` — **a cabana modelled as a product against an availability envelope**, which sells a time slot and cannot express *this* cabana, checked out to this guest, returned at four. **Five capability blocks are absent**: the resource entity with types, categories, parent-child and attributes (10 reqs); the calendar with conflict detection and reassignment (13); rental check-out, check-in and deposit (3); staff skills, certifications with expiry and qualification validated before assignment — `RotaAssignment` carries `requiredRoleId` and nothing else (8); and leave, overtime, minimum staffing, working-hour limits, rest periods, union rules and labour cost, where the search again returned nothing (5). **The gap rate in this section is 46% against 12–28% in the three walked before it**, and it is one missing context rather than a scatter of omissions. **The home is not obvious and is not being decided now**: the resource entity belongs somewhere new, staff scheduling extends `workforce`, equipment overlaps `maintenance` assets, and rental stock overlaps `inventory`. **What needs a client answer is scope** — a resource module is a module, and it is not in the phasing paper **Reclassified 18 August: Softlabs decides, client is informed.** Whether a bookable-resource bounded context exists is a design decision, not a scope one — a cabana is currently a product with an availability envelope, which sells a slot and cannot express a specific object checked out and returned. **The phasing question is separate and belongs to CF-140**: the delivery plan puts 98 row citations at P3. **In scope — Chinmay, 18 August.** The bookable-resource bounded context is built. See `docs/active/positions-for-noting.md`. **Built 18 August — the `resources` contract, 27th in the package. Closed.** **A product with an availability envelope sells a slot** and cannot express which cabana, held by whom, returned when, with what deposit against it. Nine operations, six schemas, two state models. **Three things make it a context rather than a table.** A calendar with conflict detection — *two bookings on one cabana is a guest arriving to find somebody in their chair*, and it must be impossible rather than unlikely. Check-out and check-in — **a booking outlives its order**, which is why `orders` could not carry it. Setup and teardown outside the booking, because **an auditorium booked 14:00–16:00 is unavailable from 13:30** and a calendar that cannot say so double-books every turnaround. **The deposit is held through `authoriseStoredValue` rather than taken** (CF-126): a deposit taken and refunded is two transactions and a fee, held and released is neither. **Damage routes a resource to `maintenance` directly from `checkedOut`**, because routing it through `available` leaves a window in which the next guest books a broken item. **Deliberately not owned**: `workforce` keeps rotas, and `inventory` keeps quantities — **a resource is not interchangeable, which is the whole distinction.** Qualifications carry an expiry, because 1.2.36 wants the check *before* assignment and **a lifeguard certificate that lapsed last month is a safety failure rather than a data-quality one** |
| **CF-139** | **The RFP weights AI at 15% of the evaluation and names the four capabilities CF-73 parked.** `sources/rfp/TAIS_Platform_RFP_From_Miracle_Star_Trading_2.pdf` had never entered the package until 18 August. Its evaluation criteria are **Total Cost of Ownership 30%, Technical Architecture 20%, Functional Coverage 15%, AI Capabilities & Innovation 15%, Implementation Approach 10%, Vendor Experience 5%, Integration & Scalability 5%** — and the AI criterion is spelled out as *"the vendor's ability to implement AI-driven capabilities including demand forecasting, dynamic pricing, fraud detection, recommendation engines, and AI-assisted system configuration."* **Four of those five are exactly what CF-73 parked on 14 August** — Demand Forecasting (58), Fraud Detection (54), Dynamic Pricing (46) and Recommendation (36), 194 requirements, deferred because there is no season of history to train on. **The fifth, AI-assisted configuration, is built**, and CF-74 found it is five requirements rather than twenty. **CF-73's engineering reasoning is sound and was never tested against the commercial position.** A model trained on no data is worse than no model, and that argument does not change; what changes is who needs to accept it and when. **The distinction that matters is buildable-now versus needs-history**, and it is finer than CF-73 drew it: rule-based fraud checks need no model at all — one media code presented at two gates simultaneously is a rule, not a prediction (BL-104) — configured peak and off-peak pricing is a price list rather than an engine, and `promotions.getUpsellSuggestions` already does rule-driven recommendation (BL-068). **A defensible position is available: ship the rule-based half, name the model-based half as a post-season phase with the data dependency stated, and say so rather than let 194 requirements read as absent.** What is not available is discovering at acceptance that 15% of the award rested on them **Noted — closed 18 August.** The RFP weights TCO 30%, Architecture 20%, Functional 15%, **AI 15%**, Implementation 10%, Vendor 5%, Integration 5%, and names demand forecasting, dynamic pricing, fraud detection and recommendation engines — **the four capabilities CF-73 parked**. Chinmay: the weighting is understood and will be reviewed against what is delivered at the end. **The parking decision stands and is now made with the evaluation criteria known**, which is the difference this closes. |
| **CF-131** | **There is no payment gateway abstraction, no routing and no card tokenisation, and three other requirements are blocked behind the third one.** `orders.Payment` carries `providerName` and `providerReference` — it **records** which provider took the money and does not abstract one, select one, or store a credential. Searching `paymentGateway`, `gatewayRouting`, `tokenization` and `cardOnFile` returns **nothing anywhere** across 778 operations. **4.2.14 asks for a unified payment layer** so a provider can be changed without touching every surface; **4.2.15 asks for routing** by country, currency, card type or cost, which is a real commercial lever for a multi-region operator and interacts directly with ADR-0014's cell-per-region; **4.2.16 asks for PCI-compliant tokenisation** so a guest can save a card. **The third is the one that blocks others**: recurring membership billing (CF-129, 2.14.19–2.14.23), wallet auto-reload and recurring funding (4.3.28, 4.3.29), and one-click guest checkout all require a stored payment credential and **none of them can be designed until tokenisation is decided**. **Tokenisation also moves the PCI boundary**, which is a compliance position rather than an engineering choice — whether card data ever touches our infrastructure, and if it does not, which provider's vault holds it and under what residency. That last question is ADR-0009's territory. **The integration register already flags this from the other end**: Stripe amber as *payment exists; the provider does not*, and **NI Payment Gateway red with no trace at all — Network International being the dominant UAE acquirer.** Decision needed on the provider set before the layer can be designed **Network International and Stripe for Phase 1 — Chinmay, 18 August.** Two gateways, which is why the abstraction in CL-06 is needed rather than optional: **one gateway can be hard-coded and two cannot.** **Network International and Stripe for Phase 1 — Chinmay, 18 August. Closed.** Two gateways, which is why the abstraction in CL-06 is required rather than optional: **one gateway can be hard-coded and two cannot.** |
| **CF-61** | **On-premise deployment was never designed for.** Settled 14 Aug as the third client-facing model: the platform on the venue's own hardware, nothing leaving the site. Seven mechanisms assume TICVAI can reach the cell and cannot — the migration orchestrator cannot push, licensing cannot be Control-Plane enforced, support is blind, DR becomes the client's responsibility, and in-region AI inference has no endpoint. **Three decisions are needed before the first on-premise sale**, all commercial as much as technical: whether on-premise venues participate in cross-cell entitlements (honest default: no), what an expired licence does (recommendation: degrade, never stop — a gate refusing entry because a licence lapsed over a weekend is worse than running unlicensed until Monday), and who is contractually responsible for backups. ADR-0017 written **On-premise deployment is required — Chinmay, 18 August.** Some venues will run on their own hardware, so the AI provider must have a deployable option. ADR-0017 already carries the deployment model; what this settles is that a cloud-only AI provider is not sufficient. **On-premise deployment is required — Chinmay, 18 August. Closed.** ADR-0017 already carries the deployment model; what this settles is that **a cloud-only AI provider is not sufficient**, which is now a constraint on provider selection rather than an open question. |
| **CF-51** | **Location-aware ticket activation is not contracted.** 3.1.9 requires BLE beacon validation, geofencing, and entitlements that "shall only become active when the guest is physically located within authorized venues". Access control has geofencing for **staff handhelds**; there is nothing for guest-side activation. A dynamic QR that only works inside the venue is a real anti-fraud control and it does not exist. **Needs a decision on enforcement strength** — refusing activation outside a geofence will strand guests with poor GPS at an indoor gate **In scope — Chinmay, 18 August.** BLE and geofenced activation are built. **In scope — Chinmay, 18 August. Closed.** `setAccessPointGeofence` already carries the geofence and 3.1.9's location-aware tickets resolve against it. **The decision was the whole conflict** — the mechanism was never missing. |
| **CF-74** | **"AI Configuration Assistant" is two unrelated things, and six ids exist twice.** 8.1.1–8.1.6 are no-show revenue recognition and complimentary/invitation PLUs; 8.8.1–8.8.9 are future-dated pricing calendars. **Only 8.8.10–8.8.14 are the assistant.** So CF-57's Wave 1 commitment — the client's stated first AI priority — is **five requirements, not twenty**. Separately, 8.1.1 through 8.1.6 appear under two sub-domains with entirely different text, so any sub-domain analysis of domain 8 is wrong — **that half is now CF-120**, which found the same defect on three further blocks and resolves all four package-side. **8.1's A block is also filed in the wrong domain**: no-show revenue recognition belongs in `finance` and complimentary/invitation PLUs in `catalogue` and `orders`, and neither is AI or reporting **Split accepted — Chinmay, 18 August, and it shrinks CF-57.** *AI Configuration Assistant* covers two unrelated blocks: 8.1.1–8.1.6 are no-show revenue recognition and complimentary PLUs, 8.8.1–8.8.9 are future-dated pricing calendars, and **only 8.8.10–8.8.14 are the assistant**. The client's stated first AI priority is **five requirements, not twenty** — products, pricing, promotions, seating, calendar. 8.1's A block refiled to `finance` and the numbering resolved in `reference-map.json` (CF-120, closed). **Split accepted and applied — closed 18 August via CF-120.** *AI Configuration Assistant* covered two unrelated blocks. **Only 8.8.10–8.8.14 are the assistant**, so the client's stated first AI priority is five requirements rather than twenty: products, pricing, promotions, seating, calendar. 8.1's A block refiled to `finance`, the numbering resolved in `reference-map.json`, and all six 8.1b rows verify CONTRACTED against ADR-0020. |
| **CF-128** | **The integration register was built from the Integrations sheet and never from the 3,184 functional rows, and ten named third parties are missing from it.** `handoff/integration-register.md` itemises 35 systems — 19 software, 16 hardware — tested against every contract, and it is a good document. **It counted the wrong population.** The Integrations sheet is one of five sheets; the functional sheet names vendors too, and a sweep of it finds **Viator, Klook, Headout and GetYourGuide at 2.7.53**, **Genesys, Avaya, Amazon Connect, Microsoft Teams and 3CX at 2.8.11**, and **Oracle at 5.12.7** — none of which appears in the register. **Worse than absent: one is marked covered when it is not.** The register records *Resellers & OTA* against `orders` and `subscription` as *covered — partner portal, allocations, credit*. That is true of the commercial model and false of the integration: `PartnerAgreement`, channel allocations and `CreditPosition` are excellent and a **channel manager is a live two-way feed of availability and price with mapping and rate-limit concerns**, not a portal a partner logs into. **This is CF-117's failure in a third field** — a check that catches a thing that does not exist and never a thing paired with the wrong claim. **Telephony is the sharpest instance.** `marketing-crm.ConversationChannel` includes `voice` **with nothing behind it**: the conversation model anticipated a phone system and stops at the enum value, while CF-99 built agent state, queueing and handover that voice would reuse the moment something fed it. **The fix is a sweep, not a decision** — re-derive the register from every sheet, then take the genuinely new names to the client as scope. Ten is what one section pair surfaced; twelve of sixteen sections in domain 2 alone are still unwalked **Superseded by the requirement walk — Chinmay, 18 August.** The integration register was built from the Integrations sheet and never from the 3,184 functional rows. **The walk went row by row across all of them**, which is the check this conflict asked for. **Superseded by the requirement walk — closed 18 August.** The integration register was built from the Integrations sheet and never from the 3,184 functional rows. **The walk went row by row across all of them**, which is precisely the check this conflict asked for. |
| **CF-114** | **Ten screens carry an entire contract, and the white-label builder is drawn tenant-facing while specified platform-facing.** The median screen has four operations. **`CMS-003 Typography`, `CMS-006 Component Preview`, `SUP-006 Knowledge Base Search` and `ADM-016`–`ADM-018` each carry all 41 white-label operations**; four more across P04, P06, P08 and P13 each carry all 10 assets operations. **A Typography screen does not create banners, delete content pages or manage domains**, and a knowledge-base search on the support console does not configure a tenant's branding at all. **This is CF-87 in a different shape** — that attached sibling operations to guest screens; this attaches a whole contract to any screen whose module resembles it, and **it passed every validator because each operation exists, resolves to a real table and carries a permission the platform is entitled to.** **The second half is commercial.** Guest storyboard boards 1 and 2 draw the white-label builder as tenant-facing — *"Tenant: Dubai Museum (Demo)"*, a Publish Changes button, version history with Restore Previous Version, a live mobile preview — **a tenant using it unaccompanied.** Our white-label operations sit on P09 TICVAI Web, the platform admin console. **Whether a tenant self-serves their branding or TICVAI does it for them changes who is trained, who is blamed for a broken theme, and whether a venue can rebrand on a Friday without raising a ticket — and the client has been shown self-service.** The right split follows boards 1 and 2, which name ten builder screens each **Separated 18 August — Chinmay asked for the suggestion and this is it.** **Not a contract split.** `assets` is one coherent contract and the defect was attachment, not design: ten operations landed on any screen that touched media. **Six were already corrected; four remained**, and only `CMS-010 Media Library` legitimately needs all ten — it *is* the library. `POS-010` and `EMP-036` read one asset; `BO-027` reads and replaces. **A till adding an item to a ticket does not manage a media library**, and a knowledge-base search does not configure a tenant's branding. **The rule is the fix**: a screen declares the operations its purpose needs, and a screen needing an entire contract is either the console for that contract or wrong. Same shape as CF-87 one level up — that attached sibling operations, this attached a whole contract **Separated 18 August, and not by splitting the contract.** `assets` is coherent; the defect was attachment — ten operations landed on any screen that touched media. Six were already corrected and **26 operations were removed from the remaining four**. Only `CMS-010 Media Library` keeps all ten, because **it is the library**. A till adding an item to a ticket does not manage a media library. Same shape as CF-87 one level up: that attached sibling operations, this attached a whole contract. |
| **CF-134** | **Nothing in the platform raises an alert, and five sections ask for one.** 6.1.57 wants exception reports when a KPI leaves its range; 8.7.22 wants KPI alerts; 8.2.20 and 8.2.41 want attendance and capacity forecast alerts; 8.9.3 wants queue alerts; 8.9.10 wants alerts on the command centre; 2.1.15 wants an email when a machine stops working. **The striking part is that every condition is already computed.** `RegisteredDevice.status` reaches `error` and `consumableLow` — the heartbeat description names paper low, drawer open and reader offline. `ShiftCloseResult.requiresAcceptance` fires the moment a till is over or short. `QueueFeedHealth.isHealthy` goes false and `isQuiet` marks a sensor that has stopped reporting. `WaitTime.isStale` marks an estimate nobody should trust. `getSuggestedRequisitions` knows stock is below par. `PromotionUsage.isBudgetExhausted` knows the money ran out. **Six contracts detect their own trouble and none of them tells a person.** This is the same absence as the event-trigger gap recorded against pre-arrival reminders, queue-turn notifications, agreement expiry and wallet expiry — one is guest-facing and this one is operational, and they are the same missing mechanism seen from two sides. **An operations command centre whose alerts a supervisor has to notice on a screen is a screen, not a command centre** — 8.9 is specified as the room a duty manager sits in. **The decision is where it lives**: a platform capability that any contract publishes to, with routing, severity, acknowledgement and escalation, or a per-contract notification each team builds again. The first is one build and a dependency for every contract; the second is what happens by default and is how six inconsistent implementations appear. `approvals` already has escalation with SLA and `workforce.publishAnnouncement` already has targeted delivery with acknowledgement tracking — **the parts exist in two contracts and neither is general** **On-platform notification, marked as read — Chinmay, 18 August.** Alarms belong on the dashboard notification panel. **WhatsApp or email only where the matrix asks for them by name**, because an operational alert that arrives by email is an alert nobody sees in time. **On-platform notification, marked as read — closed 18 August.** `VenueSettings.alerting` with a dashboard panel default, acknowledgement required, and optional escalation. **Email or WhatsApp only where the matrix names them**, because an operational alert that arrives by email is an alert nobody sees in time. |
| **CF-84** | **Donation VAT treatment is unstated and it is a tax question.** 1.1.128–1.1.133 require donation collection through all POS channels. I posted donations to a liability account rather than revenue, which is right — money collected for a charity is not the venue's to recognise. **What I could not decide is the VAT position.** A genuine donation is typically outside the scope of UAE VAT; a payment carrying any benefit to the giver is not, and a round-up at a till is arguable either way. **Getting this wrong is a filing error with a regulator at the end of it**, and it also decides whether the donation appears on the guest's tax receipt. Needs the client's tax advisor, not a developer **Configurable per region, and 0% is a valid value — Chinmay, 18 August.** Donation tax treatment varies by jurisdiction and is set on the tax code rather than assumed. **The liability-account posting I chose stands as the default and is no longer the only option.** **Configurable, and 0% is a valid rate — closed 18 August.** `TaxCode.appliesTo` now carries `donation` among eight categories, so treatment is set per jurisdiction rather than assumed in the posting. **The liability-account default stands and is no longer the only option**, which was the part I was not qualified to decide. |
| **CF-100** | **Agent hours for the conversation queue are unstated, and the handover depends on them.** Raised 17 August when CF-99 was closed. 22.8.5 requires seamless transfer to a human and 22.8.18 requires queue management with service levels — **neither says when a human is there.** A handover path with nobody on the other end after 18:00 is a queue that never empties, and the guest experience is worse than an assistant that had said no. Needed: **staffed hours per venue or per tenant, what happens outside them** — a case, a callback, or the assistant continuing — and whether the assistant may take a conversation back when no agent comes, which the state model allows and no venue has asked for. **Not a blocker**: `handoverToAgent` already returns `noAgentAvailable` and offers a case, and the code is identical whether the hours are 09:00–18:00 or 24/7 because availability is a runtime query against the rota rather than a constant. **It blocks go-live, not build.** The wave mismatch behind it was CF-101, closed 17 August — `SUP-004` and `SUP-005` moved to Wave 2 so the handover has a destination **The venue configures its own support hours — Chinmay, 18 August.** 24/7 or bounded is a venue decision, not a platform one, so this is a configuration screen rather than an open question. **It never blocked build**; the code is identical either way. **Never a design question — closed 18 August.** The code is identical whether a support desk runs 24/7 or nine to six. **What was missing was somewhere to put the answer**, and `VenueSettings.supportHours` is it: mode, timezone, windows and an out-of-hours message, set by the venue because the venue is who knows. |
| **CF-97** | **A superseded ADR was cited as current, and nothing in the package could have stopped it.** ADR-0021 reasoned from ADR-0001's *"Cell = Tenant × Jurisdiction"* and concluded `tenant_id` in a Qdrant collection named nothing. **Everything was correctly labelled and I read past all of it**: ADR-0001's status line, four lines from the top, says *split rule superseded by ADR-0014*; the ADR index says *Superseded in part by 0014*; ADR-0014 says plainly *"a cell holds exactly one tenant is no longer true"*; and ADR-0001 carries a section titled *Two claims in this ADR that the shared cell breaks*. **The documents were right. The reading was wrong** — I searched for a decision sentence and used it without reading the status above it. The consequence was real: **Postgres enforces RLS, Qdrant enforces nothing**, so on shared placement a query missing its filter returns every tenant's vectors. Corrected in ADR-0021 with uniform shard-per-tenant. **This is the second defect from the same assumption** — ADR-0014 records the first, `platform.tenant` written as a single-row projection with no RLS, which on a shared cell is a cross-tenant leak, corrected in V0003a. **The fix that helps is not another label**, since two already exist and were ignored. `check-package.py` now fails any ADR citing a superseded one without naming the supersession. **Open for decision: the expected tenant count on a shared installation.** ADR-0021 holds because it is tens; past roughly a hundred the sharding strategy changes, and that number is in no ADR **Retire ADR-0001 completely — Chinmay, 18 August.** It is already marked superseded by ADR-0014 and ADR-0017, and four ADRs cited it without saying so. **A superseded ADR that still reads as guidance is how CF-97 happened in the first place** — somebody reasoned from a stale isolation sentence and produced a cross-tenant defect. **Retired completely, 18 August.** Marking it superseded was not enough and striking it through would not have been either — **a superseded decision left on the page is a decision somebody reads**, and that is not hypothetical: reasoning from ADR-0001's Decision section produced a cross-tenant isolation defect in ADR-0021, by a careful reader, six days after it was superseded. **The Decision section is removed rather than struck.** The Context and Alternatives stay as history. Ten files annotated so no bare citation remains, and `check-package.py` refuses one. |
| **CF-123** | **The venue map is built — a contract of its own, 26 contracts now. Closed 18 August.** **A venue map is not a seat map, and conflating them is the mistake this exists to avoid.** A seat map is an auditorium where every seat is sellable inventory; a venue map is a park where **nothing on it is sold** — it is what a guest navigates by and what an operator watches. `seating` did the first and nothing did the second. **The pipeline is upload → extract → propose → place → publish**, and the split between extract and propose is the load-bearing decision. `importVenueGeometry` is deterministic and carries every CF-122 lesson — layer roles as lists because one real file had two layers both named `Layer 1`, Arabic-Indic digit normalisation, UTF-16BE decoding, native DWG and DXF because **converting CAD to PDF loses the layer names the whole thing depends on**. `ai.proposeVenueLabels` reads those shapes and suggests what each is. **Two failure modes kept apart: a mis-parsed layer and a bad suggestion look identical if one operation does both**, and the operator is left able to say only that the map is wrong. **ADR-0020 caught this twice while it was being written** — `venue-map` tried to write `ai.proposed_action` on the day it was drafted, so the model call moved to `ai`; then the acceptance operation tried to close the proposal, and that moved too. **A map is published, not saved**: editing a live map under a guest standing in front of it is how a route ends at a wall, and the previous version stays readable so a guest mid-route on version 3 finishes on version 3. Points carry `outletId`, `productId` and `accessPointId` — **tapping a restaurant should open its menu, and that only works if the map knows which outlet it is** — and `emergencyExit` is a separate kind from `exit`, because **a map that cannot tell them apart routes a normal departure through a fire door**. `VenuePath.isStepFree` is the single most important field: **a wheelchair user routed up a staircase was failed by the map, not the venue.** Eight operations, six schemas, three back-office screens, three permissions, three state models, and `GST-021` and `EMP-030` wired. **No routing engine and no indoor positioning**: the map supplies the graph and a phone with it cached routes offline **Completed 18 August — the base image and the pathfinding guarantee.** Two gaps the first pass left. **The illustrated map is not the CAD drawing**: a park hands you an architect's plan and a beautiful painted map, and the guest wants the second while the platform needs the first. `baseAssetId` holds the illustration in `assets`, `baseImageAlignment` reconciles the two coordinate systems — **a point placed on the plan lands in the wrong place on the painting unless something does** — and `tileSetRef` exists because **a 12,000-pixel park map is not something a phone downloads on arrival**, and venue wifi at the gate is the worst possible moment to send twenty megabytes. **And *routing is a client concern* was only half an answer** — true about the algorithm, and it left nobody guaranteeing the graph was worth routing over. `getVenueMapGraph` returns nodes, edges, distances and the connected-component count, versioned so a stale route is detectable. `validateVenueMapGraph` reports four things and **the second is the one venues get wrong**: a point reachable only by steps is a map that works perfectly until a wheelchair user opens it, and nothing in the drawing makes it visible. **Critical points are listed apart** — an unreachable gift shop is a defect and an unreachable assembly point is a safety finding. `setPathClosure` makes a closure an operational act rather than a map edit, bumping the graph version, and **refuses to strand a point unless forced, warning by name when a forced closure cuts off first aid.** The map now ships in the offline package in three parts with three lifetimes — the graph changes on every closure, the points monthly, the image rarely — **cached separately, because caching them together means re-sending the image for a closure or serving a stale graph** **Input spec written 18 August.** `handoff/venue-map-input-spec.md` states exactly what a venue sends, in what format, and the contract enforces it rather than describing it. **Written against the sample files TICVAI already supplied**, so every rule is one the existing drawings either meet or missed in a way that broke the import. `VenueMapImportJob.findings` carries a **closed set of 14 codes**, each naming a spec section — `duplicateLayerName`, `geometryOnLayerZero`, `digitScriptMismatch`, `exitLayerNotSplit` — because **a free-text finding is one a drawing office cannot act on**. `manifestRowsJoined` is the figure to check against your own count: **a manifest of 396 seats that joins 220 looks like success without it.** `layerMapping` became optional — the platform matches the accepted names itself, `VC-` prefix ignored — and is now an override rather than a requirement. **`rasterOnly` and `noGeoreference` are warnings, not errors**, because refusing a venue that sent everything it had is the wrong answer: each part is independently useful and none blocks the next, so **a venue that can only send a manifest still gets sellable seats** **Two corrections from Chinmay, 18 August.** **(1) The walkway layer is no longer required.** Most drawings prepared for ticketing have none, and **walkable space is the negative space** — the site minus buildings, seating, water and planting. **That is subtraction, not intelligence, and it is better than intelligence here**: exact and repeatable, where a model asked to guess where people walk will eventually route somebody into a lake. A `keepOutLayer` was added and is now what a venue sends if it sends nothing else about circulation. **AI is kept for the one case that has no alternative** — a raster plan with no geometry to subtract — and `ai.proposeWalkways` **refuses where vector geometry exists**, because running a model where subtraction is available is how a worse answer gets preferred for arriving in a nicer format. Its acceptance gate is stricter than for labels: **a mislabelled toilet is cosmetic and a wrongly accepted walkway routes a guest into a service yard.** **(2) `isOneWay` removed.** A pedestrian walkway has no direction, and the three cases that look one-way are all something standing on the path — a turnstile, a queue line, an exit-only gate. **`access.AccessPoint.direction` already carries it and `queue` owns its own flow**, so the path now names the point that restricts it and the router reads direction from there. **Marking the path as well would have drifted**: a gate reconfigured to bidirectional would have left a path still marked one-way and nothing would have noticed | Chinmay | 18 Aug |
| **CF-120** | **Twenty-eight references name two requirements each — all four blocks now resolved, closed 18 August.** 56 rows across `5.5`, `5.6`, `8.1` and `22.3`, and only one had ever been registered. Every pair has different text: **these are two capabilities sharing one number, not duplicate rows.** **`5.5`** — B supersedes A, a strict superset in all four pairs, with two details carried forward as new package references. **`5.6`** — *not* a supersession: the pairing by number is coincidental and only `5.6.1` matches concept to concept, so both blocks are retained in full. **`8.1` — A is finance, B is AI governance.** Rows 1728–1733 are no-show deferred-to-earned revenue, no-show reporting, complimentary and invitation PLUs, who issued an invitation, and issuance restricted by profile — **none of it AI or reporting, all of it `finance`**. Rows 1734–1739 are prompt logging, response logging, approval before execution, explainability with confidence indicators and an AI audit trail — **and B is nearly contracted already**: `ai.interaction` records prompt and response under ADR-0020, and `x-ticvai-permission-escalated` is the approval gate 8.1.4 asks for. All six B rows verify CONTRACTED. **`22.3` was the dangerous one.** A is case management and SLA, B is marketing journey automation, and **`22.3.6` named both *Case Escalation Management* and *Abandoned Cart Recovery*** — so CF-99 built the conversation contract against A while CF-83 had counsel reviewing B, under one reference. B is CF-137's journey automation. **The convention throughout: the first occurrence keeps the bare reference and the second takes a `b` suffix**, following CF-33a, and `traceability.json` is keyed on `packageRef` plus `xlsxRow` so the reference alone is never a key. **The splits were ours to make and only the numbering needed sign-off** — given by Chinmay, 18 August |
| **CF-151** | **The traceability verdicts were stale by a day's work, and re-verdicting them caught me over-claiming.** 197 rows still read `GAP_CONTRACT` or `CONTRACTED_PARTIAL` while the backlog entry that answered them was closed. **Re-verdicting is not a flip** — three checks had to pass first, and two failed. **`BL-053` was reopened**: it carries 75 rows, and `reporting.MetricSource` answered **five of them**. The rest name metrics the enum does not reach — churn and renewal counts, a graphical access-point map, revenue per entitlement, supplier delivery performance, resale volume, sales by instructor. **Closing it on the enum alone would have been exactly the over-claim the refs back-fill had exposed an hour earlier.** And **19 rows were reverted** because the edit that closed their entry landed in a different contract than the row is scoped to — a `catalogue` version history does not contract an `fnb` recipe-versioning row. **`check-traceability.py` caught both**, first refusing a CONTRACTED row with no evidence and then refusing evidence naming a field rather than a schema, and finally refusing evidence that does not exist in the row's own contract. **Corrected position: 1,652 contracted (52%), 608 gaps, 519 partial, 378 parked, 27 decisions** | Chinmay | 18 Aug |
| **CF-150** | **The backlog understated its own work by two and a half times, and both artefacts were individually valid.** Challenged on 18 August that *675 gap references exist and 349 have no backlog entry*, the count was right and the conclusion inverted. **Every gap row is routed — zero orphaned**, and `check-traceability.py` already enforced that. **What the 349 measured was the `refs` arrays inside entries, which were written as illustrative citations rather than exhaustive indexes.** `BL-053` is cited by 75 rows and listed 3. `BL-013` is cited by 35 and listed 1. **The hazard is estimation, not routing**: anyone scoping from `contract-backlog.md` sizes `BL-013` as one requirement when it answers 35. **754 refs back-filled across 94 entries from `traceability.json`** — mechanical, no judgement, because the routing already existed on every row. `check-backlog.py` gained a completeness check, verified by truncating `BL-013` to one ref: *refs array omits 34 rows that cite it*. **This drift was invisible because neither artefact was wrong on its own terms** — the traceability row named its entry, the entry named some refs, and nothing failed. **And it changed my own reporting**: eighteen entries closed today were reported as 35 requirement rows and are **86** | Chinmay | 18 Aug |
| **CF-126** | **Six balance implementations, and only one could hold an authorisation — fixed 18 August.** `retail.Wallet`, `retail.GiftCard`, `games.GameCard`, `promotions.Voucher`, `marketing-crm.LoyaltyPosition` and `catalogue.EntitlementTemplate` each grew a balance, a bonus balance, a status, a blocked reason and an expiry, built separately across three sessions. **The concern was never tidiness.** `authoriseWalletSpend` gave two-phase spend to the retail wallet alone, so **a guest with 200 game credits starting a play the machine then failed had no held balance** — the credits were either taken or not, with no third state. **The entities stay distinct because their lifecycles genuinely differ** — a gift card activates at a till, a loyalty position never expires the same way — **and the spend mechanism is now shared.** `StoredValueKind` and `StoredValueAuthorisation` in `shared/common.yaml`, with `authoriseStoredValue`, `captureStoredValue` and `releaseStoredValue` in `orders`. **The lifecycle mirrors `cross-cell.WalletAuthorisation` exactly**, which is the point — one lifecycle rather than six. Partial capture is the normal case, not the exception: a tab authorised at 200 and settled at 140. **`holdSeconds` belongs to the caller** because an arcade play is seconds and a table tab is hours, and held balance is balance a guest cannot spend elsewhere. Expiry releases automatically — **a hold nobody releases is a guest whose balance is short with no explanation** |
| **CF-122** | **Four silent failures in the seat importer — fixed 18 August, and the client files validated the design first.** `VC-Seats`, `VC-wheelchairseating` and `VC-Steps` map straight onto the three layer roles, so **layer-aware extraction was the right bet and CF-17 is settled**: AutoCAD-origin PDF plus a per-section xlsx manifest. **The defects were all in how it reads the file.** `layerMapping` declared each role as a single string and the real file carries **two distinct layers both named `Layer 1`**, steps on both `steps` and `VC-Steps`, and AutoCAD's default `0` — roles now take lists, and `unmappedLayers` reports what no role claimed. **Section codes with Arabic-Indic digits never joined**: `A١` and `A1` are one section to a person and two to a comparison, normalised now by default. **PDF layer names are UTF-16BE** and read as mojibake taken as bytes. **And native CAD and raster were unsupported** — `dwgPlan`, `dxfPlan` and `rasterPlan` added, because converting to PDF loses the layer names the importer depends on. **All four failed quietly, which is the real defect**: `ImportJob.outcome` now distinguishes `noSeatsFound` from `noLayersMatched`, and `layersFound` lists every decoded name so an operator maps a role by reading rather than guessing |
| **CF-124** | **Guest parking payment is out of scope — decided by Qossai on 14 August, recorded 18 August.** 19.2.78 requires the app to take parking payments and `access.yaml` scoped it out in its own words. The minute settles it: all three integration models are entitlement-based, and *"a separate, pay-per-hour parking model unrelated to ticketing can be handled independently via the parking system's own POS."* **Taking that money here would make the venue an acquirer for parking** — settlement path and tax treatment, neither designed. `ParkingFacility.takesPayment` is now a read-only false **stated rather than assumed, so a future reversal is a value change with a visible blast radius rather than a silent gap somebody rediscovers.** Also recorded: `vendorSwapTargetDays`, Qossai's requirement that **a new parking vendor should take days rather than weeks** — a design constraint, with the three modes as the adaptor surface and a vendor needing a fourth mode as the signal it has been violated |
| **CF-121** | **Two domain-numbering schemes coexisted and the walk had already fixed it — verified and closed 18 August.** Nine contracts once ran on a compacted 1–21 sequence that closed the matrix's gaps at 9 and 14, so `15.1.1 Inventory Item Master` resolved to `maintenance`. **All 25 contracts now declare the matrix's own number and name, checked against the matrix itself — zero mismatches.** `check-package.py` already enforced it; **I wrote a second copy of the same check before noticing and removed it.** A duplicate checker is worse than none, because two failures for one defect teaches a reader to skim |
| **CF-149** | **The register said three of these were client decisions while the position note said they were ours.** CF-125 resource management, CF-129 waivers and forms, and CF-132 portfolio sat under *Open — needs a client decision* with Qossai as owner, and `positions-for-noting.md` had already recorded them as Softlabs decisions to be informed. **Two documents disagreeing about who decides is worse than either answer**, because the meeting agenda comes from one and the position from the other. Moved to *Open — Softlabs to resolve*, owner **Softlabs — client informed**, each carrying its reasoning. Client decisions drop 34 to 31. **The three stay open rather than closing** — a position taken is not a position communicated, and they close when the client has been told | Chinmay | 18 Aug |
| **CF-148** | **Four of the six decision clusters were misclassified as client decisions, and taking all six to one meeting would have produced six threads and no closure.** Chinmay reclassified them on 18 August and the reclassification holds against the cluster contents. **CL-01 resource management, CL-04 waivers and forms, and CL-05 portfolio are Softlabs decisions to be noted**, not asked. **CL-06 and CL-12 split** — the build gap is ours, the funds-flow decision and the legal basis are his. **CL-11 is the only one correct as framed.** Net: **two decisions, one written input, three positions** — a 45-minute conversation with a recorded outcome rather than a two-hour survey. **The sharpest call is CL-05.** Section 5.5's primary holder assigning entitlements, transfer between linked portfolios and shared wallets with individual tracking are not a household model — **they are delegated authority, the same question CL-12's policy engine answers.** `Grant` already carries principal, role, permission, scopePath, effect and validity, so **49 rows of new data model become 49 rows an existing mechanism covers**, with the audit and expiry already in it. It also absorbs BL-028 group leader and BL-035 corporate allocation, which a family structure would have left unserved. **Also separated: CL-01's phasing.** The delivery plan puts 98 row citations at P3, and that contradiction belongs to CF-140 rather than leaking back in as a seventh thread. **And `BL-144` was pulled out of the meeting entirely** — password policy and lockout are settled-lane, buildable today, no conversation. `docs/active/positions-for-noting.md` gives each position its reasoning and its reversal cost, **because a position nobody can price is a position nobody can disagree with usefully** | Chinmay | 18 Aug |
| **CF-138** | **F&B and retail configuration moves to outlet — decided by the client 18 August, implemented 18 August. Closed.** The workshop minute reads *"F&B (and, by extension, Retail) configuration will be managed at the outlet level rather than the venue level"*, against ADR-0018 recording the client confirming venue on 14 August and naming outlet a rejected alternative. **The two were rejected together on 14 August and should not have been.** The argument that carried that decision — *forty workstations configured individually is forty things that drift* — is about tills, and **an outlet is a business rather than a device.** A restaurant has its own menu, hours, kitchen and prep times; a workstation has a serial number and whoever is standing at it. **The deeper finding: outlet was not on the scope tree at all.** `ScopeLevel` had seven organisational levels and `platform.outlet` hung off `venue_id` as a business unit — so configuration could not resolve there even in principle. **`outlet` added as a sibling of `department`, not a child**: a department has requisitions, rotas and workstations, and modelling a restaurant as one would put it in the staffing tree. **The package had already modelled it this way** — fourteen tables reference an outlet and every one is F&B or retail, and `Outlet` carried five configuration values (`openingHours`, `stockLocationId`, `costCenterId`, `kind`, `zone`) **outside the configuration system that said it could not configure.** ADR-0018 and ADR-0011 amended; `check-config-scope.py` accepts outlet in F&B and retail only, verified to reject it elsewhere; five operations moved — the three menu ones, the table layout and the return policy. **Workstation stays assigned, and venue is still the floor for everything else** |
| **CF-147** | **Two conflict registers were written the same day against the same package, and six ids collided.** A requirement walk covering all 3,184 matrix rows and a cross-artefact audit ran in parallel sessions, neither aware of the other. **Both independently reached CF-120 and both kept going to CF-125** — so `CF-123` meant *the RFP closes a blocked artefact class* in one register and *the guest venue map has no contract* in the other. **Merged 18 August onto the walk's register**, which is the one cross-referenced by `clusters.md`, `contract-backlog.md` and `traceability.json` — moving those ids would have broken three files and 19 backlog blockers. The audit's six became **CF-141 to CF-146**, each carrying a note saying what it was raised as. **Everything below CF-120 already matched**, which is the useful part: two sessions writing 103 and 118 rows against one package produced no disagreement until they ran off the end of the shared numbering. **Also merged**: `derive-platform.py` and twelve platform pages, the audience index, and four analysis documents. **The walk's `sources/` layout was kept** — filed by kind rather than by supplier, which is right, and the audit's `sources/client/` duplicates were dropped. Nine validators green, 178 backlog entries and 3,184 traceability rows intact, and no backlog entry blocks on a conflict that no longer exists | Chinmay | 18 Aug |
| **CF-141** | **All 514 relationships were invisible at column level.** Asked where `facility_id` on `access.parking_entitlement` comes from, the answer was in `relationship-graph.json` and nowhere a reader would look — **the contract declares it as a bare `uuid` with no `$ref`**, so the column in the workbook said `uuid` and stopped. One column in 2,574 carried a reference. **The cause is that contracts express almost none of these as `$ref`** — 486 of 514 are conventions rather than declared references — so the graph is the source and the column had to be annotated from it. `derive-schema.py` now does that, and **the graph turned out to know about 180 columns the contracts do not expose** — `ai.policy.tenant_id`, `identity.authz_audit.actor_principal_id`, `fnb.location_code.location_id`. **An API returns what a caller needs and a table carries what RLS and the joins need**, and scope columns are the usual case: a policy table with no visible tenant column looks unscoped. **Tables with columns went 211 to 283 of 287**; the four remaining are two default partitions and two platform-written tables. The workbook's Columns sheet gains **References, Link and Enforced** — the last being the number that matters: **28 of 514 are enforced by a DDL foreign key and the other 486 are conventions the database does not check** **Raised as CF-120 in a parallel session on 18 August and renumbered on merge** — the requirement walk had independently used CF-120 for a different finding, and its register is cross-referenced across `clusters.md`, `contract-backlog.md` and `traceability.json`. Two registers were written the same day against the same package and neither knew the other existed. | Chinmay | 18 Aug |
| **CF-142** | **A page per platform, with the gaps derived rather than listed — and the screen `module` field was unusable before it could be built.** **32 hand-typed values across three different granularities, and 124 screens said `TODO`** — P02, P09, P10 and P12 entirely. Three things were called module: the matrix domain on a contract, the Postgres schema, and a UI grouping on a screen, with no relationship between them. **Now derived from the operations a screen calls**, which makes it the contract name — the one taxonomy that is consistent, 25 of them against 24 schemas. 361 screens; the two without one call no operations. **`tools/derive-platform.py` emits a page per platform**, and **the gaps section is the point**: a page listing what exists is a directory, one listing what is missing is a worklist. Four gap kinds, none typed — operations reaching no screen, **modules split across waves** (*a platform that sells in one wave and cannot refund until a later one can take money and not give it back*), screens nobody has drawn, and flows naming a screen that does not exist. **The gap rule was wrong twice before it reconciled**: first it claimed any operation in a contract the platform used, giving P08 309 gaps; then it required a screen on the same audience, giving 229 — **against a platform-wide total of 180 operations reaching no screen at all. A number larger than the population it is drawn from is a rule that is wrong.** Now the twelve pages name exactly 180 distinct operations between them. **P01 Guest Web has zero orphan operations and six undrawn screens**, all added in the last two days **Raised as CF-121 in a parallel session on 18 August and renumbered on merge** — the requirement walk had independently used CF-121 for a different finding, and its register is cross-referenced across `clusters.md`, `contract-backlog.md` and `traceability.json`. Two registers were written the same day against the same package and neither knew the other existed. | Chinmay | 18 Aug |
| **CF-143** | **Workshop tracker assessed against the package rather than self-reported: 37 of 62 tasks done, 10 of 30 client inputs received.** Every verdict names its evidence — an ADR, a contract operation, a screen or a conflict — because a tracker whose status column says *In Progress* for three weeks is a tracker nobody trusts. **Two MoMs were missing from the package**: the 14 August session and the 18 August F&B one, both present in the project and absent from `sources/mom`, so every provenance search this session ran against seven MoMs rather than nine. Now nine. **The finding that matters: six of the fourteen pending client inputs sit behind one thing** — MongoDB approval, the NoSQL list, the WAF list, the security services, the authority documents and **Qdrant's compliance status**, all blocked on the Dubai Compliance Authority meeting Allam scheduled for 14 August. **That meeting happened and its outcome was never documented** — a pre-session with Dinesh, Siddhesh and Allam, no MoM. **ADR-0021 assumes Qdrant clears.** Also corrected: one MoM reference to Bahrain is an outage anecdote, not the AutoCAD seating drawing that input 18 asks for, so that input is still pending despite matching a keyword search. The workbook was rebuilt — **its New Inputs sheet had 16,384 columns**, one stray cell stretching the sheet across the whole grid **Raised as CF-122 in a parallel session on 18 August and renumbered on merge** — the requirement walk had independently used CF-122 for a different finding, and its register is cross-referenced across `clusters.md`, `contract-backlog.md` and `traceability.json`. Two registers were written the same day against the same package and neither knew the other existed. | Chinmay | 18 Aug |
| **CF-144** | **The RFP had never been read, and it closes an artefact class we have been recording as blocked.** `sources/client/TAIS_Platform_RFP.pdf` — 13 pages, 11 domains, 50 capabilities, **the document we were asked to bid on.** Every scope argument in this package traced to the matrix and the minutes, and neither is the RFP. **41 of 50 capabilities are contracted** and the domain structure maps near one-to-one onto the 25 contracts without anyone having tried. **But it states the non-functional requirements plainly — *minimum platform uptime of 99.9%* — and `performance/SLA (no targets)` has been one of three blocked artefact classes since the audit.** It was never blocked; the target was in the RFP, and three sweeps recorded it as open because the document was unread. 99.9% is 8.8 hours a year, which bears directly on the answer given yesterday about one database holding all 24 schemas. **Nine capabilities have nothing behind them**; four are the parked AI 194 and two are CF-21's developer requirements, but **five are tracked nowhere: chargeback management, ticket resale marketplace, hotel integration, locker and rental, and an operations command centre.** **Chargeback is the one to act on** — it is not a refund, it has a bank-imposed timetable and an evidence submission, and a platform that cannot evidence one loses it by default. `handoff/rfp-vs-package.md` **Raised as CF-123 in a parallel session on 18 August and renumbered on merge** — the requirement walk had independently used CF-123 for a different finding, and its register is cross-referenced across `clusters.md`, `contract-backlog.md` and `traceability.json`. Two registers were written the same day against the same package and neither knew the other existed. | Chinmay | 18 Aug |
| **CF-145** | **A delivery plan with 7,552 person-days existed for six days and the package sequenced itself independently.** `TAIS_Product_Planning_and_Delivery_Plan.xlsx` — 23 epics, 444 features, 2,173 tasks, six milestones, a Jira import of 2,640 issues. **The plan sequences by milestone and the package by wave, and neither references the other.** CF-101 re-waved nine screens and three flows yesterday on flow dependencies, sound reasoning made without knowing this existed. **The plan has effort and the waves have dependencies, and neither alone is a schedule.** Two figures worth arguing with: **M4 Integrations is 3,900 person-days, more than M1, M2 and M3 combined**, against a package that made integrations adaptor-shaped through ADR-0012 and ADR-0015 precisely so they would not be; and **M5 Testing at 58 days plus M6 Release at 26 is 1.1% of build** on a platform whose gates cannot fail. **Where they agree is reassuring**: the plan's Missing Components sheet found audit logging, notifications, configuration, localisation and offline-first independently, and marks **DevOps/IaC and CI/CD as outright MISSING** — the same finding as Sprint 0 sitting at 0 of 11 since 30 July. **And it counts requirements a third way**: 3,332 against the matrix's 3,184 rows and 3,156 distinct. `handoff/delivery-plan-vs-package.md` **Raised as CF-124 in a parallel session on 18 August and renumbered on merge** — the requirement walk had independently used CF-124 for a different finding, and its register is cross-referenced across `clusters.md`, `contract-backlog.md` and `traceability.json`. Two registers were written the same day against the same package and neither knew the other existed. | Chinmay + Qossai | 18 Aug |
| **CF-146** | **Two client files marked pending in the tracker had been supplied.** `Sample_Amphitheater_Seating.pdf` and `Seating_Manifest.xlsx` answer inputs 18 and 30, and `TICVAI_FnB_POS_Visual_Reference_Revised.pdf` answers task 37 — **all three assessed as outstanding hours earlier, because the assessment ran against the package and the package was missing files the client had shared.** The manifest is 396 seats across four sections as description, section, row, seat — **which maps directly onto `seating.Seat` and is exactly what `ImportJob` was built to consume.** `generateVenueLayout` has had something to run against for six days **Raised as CF-125 in a parallel session on 18 August and renumbered on merge** — the requirement walk had independently used CF-125 for a different finding, and its register is cross-referenced across `clusters.md`, `contract-backlog.md` and `traceability.json`. Two registers were written the same day against the same package and neither knew the other existed. | Chinmay | 18 Aug |
| **CF-17** | Venue-map format and guidance standard. Which CAD or SVG conventions venues will supply, so the importer targets something real | **Closed 18 August by a sample from the client.** `Sample-Ampitheater_Seating.pdf` and `Seating_Manifest.xlsx`: **AutoCAD-origin PDF carrying named layers** — `VC-Seats`, `VC-wheelchairseating`, `VC-Steps`, `VC-Balcony`, `VC-HANDRAILS` — with a per-section xlsx manifest of section, row and seat. The importer already targets this: `importSeatGeometry` takes `pdfPlan` with `layerMapping`, `importSeatManifest` takes `xlsxManifest` with `sheetName`, `headerRow` and `columnMapping`, and `ai.generateVenueLayout` drafts the selectable sections into the same preview step for a person to commit. **Four defects the real files expose are CF-122**; the convention question this raised is answered |
| **CF-119** | **76 of 287 tables had no columns, including all 13 AI tables, and nothing failed.** The schema reference was derived once by an ad-hoc script that never entered the package, then hand-patched — so **every table belonging to a contract written after that run was an empty row in the workbook.** All of `ai.*`, 11 `orders.*`, 8 `identity.*`, 6 `approvals.*`. **A table with no columns is not an error to any checker that only asks whether the table exists**, which is why an external audit and two of my own sweeps missed it — I had even added fields to the vector and cache tables on 18 August without noticing the AI tables beside them had none. **`tools/derive-schema.py` written**: it reads `x-ticvai-persistence` off every schema and derives columns from the properties, converting camelCase to snake_case and mapping OpenAPI types to Postgres. **Nested arrays are skipped rather than flattened into jsonb** — a child table silently becoming a column is how a child table goes missing. **And a schema may name two tables** — `fnb.fnb_order + fnb.fnb_order_line` — where the properties belong to the parent; taking the whole string created 25 tables that do not exist, caught by the tooltip check on the first run. **251 of 287 tables now carry columns, up from 211**; the remaining 36 are child rows, partitions, platform-written tables and projections, each stating why. `check-package.py` now fails any schema that names a table with no columns | Chinmay | 18 Aug |
| **CF-118** | **Multi-currency swept across every surface that takes money, and the sweep found three defects worse than the currency gap.** **(1) Ten money operations were attached to screens that handle no money** — `GST-033 AI Concierge Contextual Help` could take a payment, `SCN-001 Sign in` could move cash, `BO-003 Queue Integration Setup` and `PTR-002 Partner Dashboard` could refund. **CF-87's class again**: attached by module resemblance, and it passed every validator because each operation exists and resolves. **(2) `GST-009 Review & Payment` did not declare `createPayment`** — the guest app's payment screen could not take a payment — **and F11 routed food payment through the AI concierge instead**, because it was written against a screen numbering that later changed and every id it named still existed. Steps 1–4 and 6 pointed at notifications, the concierge and lost-and-found. Repointed to `GST-024`, `GST-009`, `GST-025`. **(3) `PartnerAgreement` had a credit limit and no currency.** A UK operator selling a Dubai attraction is invoiced in GBP against sales booked in AED — **`creditLimit` in the wrong currency is a limit that moves with the exchange rate.** Added `settlementCurrency` and `fxPolicy`, which is a commercial term rather than a default: `rateAtSale` puts the movement on the partner, `rateAtInvoice` on the venue, `fixedRate` on whoever guessed wrong when the agreement was signed, and **on a monthly statement across a moving rate the three produce materially different numbers.** All six genuine payment screens now state their currency behaviour — POS takes foreign cash, everything else is card-only where the issuer converts at a rate that is not ours | Chinmay | 18 Aug |
| **CF-117** | **Two package contradictions from an external run — one was eight platforms, not one, and the other was a state model excusing its own broken anchor.** **P09 was written three ways**: the screens declare *TICVAI Web*, the contracts said *Platform Admin Console* and *Platform Admin*. Checking the rest found **eight of twelve platforms named differently in the contracts than in their own screens file** — P05 *Kiosk*, P06 *Staff Ops App*, P07 *Access Handheld*, P08 *Venue Back Office*, P10 *Partner Portal*, P12 *Support Console*, P13 *White-Label CMS*. **The existing check caught a code that does not exist and never a code paired with the wrong name**, which is the failure a reader actually hits. 56 lines across 24 files renamed to the declared short name, and the new check excludes the conflict register — which quotes past wordings deliberately and should not have to launder its own history. **`states/entitlement.yaml` modelled `access.TicketStatus`, which is an object**: a scan-response payload carrying `isValid`, `entriesUsed` and `isInsideVenue`, computed at scan time. **Six states were checked against nothing.** It was also a duplicate — I wrote `entitlement-status.yaml` for `orders.EntitlementStatus` on 17 August with the same six states and did not notice this file existed. **The mechanism is the finding: `openQuestions` downgraded a missing anchor from an error to a warning**, so a model could excuse its own broken anchor by declaring an open question about something else, and the warning read as a known exception for a day. Now always an error, with a hint naming the object. **`access.TicketStatus` now says what it is** — a validation result, not a lifecycle, and the name is what misled the model | Chinmay | 18 Aug |
| **CF-116** | **Multi-currency was modelled as a guest display feature and it is a payment concern on every surface that takes money — corrected 18 August.** Three questions were being answered as one: **display** (what a guest sees), **tender** (what they handed over) and **base** (what the venue books), and they differ for the same sale — a tourist sees USD, pays USD cash, and the venue books AED. **The sharpest finding: `getForeignTenderReport` promised *what was taken in which currency* and `Payment` recorded no currency at all.** The report had no source. `Payment` now carries `tenderCurrency`, `tenderAmount`, `fxRate`, `fxRateSource` and `changeCurrency`. **The rate is stored on the payment, never looked up later** (CF-37) — a payment reconciled next month is reconciled at the rate of the day it was taken. **`fxRateSource: cardScheme` is the case worth naming**: the terminal did the conversion and told us, so the rate is the scheme's rather than ours, and recording it as ours would misstate the margin. **4.6.11 is deliberately asymmetric — accept foreign currency, refund in local — and that asymmetry is what keeps a till reconcilable**: a till giving change in five currencies needs five floats and five counts, and a variance becomes unattributable. `DepositBox.foreignHoldings` counts each currency separately for the same reason. **POS takes foreign cash; the kiosk does not** — a machine accepting foreign notes needs a note reader and a per-currency float and neither is specified. Covers 4.2.8, 4.6.11, 5.11.6 and 6.1.10, none of which had an implementation. `docs/architecture/data-model.md` | Chinmay | 18 Aug |
| **CF-115** | **Storyboards mapped panel by panel against the screens — two duplicates merged, one pair confirmed distinct.** 90 panels against 376 screens. The guest app maps almost one to one, and the value was where it does not. **`GST-064` folded into `GST-036`: I created it in the parity sweep on 17 August without noticing `GST-036` already existed** — same two operations, same name, and **it survived a full audit the same day** because two screens with different ids and identical purpose is not a defect any rule looks for. **`GST-060` folded into `GST-047`**: the storyboard draws the maintenance page on two boards, which is **a convention for a screen every journey can land on, not two screens**, and we had built two. **`GST-050` and `GST-058` checked and kept separate** — one cabana with a Book Now against availability across areas, a detail and a list, and merging them would put a booking action on a browsing screen. **Three real employee gaps**: universal search, which exists only on guest surfaces so **a steward looking up an asset or a colleague has none**; a role-gated module grid, where `resolvePermissions` reaches no screen at all; and a create-request hub fronting nine request types we have individually and behind no single door. P02 is 63 screens, the package 374. `handoff/storyboard-merge-analysis.md` | Chinmay | 18 Aug |
| **CF-111** | **Multi-currency belongs on both surfaces — decided by Chinmay, 18 August. Closed.** CF-91 moved it to the web on 17 August because 2.6.33 and 2.9.1 both name the website and neither names the app. **Guest storyboard board 7 panel 4 draws it on the app** — a selector across AED, USD, EUR, GBP and SAR with *"payment will be processed in AED"* — and board 8 carries an AED selector in its header. **Both are client documents; the answer is both**, which is what CF-93 concluded for every other guest capability. `WEB-035` and `GST-044` now carry the same purpose, states and operations. **Waves differ deliberately**: web is Wave 1 because **that is where an overseas guest compares before booking**; the app is Wave 2 because that is where they check after. **The error state is the part that matters** — rates unavailable shows base currency rather than a stale conversion, because a wrong rate is worse than no rate for a guest budgeting against it. **And `getRegionSettings` was deliberately dropped from both**: a guest does not need the venue's scope configuration to pick a currency, and `listFxRates` is already the currency list — a rate exists only for a currency the venue enabled, so the two questions have one answer. Display only; the sale settles in base currency (CF-37) |
| **CF-113** | **The client storyboards had never been read, and they settle four open items and contradict one closed one.** Three PDFs in `sources/designs/` — **8 guest boards and 6 employee boards, about 140 screens — are images with no text layer**, so every grep for provenance in this package missed them. Rasterised and reviewed on 18 August: all 8 guest boards, employee board 1 of 6. **`TICVAI_White_Label_Guest_App_UI_Reference` is not the guest app** — boards 1 and 2 are the CMS, and twenty CMS screens sat in a file named for something else. **What it settles: CF-92's eight unsourced screens are all drawn**, with board 8 carrying five itinerary screens as one flow and *AI Optimized Itinerary* labelled *Powered by TICVAI*, and **board 6 panel 8 is Digital Companion Mode — the one screen I could not source at all**, drawn as *Your Visit Journey*. **CF-14's guest concierge is board 6 panels 1–3 exactly**, and board 2 panel 8 lists *AI Concierge Chat* as a per-tenant toggle, which is the commercial model drawn as a switch. **CF-41's AI navigation tab is employee board 1** — Home · Tasks · AI · More. **CF-99's guest entry point is board 6 panel 10**, *Live Chat* beside Call Us and Email Us. **Also: 146 of 376 screens cite no design reference, and that is a gap in the storyboards rather than the package** — nothing was drawn for partner, accreditation, support or most of the back office. And `designs/` duplicates `sources/designs/` byte for byte, 7 MB twice. `handoff/storyboards-vs-package.md` | Chinmay | 18 Aug |
| **CF-110** | **Step-by-step check of screens, APIs and the workbook — one real gap: the vector store and the four caches were in the workbook with no fields.** They appeared on the Tables and Where-used sheets with `Columns: —`, so **the one document a reviewer opens to understand the data showed the vector collection as an opaque blob** — and its payload has been specified in ADR-0021 since 17 August. 27 rows added across five tables: `qdrant:knowledge` now lists the vector, **`tenant_id` as the shard key and the tenant boundary on shared placement**, `scope_path` filtered from the resolved session and indexed so Qdrant filters before the vector search rather than after, plus `parent_id` for parentChild chunking and `updated_at` for the drift `staleCount` reports. The caches carry their key composition — **`cache:answer` is keyed on question, scope and locale together, because a cache keyed on the question alone is a cross-venue leak**, and `cache:idempotency` records that its expiry must exceed the longest offline window. **Everything else was clean**: 376 screens with no unknown operation and no dangling exit, 776 operations matching the lineage exactly, and every workbook sheet agreeing with its source. **One of my own checks was wrong before the data was** — it read the Columns sheet on the wrong axis and reported 200 tables missing when the count was zero, then truncated the list before counting it, so the summary said four | Chinmay | 18 Aug |
| **CF-109** | **Vector store separation audited end to end, and the AI page now shows what AI is walled off from rather than only what it is.** The separation holds at every layer: **one collection, eight operations, one contract, one service**, and no screen, service or other contract reaches `qdrant:knowledge` directly. `KnowledgeCollection` carries `shardKey`, `idfScope`, `retrieval` and `embeddingModel`, so the tenant boundary, the sparse-score population and the collection-splitting criterion are all declared rather than implied. **What was missing was visibility, not separation.** `status-ai.json` gained a `boundaries` section — five guarantees, each with what enforces it and whether it currently holds — and `ai-index.md` renders it. **The section reports breaches rather than always reading yes**: injecting a write from `sendAiMessage` into `orders.sales_order` flips *Writes stay inside the domain* to false and names the table. **A page showing only what a domain is answers half the question**; the other half is what it cannot reach, and what keeps that true rather than merely true today — which matters here because three of the five boundaries were breached at some point on 17 August and each was found by a sweep rather than a checker | Chinmay | 18 Aug |
| **CF-108** | **Two vocabularies for who may call an operation, collapsed into one — a decision rather than a patch, forced by the viewer's guest mode.** `x-ticvai-auth` on 46 operations and `x-ticvai-guest-callable` on 59, three carrying both, **nothing stating the difference**, so each was applied by whoever touched the file last. Documenting them on 17 August was not enough: **a viewer cannot filter against two fields**, and V-06 was blocked on exactly that. **ADR-0025: `x-ticvai-audience`, a list, on all 776 operations, both old markers removed.** A list because the answer is genuinely plural — `cancelReservation` is called by staff cancelling anyone's and by a guest cancelling their own, and a scalar forces one of them to be a lie. **Two values did not exist in either vocabulary**: `partner`, reached by 105 operations on partner-web, and `public` — an accreditation reviewer holds `APPROVAL_DECIDE` and signs in from outside the organisation, **neither staff nor guest, and the absence of that value is what produced the misclassification behind CF-106.** Three operations carry it, and being rare is why it was invisible. `session` described a mechanism rather than an audience and folded into `staff`. **`handoff/audience-index.json` gives the viewer its filter in one lookup: guest mode is 96 operations, 111 screens, 9 flows and 95 tables against 776, 376, 24 and 287.** **Platform decides which screens count, not the operation** — an operation marked `[staff, guest]` appears on a back-office screen and a guest-app screen, and filtering by operation audience alone would put the back office in guest mode | Chinmay | 18 Aug |
| **CF-107** | **The archive unpacked as `ticvai-full/` beside the tree instead of into it, and the package was not self-contained.** Two problems from the same cause. **Every zip sent this session created a sibling directory** rather than merging with the working tree, so the layout on disk and the layout being shipped were never the same thing. Root is now `ticvai`, matching the directories Chinmay confirmed locally, and **`backend/`, `viewer/` and `arabic-embed-eval/` are deliberately absent from the archive** — they are built outside it and unpacking must not touch them. **And `schema-reference.json` and `relationship-graph.json` were read from a sibling `work/` directory**, so `build-status` and `derive-domain` resolved them in one working copy and **silently returned empty defaults everywhere else** — which is how the analytical store split vanished from the derived AI set earlier the same day without an error. This is the third instance of the same class after the contracts path and the ADR status field: **a tool that reads outside the package fails silently and looks like success.** Both files now ship in `handoff/`, and the tools were verified from an isolated copy with nothing beside it — 287 tables and four stores, identical. `STRUCTURE.md` written | Chinmay | 18 Aug |
| **CF-106** | **The remaining 15 of the external audit's 21 contradictions — all closed, and four were losing data silently.** **Four duplicate YAML keys**, where the loader keeps the last and discards the rest with no error: a description explaining ADR-0021's central rule was overwritten by a second one, and **`x-ticvai-auth: guest` on three operations was discarded by a later `service`** — a guest operation silently becoming a service one. A duplicate-key check now runs over every contract. **Two guest vocabularies, 105 operations, three overlapping, nothing stating the difference**: `x-ticvai-auth: guest` means guest-only, `x-ticvai-guest-callable` means a staff operation a guest may also call scoped to themselves. Both now documented in `shared/common.yaml`. **And the reason they diverged was a misclassification** — `check-screens` treated P11 Accreditation as a guest surface when it declares itself `public`, which forced `decideApprovalRequest` to be marked guest-callable. **An external reviewer holding a real permission is not a guest**, and the marking read as a guest approving their own refund. Guard corrected, 9 forced marks removed. **`catalogue.productPublished` had no AI consumer**, so the busiest RAG source went stale silently. **`check-flows` appended the wave rule to WARNINGS and exited 0** while CF-101 recorded it as enforced — a claim of enforcement that enforced nothing, which is worse than no rule. Promoted to an error. **Flow `capability` ids were noise**: 12 of 24 unresolvable and every resolvable one wrong, F24's AI concierge declaring *white-label branding*. Replaced with derived `primaryContract` and `contracts`. **ADR-0020 stated an absolute rule with an undocumented allowlist**; both exceptions are now written down, because **an absolute rule with a hidden exception invites a third nobody argues for**. **ADR-0022's own review trigger had fired unnoticed** — it says ten is defensible and it is eleven. **`access.validated`'s notes denied a change made to the same file in the same session.** Plus: LayoutDraft's persistence, the parity document miscounting its own work both ways, both optimisation documents proposing a `rerank` enum value that already existed, and every tool now specifying UTF-8 so `check-package` runs on a default Windows shell | Chinmay | 17 Aug |
| **CF-105** | **External audit of the package found 21 contradictions; the six that mattered are closed, and five were mine from the same day.** **The worst was silent: six validators read contracts from a sibling repo outside the package** — `ROOT.parent/ticvai/ticvai-contracts` — so every check passed for whoever had that repo checked out and read nothing for anyone working from the zip. **A checker that reports success while reading an empty directory is worse than no checker**, and it is why this was found externally rather than here. Now `ROOT/contracts`, verified from a bare copy. **The retired Qdrant rule was still normative**: `ai.yaml` carried *"tenant isolation is partition-level, never filter-level"* — the exact string ADR-0021 retires — 1,300 lines from `shardKey`, which says the opposite, **in the file servers and clients are generated from**. CF-97 exists because someone reasoned from that sentence. Corrected there and in `ai-applications.md`. **ADR-0022 and ADR-0023 both cited ADR-0011 for a three-class offline model that is not in it** — ADR-0011 is The Hierarchy Is Binding and contains the word *offline* zero times. The model is ADR-0013. **Two Accepted ADRs rested on a document that did not say what they attributed to it.** **The edit that made ADR-0001's supersession unmissable made it invisible**: `**Status:** **Superseded**` broke the viewer's first-token parse on six ADRs, while `check-package.py` stripped asterisks before comparing and passed. **Emphasis added for a reader hid the value from every parser** — the same shape as CF-97 one level down. Unbolded, and the checker now requires the raw value to start with a letter. **31 of 80 state models anchored on a dotted enum** (`Wallet.status`) that a naive lookup cannot resolve; the dotted form carries real information, so `enumKind`, `enumSchema` and `enumProperty` were added and **79 of 80 verified against the contracts**. **README carried two different operation counts in one file**, 737 and 753, neither current, and COVERAGE disagreed with both — now generated by `tools/sync-counts.py` from `status.json`. **And ADR-0024 claimed zero lines of SQL while the build workspace holds six migrations and 1,822 lines** — both true, the sentence wrong, corrected the same day the migrations appeared | Chinmay | 17 Aug |
| **CF-104** | **Full audit of contracts, spine, API, data, screens, journeys and derived artefacts — three findings, and the largest was a schema nobody defined.** **`SalesChannel` was referenced by `orders`, `catalogue` and `subscription` and defined by none of them** — four `$ref`s pointing at nothing, which every parser accepted because **a missing local schema is not a YAML error**. Defined in shared, with the distinction stated: a channel is a commercial surface, a platform is a codebase, and `guestApp` and `kiosk` are one app and two channels because a price list may be offered on one and not the other. **`updateGuestProfile` in marketing wrote `pii.subject_biometric`** — my own error from the orphan sweep, and marketing has no business touching a face template. Checking it found that **nothing enrolled a biometric at all**: 3.2.43 Face Pass Enrolment is a real requirement and had no operation. Three added to `access` — **registrable only through the app, a ticket counter or the annual pass counter**, never at a gate; **one facial profile may not be associated with more than one annual pass**, refused explicitly because the reason matters at a counter; consent captured in the same transaction, since **a consent record written afterwards is consent for something that already happened**; and `revokeFacePass` destroys rather than flags, because **a biometric marked inactive is a biometric still held.** **Three lifecycles had no state model** — `EntitlementStatus`, and `DepositBoxStatus` and `PartnerAgreementStatus`, both added earlier today. Written, and the deposit box one carries the control: **`closed` is counted, `reconciled` is agreed, and a box that closes itself is a box nobody checked.** **Everything else was clean**: 776 unique operationIds, no broken refs, every derived artefact agreeing with its source, no broken markdown link | Chinmay | 17 Aug |
| **CF-103** | **The rest of the optimisation work — landed 17 August, and two of my own defects with it.** **`cache:resolution` and `cache:idempotency` were declared as stores and wired to nothing** — two orphan tables added an hour earlier. Now 31 operations and 482. **`cache:idempotency` closes a real gap**: every write has carried a key since this morning and nothing said where seen keys lived or for how long. **Retention must exceed the longest offline window** — a scanner out for a weekend replays on Monday, and a key store that expired on Sunday lets every one of those writes through twice. **`getOfflinePackage` built per device**: a bundle identical for every scanner at a venue, rebuilt once per scanner, so twelve scanners at shift start meant twelve identical builds against the primary at the busiest moment of the day. Now built once per venue per version. **Prompt assembly order fixed** — providers cache a stable prefix and **a trace ID in the system prompt invalidates it for every request that follows**, silently and with no error other than a larger bill. **`frontend-delivery.md` written**: bundles split by wave, and **`guest-app` ships 80 routes to deliver 21** on hotel wifi before a visit; per-platform shells, where the kiosk needs 15 of 80 and the other 65 are attack surface on a machine the public can touch; the offline bundle's three undeclared numbers; and performance budgets, where **three of the five screen classes are guest-facing, time-critical and the smallest apps**. **And the ADR-0020 check fired on me twice** — the cache exemption was hand-listed to two tables and broke the moment `cache:idempotency` reached every write. Generalised to the store tier, and verified it still catches a real breach | Chinmay | 17 Aug |
| **CF-102** | **RAG optimisation: the contract shape that must exist before anything is indexed — landed 17 August, and one claim retracted.** Seven of fourteen techniques adopted. **Three change what is stored and cannot be retrofitted without a per-tenant rebuild**, so they landed now rather than after the embedding benchmark: `KnowledgeCollection.retrieval` defaults to **hybrid**, because a venue corpus is mostly proper nouns — *Yas Waterworld*, *Bronze Annual Pass*, menu item names — and dense retrieval is good at meaning and poor at exact tokens; `chunkStrategy` gains **`parentChild`**, since **a refund policy paragraph retrieved without its heading answers the wrong question confidently**, and `semanticSection`, since fixed-token splitting cuts a rule in half. **And hybrid forced an ADR-0021 consequence**: Qdrant computes IDF shard-wide, so shard-per-tenant fixes the cross-tenant case but **inside a dedicated cell the shard is the whole tenant and venues share it** — `idfScope` added, defaulting to tenant. **Four cache stores added**, on a fourth store tier that did not exist: `cache:answer` — the largest lever at a kiosk, where forty guest questions repeat thousands of times a day, **invalidated by the same nine events that invalidate the index** and keyed with `scope_path`, because a cache keyed on the question alone is a cross-venue leak wearing a performance hat; `cache:embedding`, so a tenant editing fifty of twenty thousand chunks re-embeds fifty; `cache:resolution`; and **`cache:idempotency`, which closes a real gap** — every write has carried a key since this morning and nothing said where seen keys lived. **Retracted: the assessment's largest claim.** It said 485 of 773 operations declared no read routing and called it the first thing to fix. **All 285 GETs declare routing**; the 485 are writes, which ADR-0016 says are not declared. **The error was counting operations rather than reads**, and both documents now say so | Chinmay | 17 Aug |
| **CF-101** | **Eight flows stepped through screens from a later wave, and nothing checked it.** Found 17 August while establishing that CF-100 was not a blocker. **Corrected with one rule: if a wave's flow needs a screen, that screen is in that wave — and where the whole platform is later, the flow was mis-waved rather than the platform.** **Nine screens pulled forward.** `BO-074` chart of accounts and `BO-075` account mapping to Wave 1, because **F16 says so itself** — no chart of accounts blocks the first sale, and it cannot be Wave 2 while venue provisioning is Wave 1. `BO-052` goods receipt from Wave 3 to Wave 2, since requisitions were already Wave 1 and **the end of the chain was arriving two waves after the start**. `EMP-019` and `EMP-020` to Wave 1 together — **the question and the answer are one screen pair** and splitting them ships half a feature. `SUP-004` and `SUP-005` to Wave 2, because **the guest concierge is Phase 1 and a handover needs somewhere to land**; the rest of the console stays Wave 3, since an agent needs a queue and a place to answer from before they need canned responses. **Three flows moved back.** F10 to Wave 2 — the partner portal is entirely Wave 2. F23 to Wave 3 — accreditation is workshop-blocked and entirely Wave 3, and the flow was written as a proposal to take into that workshop. F24 to Wave 2 with the kiosk it runs on. **One step repointed**: F21 used `GST-059 Plan My Day`, which is Wave 3, where choosing a shorter queue needs no saved plan. **And one was mine** — `KSK-015` was set to Wave 1 an hour earlier, so an assistant that guides a guest through checkout would have shipped before the checkout screens. **`check-flows.py` now fails a flow stepping through a later-wave screen.** Waves now 149 · 167 · 60, flows 6 · 15 · 3 |
| **CF-99** | **22.8.5 live-agent handover — closed 17 August, and it was 26 requirements rather than one.** Domain 22.8 is a whole omnichannel conversation platform: inbox, history, identity resolution, routing, queue management, canned responses, rich messaging, translation, payment links, analytics and audit. **The decisive finding is that the support screens were wired to `listCases`** — and a conversation is not a case. **A case is a ticket measured in hours; a conversation is a live session measured in seconds with somebody waiting**, and using one for the other left the queue, the routing and the agent state with nowhere to live. A conversation may create a case (22.8.12); it is not one. **10 operations, 4 tables, a state model, `conversation.handedOver`, and F24.** Three decisions inside it: **the whole thread and the assistant's own summary travel with the handover**, because a handover that loses context is worse than no assistant — the guest already spent the effort once; **`withAssistant` and `queued` are separate states**, since time with a bot is not time in a queue and merging them makes the service level unmeasurable; and **`abandoned` from `queued` is the number to watch** — a guest who asked for a person and gave up before getting one is a staffing failure that looks identical to a resolved conversation in any report that does not separate them. **2.1.25 closed with it**: `startKioskAssist` and `endKioskAssist`, so `KSK-013 Call staff` is no longer a button that calls nothing. The staff member acts on the kiosk session **under their own permission**, and **the guest can always see it and always end it — assistance a guest cannot stop is surveillance.** **Still open, and both are operational: agent hours** — a handover path with nobody on the other end after 18:00 is a queue that never empties, and 22.8.5 does not say — and whether the assistant may take a conversation back when no agent comes |
| **CF-14** | **Guest concierge in Phase 1, charged per token, capped as a warning rather than a stop — decided by Chinmay, 17 August. Closed.** Three decisions and each changed the build. **(1) The cap warns; the venue manager decides.** `ceilingBehaviour` defaults to `warn` and the assistant keeps answering — a cap that stops mid-visit turns a cost control into a guest-facing outage the venue had no warning of, which is CF-61's *degrade, never stop* applied to spend. Warning fires at 80% and again at 100%, because **a manager told at 100% has already spent it**. `ai.ceilingApproaching` written, with staff and guest spend separated since a venue can manage the first and cannot stop guests asking questions. **(2) The assistant is bounded to what 2.1.28 asks for and nothing else** — `guestCapabilityScope`: ticket selection, promotions, FAQs, recommendations, checkout. **Bounding the capability is what makes the cost predictable and the safety posture tractable.** **(3) One provider key per tenant, optionally per venue**, so a tenant's spend bills against their own key and the provider's invoice is an independent check on our meter — **one platform key would leave us apportioning the largest variable cost from our own numbers**. `setAiCredential` and `testAiProvider` written; `docs/architecture/ai-credentials.md` holds the secret-handling design, whose rule is that **no surface ever holds a provider key** — every surface calls TICVAI and TICVAI calls the provider, because a token on a kiosk in an entrance hall bills the tenant from a machine anyone can walk up to. **And the kiosk finally has an assistant**: 2.1.28 named it explicitly and it was the only surface with none, while four existed on the guest app, which no requirement names. `ADM-037`, `BO-091`, `KSK-015` added. **Still open and now separate: 22.8.5 requires live-agent handover**, which makes the concierge a support channel with a staffing model — tracked under CF-99 |
| **CF-98** | **An orphan sweep across tables, permissions, events and schemas found four gaps no checker looks for.** **(1) Deposit boxes are nine requirements (5.8.1–5.8.9) with no contract** — allocation to a cashier, opening float by total or denomination, mid-day withdrawal, automated close-out of one or more boxes. The permissions existed (`CASH_ADD`, `CASH_LIFT`, `DEPOSIT_BOX_MODIFY_OWN/OTHER`) and nothing used them, which is how it surfaced. **A deposit box belongs to a cashier, not a workstation** — a cashier moving between tills takes their float, and that is what makes a variance attributable to a person. **A mid-day skim for banking reduces the expected close**; treating it as a variance makes every busy cashier look short. 5 operations, 1 table. **(2) ADR-0023 says `REPORT_EXPORT_PII` is a distinct permission and no operation used it** — the ADR described a control that did not exist. Six operations now carry `x-ticvai-permission-escalated`, because a permission can depend on the target rather than the operation: closing your own shift and closing somebody else's are one operation and two rights. **(3) 38 tables were touched by no operation.** 26 are children reached through a parent — **the lineage is aggregate-level, which was never written down** — and nine are written by the platform rather than by an operation and now say so. **Three were real: `pii.subject_biometric`, `orders.credit_override` and `identity.mfa_recovery_code` had no reader at all.** **(4) The Excel prose said 654 rows and ten stored procedures**; the truth is 760 and eight | Chinmay | 17 Aug |
| **CF-96** | **Full recheck of screens, flows, states, events and contracts — five gaps no validator was looking for.** All seven passed throughout. **(1) The guest-permission strip caused a regression**: 28 guest screens lost their operations, including `WEB-002` listing, `GST-003` attractions and `GST-049` seat selection — core purchase path. The strip was right and simply never put back what a guest legitimately calls. **(2) 266 of 373 screens had no navigation exit** — a model you can arrive at and never leave. **(3) 93 writes had no idempotency key**, which on an offline-first platform where a till journals locally and replays through `/sync/orders` means **a replayed write creates a duplicate order**. Seven are exempt by design; `forceLogout` additionally had two `parameters:` blocks, YAML keeping the second and silently discarding the first. **(4) Four events had no critical consumer and should have** — `access.validated` releases a held virtual queue slot, so losing it **leaks capacity**; `assets.documentIndexed` is the only consumer of an upload; `fnb.orderReady` means a guest never learns their food is ready, with no recovery path; `retail.merchandisePublished` leaves a delisted product searchable. Three others are correctly non-critical and now say why. **(5) ADR-0019, 0020 and 0021 were missing from the ADR index.** `check-package.py` now enforces AI isolation from the lineage rather than from prose. **220 operations still have a summary and no description — deliberately not generated, because filler would make them look documented without being it** |
| **CF-95** | **Qdrant collection-per-tenant was wrong twice, and both were visible without new information.** The rule *"tenant isolation is partition-level, never filter-level"* produced `qdrant:tenant_{id}_catalogue` and `qdrant:tenant_{id}_knowledge`. **First: it contradicts the vendor at a hard limit** — Qdrant documents one collection per embedding model with payload partitioning as the default, and their scaling guidance says collection-per-tenant does not scale past a few hundred, citing a company that hit the 1000-collection limit and had to migrate. **Second, and worse: ADR-0001 says Cell = Tenant × Jurisdiction, so a cell holds one tenant** — `tenant_{id}_` inside a single-tenant cell names nothing, and the real filtering unit inside a collection is `scope_path` for venues. **We had partition-by-tenant and filter-by-scope and described it as partition-only.** ADR-0021: one collection per embedding model per cell, `scope_path` as an indexed payload the caller cannot supply, tiered sharding available if one venue's corpus starts degrading the others. **The rule is rewritten: tenant isolation is the cell; a filter is not the weak point, a filter the caller can influence is.** Raised by Chinmay asking whether one server with further segregation could work — it can, and it is the recommended shape. **Open: which embedding model**, since it decides one collection or two and Arabic and English in one collection needs a multilingual one | Chinmay + Dinesh | 17 Aug |
| **CF-94** | **Two AI isolation breaches, both invisible to every checker.** `generateVenueLayout` **wrote directly into `seating.import_job`** — AI writing into a transactional contract, which breaks the rule that AI is read-only against the core and would have let a generated layout reach a seat map without a person seeing it. Now writes `ai.layout_draft` and ends at `previewReady`, entering seating at its existing human commit step. And **`askReportingQuestion` still wrote no `ai.interaction` in the lineage** — the contract was brought under governance on 17 Aug and the lineage was never updated, so 8.3.55 was satisfied on paper and not in the data. Both found by an isolation sweep rather than a validator, because **every operation existed and resolved to a real table** | Chinmay | 17 Aug |
| **CF-92** | **Eight guest-app screens had no requirement behind them.** **Closed 17 Aug, and it was my error: I searched the matrix and never the minutes.** Seven of the eight are minuted and agreed. **10 Aug §4.9 specifies "Plan Your Adventure" in full** — a guided itinerary planner benchmarked against Skidata, tied to a ticket purchase, saved as a personal visit profile, **with group sharing via an in-app QR that Chinmay raised and Qossai confirmed**. That covers GST-051 through 054 and GST-059. §4.8 covers cabana as a capacity-decrementing resource booking; §4.1 covers accessibility as tenant-authored content. Provenance now recorded on each screen. **One is genuinely unsourced — `GST-038` Digital Companion Mode**, which reads as a framing for the in-venue capabilities rather than a capability itself. **And checking exposed a bigger issue: four guest screens assume an AI concierge that is not contracted** — GST-031, 032, 033 and 054, where CF-14 is still open on the mechanism and billing, and the AI scope agreed 17 Aug is staff-facing. Flagged rather than resolved; `GST-054` now falls back to the manual planner rather than to an error |
| **CF-91** | **Multi-currency was built on the app and the matrix asks for it on the website.** 2.6.33 and 2.9.1 both name the website; neither names the app. **Closed 17 Aug** — `WEB-035` added, and the reasoning holds: the website is where an overseas guest compares prices before travelling. Both requirements are explicit the sale settles in base currency (CF-37), so this is a comparison aid. **Rates unavailable shows base currency rather than a stale conversion** — a wrong rate is worse than no rate |
| **CF-93** | **Guest web and guest app had drifted apart and nobody decided they should.** **Closed 17 Aug: nine screens added, two operations written, fourteen marked guest-callable.** Only 54 of 3,184 requirements name a guest surface, so parity is the default. **Two operations genuinely did not exist:** `searchCatalogue` — the app could browse by category and not search, and this is keyword search over the catalogue rather than AI, so **it works with no assistant configured**; and `getLoyaltyPosition` — 158 loyalty requirements and **nothing returned a guest their own balance**. **Fourteen operations were staff-only and should not have been**, including `cancelReservation`, so a guest could make a reservation on the web and not cancel it. Eight app-only capabilities stay app-only by design — map, wait times, virtual queue, F&B ordering, tracking, in-venue notifications, dynamic QR, shop-and-drop collection. **A guest standing at a ride does not open a laptop** |
| **CF-90** | **AI had four lifecycles and no state model, found by asking whether it needed a folder.** It does not — the package is organised by artefact kind — but the question surfaced that `ProposedAction`, `IndexJob`, `KnowledgeDocument` and `LayoutDraft` all carried a status enum nothing modelled. Written, and three distinctions came out of it. **`approved` is not `applied`**: the gap between them is where a failure hides, because an approval nobody acted on looks identical to one nobody made — and an approved proposal that expires means someone believes they authorised a change that never happened. **An index job swaps atomically or the assistant answers from a half-built index**, which is worse than a stale one because stale is wrong in a knowable way. **A knowledge document is not retrievable while `processing`**, and a screen showing "added" too early sends a user searching for something that does not exist yet. `handoff/ai-index.md` written as the index a folder would have been |
| **CF-89** | **ADR-0009 answered whether AI data may leave the country and never where the AI service runs.** Raised by a question about folders that turned out to be about isolation. **Every AI table was specified on the transactional primary**, so `ai.interaction` — a row per prompt, with response, sources and tokens, and no retention (CF-64) — was the fastest-growing table in the platform, sharing a connection pool with `access.scan_event`. **A gate scan runs tens of thousands of times a day and must not queue behind a report-generation prompt**; ADR-0016 already separates analytical reads for exactly this reason and AI writes were never considered. **ADR-0020 proposed:** retrieval and the index stay in the cell, so residency is automatic rather than argued; inference is a call out and **the prompt is the only thing that crosses a border**; and the three AI log tables move to the analytical store while policy, provider and index configuration stay on the primary. **Two consequences worth naming**: a cell without Qdrant has no AI and cannot gain it by configuration, and **on-premise venues have no route to a provider — the honest default is no assistant there** (CF-61) | Chinmay + Dinesh | 17 Aug |
| **CF-88** | **The state checker had a blind spot covering 31 lifecycles.** It collected enums declared as named schemas — `OrderStatus`, `WorkOrderStatus` — and never looked at a lifecycle written inline on a property. **`Payment.status`, `Refund.status`, `Performance.status`, `RedemptionRight.status` and 27 others were invisible to it**, so 45 state models reported 100% coverage against 46 enums while 31 more existed and were unchecked. Found by asking whether AI needed its own folder — it does not, but **`ai.yaml` has four inline lifecycles and no state model**, which is what the question surfaced. Checker extended; it now reports every inline lifecycle with no model. **31 warnings, and they are a backlog rather than a defect** — an object with a documented enum and no model is under-specified, not wrong. The sharpest are `Payment.status` (7 states including `pendingConfirmation`, which is the unknown-payment case) and `RedemptionRight.status`, which carries cross-cell entitlement validity | Chinmay | 17 Aug |
| **CF-87** | **A warning I chose to leave was hiding two defects.** `check-states` reported that `states/entitlement.yaml` referenced `access.TicketStatus`, which does not exist. I noted that the model documented the absence and left it — **and documented is not resolved.** Checking properly: `MediaEntitlements` exposed `isRedeemed`, a boolean, where the state model had held six states since 14 August. **A boolean cannot distinguish a ticket that was used from one that expired, was refunded, or was transferred to somebody else** — four different conversations at a gate, and the steward could see only "not valid". `EntitlementStatus` added; `isRedeemed` replaced with `status`, `entriesUsed` and `transferredToSubjectId`. **And the same check exposed 659 staff operations declared on guest screens** — `applyManualDiscount` and `exchangeOrderLines` on a guest's own ticket list, from the sibling-attachment pass ignoring the guest guard. Every other checker passed, because each operation existed and resolved to a table. Removed, and `check-screens` now fails a guest surface declaring an operation with a staff permission. **Screens with operations drops 95% to 84%, which is the honest figure** |
| **CF-86** | **Two commercial surfaces had screens and no contract behind them.** **Partner agreements**: `getB2bCredit`, `setB2bCreditLimit`, `getChannelAllocations` and settlement all existed, and **the agreement that says what a partner is entitled to did not** — 2.7.32 asks for commercial agreements with resellers, 2.7.13 for commissions, 2.7.36 for credit terms, 2.7.37 for expiry alerts. `PTR-011 Quote Management` and `PTR-017 Commission Statement` drew screens over nothing. **Net rate and commission are alternatives, not both** — a partner on both is paid twice for the same sale; and **amending an agreement creates a version**, because an order placed last week used last week's rate and settlement must say which. **Parking**: CF-52 settled three integration modes on 14 August and **no operation was written for three days**. `plateWhitelist` is the only mode needing a plate at sale and the only one where the barrier opens with nothing presented. **A failed push does not void the entitlement** — the guest paid, and a barrier that will not open is a staffed problem, not a refund. **9 operations, 3 tables, 737 total** |
| **CF-81** | **There was no cart, and nine requirements needed one.** `createOrder` takes a whole basket at once — correct for a till, which builds locally and submits atomically offline, and it looked enough like a cart that nobody checked. **Nothing client-side can satisfy 2.1.31** (start on one channel, finish on another), **2.6.43** (resume on a different device), **2.6.45** (identify abandoned and send a resume link), **2.9.4** (adding a capacity product reserves it for X minutes) or **2.9.5** (conflict detection across the whole cart). It also blocks the 14 August decision that ticketing, F&B and retail share one cart. **Built 17 Aug: 10 operations, 2 tables, a state model and `cart.abandoned`.** A cart holds leases; an order holds money. **`abandoned` and `expired` are deliberately distinct** — one is a decision and only the other should be chased; and `expired` is not terminal, because a guest following a recovery link should revive their cart rather than start again. POS keeps its client-side model |
| **CF-82** | **Three more concepts the matrix names and the contracts did not.** Found by sweeping every repeated noun in the matrix against the operation and schema vocabulary — the same sweep that found the cart. **Donations** (1.1.128–1.1.133): campaigns, fixed or free or round-up amounts, several campaigns in one transaction, and **posting to a liability account rather than revenue** — money collected for a charity is not the venue's to recognise. **Waitlists** (1.1.25, 4.9.11, 4.9.17): **not the queue** — a queue is people standing at a ride, a waitlist is people who wanted a sold-out session; joining reserves nothing, and an offer holds capacity for a window then moves on. **Table reservations** (4.9.13, 5.1.1, 5.1.2): distinct from `claimTableSession`, which identifies a table a guest is already sitting at. **11 operations, 4 tables. 728 operations.** Verified against 50 candidate nouns; the rest were covered under other names |
| **CF-23** | **10 Aug §5.1 has a lost subject** — "agreed this makes more sense and will be adopted" names nobody. **Closed 17 August: the missing name no longer matters, because the decision was superseded two days later.** The 10 Aug text is recoverable from context — **Qossai proposed removing the role-selection screen entirely**, taking an employee straight to a role-specific home, and someone unnamed agreed. **12 Aug §4 replaced it**: a user with one role goes straight through, a user with several is prompted. That is what `selectRole` implements and what EMP-002 draws. The unattributed sentence is a minuting defect worth fixing in the record, not an open decision — **and the reason it resolved itself is that the later decision was minuted with a subject.** Chitrangi to correct the 10 Aug minutes for the record |
| **CF-18** | **Dynamic bundle auto-discounting design.** **Closed 17 August — ADR-0019**, and the gap was two things wearing one name. **The contract could not express a dynamic bundle at all:** `BundleComponent` names specific variants with swaps, which is a fixed bundle, not "pick any three attractions" (3.5.10). **And the pricing rule was undecided.** Both settled: **a dynamic bundle has a fixed price and a variable allocation.** The guest picks; the total does not move. Computing the total from the selection was rejected — it quotes a different number every time the guest changes their mind, it is an unforecastable revenue line, and **it makes the highest-value combination the one every guest picks**, which is exactly what a venue least wants to sell at a discount. **Allocation is proportional to list price at the moment of sale and frozen on the order line**, so attraction-level revenue and inter-entity settlement reflect what was actually consumed. A component's list price changing later does not reprice orders already placed — **putting up a cinema ticket has not repriced the family pass**. New `BundleChoiceGroup` shape; a bundle may carry fixed components and choice groups at once |
| **CF-42** | **RTL and dark theme are Sprint 1 scope, not a later localisation pass.** **Closed 17 August with a specification rather than a restatement** — `docs/architecture/rtl-and-theming.md`. All twelve platforms declared `directions: [ltr, rtl]` and **the declaration had nothing behind it**. The spec says what mirrors and what does not, because that is where teams go wrong: **layout, icons and steppers mirror; numbers, times, phone numbers and QR payloads do not**, and `AED 213.75` inside a right-aligned Arabic line is the case that breaks naive implementations. Rules for `design-tokens`: no physical direction properties with a lint rule that fails the build, one icon set mirrored by transform with an explicit no-mirror list, `dir` on the document rather than a class, one font stack pairing Arabic and Latin at matched optical size. **Three themes, audience-driven not a preference toggle** — light white-labelled for guests, light TICVAI-branded for staff, dark for the staff app because the client's own reference is dark. **Dark is not an inverted light theme.** ~4 days now; weeks and a worse result after 347 screens exist. **Open: whether French ships** — the white-label board shows a Francais tab and nobody has confirmed it is real |
| **CF-49** | **Release-management scope skipped the matrix.** Thirteen Platform Admin screens raised 30 July, minuted, contracted as `platform-ops`, never written as a requirement. **Closed 17 August by measuring it rather than restating it:** `handoff/traceability-register.md` maps every contract to the requirement domain it serves, and **exactly two serve none — `platform-ops` (24 ops) and `cross-cell` (16 ops), 6% of the platform.** The two are not equivalent. **`cross-cell` is defensible**: data residency forces separate cells (ADR-0001) and the matrix asks for cross-venue membership (CF-31) without saying how, so it is downstream of a requirement even though none names it. **`platform-ops` is not** — it exists because TICVAI must ship software to cells, which is necessary and **entirely absent from a matrix the client signed**, so it is scope the client is paying for and has never seen itemised. **Proposed: add a Platform Operations domain to the matrix** and take the 24 operations to the client as an addition. `check-package.py` now fails a contract naming neither a requirement domain nor a cross-cutting reason — **a contract without a requirement is sometimes right, but never silent** |
| **CF-24** | **9.2% actor coverage — 292 of 3,184 requirements name a human actor.** **Closed 17 August, and the measure was wrong.** Actor coverage in requirement *text* is not what makes permission design sound; what matters is whether every operation says who may call it. **642 of 707 carry a staff permission, 110 distinct.** The other 65 are guest, device, service-to-service or pre-session by design. **But 54 of them carried `x-ticvai-permission: null` with no statement of what protected them instead** — and unauthenticated by accident is indistinguishable from unauthenticated by design when you are reading a contract. Every one now declares `x-ticvai-auth`: anonymous, session, guest, device or service, each with the reason. **All 707 declare an auth model**, and `check-package.py` fails any operation with neither |
| **CF-65** | **52 reports named by title and nothing listed them.** **Closed 17 August, and counting properly found 133 rather than 52** — plus a split that matters more than the number. **20 are engine capabilities** — scheduling, export formats, saved layouts, access rights, reporting ranges — and every one is already satisfied by `reporting.yaml`'s 23 operations. **113 are report definitions**: a specific report a venue expects to exist on opening day. **A report engine and a report are different deliverables**, and conflating them is why this looked like 133 units of work when it is one built thing and 113 seeded rows. Each becomes a `reporting.report_definition`, and `askReportingQuestion` can generate most from the requirement text. `handoff/report-register.md`. **Two genuinely missing:** tax reports in approved government formats (6.1.23) — a UAE FTA specification nobody has supplied, and the one report where a wrong layout has a regulator at the end of it — and five-year retention (6.1.43), which sits inside CF-64 |
| **CF-60** | **113 requirements on three matrix sheets had never been counted.** Every figure quoted since 30 July was the Functionality sheet alone. **Counted 17 August, and adding 113 to the coverage figure would have been the wrong correction** — two of the three sheets measure something other than software. **Training & Knowledge Transfer (40) is a delivery commitment**: "System provider shall deliver administrator training" has no screen, no operation and no table, and belongs in the SOW. **Disaster Recovery & BCP (62) is infrastructure** — backup schedules, replication and restore drills are Dinesh's layer, not a contract; only RPO and RTO sit in the platform, and they refine CF-64. **Compliance & Security (11) is the only sheet competing for the same denominator**, and seven of the eleven are already covered by identity and pii. **Three denominators, not one bigger one:** 3,195 platform requirements, 62 infrastructure, 40 delivery. `handoff/other-sheets-coverage.md` |
| **CF-59** | **Two domains had no owning contract, and neither was workshop-blocked.** Approval Workflows & Governance (80 reqs) and Employee Mobile App & AI Assistant (50 reqs). **Both closed 17 August** — CF-59a built `approvals`, and CF-71 resolved the second. **Closed** |
| **CF-71** | **A task has no contract, and the employee app is fifty screens built around them.** **Closed 17 August, and the premise was half wrong.** Reading all fifty requirements showed that **18.2.x is Work Order Inbox, Accept, Reject, Start, Pause, Complete and Close** — a task in the employee app *is* a work order, and building a separate `Task` object would have given a venue two inboxes for one job. The real gap was that **`updateWorkOrder` did six different jobs** — assign, start, pause, resume, block on parts and reopen were all one PATCH, so neither the state model nor an audit could say which operation caused which move. Seven named operations added to `maintenance` (accept, reject, start, pause, resume, close, attach evidence), plus a new **`workforce` contract** for the three things nothing owned: rota (18.9.2), attendance (18.9.1) and announcements (18.9.3–5). **707 operations. F08 now names an operation for all eight steps** |
| **CF-79** | **The register drifted from its own rows a second time** | Eleven closed conflicts were still filed under an open section, and five belonging to the client were under Softlabs. **The same failure as the count drift on 14 August**: new rows are inserted at the top of the open table and completed ones are never moved. `build-cf-index.py` now warns on any row whose text reads as resolved while it sits under an open heading — the count check was not enough because a correctly-counted row can still be in the wrong table |
| **CF-80** | **Table reference audit — one duplicate and 59 unlinked tables.** `whitelabel.faq` was added by hand for the RAG source list and duplicated `whitelabel.faq_entry`, which is derived and carries the columns; removed. **Fifty-nine tables had no relationship in either direction** — every AI and approvals table, and the ten added for the lineage, because I created them without deriving their keys. **56 foreign keys added; six orphans remain and all six are correct**: two default partitions, the deliberately exempt outbox, version bookkeeping and two Qdrant collections, which have no foreign keys at all. 462 relationships |
| **CF-53** | **73 back office screens are counted and never itemised.** The page inventory carries "POS · Scanner · Back office — 73" from v1 with no list behind it. Six are now written (P08 queue and parking); the remaining ~67 have no ids, no names and no owner, and were nonetheless in every estimate | Chinmay | 14 Aug |
| **CF-62** | **`createReservation` had been silently absent from the contracts.** `/reservations` was declared twice in `orders.yaml`; YAML keeps the last block and drops the first, so a POST was lost to a GET. Every check passed on what survived, because there was nothing left to be inconsistent with. Found on 14 Aug by the state-model checker looking for something else. **Recovered — 624 operations.** A duplicate-path check now runs in `check-states.py`. One further duplicate found and removed in `seating.yaml`, where an operation had been added under a name that already existed | Chinmay | 14 Aug |
| **CF-77** | **Package audit, 17 August — five defects across artefacts that each validated cleanly on their own.** (1) **`permissions.yaml` existed three times** — `shared/`, `spine/` and `satellite/` — and **the satellite copy had already drifted four permissions behind**, missing every approval permission. Duplicated by my own `cp` lines; removed, and `check-package.py` now fails any shared file copied into a tier. (2) **Two derived tables carried a doubled suffix** — `homepage_section_section`, `navigation_item_item` — a deriver appending a child noun to a name that already had it. (3) **A two-table persistence marker was read as one**: `orders.b2b_credit + orders.credit_override` produced a table called `orders.b` and lost `credit_override` entirely. (4) **22 lineage entries named tables the reference does not have**, from hand-mapping against remembered names; 13 renamed, 10 genuinely missing and added. (5) **17 broken markdown links** from flattening `ticvai-docs` into `docs/`. **A seventh validator now checks the layers against each other** rather than each in isolation | Chinmay | 17 Aug |
| **CF-78** | **`askReportingQuestion` is an AI capability outside the AI governance boundary.** Reporting's natural-language query calls a model, and wrote no `ai.interaction` — so requirement 8.3.55, log all AI prompts, did not cover it. It is genuinely distinct from `sendAiMessage`, which retrieves from documents where this generates a query and returns it for checking, but **distinct capability is not the same as ungoverned**. Now resolves its provider through `ai.provider`, respects `ai.policy` including the masking list, and writes the same audit record | Chinmay | 17 Aug |
| **CF-59a** | **Approval Workflows — 80 requirements, no contract, implemented four times.** Refund thresholds, shift variance, release promotion and manual discount each carried their own approval logic; a fifth was about to be written for AI proposed actions. None shared a rule, an audit trail or a delegation model, so "who approved this and why" had four answers and "I am on leave, route my approvals to Sara" had none. **Closed 17 Aug: `approvals.yaml`, 13 operations, one state model, two events, six tables — 689 operations.** Four rules are not configurable: a requester may never approve their own request, a rejection carries a reason, a completed record is immutable, and **approval records an authorisation without performing the action** — the owning contract executes afterwards under the approver's own permission. Risk scoring (11.1.12) and AI recommendations (11.1.72–75) are parked with the model-dependent AI; the routing rule accepts a `riskScore` today and nothing supplies one | Chinmay | 17 Aug |
| **CF-75** | **The lineage read 51% because the deriver gave up at six levels of nesting.** A paged response nests nine before its item ref — `responses → 200 → content → json → schema → allOf → item → properties → items → $ref` — so **every list operation in the platform resolved to no table**, `listProducts` included, while `getProduct` on the same schema worked. Raising the limit took it to 73%; path inference for commands and hand-mapping the ninety genuine projections took it to **676 of 676, 100%**. The figure had been quoted as "51% resolved, and the rest are projections" for three days, and half of that was wrong | Chinmay | 17 Aug |
| **CF-76** | **Seven domain events are needed for RAG indexing and none exists.** `whitelabel.contentPublished`, `fnb.menuPublished`, `retail.merchandisePublished`, `maintenance.templatePublished`, `marketing.caseClosed`, `reporting.definitionPublished`, `assets.documentIndexed`. **None is AI-specific** — each is a fact the owning service should publish anyway, and other consumers will want them. Writing them for indexing gets them written for everything else. `catalogue.productPublished` already exists and is the model. **Closed 17 Aug — all seven written, 23 events total.** Four carry a stated reason for having no state model behind them: their entities publish via a field rather than a status enum, which is defensible and now recorded rather than left as a warning |
| **CF-73** | **288 AI requirements had no contract, and the reqs-per-operation ratio hid it.** The matrix files the entire AI platform under "Unified Operations Dashboard", which was mapped to the `reporting` contract — 23 operations against 332 requirements read as *thin reporting* rather than *absent AI*. **Scope set 14 Aug: build anything that needs no trained model.** In — AI Engine (38), Governance (29+7), the real configuration assistant (5) and map generation from the MoM = **80 requirements, `ai.yaml`, 17 operations.** Parked — Demand Forecasting (58), Fraud Detection (54), Dynamic Pricing (46), Recommendation (36) = **194**, buildable front-end, back-end and tables, engines deferred until there is history to train on. **Not AI at all: 59** — 15 misfiled no-show/invitation/future-dated-pricing requirements belonging in `orders` and `catalogue`, 34 BI already covered by `reporting`, 10 Operations Command Center which is a live dashboard | Chinmay | 14 Aug |
| **CF-72** | **Twelve more lifecycle operations were missing, found the same way as CF-66.** Writing the remaining eighteen state models exposed `rejectRequisition`, `returnRequisition`, `cancelRequisition`, `closeTransferShort`, `recordInvoicePayment`, `disputeInvoice`, `resolveInvoiceDispute`, `cancelInvoice`, `rejectRelease`, `withdrawRelease`, `startRollout` and `rollbackTenantMigration` — every one an enum value nothing could reach. **The pattern is now confirmed rather than suspected:** the contracts were written for the happy path of each object and not its refusals, returns and reversals. `returnRequisition` is the clearest — without it every incomplete request is a rejection and the queue stops moving. Added — 654 operations, and **all 39 status enums now have a checked state model** | Chinmay | 14 Aug |
| **CF-68** | **The contracts have no concept of a payment provider.** Two gateways are named in the integrations sheet — Stripe and Network International — and there is no `PaymentProvider` enum, no provider field on `Payment`, and no way to say which gateway a payment went through. **This breaks settlement reconciliation:** `ingestSettlementFile` matches an acquirer's file against payments, and with no provider there is nothing to match on but amount and time, which is how two transactions of the same value in the same minute get reconciled to each other. The platform runs two providers from day one, so this is not hypothetical | Chinmay | 14 Aug |
| **CF-70** | **Three Postgres enums disagreed with their contract counterparts.** `platform.device_kind` held nine values the API did not and the API six the database did not — I wrote V0003 from the integrations list without checking the contract. `platform.scope_level` had `sub_department` against the contract's `subDepartment`, **a hierarchy level that would not resolve.** Both reconciled to a union of 20 device kinds and camelCase throughout. **Nothing had ever compared a Postgres enum to its contract**; `check-migrations.py` now does, and it was verified by breaking one deliberately | Chinmay | 14 Aug |

### Resolved in the 14 August working session

| ID | Issue | Resolution |
|---|---|---|
| **CF-56** | **ADR-0014 contradicted by the shared-tenant hosting model.** 14 Aug confirmed two models: dedicated infrastructure per tenant, and a shared TICVAI database hosting many small tenants with logical isolation by site id. ADR-0014 states a cell holds exactly one tenant, and `platform.tenant` was written as a single-row projection **with no RLS on that basis** — on a shared cell that is a cross-tenant leak. Also affects orchestrator fan-out: a shared cell has many tenants on one schema version. **Closed 14 Aug.** ADR-0014 amended with a third cell kind. `platform.tenant` RLS added the same day. Two further points settled by the client: **a tenant can be migrated from shared to dedicated** — the shared start is not a decision they are stuck with, and that is what makes the cheaper package sellable — and **capacity is driven by traffic and concurrent users, not tenant count**, so scaling out means launching another identical cluster rather than growing one. Six operations added: `getCellCapacity`, `listCellClusters`, `launchCellCluster`, `listTenantMigrations`, `planTenantMigration`, `executeTenantMigration`. `Cell.tenantId` is now nullable |
| **CF-55** | **Four POS operations existed in the delivered design and in no contract.** The Park POS mockups show Hold Order, Discount, Add Tip and a cash drawer, none of which had an operation. Now added: `holdOrder`, `resumeOrder`, `applyManualDiscount`, `addTip`, `recordNoSale`, plus a `held` order status and the `ORDER_DISCOUNT` permission. **A design is a requirements source and had not been read as one** — the mockups arrived and were treated as visual reference rather than as a specification with buttons that must do something | Chinmay | 14 Aug |
| **CF-54** | **The `pii` schema was declared and empty.** V0001 created the schema, the V0001 rollback dropped two tables in it, and four foreign keys in V0003b referenced `pii.subject` — which no migration created. `psql` would have failed on the first FK; the structural checker reported PASS, because it verified conventions and never verified that a referenced table exists. **Both fixed:** `V0001a` writes the four PII tables and an erasure function, and the checker now fails any FK whose target no migration creates. Closed on the day it was found | Chinmay | 14 Aug |
| **CF-50** | **Guest self-ordering had no contract.** `diningAndFnb` is a module a tenant can switch on in the guest app, and nothing was reachable behind it — `listOutlets` needed `SCOPE_VIEW`, menus needed `PRODUCT_VIEW`, and `createFnbOrder` needed `ORDER_CREATE`, all staff permissions on `bearerAuth`. Six guest operations now added. **The same failure as push notifications: a feature toggle wired to nothing.** Worth auditing the remaining twelve module keys for the same gap | Chinmay | 14 Aug |
| **CF-66** | **Seventeen enum values existed that nothing could reach.** Writing state models for twenty entities found that the contracts could start and finish things but not manage them mid-flight: `paused` on campaigns and promotions, `sent` and `acknowledged` and `closedShort` on purchase orders, `assigned` on coupons, `variancePending` recount, `decommissioning` on cells, journal `reject`, period `reopen`, case `reopen`. **All seventeen were declared states with no operation reaching them.** Added — 642 operations. Also corrected: twenty-three transitions named operations that exist under different names, including `transitionProductLifecycle`, which handles every product move in one operation rather than six | Chinmay | 14 Aug |

### By client decision

| ID | Issue | Closed by | ADR |
|---|---|---|---|
| CF-01 | Multi-role login contradiction | 12 Aug §4 — conditional role prompt | [0003](../adr/0003-conditional-role-selection-at-login.md) |
| CF-02 | Session concurrency | Client — single session only | [0004](../adr/0004-single-session-per-user.md) |
| CF-03 | Role vs workstation authorisation | Client — user and role driven, any device | [0002](../adr/0002-authorisation-is-user-driven-not-workstation-driven.md) |
| CF-05 | Four competing role taxonomies | 12 Aug §9 — no predefined roles; 7.1.12 is a seed list | — |
| CF-07 | Refund authorisation | 2.12.3 — dual-auth below threshold | — |
| CF-08 | Flying POS | Client — workstation profile, offline-mandatory | — |
| CF-09 | Kiosk operator model | Client — login page plus assignable permission | — |
| CF-10 | Till handover on breaks | 12 Aug §1 — log out or suspend shift | — |
| CF-11 | Approval model depth | Client — add delegation, out-of-office, parallel, consensus | — |
| CF-12 | Universal Cashier landing screen | 12 Aug §3 — sale board per workstation | — |
| CF-13 | Guest app publishing | Client — Option C, tiered | [0006](../adr/0006-tiered-guest-app-distribution.md) |
| **CF-31** | Do entitlements cross jurisdictions? | **Client: yes.** Home-cell ownership with delegated redemption. Only a pseudonymous `guestLinkId` crosses | [0010](../adr/0010-cross-jurisdiction-entitlements.md) |
| **CF-33** | Queue management ownership | **Client: adaptor-first.** Q1 gets the inbound API, adaptor framework and a venue toggle. Q2 is infrastructure | [0012](../adr/0012-queue-integration-adaptor-first.md) |
| **CF-34** | Is the hierarchy binding or illustrative? | **Client: binding.** Seven levels confirmed; only the populating example was illustrative | [0011](../adr/0011-hierarchy-is-binding.md) |
| **CF-36** | `ORDER_REFUND_APPROVE` threshold | **Client: venue policy, not permission scope.** The permission stays binary; thresholds are venue configuration, because venues run different policies and the permission model should not encode commercial rules | — |
| **CF-38** | Price variance on re-pricing | **Client: variance account required, threshold per venue.** Quoted price honoured, difference posted, above threshold becomes a reviewable exception | — |

### By research or architecture

| ID | Issue | Closed by | ADR |
|---|---|---|---|
| CF-04 | Front-gate scanning surface | 2.13.6 plus 3.2.17 | — |
| CF-19 | KDS scope | 31 Jul §11 — integration point only, not a build | — |
| CF-22 | Accreditation vs entitlement | Precedent confirmed | — |
| **CF-15** | Online-first vs offline-first POS catalogue | **Case study. Local-first.** One read path, always local. Leases for contended inventory, local journal, server re-prices on ingest. The framing was a false binary — data classes need distinct strategies, not one architectural choice | [0013](../adr/0013-local-first-point-of-sale.md) |
| **CF-20** | AI data residency | **Research plus ADR-0009.** No single UAE AI residency rule; the obligation derives from PDPL Art. 22/23. Residency is architectural — in-region inference, in-cell prompts, logs and vectors | [0009](../adr/0009-ai-data-residency.md) |
| **CF-32** | Hyperscaler presence in the second jurisdiction | **Dissolved by ADR-0014.** The jurisdiction came from an illustrative diagram, not a deployment. Cell = Tenant × Region makes availability a provisioning-time placement check | [0014](../adr/0014-cell-per-region.md) |
| **CF-39** | White Label Builder had no owning context | **Closed.** Promoted to its own satellite contract — `white-label`, 41 operations. It is a CMS plus an app builder, not a settings page, and 20 screens in the client boards confirm it | — |
| **P08-047** | Channel-based offline inventory pooling — "design not yet agreed" (2 Aug) | **Closed by ADR-0013.** Allocation and leases are not competing designs: allocation is the commercial pool, a lease is the technical hold drawing from it. They compose | [0013](../adr/0013-local-first-point-of-sale.md) |

### By correcting an artefact

| ID | Issue | Closed by |
|---|---|---|
| CF-16 | Reference manual undelivered | Received |
| CF-25 | Party inversion in the 12 Aug MoM | Client correcting the record |
| **CF-43** | AI governance has no screens | Six screens and ten sub-screens added as P08-111A→F — activity log, approval queue, explainability centre, AI audit trail, token analytics, consent controls. Hierarchy v2.1 |
| **CF-44** | Conversational coverage 3 of 8 | Four surfaces added as sub-screens — guest concierge, kiosk, employee voice, financial. Sub-screens because they are one framework rendered per surface, which is what "eight configurations of one framework" means. Hierarchy v2.1 |
| **CF-33a** | Queue vendor selection | **Closed 14 Aug. Client: integrate with the on-site systems already installed, and allow manual entry alongside.** `manual` is now a first-class adaptor rather than the absence of one — a venue with a supervisor and a clipboard is configured, not unconfigured, and the health view must not report it as broken. A `testQueueFeed` operation was added: configuring an integration without a test is configuring it blind. Venue Back Office gains a Queue Integration Setup screen (BO-003) and a Manual Wait Time Entry screen (BO-004) |
| **CF-37** | **FX policy for cross-region revenue splits** | **Closed 14 Aug.** Allam settled the principle: **every transaction is stored in the base currency of the region it was transacted in.** A guest paying USD 100 in a UAE venue has the dirham equivalent stored; change and refunds are always in base currency; a foreign-tender report shows what each cashier took in which currency. Most of the questions FX usually raises then do not arise — there is no rate to lock at redemption, expiry or refund, because the stored amount was never foreign. **The cross-region split follows from the same principle rather than extending it:** the obligation between two legal entities arises **at redemption**, denominated in the **consuming** entity's base currency at that date's rate; movement between arising and settling is FX gain or loss on the **selling** entity, which holds the balance; revalued at close. Neither entity ever holds a currency that is not its own. Four operations added — `listFxRates`, `setFxRate`, `getForeignTenderReport`, `listInterEntityObligations`, `runFxRevaluation` |
| **CF-58** | Entitlements could not be appended to an existing ticket | **Closed 14 Aug.** `getMediaEntitlements` and `appendEntitlementToMedia` added, keyed on **media code rather than order id** — the guest has the wristband in their hand and does not know which order issued it. Appending creates a **new order**; the original is untouched, because editing a paid and reported order moves yesterday's revenue. Entitlement templates gain `canShareMedia`, false for anything surrendered at use — a single-entry ticket taken at the gate is not a claim token for a locker bought afterwards |
| **CF-63** | 263 of 273 "configurable" requirements did not say at what level | **Closed 14 Aug by rule rather than by 263 answers.** ADR-0018: configuration resolves up the scope tree, nearest ancestor wins, **three levels — tenant, region, venue.** Client correction that made it work: **venue is the floor.** A workstation is assigned a profile the venue defined; an outlet is assigned a menu the venue defined. Nothing below venue configures anything, because forty workstations configured individually is forty things that drift. Region holds law and money; tenant holds brand, identity, consent and one-scheme concerns. `x-ticvai-config-scope` now on 28 operations, checked by `check-config-scope.py`, which found two real defects — `createMessageTemplate` and `createLoyaltyProgramme` were callable by a venue manager while setting tenant-wide values |
| **CF-52** | Parking scope | **Closed 14 Aug.** Three modes: no integration (we issue a QR, security scans it) · plate number pushed to the vendor's ANPR whitelist, the expected path · QR pushed to the barrier where there is no ANPR. Hourly parking charged on exit is **out of scope** — the parking POS handles it without us. **Direction settled: we consume the vendor's API and push to it**, rather than publishing one for them to call |
| **CF-45** | Two Phase 1 screens require operating history | Both annotated. They ship in Phase 1; the predictive source arrives in Phase 2. Hierarchy v2.1 |
| **CF-46** | 3,185 / 22 domains vs 3,184 / 21 | Read Me corrected; note 4 expanded to explain the earlier figure. Hierarchy v2.1 |
| **CF-47** | ~102 screens absent from the page inventory | Five platforms added — Guest Web (29), Platform Admin (36), Partner Portal (21), Accreditation (8), Support Console (8). Inventory 203 → **305**. Surfaced 28 screens with no contract behind them |

---

## Withdrawn or absorbed

| ID | Was | Now |
|---|---|---|
| CF-06 | `User` vs `StaffResource` | **Downgraded.** 1.2.17 / 1.2.23 are `DESIGN`, not ratified. Not a conflict until one is |
| CF-26 | SOAP / ISAPI incompatibility | **Downgraded** to one question: is migration from an existing BOS tenant in scope? |
| CF-27 | Hierarchy depth vs reference model | **Absorbed into CF-34.** Previously listed as both open and closed — a register defect, now corrected |
| CF-28 | One user, two workstations | **Absorbed into CF-02** |
| CF-29 | Reference modules absent from the matrix | **Withdrawn.** The matrix is scope |
| CF-30 | Integration surface 3× the matrix | **Withdrawn as scope.** Tier 1 is the Integrations sheet, roughly 35 systems |

---

## Summary

**Counts live in `conflict-status.md`**, derived from this file by `tools/build-cf-index.py`.
They are not repeated here. Two hand-written tables sat in this section until 18 August — one
reading 41 closed against a total of 72, the other 31 against 71 — and both had been wrong for
weeks. `status.json` already carries the rule: *a hand-maintained status page is worse than
none, because somebody plans against it.* It had never been applied to the register that feeds
it.

**The three that cost most if they slip:**

**CF-37** blocks twelve finance operations against an append-only ledger. A late answer means
posting corrections rather than restating.

**CF-21** is four workshops covering ~276 requirements, and Accreditation sits on a Wave 1
surface while the other three genuinely can wait.

**CF-48** is a build-or-buy decision on infrastructure that only matters on the day it is
needed — which is also the day it is too late to procure.

---

## Raising a conflict

A conflict needs three things: the two sources that disagree, quoted; what a decision would
unblock; and a recommendation. A conflict raised without a recommendation is a question, and
questions accumulate faster than they get answered.
