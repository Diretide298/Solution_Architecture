# **Scenario (b) — centralised platform for small-to-medium tenants.** CF-162.
#
# **The hybrid placement, sized for many tenants in one cell.** Shared services carry the
# request load; venue services sit at the venue because 123 of their operations work offline
# and a shared service cannot be locally available.
#
# **pgbouncer is not optional here.** 20 Catalogue replicas at 20 connections each is 400
# against a primary that will not want them — transaction pooling is what makes horizontal
# scaling of the shared tier possible at all.
#
# **Failure**: blast radius is the cell — every tenant in it. Mitigated by scope partitioning,
# not by isolation. **This is the trade and it should be stated to a tenant, not assumed.**
# **Scaling**: Catalogue and Order autoscale on RPS. Nothing else needs to.
#
# **Replica floors are 2 for every service, and that is not derived from load.**
# It is survivability: one replica means a deploy or a crash is an outage.
#
# **The load does not need more.** A medium cell at 2,265 RPS peak implies 2 Order replicas and
# 1 of everything else - the autoscaler covers the rest, and under normal operation it has all
# day to notice a busy afternoon. **That is the whole difference from a sale**, where warming
# happens before traffic and the opening seconds are the peak.
#
# **These were 4 / 4 / 3 on Catalogue, Order and Identity** - burst-shaped numbers carried into
# the normal config by hand. **And eleven services sat at 1**, below survivability: Platform, AI,
# Reporting, Ledger, CrossRegion and every venue-scoped service was a restart away from an
# outage.
#
# **No maximum.** tools/derive-sizing.py holds the arithmetic.
#
# **The database topology, and it is the same in all seven configs** (ADR-0038 — amended by
# ADR-0040, which lets a region hold more than one instance — and ADR-0039):
#
#     postgres instance  =  region  =  the cell
#       |
#       +-- control database          once per instance: cells, tenants, licences, rollouts
#       +-- database per tenant       25 schemas -> tables -> venue_id partitions
#
#     services   shared across tenants, scaled on need and traffic, and routed per request
#                to a tenant database by tenant id
#
# **Three tenants standing in for many.** Three is enough to make the pooling arithmetic
# real: PG_POOL_MAX of 20 per tenant per service is what ADR-0032's amendment is about.
#
# **A service is not deployed per tenant.** It holds a small pool to the control database to
# resolve a tenant to its database name, then a pool per tenant database opened on that tenant's
# first request — so `PG_POOL_MIN/MAX` below are per tenant, which is exactly ADR-0032's
# amendment: 20 connections across 25 concurrent tenants is `max_connections` in full.
#
# **Every service used to point at one `ticvai` database.** That was the arrangement ADR-0038
# replaced, and it was still here in all seven configs after the ADR was accepted.


services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ticvai
      POSTGRES_PASSWORD: ticvai
      POSTGRES_DB: control
      TICVAI_TENANTS: acme:11111111-1111-1111-1111-111111111111,globex:22222222-2222-2222-2222-222222222222,initech:33333333-3333-3333-3333-333333333333
    volumes:
    - ../backend/control:/docker-entrypoint-initdb.d:ro
    - ../backend/initdb-provision.sh:/docker-entrypoint-initdb.d/zzz-provision.sh:ro
    - ../backend:/opt/ticvai:ro
    command: postgres -c max_connections=500 -c shared_buffers=4GB
    deploy:
      resources:
        limits:
          cpus: '8'
          memory: 16G
  redis:
    image: redis:7-alpine
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 8G
  pgbouncer:
    image: edoburu/pgbouncer:latest
    environment:
      DB_HOST: postgres
      DB_USER: ticvai
      DB_PASSWORD: ticvai
      POOL_MODE: transaction
      MAX_CLIENT_CONN: '2000'
      DEFAULT_POOL_SIZE: '40'
    depends_on:
    - postgres
  catalogueservice:
    build: ../services/CatalogueService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '4'
      PG_POOL_MAX: '20'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2'
          memory: 1G
  orderservice:
    build: ../services/OrderService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '4'
      PG_POOL_MAX: '20'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2'
          memory: 1G
  identityservice:
    build: ../services/IdentityService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '4'
      PG_POOL_MAX: '20'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2'
          memory: 1G
  accessservice:
    build: ../services/AccessService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '4'
      PG_POOL_MAX: '20'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2'
          memory: 1G
  tenancyservice:
    build: ../services/TenancyService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '4'
      PG_POOL_MAX: '20'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2'
          memory: 1G
  platformservice:
    build: ../services/PlatformService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '4'
      PG_POOL_MAX: '20'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2'
          memory: 1G
  marketingservice:
    build: ../services/MarketingService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '4'
      PG_POOL_MAX: '20'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2'
          memory: 1G
  aiservice:
    build: ../services/AiService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '4'
      PG_POOL_MAX: '20'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2'
          memory: 1G
  reportingservice:
    build: ../services/ReportingService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '4'
      PG_POOL_MAX: '20'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2'
          memory: 1G
  fnbservice-v1:
    build: ../services/FnbService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  fnbservice-v2:
    build: ../services/FnbService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  fnbservice-v3:
    build: ../services/FnbService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  inventoryservice-v1:
    build: ../services/InventoryService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  inventoryservice-v2:
    build: ../services/InventoryService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  inventoryservice-v3:
    build: ../services/InventoryService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  retailservice-v1:
    build: ../services/RetailService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  retailservice-v2:
    build: ../services/RetailService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  retailservice-v3:
    build: ../services/RetailService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  venueopsservice-v1:
    build: ../services/VenueOpsService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  venueopsservice-v2:
    build: ../services/VenueOpsService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  venueopsservice-v3:
    build: ../services/VenueOpsService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '6'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  whitelabelservice:
    build: ../services/WhiteLabelService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '8'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  ledgerservice-r1:
    build: ../services/LedgerService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '10'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
  crossregionservice-r1:
    build: ../services/CrossRegionService
    environment:
      PG_CONTROL_DSN: postgres://ticvai:ticvai@pgbouncer:5432/control
      PG_TENANT_DSN_TEMPLATE: postgres://ticvai:ticvai@pgbouncer:5432/{database}
      TENANT_HEADER: x-ticvai-tenant
      REDIS_URL: redis://redis:6379/0
      PG_POOL_MIN: '2'
      PG_POOL_MAX: '10'
    depends_on:
    - pgbouncer
    - redis
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 512M
