platform:
  code: P08
  audience: staff
  formFactor: web
  shortName: Venue Management
  name: Venue Management — Back Office
  surface: staffFacing
  runtime: reactWeb
  offlineCapable: false
  themes:
  - light
  - dark
  directions:
  - ltr
  - rtl
  screenCount: 363
  app: venue-management-web
  appStatus: scaffolded
  packages:
  - design-tokens
  - ui
  - api-client
  deployment:
    target: browser, desktop
    distribution: served from the cell
    bundleUpdate: immediate on deploy
    hosting: in-cell, in-region
    storeReview: false
    deviceOwnership: venue-owned workstation
    networkAssumption: venue LAN
    releaseCadence: weekly
  operator: venue
  wireframeBoard: wireframes/P08 Venue Management.dc.html
  wireframeBoardNote: '**The generated board for the whole platform.** Individual screens keep the client-pack
    anchor where one exists — a screen the client drew should open the client''s frame — and this renders
    every screen on the platform including the ones no pack covers.


    **Declared 24 August.** The file was written from the first generation and nothing pointed at it:
    `check-wireframes` has no unmatched-board concept, so the Python suite passed while the viewer flagged
    an orphan. **A board nobody references is a board nobody reviews.**'
screens:
- id: BO-001
  name: Queue Directory
  module: Access & Venue
  purpose: See every queue in the venue and whether its data is arriving.
  capability: C05
  wave: 1
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: dataTable
        bindsTo: Queue[]
        notes: Current wait, source, feed health, status
      - kind: banner
        notes: Queues whose feed has gone quiet. The first thing a duty manager checks
  states:
    loading: Table skeleton
    emptyFirstRun: No queues configured, with the action to create one
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `EVENT_CONFIGURE`, `PERFORMANCE_CONFIGURE`, `PRODUCT_VIEW` and 2 more to configure events;
      a person without it sees the reason and who to ask, because an empty table reads as *there is no
      data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `queue` is not licensed.** The tenant has not bought this module, which is a
      commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Queue service unavailable. Manual wait times can still be set — the fallback must not depend
      on the thing that failed
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-001
  permission: QUEUE_VIEW
  apis:
  - operationId: listQueues
    contract: queue
    purpose: Every queue with its current wait
    trigger: onLoad
  - operationId: listQueueFeeds
    contract: queue
    purpose: Feed health per queue
    trigger: onLoad
  - operationId: listPerformances
    contract: catalogue
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: callNextParties
    contract: queue
    purpose: Call the next parties forward
    trigger: onAction
  - operationId: configureQueueFeed
    contract: queue
    purpose: Configure a sensor feed
    trigger: onAction
  - operationId: createEvent
    contract: catalogue
    purpose: Create an event
    trigger: onAction
  - operationId: createPerformances
    contract: catalogue
    purpose: Create performances, singly or by schedule
    trigger: onAction
  - operationId: createQueue
    contract: queue
    purpose: Create a queue
    trigger: onAction
  - operationId: getEvent
    contract: catalogue
    purpose: Read an event
    trigger: onLoad
  - operationId: getQueue
    contract: queue
    purpose: Read a queue with live position
    trigger: onLoad
  - operationId: getQueueFeedHealth
    contract: queue
    purpose: Feed health
    trigger: onLoad
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  - operationId: listEvents
    contract: catalogue
    purpose: List events
    trigger: onLoad
  - operationId: listQueueEntries
    contract: queue
    purpose: List entries in a queue
    trigger: onLoad
  - operationId: setQueueStatus
    contract: queue
    purpose: Open, pause or close a queue
    trigger: onAction
  - operationId: setWaitTime
    contract: queue
    purpose: Manually set a wait time
    trigger: onAction
  - operationId: testQueueFeed
    contract: queue
    purpose: Test a feed before trusting it
    trigger: onAction
  - operationId: updateEvent
    contract: catalogue
    purpose: Amend an event
    trigger: onAction
  - operationId: updateQueue
    contract: queue
    purpose: Amend queue configuration
    trigger: onAction
  notes: A queue on the manual adaptor shows "manual" rather than "no feed". It is configured and working;
    treating it as unconfigured makes the health view lie. **Was the declared entry point to the whole
    back office until 20 August**, which is why 93 screens were unreachable. Now reached from BO-100 through
    its section.
  implementation:
    app: venue-management-web
    route: /queue-management/queue-directory
    component: apps/venue-management-web/src/routes/queue-management/QueueDirectoryList.tsx
    status: notStarted
  navigation:
    exitTo:
    - BO-002
    - BO-003
    - BO-004
    - BO-005
    inferred: true
  density: compact
  entryState:
    params:
    - name: eventId
      from: deepLink
    - name: feedId
      from: deepLink
    - name: queueId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `eventId`, `feedId`, `queueId`.'
  requiresModule: queue
  statesDerived: true
- id: BO-002
  name: Queue Configuration
  module: Access & Venue
  purpose: Create a queue and set how it behaves.
  capability: C05
  wave: 1
  layout:
    template: form
    regions:
    - name: contentBody
      components:
      - kind: textField
        label: Name
      - kind: selectField
        label: Attraction or resource
      - kind: numberField
        label: Capacity per call
      - kind: toggle
        label: Guests may join from the app
      - kind: numberField
        label: Redemption window (minutes)
        notes: How long after being called before the entry expires
    - name: actionBar
      components:
      - kind: primaryButton
        label: Save
  states:
    loading: Form skeleton
    emptyFirstRun: Not applicable
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PERFORMANCE_CONFIGURE`, `PRODUCT_VIEW`, `QUEUE_MANAGE` and 1 more to configure performances;
      a person without it sees the reason and who to ask, because an empty table reads as *there is no
      data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `queue` is not licensed.** The tenant has not bought this module, which is a
      commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Save failed, with the field named. Values are never silently dropped
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Board 2.dc.html#seat-2c
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-002
  permission: QUEUE_MANAGE
  apis:
  - operationId: createQueue
    contract: queue
    purpose: Create
    trigger: onAction
  - operationId: updateQueue
    contract: queue
    purpose: Amend
    trigger: onAction
  - operationId: cancelPerformance
    contract: catalogue
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: callNextParties
    contract: queue
    purpose: Call the next parties forward
    trigger: onAction
  - operationId: getPerformance
    contract: catalogue
    purpose: Read a performance
    trigger: onLoad
  - operationId: getQueue
    contract: queue
    purpose: Read a queue with live position
    trigger: onLoad
  - operationId: getSeatAvailability
    contract: seating
    purpose: Seat status for a performance
    trigger: onLoad
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  - operationId: listQueueEntries
    contract: queue
    purpose: List entries in a queue
    trigger: onLoad
  - operationId: listQueues
    contract: queue
    purpose: List queues
    trigger: onLoad
  - operationId: recommendSeats
    contract: seating
    purpose: Recommend seats for a party
    trigger: onAction
  - operationId: setQueueStatus
    contract: queue
    purpose: Open, pause or close a queue
    trigger: onAction
  - operationId: setWaitTime
    contract: queue
    purpose: Manually set a wait time
    trigger: onAction
  - operationId: updatePerformance
    contract: catalogue
    purpose: Amend a performance
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /queue-management/queue-configuration
    component: apps/venue-management-web/src/routes/queue-management/QueueConfigurationForm.tsx
    status: notStarted
  navigation:
    entryFrom:
    - BO-001
    inferred: true
    exitTo:
    - BO-001
    - BO-003
    - BO-004
    fromFlows: true
  density: compact
  entryState:
    params:
    - name: performanceId
      from: BO-001
    - name: queueId
      from: BO-001
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: queue
  statesDerived: true
  boardFrames:
  - Seat Board 2.dc.html#seat-2c
  notes: '**Drawn 26 August** — `Seat Board 2.dc.html` frame `seat-2c`. **The frame names this screen
    on its own face**, which is the first pack to do that: the earlier F&B, POS and Retail boards had
    to be hand-assigned by purpose after three derivation attempts produced nonsense. **A board that says
    what it draws removes the guess entirely.**'
- id: BO-003
  name: Queue Integration Setup
  module: Access & Venue
  purpose: Connect a queue to an on-site system, or leave it on manual entry.
  capability: C05
  wave: 1
  layout:
    template: form
    regions:
    - name: contentBody
      components:
      - kind: selectField
        label: Source
        notes: Manual, sensor API, webhook, MQTT, vendor poll, turnstile count, CV camera. Manual is the
          default and a valid end state
      - kind: textField
        label: Endpoint or topic
        notes: Hidden when the source is manual
      - kind: textField
        label: Credential reference
        notes: A key vault reference, never the secret. A secret typed into a form ends up in a screenshot
      - kind: numberField
        label: Expected interval (seconds)
        notes: Drives the went-quiet alarm. Too tight and a duty manager learns to ignore it
      - kind: secondaryButton
        label: Test connection
        notes: Runs testQueueFeed. Configuring an integration without a test is configuring it blind
      - kind: detailPanel
        bindsTo: FeedTestResult
        notes: Five checks, plus the mapped sample reading and the raw payload. A source that is reachable
          and returns a shape nobody mapped looks like silence, not an error
      - kind: toggle
        label: Enabled
        notes: Disabled by default. A feed goes live only after a passing test
    - name: actionBar
      components:
      - kind: primaryButton
        label: Save
  states:
    loading: Form skeleton
    emptyFirstRun: Not applicable
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `QUEUE_MANAGE` to configure a queue feed; a person without it sees the reason and who to ask.


      **Also reached when `queue` is not licensed** — a commercial answer, not a permission one, and the
      person to ask is different.'
    error: Save failed. The test result stays on screen — losing it forces the configurer to run the test
      again
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-003
  permission: QUEUE_MANAGE
  apis:
  - operationId: configureQueueFeed
    contract: queue
    purpose: Set the source
    trigger: onAction
  - operationId: testQueueFeed
    contract: queue
    purpose: One exchange with the configured source
    trigger: onAction
  - operationId: getQueueFeedHealth
    contract: queue
    purpose: Current feed state
    trigger: onLoad
  - operationId: listQueueFeeds
    contract: queue
    purpose: List configured sensor feeds
    trigger: onLoad
  notes: 'ADR-0012, adaptor-first. A named vendor is a driver behind a stable inbound shape, so adding
    one is configuration plus a driver rather than a core change. Vendor selection is CF-33a and deliberately
    late. **createRefund removed 18 August** — attached by module resemblance, not by what this screen
    does. A screen that does not handle money should not be able to move it (CF-87''s class). **Corrected
    24 August**: removed applyManualDiscount, createOrder, exchangeOrderLines, getOrder, getOrderStatement,
    getRefundPolicy and 11 more. **A queue integration screen carried 15 order operations** — create an
    order, apply a discount, exchange lines, hold, refund — and four queue ones. Bulk-attach residue,
    and `requiresModule` was `ticketing` to match the operations rather than the screen.


    **Found by deriving the empty states.** `emptyNoAccess` came out reading *"needs `ORDER_CREATE` to
    create orders"* on a screen called Queue Integration Setup — **a generated sentence that was accurate
    to the data and absurd about the screen**, which is exactly what made it visible.'
  implementation:
    app: venue-management-web
    route: /queue-management/queue-integration-setup
    component: apps/venue-management-web/src/routes/queue-management/QueueIntegrationSetupForm.tsx
    status: notStarted
  navigation:
    entryFrom:
    - BO-001
    inferred: true
    exitTo:
    - BO-001
    - BO-002
    - BO-004
    fromFlows: true
  density: compact
  entryState:
    params:
    - name: feedId
      from: BO-001
    - name: orderId
      from: deepLink
    - name: venueId
      from: session
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves; if
      it was refunded or the performance passed, says which and offers the order list rather than an error.'
  requiresModule: queue
  statesDerived: true
- id: BO-004
  name: Manual Wait Time Entry
  module: Access & Venue
  purpose: Set a wait time by hand, whether or not a feed exists.
  capability: C05
  wave: 1
  layout:
    template: form
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: Queue[]
        notes: One row per queue with a stepper and the current value
      - kind: banner
        notes: Where a feed is live, a manual entry overrides it for a stated period and then reverts.
          A permanent silent override is how a broken sensor goes unnoticed for a season
    - name: actionBar
      components:
      - kind: primaryButton
        label: Publish
  states:
    loading: Rows skeleton. Current values render immediately
    emptyFirstRun: No queues configured
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_REFUND_APPROVE`, `ORDER_REFUND_BULK`, `QUEUE_MANAGE` and 1 more to approve orders;
      a person without it sees the reason and who to ask, because an empty table reads as *there is no
      data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `queue` is not licensed.** The tenant has not bought this module, which is a
      commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Publish failed. The entered value is kept on screen
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-004
  permission: QUEUE_MANAGE
  apis:
  - operationId: setWaitTime
    contract: queue
    purpose: Set or override the wait
    trigger: onAction
  - operationId: listQueues
    contract: queue
    purpose: Current values
    trigger: onLoad
  - operationId: createBulkRefund
    contract: orders
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: approveRefund
    contract: orders
    purpose: Approve a refund held for approval
    trigger: onAction
  - operationId: callNextParties
    contract: queue
    purpose: Call the next parties forward
    trigger: onAction
  - operationId: createQueue
    contract: queue
    purpose: Create a queue
    trigger: onAction
  - operationId: getQueue
    contract: queue
    purpose: Read a queue with live position
    trigger: onLoad
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  - operationId: listQueueEntries
    contract: queue
    purpose: List entries in a queue
    trigger: onLoad
  - operationId: setQueueStatus
    contract: queue
    purpose: Open, pause or close a queue
    trigger: onAction
  - operationId: updateQueue
    contract: queue
    purpose: Amend queue configuration
    trigger: onAction
  notes: The screen the client asked for on 14 August. Available regardless of integration state — a venue
    with no sensors runs entirely from here, and a venue whose sensor failed falls back to it.
  implementation:
    app: venue-management-web
    route: /queue-management/manual-wait-time-entry
    component: apps/venue-management-web/src/routes/queue-management/ManualWaitTimeEntryForm.tsx
    status: notStarted
  navigation:
    entryFrom:
    - BO-001
    inferred: true
    exitTo:
    - BO-001
    - BO-002
    - BO-003
    - BO-005
    fromFlows: true
  density: compact
  entryState:
    params:
    - name: queueId
      from: BO-001
    - name: refundId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `refundId`.'
  requiresModule: queue
  statesDerived: true
- id: BO-005
  name: Queue Monitor
  module: Access & Venue
  purpose: Watch queues during operation and call parties forward.
  capability: C05
  wave: 1
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Waiting, called, expired, average wait
      - kind: dataTable
        bindsTo: WaitingGuest[]
      - kind: chart
        notes: Wait over the day. Stale where the feed has gone quiet
    - name: actionBar
      components:
      - kind: primaryButton
        label: Call next
  states:
    loading: Tiles skeleton
    emptyFirstRun: Nobody waiting
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `MARKETING_MANAGE`, `MARKETING_SEND`, `MARKETING_VIEW` and 2 more to manage marketings; a
      person without it sees the reason and who to ask, because an empty table reads as *there is no data*
      and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `marketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Live data unavailable. Last known values shown with their age
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-005
  permission: QUEUE_VIEW
  apis:
  - operationId: listQueueEntries
    contract: queue
    purpose: Who is waiting
    trigger: onInterval
  - operationId: callNextParties
    contract: queue
    purpose: Call forward
    trigger: onAction
  - operationId: getWaitTimes
    contract: queue
    purpose: Current waits
    trigger: onInterval
  - operationId: listCampaigns
    contract: marketing-crm
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: createCampaign
    contract: marketing-crm
    purpose: Create a campaign
    trigger: onAction
  - operationId: createQueue
    contract: queue
    purpose: Create a queue
    trigger: onAction
  - operationId: getCampaign
    contract: marketing-crm
    purpose: Read a campaign with performance
    trigger: onLoad
  - operationId: getCampaignPerformance
    contract: marketing-crm
    purpose: Delivery and engagement
    trigger: onLoad
  - operationId: getQueue
    contract: queue
    purpose: Read a queue with live position
    trigger: onLoad
  - operationId: launchCampaign
    contract: marketing-crm
    purpose: Launch or schedule a campaign
    trigger: onAction
  - operationId: listQueues
    contract: queue
    purpose: List queues
    trigger: onLoad
  - operationId: pauseCampaign
    contract: marketing-crm
    purpose: Pause a campaign mid-send
    trigger: onAction
  - operationId: setQueueStatus
    contract: queue
    purpose: Open, pause or close a queue
    trigger: onAction
  - operationId: setWaitTime
    contract: queue
    purpose: Manually set a wait time
    trigger: onAction
  - operationId: stopCampaign
    contract: marketing-crm
    purpose: Stop a campaign mid-send
    trigger: onAction
  - operationId: testSendCampaign
    contract: marketing-crm
    purpose: Send a test to named recipients
    trigger: onAction
  - operationId: unscheduleCampaign
    contract: marketing-crm
    purpose: Pull a scheduled campaign before it sends
    trigger: onAction
  - operationId: updateCampaign
    contract: marketing-crm
    purpose: Amend, pause or resume a campaign
    trigger: onAction
  - operationId: updateQueue
    contract: queue
    purpose: Amend queue configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /queue-management/queue-monitor
    component: apps/venue-management-web/src/routes/queue-management/QueueMonitorDashboard.tsx
    status: notStarted
  navigation:
    entryFrom:
    - BO-001
    inferred: true
    exitTo:
    - BO-001
    - BO-002
    - BO-003
    fromFlows: true
  notes: 'Pulled to Wave 1 on 17 August (CF-101): F09 closes a cancelled event’s queue, and a Wave 1 flow
    cannot step through a Wave 2 screen. **Cross-platform navigation removed 24 August**: EMP-032. **A
    till does not navigate to a back office and a guest app does not navigate to either** — those are
    device handovers, and a flow declares them with `crossesDevice` rather than a screen pretending there
    is a link.'
  density: compact
  entryState:
    params:
    - name: campaignId
      from: deepLink
    - name: queueId
      from: BO-001
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `campaignId`.'
  requiresModule: marketing
  statesDerived: true
- id: BO-006
  name: Parking Configuration
  module: Access & Venue
  purpose: Change how parking behaves here, and see which level the current value came from.
  capability: C39
  wave: 2
  layout:
    template: form
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: TODO
      - kind: dataTable
        derived: true
        impliedBy: listAccessPoints
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: updateAccessPoint
        label: Save access point
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: updateAccessPoint
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCESS_POINT_CONFIGURE`, `PARKING_CONFIGURE`, `SCOPE_VIEW` to configure accesses; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `access` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-006
  notes: Placeholder. Parking was discussed on 14 August and the MoM has not yet been received. States
    derived from the screen pattern on 17 August, not individually considered — sound for a list, a form
    or a money screen, and worth revisiting where this screen is unusual. Purpose derived from the screen
    name and its operations on 17 August, not from a requirement.
  openQuestions:
  - 'Answered by the 14 August MoM section 10, which this question was waiting for. Parking is barrier
    integration, in three models, and it is not space counting. (1) No integration - TICVAI issues its
    own QR and on-site security verifies it manually; (2) ANPR - the guest enters a plate at checkout
    and TICVAI pushes it to the parking system''s whitelist so the barrier opens; (3) QR handoff -
    TICVAI sends a QR to the barrier for validation. Pay-per-hour parking is explicitly out of scope,
    handled by the parking vendor''s own POS. So this is one configuration screen choosing a model and
    binding a vendor adaptor, not the three different screens the question feared - and ADR-0012''s
    adaptor-first rule is what the minute asks for when it says a new parking vendor should take days
    rather than weeks. The `parking` module key in the White Label Builder now has something behind it.'
  implementation:
    app: venue-management-web
    route: /parking/parking-configuration
    component: apps/venue-management-web/src/routes/parking/ParkingConfigurationForm.tsx
    status: notStarted
  navigation:
    entryFrom:
    - BO-001
    inferred: true
    exitTo:
    - BO-001
  apis:
  - operationId: listAccessPoints
    contract: access
    purpose: List access points
    trigger: onLoad
  - operationId: updateAccessPoint
    contract: access
    purpose: Update an access point
    trigger: onAction
  - operationId: listParkingFacilities
    contract: access
    purpose: Car parks at a venue, and how each integrates
    trigger: onLoad
  - operationId: setParkingFacility
    contract: access
    purpose: Configure a car park and its integration
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: accessPointId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `accessPointId`.'
  requiresModule: access
  statesDerived: true
- id: BO-007
  name: Product Directory
  module: Sell
  purpose: Find anything sellable at this venue.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listProducts
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createProduct
        label: Create product
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createProduct
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to configure products; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-008
    - BO-009
    - BO-010
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/product-directory
    component: apps/venue-management-web/src/routes/venue-operations/ProductDirectoryDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 2.dc.html#ret-2a
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-2A, RET-2B in the client Retail pack.**'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board.** Answers 5 board screen(s): Menu & Product Command Center;
    Product / PLU Master; Variants, Modifiers & Special Selling Rules and 2 more. **The board specifies
    this screen further rather than replacing it** — the id, its flows and its navigation are unchanged,
    which is what keeps 3,184 traceability rows and every board anchor pointing at something real. **Retail
    board operations wired 24 August.**'
  apis:
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: getProduct
    contract: catalogue
    purpose: Read a product
    trigger: onLoad
  - operationId: createProduct
    contract: catalogue
    purpose: Create a product
    trigger: onAction
  - operationId: listAlternativeCodes
    contract: catalogue
    purpose: External identifiers for a product
    trigger: onLoad
  - operationId: listProductVariants
    contract: catalogue
    purpose: List generated variants
    trigger: onLoad
  - operationId: resolveProductByCode
    contract: catalogue
    purpose: Resolve a partner code to a product
    trigger: onLoad
  - operationId: setAlternativeCodes
    contract: catalogue
    purpose: Set external identifiers
    trigger: onAction
  - operationId: setProductAttributes
    contract: catalogue
    purpose: Set the attribute axes for a product
    trigger: onAction
  - operationId: transitionProductLifecycle
    contract: catalogue
    purpose: Move a product through its lifecycle
    trigger: onAction
  - operationId: updateProduct
    contract: catalogue
    purpose: Update a product
    trigger: onAction
  - operationId: createMerchandise
    contract: retail
    purpose: Create a merchandise item
    trigger: onAction
  - operationId: listMenus
    contract: fnb
    purpose: List menus
    trigger: onLoad
  - operationId: listMerchandise
    contract: retail
    purpose: List merchandise
    trigger: onLoad
  - operationId: bulkUpdateProducts
    contract: inventory
    purpose: Change many products at once, with a preview
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: productId
      from: deepLink
    coldEntry: '**A shared product link after the product retired.** Shows what replaced it where a successor
      exists, and the catalogue where none does.'
  provenance: client board, specified
  requiresModule: ticketing
  boardFrames:
  - FnB Board 2.dc.html#fnb-2c
  - Retail Board 2.dc.html#ret-2a
  - Retail Board 2.dc.html#ret-2b
  statesDerived: true
- id: BO-008
  name: Product Detail & Variants
  module: Orders & Money
  purpose: Define what is sold and the ticket types under it.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listProductVariants
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: setProductAttributes
        label: Save product attributes
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: setProductAttributes
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to configure products; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-009
    - BO-010
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/product-detail-variants
    component: apps/venue-management-web/src/routes/venue-operations/ProductDetailVariantsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 2.dc.html#ret-2c
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-2C in the client Retail pack.**'
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Carried `runFxRevaluation`,
    `getForeignTenderReport` and `listInterEntityObligations` until 20 August** — a product screen doing
    foreign-exchange revaluation. Operations were attached from the 14 August wireframe board by a heuristic
    that matched nothing. **Rewired 20 August.**
  apis:
  - operationId: getProduct
    contract: catalogue
    purpose: Read a product
    trigger: onLoad
  - operationId: listProductVariants
    contract: catalogue
    purpose: List generated variants
    trigger: onLoad
  - operationId: setProductAttributes
    contract: catalogue
    purpose: Set the attribute axes for a product
    trigger: onAction
  - operationId: updateProduct
    contract: catalogue
    purpose: Update a product
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    - name: productId
      from: deepLink
    coldEntry: A product opened from the directory or a shared link. Shows what replaced it where it retired.
  requiresModule: ticketing
  boardFrames:
  - FnB Board 2.dc.html#fnb-2d
  - Retail Board 2.dc.html#ret-2c
  statesDerived: true
- id: BO-009
  name: Pricing Rules
  module: Sell
  purpose: Set what something costs, and when that changes.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listPriceLists
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createPriceList
        label: Create price list
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createPriceList
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRICE_CONFIGURE`, `PRICE_VIEW` to configure prices; a person without it sees the reason and
      who to ask, because an empty table reads as *there is no data* and sends somebody to support with
      the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-010
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/pricing-rules
    component: apps/venue-management-web/src/routes/venue-operations/PricingRulesDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 2.dc.html#ret-2f
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-2F in the client Retail pack.**'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board.** Answers 1 board screen(s): Price Book & Retail Pricing Management.
    **The board specifies this screen further rather than replacing it** — the id, its flows and its navigation
    are unchanged, which is what keeps 3,184 traceability rows and every board anchor pointing at something
    real.'
  apis:
  - operationId: listPriceLists
    contract: catalogue
    purpose: List price lists
    trigger: onLoad
  - operationId: listPrices
    contract: catalogue
    purpose: List prices in a list
    trigger: onLoad
  - operationId: createPriceList
    contract: catalogue
    purpose: Create a price list
    trigger: onAction
  - operationId: copyPriceList
    contract: catalogue
    purpose: Copy a price list, optionally with an adjustment
    trigger: onAction
  - operationId: getPriceList
    contract: catalogue
    purpose: Read a price list
    trigger: onLoad
  - operationId: setPrices
    contract: catalogue
    purpose: Set prices in bulk
    trigger: onAction
  - operationId: updatePriceList
    contract: catalogue
    purpose: Amend a price list
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: priceListId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `priceListId`.'
  provenance: client board, specified
  requiresModule: ticketing
  boardFrames:
  - Retail Board 2.dc.html#ret-2f
  statesDerived: true
- id: BO-010
  name: Promotions & Coupons
  module: Sell
  purpose: Issue a code and control what it does.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listPromotions
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createCouponCampaign
        label: Create coupon campaign
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: voidCouponCode
        label: Void coupon code
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createCouponCampaign
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
      - kind: publishGate
        impliedBy: publishPromotion
        notes: >
          Declares `publishPromotion`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRICE_CONFIGURE`, `PRICE_VIEW`, `REPORT_VIEW_VENUE` to configure prices; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/promotions-coupons
    component: apps/venue-management-web/src/routes/venue-operations/PromotionsCouponsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 4.dc.html#pos-4e
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-4E in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board.** Answers 6 board screen(s): Retail Commercial Command Center;
    Promotion & Offer Builder; Promotion Eligibility, Priority & Conflict Rules and 3 more. **The board
    specifies this screen further rather than replacing it** — the id, its flows and its navigation are
    unchanged, which is what keeps 3,184 traceability rows and every board anchor pointing at something
    real. **Owns POS board frame(s) POS-4E** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none. **Retail board operations
    wired 24 August.**'
  apis:
  - operationId: listPromotions
    contract: promotions
    purpose: List promotions
    trigger: onLoad
  - operationId: listCouponCampaigns
    contract: promotions
    purpose: List coupon campaigns
    trigger: onLoad
  - operationId: analysePromotionConflicts
    contract: promotions
    purpose: Analyse stacking against live promotions
    trigger: onLoad
  - operationId: createCouponCampaign
    contract: promotions
    purpose: Create a coupon campaign
    trigger: onAction
  - operationId: createPromotion
    contract: promotions
    purpose: Create a promotion
    trigger: onAction
  - operationId: endPromotion
    contract: promotions
    purpose: End a promotion early
    trigger: onAction
  - operationId: evaluatePromotions
    contract: promotions
    purpose: Evaluate promotions against a cart
    trigger: onAction
  - operationId: generateCouponCodes
    contract: promotions
    purpose: Generate codes in bulk
    trigger: onAction
  - operationId: getPromotion
    contract: promotions
    purpose: Read a promotion
    trigger: onLoad
  - operationId: getPromotionUsage
    contract: promotions
    purpose: Redemption count and discount given
    trigger: onLoad
  - operationId: listCouponCodes
    contract: promotions
    purpose: List generated codes
    trigger: onLoad
  - operationId: pausePromotion
    contract: promotions
    purpose: Pause a live promotion
    trigger: onAction
  - operationId: publishPromotion
    contract: promotions
    purpose: Publish a promotion
    trigger: onAction
  - operationId: unschedulePromotion
    contract: promotions
    purpose: Pull a scheduled promotion before it starts
    trigger: onAction
  - operationId: updatePromotion
    contract: promotions
    purpose: Amend a promotion
    trigger: onAction
  - operationId: getDashboard
    contract: reporting
    purpose: Read a dashboard with tile data
    trigger: onLoad
  - operationId: simulatePromotion
    contract: promotions
    purpose: What this promotion would have cost on real history
    trigger: onAction
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  - operationId: voidCouponCode
    contract: promotions
    purpose: Void a code
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    - name: campaignId
      from: deepLink
    - name: dashboardId
      from: deepLink
    - name: promotionId
      from: deepLink
    - name: reportId
      from: deepLink
    - name: code
      from: deepLink
    coldEntry: A dashboard opened from a link or a saved view. A coupon code opened from the list, or
      scanned at a till.
  provenance: client board, specified
  requiresModule: ticketing
  boardFrames:
  - POS Board 4.dc.html#pos-4e
  - Retail Board 5.dc.html#ret-5a
  - Retail Board 5.dc.html#ret-5b
  - Retail Board 5.dc.html#ret-5c
  - Retail Board 5.dc.html#ret-5e
  statesDerived: true
- id: BO-011
  name: Packages & Bundles
  module: Sell
  purpose: Sell several products as one line.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listCatalogueBundles
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: publishBundle
        label: Publish bundle
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: publishBundle
      - kind: publishGate
        impliedBy: publishBundle
        notes: >
          Declares `publishBundle`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to configure products; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/packages-bundles
    component: apps/venue-management-web/src/routes/venue-operations/PackagesBundlesDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 5.dc.html#ret-5h
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-5H in the client Retail pack.**'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board.** Answers 2 board screen(s): Combo & Meal Builder; Bundle,
    Kit & Gift Set Builder. **The board specifies this screen further rather than replacing it** — the
    id, its flows and its navigation are unchanged, which is what keeps 3,184 traceability rows and every
    board anchor pointing at something real.'
  apis:
  - operationId: listCatalogueBundles
    contract: catalogue
    purpose: List published bundles
    trigger: onLoad
  - operationId: getLatestBundle
    contract: catalogue
    purpose: Pull the current bundle for this workstation's venue
    trigger: onLoad
  - operationId: publishBundle
    contract: catalogue
    purpose: Compute, sign and publish a catalogue bundle
    trigger: onAction
  - operationId: reportBundleApplied
    contract: catalogue
    purpose: Report that a workstation applied a bundle
    trigger: onAction
  - operationId: createBundle
    contract: promotions
    purpose: Create a bundle
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: version
      from: deepLink
    coldEntry: '**A version link is expected to point at something superseded — that is what versions
      are for.** The screen opens the requested version read-only, says it is not current, and links to
      the one that is. **An old version is history, not an error.** Arrives with `version`.'
  provenance: client board, specified
  requiresModule: ticketing
  boardFrames:
  - FnB Board 2.dc.html#fnb-2g
  - Retail Board 5.dc.html#ret-5h
  statesDerived: true
- id: BO-012
  name: Membership Products
  module: Sell
  purpose: Define what a membership includes.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listProducts
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createEntitlementTemplate
        label: Create entitlement template
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createEntitlementTemplate
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to configure products; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/membership-products
    component: apps/venue-management-web/src/routes/venue-operations/MembershipProductsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-012
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: listEntitlementTemplates
    contract: catalogue
    purpose: List entitlement templates
    trigger: onLoad
  - operationId: createEntitlementTemplate
    contract: catalogue
    purpose: Create an entitlement template
    trigger: onAction
  - operationId: createProduct
    contract: catalogue
    purpose: Create a product
    trigger: onAction
  - operationId: getProduct
    contract: catalogue
    purpose: Read a product
    trigger: onLoad
  - operationId: listAlternativeCodes
    contract: catalogue
    purpose: External identifiers for a product
    trigger: onLoad
  - operationId: listProductVariants
    contract: catalogue
    purpose: List generated variants
    trigger: onLoad
  - operationId: resolveProductByCode
    contract: catalogue
    purpose: Resolve a partner code to a product
    trigger: onLoad
  - operationId: setAlternativeCodes
    contract: catalogue
    purpose: Set external identifiers
    trigger: onAction
  - operationId: setProductAttributes
    contract: catalogue
    purpose: Set the attribute axes for a product
    trigger: onAction
  - operationId: transitionProductLifecycle
    contract: catalogue
    purpose: Move a product through its lifecycle
    trigger: onAction
  - operationId: updateProduct
    contract: catalogue
    purpose: Update a product
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: productId
      from: deepLink
    coldEntry: '**A shared product link after the product retired.** Shows what replaced it where a successor
      exists, and the catalogue where none does.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-013
  name: Channel & Distribution
  module: Sell
  purpose: Decide where each product can be sold.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: primaryButton
        derived: true
        impliedBy: setChannelAllocations
        label: Save channel allocations
        notes: The act the screen exists for.
      - kind: dataTable
        derived: true
        impliedBy: listChannelCapacities
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: setChannelAllocations
      - kind: publishGate
        impliedBy: publishBundle
        notes: >
          Declares `publishBundle`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  states:
    loading: Detail loads
    emptyFirstRun: Not found — it may have been deleted or moved out of scope
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `CAPACITY_CONFIGURE`, `PARTNER_MANAGE`, `PARTNER_VIEW` and 2 more to configure capacitys;
      a person without it sees the reason and who to ask, because an empty table reads as *there is no
      data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load. Editing is blocked
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/channel-distribution
    component: apps/venue-management-web/src/routes/venue-operations/ChannelDistributionDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 5.dc.html#ret-5g
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-5G in the client Retail pack.**'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board.** Answers 2 board screen(s): Catalog Builder & Store Assortment;
    Ticketing, Event & Experience Commerce Integration. **The board specifies this screen further rather
    than replacing it** — the id, its flows and its navigation are unchanged, which is what keeps 3,184
    traceability rows and every board anchor pointing at something real. **Improved 20 August against
    the client design board**, answering 1 board screen(s): Sales Channel Configuration. **The id, flows
    and navigation are unchanged** — a board specifies a screen further; it does not replace it. **Retail
    board operations wired 24 August.**'
  apis:
  - operationId: getChannelAllocations
    contract: catalogue
    purpose: Capacity allocated to each channel
    trigger: onLoad
  - operationId: setChannelAllocations
    contract: catalogue
    purpose: Allocate envelope capacity across channels
    trigger: onAction
  - operationId: createChannelCapacity
    contract: catalogue
    purpose: Create a capacity envelope
    trigger: onAction
  - operationId: listChannelCapacities
    contract: catalogue
    purpose: List capacity envelopes
    trigger: onLoad
  - operationId: relinquishChannelAllocation
    contract: catalogue
    purpose: Return unsold channel allocation to the general pool
    trigger: onAction
  - operationId: updateChannelCapacity
    contract: catalogue
    purpose: Amend an envelope
    trigger: onAction
  - operationId: listChannelListings
    contract: subscription
    purpose: What is listed on which OTA
    trigger: onLoad
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: publishBundle
    contract: catalogue
    purpose: Compute, sign and publish a catalogue bundle
    trigger: onAction
  - operationId: setChannelListing
    contract: subscription
    purpose: List a product on a channel, with its own allocation
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: channelCapacityId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `channelCapacityId`.'
  provenance: client board, specified
  requiresModule: ticketing
  boardFrames:
  - Retail Board 5.dc.html#ret-5g
  statesDerived: true
- id: BO-014
  name: Catalogue Publishing
  module: Sell
  purpose: Push catalogue changes live, or schedule them.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listProducts
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createProduct
        label: Create product
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createProduct
      - kind: publishGate
        impliedBy: publishBundle
        notes: >
          Declares `publishBundle`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRICE_VIEW`, `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to read prices; a person without it sees
      the reason and who to ask, because an empty table reads as *there is no data* and sends somebody
      to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/catalogue-publishing
    component: apps/venue-management-web/src/routes/venue-operations/CataloguePublishingDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 2.dc.html#ret-2h
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-2H in the client Retail pack.**'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board.** Answers 2 board screen(s): Availability, Pricing, Channels
    & Publishing; Product Availability, Lifecycle & Publishing. **The board specifies this screen further
    rather than replacing it** — the id, its flows and its navigation are unchanged, which is what keeps
    3,184 traceability rows and every board anchor pointing at something real.'
  apis:
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: getProduct
    contract: catalogue
    purpose: Read a product
    trigger: onLoad
  - operationId: createProduct
    contract: catalogue
    purpose: Create a product
    trigger: onAction
  - operationId: listAlternativeCodes
    contract: catalogue
    purpose: External identifiers for a product
    trigger: onLoad
  - operationId: listProductVariants
    contract: catalogue
    purpose: List generated variants
    trigger: onLoad
  - operationId: resolveProductByCode
    contract: catalogue
    purpose: Resolve a partner code to a product
    trigger: onLoad
  - operationId: setAlternativeCodes
    contract: catalogue
    purpose: Set external identifiers
    trigger: onAction
  - operationId: setProductAttributes
    contract: catalogue
    purpose: Set the attribute axes for a product
    trigger: onAction
  - operationId: transitionProductLifecycle
    contract: catalogue
    purpose: Move a product through its lifecycle
    trigger: onAction
  - operationId: updateProduct
    contract: catalogue
    purpose: Update a product
    trigger: onAction
  - operationId: listPrices
    contract: catalogue
    purpose: List prices in a list
    trigger: onLoad
  - operationId: publishBundle
    contract: catalogue
    purpose: Compute, sign and publish a catalogue bundle
    trigger: onAction
  - operationId: setItemAvailability
    contract: fnb
    purpose: Mark an item available or eighty-sixed
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    - name: itemId
      from: deepLink
    - name: priceListId
      from: deepLink
    - name: productId
      from: deepLink
    coldEntry: An item opened from the catalogue. A price list opened from the price book. A product opened
      from the directory.
  provenance: client board, specified
  requiresModule: ticketing
  boardFrames:
  - FnB Board 2.dc.html#fnb-2h
  - Retail Board 2.dc.html#ret-2h
  statesDerived: true
- id: BO-015
  name: Session Calendar
  module: Sell
  purpose: See what is scheduled and how full it is.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listPerformances
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createPerformances
        label: Create performances
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: cancelPerformance
        label: Cancel performance
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createPerformances
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `EVENT_CONFIGURE`, `PERFORMANCE_CONFIGURE`, `PRODUCT_VIEW` to configure events; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/session-calendar
    component: apps/venue-management-web/src/routes/venue-operations/SessionCalendarDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Board 2.dc.html#seat-2a
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-015
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Drawn 26 August**
    — `Seat Board 2.dc.html` frame `seat-2a`. **The frame names this screen on its own face**, which is
    the first pack to do that: the earlier F&B, POS and Retail boards had to be hand-assigned by purpose
    after three derivation attempts produced nonsense. **A board that says what it draws removes the guess
    entirely.**'
  apis:
  - operationId: listPerformances
    contract: catalogue
    purpose: List performances of an event
    trigger: onLoad
  - operationId: getPerformance
    contract: catalogue
    purpose: Read a performance
    trigger: onLoad
  - operationId: createPerformances
    contract: catalogue
    purpose: Create performances, singly or by schedule
    trigger: onAction
  - operationId: cancelPerformance
    contract: catalogue
    purpose: Cancel a performance
    trigger: onAction
  - operationId: createEvent
    contract: catalogue
    purpose: Create an event
    trigger: onAction
  - operationId: getEvent
    contract: catalogue
    purpose: Read an event
    trigger: onLoad
  - operationId: getSeatAvailability
    contract: seating
    purpose: Seat status for a performance
    trigger: onLoad
  - operationId: listEvents
    contract: catalogue
    purpose: List events
    trigger: onLoad
  - operationId: recommendSeats
    contract: seating
    purpose: Recommend seats for a party
    trigger: onAction
  - operationId: updateEvent
    contract: catalogue
    purpose: Amend an event
    trigger: onAction
  - operationId: updatePerformance
    contract: catalogue
    purpose: Amend a performance
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: eventId
      from: deepLink
    - name: performanceId
      from: deepLink
    coldEntry: '**A link to a performance that has happened.** Offers the next performance of the same
      event.'
  requiresModule: ticketing
  statesDerived: true
  boardFrames:
  - Seat Board 2.dc.html#seat-2a
- id: BO-016
  name: Session Template
  module: Sell
  purpose: Define the pattern the calendar is generated from.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listPerformances
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createPerformances
        label: Create performances
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: cancelPerformance
        label: Cancel performance
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createPerformances
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `EVENT_CONFIGURE`, `PERFORMANCE_CONFIGURE`, `PRODUCT_VIEW` to configure events; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/session-template
    component: apps/venue-management-web/src/routes/venue-operations/SessionTemplateDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Board 2.dc.html#seat-2b
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-016
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Drawn 26 August**
    — `Seat Board 2.dc.html` frame `seat-2b`. **The frame names this screen on its own face**, which is
    the first pack to do that: the earlier F&B, POS and Retail boards had to be hand-assigned by purpose
    after three derivation attempts produced nonsense. **A board that says what it draws removes the guess
    entirely.**'
  apis:
  - operationId: listPerformances
    contract: catalogue
    purpose: List performances of an event
    trigger: onLoad
  - operationId: getPerformance
    contract: catalogue
    purpose: Read a performance
    trigger: onLoad
  - operationId: createPerformances
    contract: catalogue
    purpose: Create performances, singly or by schedule
    trigger: onAction
  - operationId: cancelPerformance
    contract: catalogue
    purpose: Cancel a performance
    trigger: onAction
  - operationId: createEvent
    contract: catalogue
    purpose: Create an event
    trigger: onAction
  - operationId: getEvent
    contract: catalogue
    purpose: Read an event
    trigger: onLoad
  - operationId: getSeatAvailability
    contract: seating
    purpose: Seat status for a performance
    trigger: onLoad
  - operationId: listEvents
    contract: catalogue
    purpose: List events
    trigger: onLoad
  - operationId: recommendSeats
    contract: seating
    purpose: Recommend seats for a party
    trigger: onAction
  - operationId: updateEvent
    contract: catalogue
    purpose: Amend an event
    trigger: onAction
  - operationId: updatePerformance
    contract: catalogue
    purpose: Amend a performance
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: eventId
      from: deepLink
    - name: performanceId
      from: deepLink
    coldEntry: '**A link to a performance that has happened.** Offers the next performance of the same
      event.'
  requiresModule: ticketing
  statesDerived: true
  boardFrames:
  - Seat Board 2.dc.html#seat-2b
- id: BO-017
  name: Capacity Management
  module: Sell
  purpose: Change how many people a session can take.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listChannelCapacities
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createChannelCapacity
        label: Create envelope
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createChannelCapacity
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `CAPACITY_CONFIGURE`, `PRODUCT_VIEW` to configure capacitys; a person without it sees the
      reason and who to ask, because an empty table reads as *there is no data* and sends somebody to
      support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/capacity-management
    component: apps/venue-management-web/src/routes/venue-operations/CapacityManagementDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-017
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listChannelCapacities
    contract: catalogue
    purpose: List capacity envelopes
    trigger: onLoad
  - operationId: getChannelAllocations
    contract: catalogue
    purpose: Capacity allocated to each channel
    trigger: onLoad
  - operationId: createChannelCapacity
    contract: catalogue
    purpose: Create a capacity envelope
    trigger: onAction
  - operationId: relinquishChannelAllocation
    contract: catalogue
    purpose: Return unsold channel allocation to the general pool
    trigger: onAction
  - operationId: setChannelAllocations
    contract: catalogue
    purpose: Allocate envelope capacity across channels
    trigger: onAction
  - operationId: updateChannelCapacity
    contract: catalogue
    purpose: Amend an envelope
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: channelCapacityId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `channelCapacityId`.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-018
  name: Allocation & Holds
  module: Sell
  purpose: See who is holding capacity, and release it.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listInventoryHolds
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: acquireInventoryHold
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: relinquishInventoryHold
        label: Release lease
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: acquireInventoryHold
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `CAPACITY_CONFIGURE`, `ORDER_CREATE`, `PRODUCT_VIEW` to configure capacitys; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/allocation-holds
    component: apps/venue-management-web/src/routes/venue-operations/AllocationHoldsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-018
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listInventoryHolds
    contract: catalogue
    purpose: List leases
    trigger: onLoad
  - operationId: acquireInventoryHold
    contract: catalogue
    purpose: Acquire an inventory lease
    trigger: onAction
  - operationId: forceReleaseInventoryHold
    contract: catalogue
    purpose: Reclaim a stranded lease
    trigger: onAction
  - operationId: relinquishInventoryHold
    contract: catalogue
    purpose: Return unsold units
    trigger: onAction
  - operationId: renewInventoryHold
    contract: catalogue
    purpose: Extend a lease TTL
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: inventoryHoldId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `inventoryHoldId`.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-019
  name: Closures & Blackouts
  module: Sell
  purpose: Stop selling something, for a reason.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listPerformances
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: destructiveButton
        derived: true
        impliedBy: cancelPerformance
        label: Cancel performance
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: primaryButton
        derived: true
        impliedBy: createEvent
        label: Create event
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: cancelPerformance
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `EVENT_CONFIGURE`, `PERFORMANCE_CONFIGURE`, `PRODUCT_VIEW` to configure events; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/closures-blackouts
    component: apps/venue-management-web/src/routes/venue-operations/ClosuresBlackoutsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Board 2.dc.html#seat-2d
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-019
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Drawn 26 August**
    — `Seat Board 2.dc.html` frame `seat-2d`. **The frame names this screen on its own face**, which is
    the first pack to do that: the earlier F&B, POS and Retail boards had to be hand-assigned by purpose
    after three derivation attempts produced nonsense. **A board that says what it draws removes the guess
    entirely.**'
  apis:
  - operationId: listPerformances
    contract: catalogue
    purpose: List performances of an event
    trigger: onLoad
  - operationId: cancelPerformance
    contract: catalogue
    purpose: Cancel a performance
    trigger: onAction
  - operationId: createEvent
    contract: catalogue
    purpose: Create an event
    trigger: onAction
  - operationId: createPerformances
    contract: catalogue
    purpose: Create performances, singly or by schedule
    trigger: onAction
  - operationId: getEvent
    contract: catalogue
    purpose: Read an event
    trigger: onLoad
  - operationId: getPerformance
    contract: catalogue
    purpose: Read a performance
    trigger: onLoad
  - operationId: getSeatAvailability
    contract: seating
    purpose: Seat status for a performance
    trigger: onLoad
  - operationId: listEvents
    contract: catalogue
    purpose: List events
    trigger: onLoad
  - operationId: recommendSeats
    contract: seating
    purpose: Recommend seats for a party
    trigger: onAction
  - operationId: updateEvent
    contract: catalogue
    purpose: Amend an event
    trigger: onAction
  - operationId: updatePerformance
    contract: catalogue
    purpose: Amend a performance
    trigger: onAction
  - operationId: setPathClosure
    contract: venue-map
    purpose: Close a path or an area
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: eventId
      from: deepLink
    - name: performanceId
      from: deepLink
    - name: mapId
      from: navigation
    - name: pathId
      from: navigation
    coldEntry: '**A link to a performance that has happened.** Offers the next performance of the same
      event.'
  requiresModule: ticketing
  statesDerived: true
  boardFrames:
  - Seat Board 2.dc.html#seat-2d
- id: BO-020
  name: F&B Order Management
  module: Food & Beverage
  wave: 1
  purpose: Take, amend and route an F&B order from the back office — and see what the kitchen is working
    on.
  density: compact
  apis:
  - operationId: acceptFnbOrder
    contract: fnb
    purpose: The outlet takes the order
    trigger: onAction
  - operationId: setKitchenTicketStatus
    contract: fnb
    purpose: Advance a kitchen ticket
    trigger: onAction
  - operationId: amendFnbOrder
    contract: fnb
    purpose: Amend an order before it is prepared
    trigger: onAction
  - operationId: cancelFnbOrder
    contract: fnb
    purpose: Cancel an order
    trigger: onAction
  - operationId: createFnbOrder
    contract: fnb
    purpose: Place an F&B order
    trigger: onAction
  - operationId: getFnbOrder
    contract: fnb
    purpose: Read an F&B order
    trigger: onLoad
  - operationId: listFnbOrders
    contract: fnb
    purpose: List F&B orders
    trigger: onLoad
  - operationId: listKitchenStations
    contract: fnb
    purpose: List preparation stations and their routing
    trigger: onLoad
  - operationId: listKitchenTickets
    contract: fnb
    purpose: Kitchen ticket queue
    trigger: onLoad
  - operationId: prioritiseKitchenTicket
    contract: fnb
    purpose: Move a ticket up the queue
    trigger: onAction
  - operationId: setKitchenStations
    contract: fnb
    purpose: Configure stations and item routing
    trigger: onAction
  states:
    loading: Content loads
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `createFnbOrder`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Take, amend and route an F&B order
      from the back office — and see what the kitchen is working on.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission. The list is not empty.
    error: Could not load.
  entryState:
    params:
    - name: venueId
      from: session
    - name: orderId
      from: deepLink
    - name: ticketId
      from: deepLink
    coldEntry: An order opened from a list or a link. A kitchen ticket opened from the rail.
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: primaryButton
        derived: true
        impliedBy: acceptFnbOrder
        label: Accept fnb order
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: cancelFnbOrder
        label: Cancel fnb order
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: detailPanel
        derived: true
        impliedBy: getFnbOrder
        notes: One record, read-only.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: acceptFnbOrder
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  navigation:
    entryFrom:
    - BO-104
    exitTo:
    - BO-104
    inferred: false
    notes: '**Returns to BO-104.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /fnb/bo-020
    component: apps/venue-management-web/src/routes/fnb/BO020List.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-020
  notes: 'Restored 20 August when the P15 build was rolled back. **Named `Timed Entry Rules` and carrying
    eleven F&B order operations.** Timed entry is an admission profile; this is the order desk. The client
    board calls it *Active Order Management & Fulfilment Journey*. **Purpose corrected 24 August.** The
    screen was renamed *F&B Order Management* and its purpose still read *"Control how early and how late
    a ticket admits"* — **timed entry, on a screen whose every operation is `fnb`.** A rename that moves
    the label and leaves the sentence is worse than no rename: the name is what a reader scans and the
    purpose is what they trust.'
  requiresModule: fnb
  statesDerived: true
- id: BO-021
  name: Order Search
  module: Food & Beverage
  wave: 1
  purpose: Find any order taken at this venue.
  density: compact
  apis:
  - operationId: recordOrderHandover
    contract: fnb
    purpose: Record that an order reached the guest
    trigger: onAction
  - operationId: getGuestOrderStatus
    contract: fnb
    purpose: Track an order
    trigger: onLoad
  states:
    loading: Content loads
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `recordOrderHandover`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Find any order taken at this venue.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission. The list is not empty.
    error: Could not load.
  entryState:
    params:
    - name: venueId
      from: session
    - name: orderId
      from: deepLink
    coldEntry: An order opened from a list or a link.
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: primaryButton
        derived: true
        impliedBy: recordOrderHandover
        notes: The act the screen exists for.
      - kind: detailPanel
        derived: true
        impliedBy: getGuestOrderStatus
        notes: One record, read-only.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: recordOrderHandover
  navigation:
    entryFrom:
    - BO-104
    exitTo:
    - BO-104
    inferred: false
    notes: '**Returns to BO-104.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /fnb/bo-021
    component: apps/venue-management-web/src/routes/fnb/BO021List.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 3.dc.html#ret-3a
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-3A in the client Retail pack.**'
  notes: Restored 20 August when the P15 build was rolled back.
  requiresModule: fnb
  boardFrames:
  - Retail Board 3.dc.html#ret-3a
  statesDerived: true
- id: BO-022
  name: Order Detail
  module: Orders & Money
  purpose: See everything about one order in one place.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listOrders
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createOrder
        label: Create order
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: voidOrder
        label: Void order
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createOrder
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_CREATE`, `ORDER_DISCOUNT`, `ORDER_EXCHANGE` and 6 more to create orders; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/order-detail
    component: apps/venue-management-web/src/routes/venue-operations/OrderDetailDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-022
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listOrders
    contract: orders
    purpose: List orders
    trigger: onLoad
  - operationId: getOrder
    contract: orders
    purpose: Read an order
    trigger: onLoad
  - operationId: createOrder
    contract: orders
    purpose: Create an order
    trigger: onAction
  - operationId: applyManualDiscount
    contract: orders
    purpose: Apply a discount a cashier chose
    trigger: onAction
  - operationId: createRefund
    contract: orders
    purpose: Refund an order, wholly or in part
    trigger: onAction
  - operationId: exchangeOrderLines
    contract: orders
    purpose: Exchange lines for different products or dates
    trigger: onAction
  - operationId: getOrderStatement
    contract: orders
    purpose: Full financial history of an order
    trigger: onLoad
  - operationId: holdOrder
    contract: orders
    purpose: Park a sale and free the till
    trigger: onAction
  - operationId: listOrderRefunds
    contract: orders
    purpose: List refunds against an order
    trigger: onLoad
  - operationId: modifyOrder
    contract: orders
    purpose: Add or remove lines on an existing order
    trigger: onAction
  - operationId: reprintOrder
    contract: orders
    purpose: Reprint or resend tickets
    trigger: onAction
  - operationId: rescheduleOrder
    contract: orders
    purpose: Move an order to another performance
    trigger: onAction
  - operationId: resumeOrder
    contract: orders
    purpose: Bring a parked sale back to a till
    trigger: onAction
  - operationId: voidOrder
    contract: orders
    purpose: Void an order
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves; if
      it was refunded or the performance passed, says which and offers the order list rather than an error.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-023
  name: Refunds & Exchanges
  module: Orders & Money
  purpose: Give money back, or move the booking.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listOrderRefunds
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createRefund
        label: Create refund
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: voidOrder
        label: Void order
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createRefund
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: Figures load; the expected total renders last
    emptyFirstRun: Nothing in the period — reported as a result, not an empty screen
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_CREATE`, `ORDER_DISCOUNT`, `ORDER_EXCHANGE` and 6 more to create orders; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Every action that moves money is blocked** — a figure nobody read must not
      be reconciled against'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/refunds-exchanges
    component: apps/venue-management-web/src/routes/venue-operations/RefundsExchangesDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-023
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **`getRefundPolicy`
    wired 24 August.** **A refunds screen that cannot read the refund policy** judges every request by
    memory — and F09 was reaching it through `BO-003 Queue Integration Setup`, which held the operation
    only because of the 18 August bulk attach.
  apis:
  - operationId: listOrderRefunds
    contract: orders
    purpose: List refunds against an order
    trigger: onLoad
  - operationId: createRefund
    contract: orders
    purpose: Refund an order, wholly or in part
    trigger: onAction
  - operationId: applyManualDiscount
    contract: orders
    purpose: Apply a discount a cashier chose
    trigger: onAction
  - operationId: createOrder
    contract: orders
    purpose: Create an order
    trigger: onAction
  - operationId: exchangeOrderLines
    contract: orders
    purpose: Exchange lines for different products or dates
    trigger: onAction
  - operationId: getOrder
    contract: orders
    purpose: Read an order
    trigger: onLoad
  - operationId: getOrderStatement
    contract: orders
    purpose: Full financial history of an order
    trigger: onLoad
  - operationId: holdOrder
    contract: orders
    purpose: Park a sale and free the till
    trigger: onAction
  - operationId: listOrders
    contract: orders
    purpose: List orders
    trigger: onLoad
  - operationId: modifyOrder
    contract: orders
    purpose: Add or remove lines on an existing order
    trigger: onAction
  - operationId: reprintOrder
    contract: orders
    purpose: Reprint or resend tickets
    trigger: onAction
  - operationId: rescheduleOrder
    contract: orders
    purpose: Move an order to another performance
    trigger: onAction
  - operationId: resumeOrder
    contract: orders
    purpose: Bring a parked sale back to a till
    trigger: onAction
  - operationId: voidOrder
    contract: orders
    purpose: Void an order
    trigger: onAction
  - operationId: getRefundPolicy
    contract: orders
    purpose: The policy this refund is judged against
    trigger: onLoad
  density: compact
  entryState:
    params:
    - name: orderId
      from: deepLink
    - name: venueId
      from: session
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves; if
      it was refunded or the performance passed, says which and offers the order list rather than an error.
      **The venue comes from the session** — a back-office user works one venue at a time and the refund
      policy is a venue setting, so the policy read needs it. Added 24 August with `getRefundPolicy`.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-024
  name: Payment Exceptions
  module: Orders & Money
  purpose: Resolve payments that never resolved themselves.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: primaryButton
        derived: true
        impliedBy: createPayment
        label: Create payment
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createPayment
  states:
    loading: Figures load; the expected total renders last
    emptyFirstRun: Nothing in the period — reported as a result, not an empty screen
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_POST`, `ORDER_CREATE`, `ORDER_MODIFY` and 1 more to use ledgers; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Every action that moves money is blocked** — a figure nobody read must not
      be reconciled against'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/payment-exceptions
    component: apps/venue-management-web/src/routes/venue-operations/PaymentExceptionsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 3.dc.html#ret-3k
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-3K in the client Retail pack.**'
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. Payment exceptions
    across currencies. **The rate shown is the one stored on the payment, not today's** (CF-37) — an exception
    opened in March and worked in August is worked at March's rate, or the reconciliation will never close.
  apis:
  - operationId: createPayment
    contract: orders
    purpose: Take a payment against an order
    trigger: onAction
  - operationId: addTip
    contract: orders
    purpose: Record a tip against a payment
    trigger: onAction
  - operationId: capturePayment
    contract: orders
    purpose: Capture a previously authorised payment
    trigger: onAction
  - operationId: inquirePaymentStatus
    contract: orders
    purpose: Ask the provider what actually happened
    trigger: onAction
  - operationId: closeDepositBoxes
    contract: shift
    purpose: Close one box or all of them
    trigger: onAction
  - operationId: settleDeposit
    contract: finance
    purpose: Convert to revenue, return it, or forfeit it
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: paymentId
      from: deepLink
    - name: depositId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `paymentId`, `depositId`.'
  requiresModule: ticketing
  boardFrames:
  - Retail Board 3.dc.html#ret-3k
  statesDerived: true
- id: BO-025
  name: Chargebacks & Disputes
  module: Orders & Money
  purpose: Answer the acquirer with the evidence attached.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listSettlementExceptions
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: resolveSettlementException
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: resolveSettlementException
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `SETTLEMENT_RECONCILE`, `SETTLEMENT_VIEW` to use settlements; a person without it sees the
      reason and who to ask, because an empty table reads as *there is no data* and sends somebody to
      support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/chargebacks-disputes
    component: apps/venue-management-web/src/routes/venue-operations/ChargebacksDisputesDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-025
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listSettlementExceptions
    contract: finance
    purpose: Unmatched or mismatched settlement lines
    trigger: onLoad
  - operationId: resolveSettlementException
    contract: finance
    purpose: Resolve a settlement exception
    trigger: onAction
  - operationId: getSettlement
    contract: finance
    purpose: Settlement detail with match results
    trigger: onLoad
  - operationId: ingestSettlementFile
    contract: finance
    purpose: Ingest a provider settlement file
    trigger: onAction
  - operationId: listSettlements
    contract: finance
    purpose: List settlement batches
    trigger: onLoad
  density: compact
  entryState:
    params:
    - name: settlementId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `settlementId`.'
  requiresModule: core
  statesDerived: true
- id: BO-026
  name: Group Bookings
  module: Orders & Money
  purpose: Handle a booking that arrives as a school, not a guest.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listOrders
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createOrder
        label: Create order
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: voidOrder
        label: Void order
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createOrder
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_CREATE`, `ORDER_DISCOUNT`, `ORDER_EXCHANGE` and 6 more to create orders; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/group-bookings
    component: apps/venue-management-web/src/routes/venue-operations/GroupBookingsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Platform Board 7.dc.html#seatp-7e
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-026
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Drawn 31 August**
    — `Seat Platform Board 7.dc.html` frame `seatp-7e`. **Matched on frame title against screen name,
    constrained to this board’s platforms.** These packs label by board position (`GM-6C`) rather than
    naming the screen, so the title is the only join — *Group Booking* matched at 0.96. **A cross-platform
    title match was refused**: `Outlet Management` scored 0.85 against a partner-portal screen, which
    is how a mapping goes wrong quietly.'
  apis:
  - operationId: listOrders
    contract: orders
    purpose: List orders
    trigger: onLoad
  - operationId: createOrder
    contract: orders
    purpose: Create an order
    trigger: onAction
  - operationId: applyManualDiscount
    contract: orders
    purpose: Apply a discount a cashier chose
    trigger: onAction
  - operationId: createRefund
    contract: orders
    purpose: Refund an order, wholly or in part
    trigger: onAction
  - operationId: exchangeOrderLines
    contract: orders
    purpose: Exchange lines for different products or dates
    trigger: onAction
  - operationId: getOrder
    contract: orders
    purpose: Read an order
    trigger: onLoad
  - operationId: getOrderStatement
    contract: orders
    purpose: Full financial history of an order
    trigger: onLoad
  - operationId: holdOrder
    contract: orders
    purpose: Park a sale and free the till
    trigger: onAction
  - operationId: listOrderRefunds
    contract: orders
    purpose: List refunds against an order
    trigger: onLoad
  - operationId: modifyOrder
    contract: orders
    purpose: Add or remove lines on an existing order
    trigger: onAction
  - operationId: reprintOrder
    contract: orders
    purpose: Reprint or resend tickets
    trigger: onAction
  - operationId: rescheduleOrder
    contract: orders
    purpose: Move an order to another performance
    trigger: onAction
  - operationId: resumeOrder
    contract: orders
    purpose: Bring a parked sale back to a till
    trigger: onAction
  - operationId: voidOrder
    contract: orders
    purpose: Void an order
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves; if
      it was refunded or the performance passed, says which and offers the order list rather than an error.'
  requiresModule: ticketing
  statesDerived: true
  boardFrames:
  - Seat Platform Board 7.dc.html#seatp-7e
- id: BO-027
  name: Reissue & Media Replacement
  module: Orders & Money
  purpose: Replace media a guest has lost or damaged.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: primaryButton
        derived: true
        impliedBy: appendEntitlementToMedia
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: appendEntitlementToMedia
  states:
    loading: Detail loads
    emptyFirstRun: Not found — it may have been deleted or moved out of scope
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ASSET_LIBRARY_MANAGE`, `ASSET_LIBRARY_VIEW`, `ORDER_CREATE` and 1 more to manage assets;
      a person without it sees the reason and who to ask, because an empty table reads as *there is no
      data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load. Editing is blocked
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/reissue-media-replacement
    component: apps/venue-management-web/src/routes/venue-operations/ReissueMediaReplacementDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-027
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **8 assets operations
    removed 18 August (CF-114).** The whole media contract was attached to this screen. **A till adding
    an item to a ticket does not manage a media library** — it reads the asset it needs and nothing else.
    Same shape as CF-87, one level up: that attached sibling operations, this attached a whole contract.'
  apis:
  - operationId: getMediaEntitlements
    contract: orders
    purpose: What is already on this media
    trigger: onLoad
  - operationId: appendEntitlementToMedia
    contract: orders
    purpose: Add something to a ticket the guest already holds
    trigger: onAction
  - operationId: getMediaAsset
    contract: assets
    purpose: Read an asset with derivatives and usage
    trigger: onLoad
  - operationId: replaceMediaAsset
    contract: assets
    purpose: Replace the file behind an asset
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: mediaCode
      from: deepLink
    - name: mediaId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `mediaCode`, `mediaId`.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-028
  name: Refund Approval Queue
  module: Orders & Money
  purpose: Approve the refunds a cashier is not allowed to make alone.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: primaryButton
        derived: true
        impliedBy: createRefundRequest
        label: Create refund request
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createRefundRequest
  states:
    loading: Figures load; the expected total renders last
    emptyFirstRun: Nothing in the period — reported as a result, not an empty screen
    error: '**Could not load. Every action that moves money is blocked** — a figure nobody read must not
      be reconciled against'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/refund-approval-queue
    component: apps/venue-management-web/src/routes/venue-operations/RefundApprovalQueueDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-028
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: createRefundRequest
    contract: orders
    purpose: Guest-initiated refund request
    trigger: onAction
  density: compact
  requiresModule: ticketing
- id: BO-029
  name: Report Builder
  module: Orders & Money
  purpose: Build the report nobody wrote down, without asking for a release.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listReports
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createReport
        label: Create report
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: deleteReport
        label: Delete report
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createReport
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `REPORT_MANAGE`, `REPORT_VIEW_VENUE` to manage reports; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `analytics` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/report-builder
    component: apps/venue-management-web/src/routes/venue-operations/ReportBuilderDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-029
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: getFinancialReport
    contract: finance
    purpose: P&L, balance sheet or cash flow
    trigger: onLoad
  - operationId: listReports
    contract: reporting
    purpose: List available report definitions
    trigger: onLoad
  - operationId: createReport
    contract: reporting
    purpose: Create a custom report definition
    trigger: onAction
  - operationId: askReportingQuestion
    contract: reporting
    purpose: Natural-language reporting query
    trigger: onAction
  - operationId: deleteReport
    contract: reporting
    purpose: Retire a report definition
    trigger: onAction
  - operationId: getReport
    contract: reporting
    purpose: Read a report definition
    trigger: onLoad
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  - operationId: saveNaturalLanguageQuery
    contract: reporting
    purpose: Save a natural-language answer as a report definition
    trigger: onAction
  - operationId: updateReport
    contract: reporting
    purpose: Publish a new version of a definition
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: conversationId
      from: deepLink
    - name: reportId
      from: deepLink
    coldEntry: A conversation link an agent opens from a notification. Resolves, or says it was closed
      and by whom.
  requiresModule: analytics
  statesDerived: true
- id: BO-030
  name: Work Order Verification
  module: Access & Venue
  purpose: See every gate and what it is doing.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: primaryButton
        derived: true
        impliedBy: verifyWorkOrder
        label: Verify work order
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: cancelWorkOrder
        label: Cancel work order
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: dataTable
        derived: true
        impliedBy: listWorkOrders
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: verifyWorkOrder
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `MAINTENANCE_APPROVE`, `MAINTENANCE_EXECUTE`, `WORK_ORDER_MANAGE` and 2 more to approve maintenances;
      a person without it sees the reason and who to ask, because an empty table reads as *there is no
      data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `maintenance` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    - BO-031
    inferred: true
    fromFlows: true
  implementation:
    app: venue-management-web
    route: /venue-operations/access-point-directory
    component: apps/venue-management-web/src/routes/venue-operations/AccessPointDirectoryDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-030
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Carried eight
    work-order operations.** An access point directory is `access`, not `maintenance` — the two share
    the word *point* and nothing else. **Rewired 20 August.** **Named `Access Point Directory` and carried
    nine work-order operations.** On 20 August I rewired it to access points; **F12 step 4 then refused,
    because a supervisor verifies a work order here.** The operations were right and the name was wrong
    — **the flow knew what the screen was for and the name did not.**
  apis:
  - operationId: verifyWorkOrder
    contract: maintenance
    purpose: Supervisor verification
    trigger: onAction
  - operationId: acceptWorkOrder
    contract: maintenance
    purpose: The assignee takes the job
    trigger: onAction
  - operationId: attachWorkOrderEvidence
    contract: maintenance
    purpose: Photo, video, document, note or signature
    trigger: onAction
  - operationId: cancelWorkOrder
    contract: maintenance
    purpose: Cancel a work order
    trigger: onAction
  - operationId: closeWorkOrder
    contract: maintenance
    purpose: Administratively closed
    trigger: onAction
  - operationId: completeWorkOrder
    contract: maintenance
    purpose: Complete a work order
    trigger: onAction
  - operationId: createWorkOrder
    contract: maintenance
    purpose: Raise a work order
    trigger: onAction
  - operationId: getWorkOrder
    contract: maintenance
    purpose: Read a work order
    trigger: onLoad
  - operationId: listWorkOrders
    contract: maintenance
    purpose: List work orders
    trigger: onLoad
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    - name: workOrderId
      from: deepLink
    coldEntry: A work order opened from a queue or an alert.
  requiresModule: maintenance
  statesDerived: true
- id: BO-031
  name: Asset Register
  module: Access & Venue
  purpose: Define what a gate is and where it is.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: primaryButton
        derived: true
        impliedBy: updateAsset
        label: Save asset
        notes: The act the screen exists for.
      - kind: dataTable
        derived: true
        impliedBy: listAssets
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: searchField
        derived: true
        impliedBy: lookupAsset
        label: Search
        notes: A search that returns nothing must say so differently from a search not yet run.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: updateAsset
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ASSET_MANAGE`, `ASSET_VIEW` to manage assets; a person without it sees the reason and who
      to ask, because an empty table reads as *there is no data* and sends somebody to support with the
      wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `maintenance` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/access-point-configuration
    component: apps/venue-management-web/src/routes/venue-operations/AccessPointConfigurationDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-031
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Carried seven
    asset operations.** A turnstile is an asset and configuring an access point is not asset management.
    **Rewired 20 August.** **Named `Access Point Configuration` and carried seven asset operations.**
    Same correction as BO-030 — F12 step 5 returns an asset to service here.
  apis:
  - operationId: updateAsset
    contract: maintenance
    purpose: Amend an asset
    trigger: onAction
  - operationId: createAsset
    contract: maintenance
    purpose: Register an asset
    trigger: onAction
  - operationId: getAsset
    contract: maintenance
    purpose: Read an asset with history and documents
    trigger: onLoad
  - operationId: getAssetHistory
    contract: maintenance
    purpose: Service history
    trigger: onLoad
  - operationId: listAssets
    contract: maintenance
    purpose: List assets
    trigger: onLoad
  - operationId: lookupAsset
    contract: maintenance
    purpose: Find an asset by tag or QR
    trigger: onLoad
  - operationId: setAssetStatus
    contract: maintenance
    purpose: Take an asset out of service or return it
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    - name: assetId
      from: deepLink
    coldEntry: An asset opened from the register or a work order.
  requiresModule: maintenance
  statesDerived: true
- id: BO-032
  name: Admission Profiles
  module: Access & Venue
  purpose: Set the rules a gate enforces, including offline.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listAdmissionRules
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createAdmissionRules
        label: Create admission profile
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createAdmissionRules
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCESS_POINT_CONFIGURE`, `SCOPE_VIEW` to configure accesses; a person without it sees the
      reason and who to ask, because an empty table reads as *there is no data* and sends somebody to
      support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `access` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/admission-rules
    component: apps/venue-management-web/src/routes/venue-operations/AdmissionProfilesDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-032
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listAdmissionRules
    contract: access
    purpose: List admission profiles
    trigger: onLoad
  - operationId: createAdmissionRules
    contract: access
    purpose: Create an admission profile
    trigger: onAction
  - operationId: updateAdmissionRules
    contract: access
    purpose: Update an admission profile
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: profileId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `profileId`.'
  requiresModule: access
  statesDerived: true
- id: BO-033
  name: Blacklist Management
  module: Access & Venue
  purpose: Bar a media code outright.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listBlacklist
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: addBlacklistEntry
        label: Add blacklist entry
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: removeBlacklistEntry
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
        label: Remove blacklist entry
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: addBlacklistEntry
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCESS_POINT_CONFIGURE`, `SCOPE_VIEW` to configure accesses; a person without it sees the
      reason and who to ask, because an empty table reads as *there is no data* and sends somebody to
      support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `access` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/blacklist-management
    component: apps/venue-management-web/src/routes/venue-operations/BlacklistManagementDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-033
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listBlacklist
    contract: access
    purpose: List blacklisted media
    trigger: onLoad
  - operationId: addBlacklistEntry
    contract: access
    purpose: Blacklist a media code
    trigger: onAction
  - operationId: removeBlacklistEntry
    contract: access
    purpose: Remove a blacklist entry
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: mediaCode
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `mediaCode`.'
  requiresModule: access
  statesDerived: true
- id: BO-034
  name: Scan Activity
  module: Access & Venue
  purpose: See what the gates have been doing.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: scanTarget
        derived: true
        impliedBy: listScans
        notes: '**A screen that validates a credential needs somewhere to point the camera.** `denied`
          and `hardwareError` look different because an operator facing a guest needs to know whether
          to try again or explain something.'
      - kind: searchField
        derived: true
        impliedBy: lookupTicket
        label: Search
        notes: A search that returns nothing must say so differently from a search not yet run.
      - kind: primaryButton
        derived: true
        impliedBy: overrideAccess
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: syncScans
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCESS_OVERRIDE`, `ACCESS_VALIDATE`, `REPORT_VIEW_VENUE` and 1 more to use accesses; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `access` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/scan-activity
    component: apps/venue-management-web/src/routes/venue-operations/ScanActivityDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-034
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listScans
    contract: access
    purpose: List scan events
    trigger: onLoad
  - operationId: syncScans
    contract: access
    purpose: Replay scans recorded offline
    trigger: onAction
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: lookupTicket
    contract: access
    purpose: Read-only validity check without admitting
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  - operationId: validateGroupAccess
    contract: access
    purpose: Admit a group on one read
    trigger: onAction
  density: compact
  requiresModule: access
  statesDerived: true
- id: BO-035
  name: Override Audit
  module: Access & Venue
  purpose: Review every admission that broke a rule.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: scanTarget
        derived: true
        impliedBy: listScans
        notes: '**A screen that validates a credential needs somewhere to point the camera.** `denied`
          and `hardwareError` look different because an operator facing a guest needs to know whether
          to try again or explain something.'
      - kind: searchField
        derived: true
        impliedBy: lookupTicket
        label: Search
        notes: A search that returns nothing must say so differently from a search not yet run.
      - kind: primaryButton
        derived: true
        impliedBy: overrideAccess
        notes: The act the screen exists for.
      - kind: dataTable
        derived: true
        impliedBy: listAuditRecords
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: overrideAccess
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCESS_OVERRIDE`, `ACCESS_VALIDATE`, `AI_AUDIT_VIEW` and 2 more to use accesses; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `access` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/override-audit
    component: apps/venue-management-web/src/routes/venue-operations/OverrideAuditDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-035
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listScans
    contract: access
    purpose: List scan events
    trigger: onLoad
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: lookupTicket
    contract: access
    purpose: Read-only validity check without admitting
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: syncScans
    contract: access
    purpose: Replay scans recorded offline
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  - operationId: validateGroupAccess
    contract: access
    purpose: Admit a group on one read
    trigger: onAction
  - operationId: listAuditRecords
    contract: tenancy
    purpose: Who did what, where, and when
    trigger: onLoad
  density: compact
  requiresModule: access
  statesDerived: true
- id: BO-036
  name: Device Registry
  module: Venue Operations
  purpose: Know which handheld is where, and whether it has synced.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listDevices
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: registerDevice
        notes: The act the screen exists for.
      - kind: searchField
        derived: true
        impliedBy: searchGuests
        label: Search
        notes: A search that returns nothing must say so differently from a search not yet run.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: registerDevice
      - kind: publishGate
        impliedBy: deployConfigurationProfile
        notes: >
          Declares `deployConfigurationProfile`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `AI_AUDIT_VIEW`, `DEVICE_CONFIGURE`, `DEVICE_VIEW` and 7 more to read ais; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `marketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-108
  implementation:
    app: venue-management-web
    route: /venue-operations/device-registry
    component: apps/venue-management-web/src/routes/venue-operations/DeviceRegistryDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 1.dc.html#pos-1a
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-1A, POS-1B, POS-1C in the client POS pack.** The pack''s frame is the specification
      — it carries operations, states, entry params and exits, and it is better specified than anything
      derived from the screen file.'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board**, answering 4 board screen(s): Workstation Overview Dashboard;
    Workstation Registry; Workstation Details & Configuration and 1 more. **The id, flows and navigation
    are unchanged** — a board specifies a screen further; it does not replace it. **Owns POS board frame(s)
    POS-1A, POS-1B, POS-1C** (client pack, 24 August). **Assigned by board purpose rather than by operation
    overlap** — three attempts at deriving that mapping produced plausible nonsense, and a reader who
    trusts a bad table is worse off than one who has none.'
  apis:
  - operationId: listDevices
    contract: tenancy
    purpose: List registered devices
    trigger: onLoad
  - operationId: listGuestDevices
    contract: marketing-crm
    purpose: A guest's registered devices
    trigger: onLoad
  - operationId: registerDevice
    contract: tenancy
    purpose: Register a device
    trigger: onAction
  - operationId: adjustLoyaltyPoints
    contract: marketing-crm
    purpose: Manually adjust points
    trigger: onAction
  - operationId: getConsentHistory
    contract: marketing-crm
    purpose: Full consent history
    trigger: onLoad
  - operationId: getGuestConsents
    contract: marketing-crm
    purpose: Read a guest's consent state
    trigger: onLoad
  - operationId: getGuestLoyalty
    contract: marketing-crm
    purpose: A guest's loyalty position
    trigger: onLoad
  - operationId: getGuestProfile
    contract: marketing-crm
    purpose: Read a guest profile
    trigger: onLoad
  - operationId: getWishlist
    contract: marketing-crm
    purpose: Read a guest's saved items
    trigger: onLoad
  - operationId: mergeGuestProfiles
    contract: marketing-crm
    purpose: Merge a duplicate profile into this one
    trigger: onAction
  - operationId: searchGuests
    contract: marketing-crm
    purpose: Search guest profiles
    trigger: onLoad
  - operationId: updateGuestProfile
    contract: marketing-crm
    purpose: Amend a guest profile
    trigger: onAction
  - operationId: configureWorkstation
    contract: tenancy
    purpose: Configure a workstation
    trigger: onAction
  - operationId: getWorkstation
    contract: tenancy
    purpose: Read a workstation
    trigger: onLoad
  - operationId: getWorkstationHealth
    contract: tenancy
    purpose: getWorkstationHealth
    trigger: onLoad
  - operationId: listWorkstations
    contract: tenancy
    purpose: List workstations
    trigger: onLoad
  - operationId: deployConfigurationProfile
    contract: tenancy
    purpose: deployConfigurationProfile
    trigger: onAction
  - operationId: listAlerts
    contract: reporting
    purpose: What is currently raised
    trigger: onLoad
  - operationId: listAuditRecords
    contract: tenancy
    purpose: Who did what, where, and when
    trigger: onLoad
  - operationId: recordDeviceHeartbeat
    contract: tenancy
    purpose: Device heartbeat and status
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    - name: subjectId
      from: deepLink
    - name: workstationId
      from: deepLink
    - name: deviceId
      from: deepLink
    - name: profileId
      from: deepLink
    coldEntry: A workstation opened from the registry or an alert. A device opened from the registry.
      A profile opened from the list.
  provenance: client board, specified
  requiresModule: marketing
  boardFrames:
  - POS Board 1.dc.html#pos-1a
  - POS Board 1.dc.html#pos-1b
  - POS Board 1.dc.html#pos-1c
  statesDerived: true
- id: BO-037
  name: Offline Package Status
  module: Sell
  purpose: Know what each device is enforcing right now.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listCatalogueBundles
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: publishBundle
        label: Publish bundle
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: publishBundle
      - kind: publishGate
        impliedBy: publishBundle
        notes: >
          Declares `publishBundle`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCESS_VALIDATE`, `ORDER_CREATE`, `ORDER_VIEW` and 4 more to use accesses; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/offline-package-status
    component: apps/venue-management-web/src/routes/venue-operations/OfflinePackageStatusDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 5.dc.html#pos-5a
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-5A, POS-5C in the client POS pack.** The pack''s frame is the specification
      — it carries operations, states, entry params and exits, and it is better specified than anything
      derived from the screen file.'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board**, answering 2 board screen(s): Offline Operations Dashboard;
    Offline Product & Data Cache Management. **The id, flows and navigation are unchanged** — a board
    specifies a screen further; it does not replace it. **Owns POS board frame(s) POS-5A, POS-5C** (client
    pack, 24 August). **Assigned by board purpose rather than by operation overlap** — three attempts
    at deriving that mapping produced plausible nonsense, and a reader who trusts a bad table is worse
    off than one who has none.'
  apis:
  - operationId: listCatalogueBundles
    contract: catalogue
    purpose: List published bundles
    trigger: onLoad
  - operationId: getLatestBundle
    contract: catalogue
    purpose: Pull the current bundle for this workstation's venue
    trigger: onLoad
  - operationId: publishBundle
    contract: catalogue
    purpose: Compute, sign and publish a catalogue bundle
    trigger: onAction
  - operationId: reportBundleApplied
    contract: catalogue
    purpose: Report that a workstation applied a bundle
    trigger: onAction
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: listSyncRejections
    contract: orders
    purpose: Entries the server refused
    trigger: onLoad
  - operationId: listWorkstations
    contract: tenancy
    purpose: List workstations
    trigger: onLoad
  - operationId: setOfflinePolicy
    contract: tenancy
    purpose: setOfflinePolicy
    trigger: onAction
  - operationId: syncOrders
    contract: orders
    purpose: Replay orders recorded offline
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: version
      from: deepLink
    coldEntry: '**A version link is expected to point at something superseded — that is what versions
      are for.** The screen opens the requested version read-only, says it is not current, and links to
      the one that is. **An old version is history, not an error.** Arrives with `version`.'
  provenance: client board, specified
  requiresModule: ticketing
  boardFrames:
  - POS Board 5.dc.html#pos-5a
  - POS Board 5.dc.html#pos-5c
  - Retail Board 3.dc.html#ret-3j
  statesDerived: true
- id: BO-038
  name: Reconciliation Queue
  module: Access & Venue
  purpose: Handle scans the server disagreed with.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listQueues
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createQueue
        label: Create queue
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createQueue
  states:
    loading: Figures load; the expected total renders last
    emptyFirstRun: Nothing in the period — reported as a result, not an empty screen
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `QUEUE_MANAGE`, `QUEUE_VIEW` to manage queues; a person without it sees the reason and who
      to ask, because an empty table reads as *there is no data* and sends somebody to support with the
      wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `queue` is not licensed.** The tenant has not bought this module, which is a
      commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Every action that moves money is blocked** — a figure nobody read must not
      be reconciled against'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/reconciliation-queue
    component: apps/venue-management-web/src/routes/venue-operations/ReconciliationQueueDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-038
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listQueues
    contract: queue
    purpose: List queues
    trigger: onLoad
  - operationId: getQueue
    contract: queue
    purpose: Read a queue with live position
    trigger: onLoad
  - operationId: createQueue
    contract: queue
    purpose: Create a queue
    trigger: onAction
  - operationId: callNextParties
    contract: queue
    purpose: Call the next parties forward
    trigger: onAction
  - operationId: getWaitTimes
    contract: queue
    purpose: Wait times across a venue
    trigger: onLoad
  - operationId: listQueueEntries
    contract: queue
    purpose: List entries in a queue
    trigger: onLoad
  - operationId: setQueueStatus
    contract: queue
    purpose: Open, pause or close a queue
    trigger: onAction
  - operationId: setWaitTime
    contract: queue
    purpose: Manually set a wait time
    trigger: onAction
  - operationId: updateQueue
    contract: queue
    purpose: Amend queue configuration
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: queueId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `queueId`.'
  requiresModule: queue
  statesDerived: true
- id: BO-039
  name: Shift Directory
  module: Orders & Money
  purpose: See every till, open or closed.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listShifts
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: openShift
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: openShift
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `CASH_LIFT`, `OVERSHORT_ACCEPT`, `REPORT_VIEW_WORKSTATION` and 5 more to use cashs; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/shift-directory
    component: apps/venue-management-web/src/routes/venue-operations/ShiftDirectoryDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-039
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listShifts
    contract: shift
    purpose: List shifts
    trigger: onLoad
  - operationId: getCurrentShift
    contract: shift
    purpose: The open or suspended shift on the session's workstation
    trigger: onLoad
  - operationId: openShift
    contract: shift
    purpose: Open a shift
    trigger: onAction
  - operationId: acceptShiftVariance
    contract: shift
    purpose: Accept an over/short beyond the threshold
    trigger: onAction
  - operationId: approveShiftOpen
    contract: shift
    purpose: Approve a shift opening outside tolerance
    trigger: onAction
  - operationId: closeShift
    contract: shift
    purpose: Blind close-out
    trigger: onAction
  - operationId: createCashMovement
    contract: shift
    purpose: Record a cash lift or add
    trigger: onAction
  - operationId: getShift
    contract: shift
    purpose: Read a shift
    trigger: onLoad
  - operationId: listCashMovements
    contract: shift
    purpose: Lifts, adds and the opening float
    trigger: onLoad
  - operationId: recordNoSale
    contract: shift
    purpose: Open the drawer without a sale
    trigger: onAction
  - operationId: reopenShift
    contract: shift
    purpose: Reopen a shift closed in error
    trigger: onAction
  - operationId: resumeShift
    contract: shift
    purpose: Resume a suspended shift
    trigger: onAction
  - operationId: suspendShift
    contract: shift
    purpose: Suspend a shift so another user can log in
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: shiftId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: core
  statesDerived: true
- id: BO-040
  name: Variance Approval
  module: Orders & Money
  purpose: Accept or refuse a till that did not balance.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listShifts
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: acceptShiftVariance
        label: Accept shift variance
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: acceptShiftVariance
  states:
    loading: Figures load; the expected total renders last
    emptyFirstRun: Nothing in the period — reported as a result, not an empty screen
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `CASH_LIFT`, `OVERSHORT_ACCEPT`, `REPORT_VIEW_WORKSTATION` and 5 more to use cashs; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**Could not load. Every action that moves money is blocked** — a figure nobody read must not
      be reconciled against'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    - BO-089
    inferred: true
    fromFlows: true
  implementation:
    app: venue-management-web
    route: /venue-operations/variance-approval
    component: apps/venue-management-web/src/routes/venue-operations/VarianceApprovalDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-040
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listShifts
    contract: shift
    purpose: List shifts
    trigger: onLoad
  - operationId: acceptShiftVariance
    contract: shift
    purpose: Accept an over/short beyond the threshold
    trigger: onAction
  - operationId: approveShiftOpen
    contract: shift
    purpose: Approve a shift opening outside tolerance
    trigger: onAction
  - operationId: closeShift
    contract: shift
    purpose: Blind close-out
    trigger: onAction
  - operationId: createCashMovement
    contract: shift
    purpose: Record a cash lift or add
    trigger: onAction
  - operationId: getCurrentShift
    contract: shift
    purpose: The open or suspended shift on the session's workstation
    trigger: onLoad
  - operationId: getShift
    contract: shift
    purpose: Read a shift
    trigger: onLoad
  - operationId: listCashMovements
    contract: shift
    purpose: Lifts, adds and the opening float
    trigger: onLoad
  - operationId: openShift
    contract: shift
    purpose: Open a shift
    trigger: onAction
  - operationId: recordNoSale
    contract: shift
    purpose: Open the drawer without a sale
    trigger: onAction
  - operationId: reopenShift
    contract: shift
    purpose: Reopen a shift closed in error
    trigger: onAction
  - operationId: resumeShift
    contract: shift
    purpose: Resume a suspended shift
    trigger: onAction
  - operationId: suspendShift
    contract: shift
    purpose: Suspend a shift so another user can log in
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: shiftId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: core
  statesDerived: true
- id: BO-041
  name: Cash Movements
  module: Orders & Money
  purpose: Track money in and out of a drawer that is not a sale.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listCashMovements
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createCashMovement
        label: Create cash movement
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createCashMovement
  states:
    loading: Figures load; the expected total renders last
    emptyFirstRun: Nothing in the period — reported as a result, not an empty screen
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `CASH_LIFT`, `OVERSHORT_ACCEPT`, `REPORT_VIEW_WORKSTATION` and 5 more to use cashs; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**Could not load. Every action that moves money is blocked** — a figure nobody read must not
      be reconciled against'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/cash-movements
    component: apps/venue-management-web/src/routes/venue-operations/CashMovementsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-041
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listCashMovements
    contract: shift
    purpose: Lifts, adds and the opening float
    trigger: onLoad
  - operationId: createCashMovement
    contract: shift
    purpose: Record a cash lift or add
    trigger: onAction
  - operationId: acceptShiftVariance
    contract: shift
    purpose: Accept an over/short beyond the threshold
    trigger: onAction
  - operationId: approveShiftOpen
    contract: shift
    purpose: Approve a shift opening outside tolerance
    trigger: onAction
  - operationId: closeShift
    contract: shift
    purpose: Blind close-out
    trigger: onAction
  - operationId: getCurrentShift
    contract: shift
    purpose: The open or suspended shift on the session's workstation
    trigger: onLoad
  - operationId: getShift
    contract: shift
    purpose: Read a shift
    trigger: onLoad
  - operationId: listShifts
    contract: shift
    purpose: List shifts
    trigger: onLoad
  - operationId: openShift
    contract: shift
    purpose: Open a shift
    trigger: onAction
  - operationId: recordNoSale
    contract: shift
    purpose: Open the drawer without a sale
    trigger: onAction
  - operationId: reopenShift
    contract: shift
    purpose: Reopen a shift closed in error
    trigger: onAction
  - operationId: resumeShift
    contract: shift
    purpose: Resume a suspended shift
    trigger: onAction
  - operationId: suspendShift
    contract: shift
    purpose: Suspend a shift so another user can log in
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: shiftId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: core
  statesDerived: true
- id: BO-042
  name: Banking & Safe
  module: Orders & Money
  purpose: Move the day’s cash out of the tills.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listCashMovements
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: acceptShiftVariance
        label: Accept shift variance
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: acceptShiftVariance
  states:
    loading: Figures load; the expected total renders last
    emptyFirstRun: Nothing in the period — reported as a result, not an empty screen
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `CASH_LIFT`, `OVERSHORT_ACCEPT`, `REPORT_VIEW_WORKSTATION` and 5 more to use cashs; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**Could not load. Every action that moves money is blocked** — a figure nobody read must not
      be reconciled against'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/banking-safe
    component: apps/venue-management-web/src/routes/venue-operations/BankingSafeDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-042
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listCashMovements
    contract: shift
    purpose: Lifts, adds and the opening float
    trigger: onLoad
  - operationId: acceptShiftVariance
    contract: shift
    purpose: Accept an over/short beyond the threshold
    trigger: onAction
  - operationId: approveShiftOpen
    contract: shift
    purpose: Approve a shift opening outside tolerance
    trigger: onAction
  - operationId: closeShift
    contract: shift
    purpose: Blind close-out
    trigger: onAction
  - operationId: createCashMovement
    contract: shift
    purpose: Record a cash lift or add
    trigger: onAction
  - operationId: getCurrentShift
    contract: shift
    purpose: The open or suspended shift on the session's workstation
    trigger: onLoad
  - operationId: getShift
    contract: shift
    purpose: Read a shift
    trigger: onLoad
  - operationId: listShifts
    contract: shift
    purpose: List shifts
    trigger: onLoad
  - operationId: openShift
    contract: shift
    purpose: Open a shift
    trigger: onAction
  - operationId: recordNoSale
    contract: shift
    purpose: Open the drawer without a sale
    trigger: onAction
  - operationId: reopenShift
    contract: shift
    purpose: Reopen a shift closed in error
    trigger: onAction
  - operationId: resumeShift
    contract: shift
    purpose: Resume a suspended shift
    trigger: onAction
  - operationId: suspendShift
    contract: shift
    purpose: Suspend a shift so another user can log in
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: shiftId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: core
  statesDerived: true
- id: BO-043
  name: Daily Reconciliation
  module: Orders & Money
  purpose: Prove the day balances before anyone goes home.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listSettlements
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: ingestSettlementFile
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: ingestSettlementFile
  states:
    loading: Figures load; the expected total renders last
    emptyFirstRun: Nothing in the period — reported as a result, not an empty screen
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_VIEW`, `SETTLEMENT_RECONCILE`, `SETTLEMENT_VIEW` to read ledgers; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**Could not load. Every action that moves money is blocked** — a figure nobody read must not
      be reconciled against'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/daily-reconciliation
    component: apps/venue-management-web/src/routes/venue-operations/DailyReconciliationDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-043
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: getTrialBalance
    contract: finance
    purpose: Trial balance for a period
    trigger: onLoad
  - operationId: listSettlements
    contract: finance
    purpose: List settlement batches
    trigger: onLoad
  - operationId: getSettlement
    contract: finance
    purpose: Settlement detail with match results
    trigger: onLoad
  - operationId: ingestSettlementFile
    contract: finance
    purpose: Ingest a provider settlement file
    trigger: onAction
  - operationId: listLedgerEntries
    contract: finance
    purpose: Query the ledger
    trigger: onLoad
  - operationId: listSettlementExceptions
    contract: finance
    purpose: Unmatched or mismatched settlement lines
    trigger: onLoad
  - operationId: resolveSettlementException
    contract: finance
    purpose: Resolve a settlement exception
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: settlementId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `settlementId`.'
  requiresModule: core
  statesDerived: true
- id: BO-044
  name: F&B Outlets
  module: Venue Operations
  purpose: See every outlet and whether it is trading.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listOutlets
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createOutlet
        label: Create outlet
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createOutlet
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `INCIDENT_MANAGE`, `INCIDENT_VIEW`, `ORDER_CREATE` and 6 more to manage incidents; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `fnb` is not licensed.** The tenant has not bought this module, which is a commercial
      answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-108
  implementation:
    app: venue-management-web
    route: /venue-operations/f-b-outlets
    component: apps/venue-management-web/src/routes/venue-operations/FBOutletsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 1.dc.html#ret-1b
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-1B, RET-1C, RET-1D, RET-1E, RET-1F, RET-1G, RET-1H, RET-1J, RET-1K in the client
      Retail pack.**'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board**, answering 9 board screen(s): F&B Command Center; Outlet
    Management; Create / Edit Outlet and 6 more. **The id, flows and navigation are unchanged** — a board
    specifies a screen further; it does not replace it. **Food-safety operations wired 20 August** — boards
    5G and 5J of the client F&B pack, and **HACCP is a regulatory obligation nothing in the package touched.**
    **This screen owns 18 board frames — the whole of F&B board 1 and the whole of Retail board 1.** No
    flow could be derived from it because **a chain of nine frames that all resolve to one screen is not
    a journey**, it is one screen the client drew nine views of. **That is the module system working**:
    a venue configuring an outlet and a venue configuring a store are the same screen with a different
    licence, and `requiresModule` is what makes them look different. **Worth stating rather than papering
    over with a single-step flow.** **Five of the 74 board chains collapse to this one screen and no others
    do.** The client drew nine F&B views and nine retail views of one outlet configuration surface — **which
    is what makes it the strongest evidence in the package that the module system is the right shape.**
    Nine frames per domain, one screen, and `requiresModule` is the only difference a tenant sees.'
  apis:
  - operationId: listOutlets
    contract: tenancy
    purpose: List outlets
    trigger: onLoad
  - operationId: getGuestMenu
    contract: fnb
    purpose: The menu a guest sees
    trigger: onLoad
  - operationId: createOutlet
    contract: tenancy
    purpose: Create an outlet
    trigger: onAction
  - operationId: getOutletStock
    contract: retail
    purpose: Stock across an outlet
    trigger: onLoad
  - operationId: getReturnPolicy
    contract: retail
    purpose: Read the retail return policy
    trigger: onLoad
  - operationId: getTableMap
    contract: fnb
    purpose: Table map with live state
    trigger: onLoad
  - operationId: recordWaste
    contract: fnb
    purpose: Record waste
    trigger: onAction
  - operationId: reserveMerchandise
    contract: retail
    purpose: Reserve an item for collection
    trigger: onAction
  - operationId: setReturnPolicy
    contract: retail
    purpose: Set the retail return policy
    trigger: onAction
  - operationId: setTableLayout
    contract: fnb
    purpose: Configure the table layout
    trigger: onAction
  - operationId: updateOutlet
    contract: tenancy
    purpose: Amend an outlet
    trigger: onAction
  - operationId: listFnbOrders
    contract: fnb
    purpose: List F&B orders
    trigger: onLoad
  - operationId: listMerchandise
    contract: retail
    purpose: List merchandise
    trigger: onLoad
  - operationId: getHaccpStatus
    contract: fnb
    purpose: getHaccpStatus
    trigger: onLoad
  - operationId: signCorrectiveAction
    contract: fnb
    purpose: signCorrectiveAction
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: outletId
      from: deepLink
    - name: actionId
      from: deepLink
      optional: true
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `outletId`, `actionId`.'
  provenance: client board, specified
  requiresModule: fnb
  boardFrames:
  - FnB Board 1.dc.html#fnb-1b
  - FnB Board 1.dc.html#fnb-1c
  - FnB Board 1.dc.html#fnb-1d
  - FnB Board 1.dc.html#fnb-1e
  - FnB Board 1.dc.html#fnb-1f
  - FnB Board 1.dc.html#fnb-1g
  - FnB Board 1.dc.html#fnb-1h
  - FnB Board 1.dc.html#fnb-1j
  - FnB Board 1.dc.html#fnb-1k
  - Retail Board 1.dc.html#ret-1b
  - Retail Board 1.dc.html#ret-1c
  - Retail Board 1.dc.html#ret-1d
  - Retail Board 1.dc.html#ret-1e
  - Retail Board 1.dc.html#ret-1f
  - Retail Board 1.dc.html#ret-1g
  - Retail Board 1.dc.html#ret-1h
  - Retail Board 1.dc.html#ret-1j
  - Retail Board 1.dc.html#ret-1k
  statesDerived: true
- id: BO-045
  name: Menu Management
  module: Food & Beverage
  wave: 1
  purpose: Change what is on sale and what is in it.
  density: compact
  apis:
  - operationId: listMenus
    contract: fnb
    purpose: List menus
    trigger: onLoad
  - operationId: getMenu
    contract: fnb
    purpose: Read a menu with sections and items
    trigger: onLoad
  - operationId: createMenu
    contract: fnb
    purpose: Create a menu
    trigger: onAction
  - operationId: setMenuSections
    contract: fnb
    purpose: Set menu sections and their item ordering
    trigger: onAction
  - operationId: updateMenu
    contract: fnb
    purpose: Amend a menu
    trigger: onAction
  - operationId: publishMenu
    contract: fnb
    purpose: Make the draft live, now or on a date
    trigger: onAction
  - operationId: scheduleMenuPublish
    contract: fnb
    purpose: Publish it on a date, not now
    trigger: onAction
  - operationId: rollbackMenu
    contract: fnb
    purpose: Put the previous version back
    trigger: onAction
  - operationId: applyMenuActions
    contract: fnb
    purpose: Do the same thing to many items at once
    trigger: onAction
  - operationId: verifyAllergens
    contract: fnb
    purpose: Does this dish still match its claim?
    trigger: onAction
  states:
    loading: Content loads
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `createMenu`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Change what is on sale and what is
      in it.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission. The list is not empty.
    error: Could not load.
  entryState:
    params:
    - name: venueId
      from: session
    - name: menuId
      from: deepLink
    - name: menuItemId
      from: deepLink
    coldEntry: A menu opened from the list. An item opened from the menu. **Allergen verification is per
      item** — a menu-wide check is a job, not a screen.
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
        derived: true
        impliedBy: getMenu
        notes: One record, read-only.
      - kind: primaryButton
        derived: true
        impliedBy: createMenu
        label: Create menu
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createMenu
      - kind: publishGate
        impliedBy: publishMenu
        notes: >
          Declares `publishMenu`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  navigation:
    entryFrom:
    - BO-104
    exitTo:
    - BO-104
    - BO-111
    inferred: false
    notes: '**Returns to BO-104.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product. **flow F31 — the manager re-verifies allergens from
      the menu being drafted.** The flow is the authority on the journey and the navigation has to agree
      with it; marking this block non-inferred is what turned the disagreement from a warning into a failure.'
  implementation:
    app: venue-management-web
    route: /fnb/bo-045
    component: apps/venue-management-web/src/routes/fnb/BO045List.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/FnB Board 2.dc.html#fnb-2a
    source: Claude Design F&B pack, 24 August
    note: '**Drawn as FNB-2A, FNB-2B, FNB-2E, FNB-2J, FNB-2K in the client F&B pack.**'
  notes: Restored 20 August when the P15 build was rolled back. **Operations from the 24 August F&B build
    wired here** — the contract grew and the screens had not caught up, which is how 92 operations reached
    49% of screens.
  requiresModule: fnb
  boardFrames:
  - FnB Board 2.dc.html#fnb-2a
  - FnB Board 2.dc.html#fnb-2b
  - FnB Board 2.dc.html#fnb-2e
  - FnB Board 2.dc.html#fnb-2j
  - FnB Board 2.dc.html#fnb-2k
  statesDerived: true
- id: BO-046
  name: Kitchen Display
  module: Food & Beverage
  wave: 1
  purpose: Show the kitchen what to make, in order.
  density: compact
  apis:
  - operationId: listKitchenTickets
    contract: fnb
    purpose: Kitchen ticket queue
    trigger: onLoad
  - operationId: setKitchenTicketStatus
    contract: fnb
    purpose: Advance a kitchen ticket
    trigger: onAction
  - operationId: listKitchenStations
    contract: fnb
    purpose: List preparation stations and their routing
    trigger: onLoad
  - operationId: prioritiseKitchenTicket
    contract: fnb
    purpose: Move a ticket up the queue
    trigger: onAction
  - operationId: setKitchenStations
    contract: fnb
    purpose: Configure stations and item routing
    trigger: onAction
  states:
    loading: Content loads
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Show the kitchen what to make, in order.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission. The list is not empty.
    error: Could not load.
  entryState:
    params:
    - name: venueId
      from: session
    - name: ticketId
      from: deepLink
    coldEntry: A kitchen ticket opened from the rail.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: dataTable
      - kind: primaryButton
        derived: true
        impliedBy: setKitchenTicketStatus
        label: Save kitchen ticket status
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: setKitchenTicketStatus
  navigation:
    entryFrom:
    - BO-104
    exitTo:
    - BO-104
    inferred: false
    notes: '**Returns to BO-104.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /fnb/bo-046
    component: apps/venue-management-web/src/routes/fnb/BO046List.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-046
  notes: Restored 20 August when the P15 build was rolled back. **Retained in P08 on 20 August when the
    P15 build was rolled back**, and P15 now owns the kitchen display for the venue floor. **This is the
    back-office view of the same tickets** — a manager watching the pass from a desk, not a screen at
    the pass.
  requiresModule: fnb
  statesDerived: true
- id: BO-047
  name: F&B Order Management
  module: Orders & Money
  purpose: Fix an order that has gone wrong.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listOrders
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createOrder
        label: Create order
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: voidOrder
        label: Void order
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createOrder
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_CREATE`, `ORDER_DISCOUNT`, `ORDER_EXCHANGE` and 6 more to create orders; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/f-b-order-management
    component: apps/venue-management-web/src/routes/venue-operations/FBOrderManagementDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-047
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listOrders
    contract: orders
    purpose: List orders
    trigger: onLoad
  - operationId: getOrder
    contract: orders
    purpose: Read an order
    trigger: onLoad
  - operationId: createOrder
    contract: orders
    purpose: Create an order
    trigger: onAction
  - operationId: applyManualDiscount
    contract: orders
    purpose: Apply a discount a cashier chose
    trigger: onAction
  - operationId: createRefund
    contract: orders
    purpose: Refund an order, wholly or in part
    trigger: onAction
  - operationId: exchangeOrderLines
    contract: orders
    purpose: Exchange lines for different products or dates
    trigger: onAction
  - operationId: getOrderStatement
    contract: orders
    purpose: Full financial history of an order
    trigger: onLoad
  - operationId: holdOrder
    contract: orders
    purpose: Park a sale and free the till
    trigger: onAction
  - operationId: listOrderRefunds
    contract: orders
    purpose: List refunds against an order
    trigger: onLoad
  - operationId: modifyOrder
    contract: orders
    purpose: Add or remove lines on an existing order
    trigger: onAction
  - operationId: reprintOrder
    contract: orders
    purpose: Reprint or resend tickets
    trigger: onAction
  - operationId: rescheduleOrder
    contract: orders
    purpose: Move an order to another performance
    trigger: onAction
  - operationId: resumeOrder
    contract: orders
    purpose: Bring a parked sale back to a till
    trigger: onAction
  - operationId: voidOrder
    contract: orders
    purpose: Void an order
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves; if
      it was refunded or the performance passed, says which and offers the order list rather than an error.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-048
  name: Retail Products
  module: Orders & Money
  purpose: Manage what the shop sells.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listMerchandise
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: searchField
        derived: true
        impliedBy: lookupMerchandise
        label: Search
        notes: A search that returns nothing must say so differently from a search not yet run.
      - kind: primaryButton
        derived: true
        impliedBy: createMerchandise
        label: Create merchandise
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createMerchandise
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to configure products; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `retail` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    fromFlows: true
  implementation:
    app: venue-management-web
    route: /venue-operations/retail-products
    component: apps/venue-management-web/src/routes/venue-operations/RetailProductsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-048
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Cross-platform
    navigation removed 24 August**: GST-026. **A till does not navigate to a back office and a guest app
    does not navigate to either** — those are device handovers, and a flow declares them with `crossesDevice`
    rather than a screen pretending there is a link.'
  apis:
  - operationId: listMerchandise
    contract: retail
    purpose: List merchandise
    trigger: onLoad
  - operationId: lookupMerchandise
    contract: retail
    purpose: Price and stock check by barcode
    trigger: onLoad
  - operationId: createMerchandise
    contract: retail
    purpose: Create a merchandise item
    trigger: onAction
  - operationId: updateMerchandise
    contract: retail
    purpose: Amend a merchandise item
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: merchandiseId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `merchandiseId`.'
  requiresModule: retail
  statesDerived: true
- id: BO-049
  name: Stock Levels
  module: Stock & Supply
  purpose: Know what is on the shelf and what is on order.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: primaryButton
        derived: true
        impliedBy: setItemAvailability
        label: Save item availability
        notes: The act the screen exists for.
      - kind: dataTable
        derived: true
        impliedBy: listStockLocations
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: setItemAvailability
  states:
    loading: Detail loads
    emptyFirstRun: Not found — it may have been deleted or moved out of scope
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_VIEW`, `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to read ledgers; a person without it sees
      the reason and who to ask, because an empty table reads as *there is no data* and sends somebody
      to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `inventory` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    - BO-079
    - BO-080
    - BO-082
    inferred: true
    entryFrom:
    - BO-105
  implementation:
    app: venue-management-web
    route: /venue-operations/stock-levels
    component: apps/venue-management-web/src/routes/venue-operations/StockLevelsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 4.dc.html#ret-4a
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-4A, RET-4H in the client Retail pack.**'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board**, answering 2 board screen(s): F&B Stock & Operations Command
    Center; Outlet Stock & Ingredient Availability. **The id, flows and navigation are unchanged** — a
    board specifies a screen further; it does not replace it.'
  apis:
  - operationId: getStockPositions
    contract: inventory
    purpose: Stock on hand by item and location
    trigger: onLoad
  - operationId: getStockValuation
    contract: inventory
    purpose: Stock value by location and category
    trigger: onLoad
  - operationId: setItemAvailability
    contract: fnb
    purpose: Mark an item available or eighty-sixed
    trigger: onAction
  - operationId: createStockTransfer
    contract: inventory
    purpose: Send stock to another location
    trigger: onAction
  - operationId: createStockMovement
    contract: inventory
    purpose: Record an issue, return or adjustment
    trigger: onAction
  - operationId: listStockLocations
    contract: inventory
    purpose: List stock locations
    trigger: onLoad
  density: compact
  provenance: client board, specified
  entryState:
    params:
    - name: venueId
      from: session
    - name: itemId
      from: deepLink
    coldEntry: An item opened from the catalogue.
  requiresModule: inventory
  boardFrames:
  - FnB Board 5.dc.html#fnb-5a
  - FnB Board 5.dc.html#fnb-5b
  - Retail Board 4.dc.html#ret-4a
  - Retail Board 4.dc.html#ret-4h
  statesDerived: true
- id: BO-050
  name: Stock Position & Valuation
  module: Stock & Supply
  purpose: Count the shelf and account for the difference.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Detail loads
    emptyFirstRun: Not found — it may have been deleted or moved out of scope
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_VIEW`, `PRODUCT_VIEW` to read ledgers; a person without it sees the reason and who
      to ask, because an empty table reads as *there is no data* and sends somebody to support with the
      wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `inventory` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-105
  implementation:
    app: venue-management-web
    route: /venue-operations/stock-count
    component: apps/venue-management-web/src/routes/venue-operations/StockCountDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-050
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Named `Stock
    Count` and carrying `getStockPositions` and `getStockValuation`** — two screens with one name, while
    `BO-079 Stock Count` holds the actual counting operations. Renamed 20 August; the operations were
    right.
  apis:
  - operationId: getStockPositions
    contract: inventory
    purpose: Stock on hand by item and location
    trigger: onLoad
  - operationId: getStockValuation
    contract: inventory
    purpose: Stock value by location and category
    trigger: onLoad
  density: compact
  requiresModule: inventory
  statesDerived: true
- id: BO-051
  name: Purchase Orders
  module: Orders & Money
  purpose: Order more of what is running out.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listOrders
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createOrder
        label: Create order
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: voidOrder
        label: Void order
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createOrder
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_CREATE`, `ORDER_DISCOUNT`, `ORDER_EXCHANGE` and 5 more to create orders; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/purchase-orders
    component: apps/venue-management-web/src/routes/venue-operations/PurchaseOrdersDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-051
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **createRefund
    removed 18 August** — attached by module resemblance, not by what this screen does. A screen that
    does not handle money should not be able to move it (CF-87's class).
  apis:
  - operationId: listOrders
    contract: orders
    purpose: List orders
    trigger: onLoad
  - operationId: getOrder
    contract: orders
    purpose: Read an order
    trigger: onLoad
  - operationId: createOrder
    contract: orders
    purpose: Create an order
    trigger: onAction
  - operationId: applyManualDiscount
    contract: orders
    purpose: Apply a discount a cashier chose
    trigger: onAction
  - operationId: exchangeOrderLines
    contract: orders
    purpose: Exchange lines for different products or dates
    trigger: onAction
  - operationId: getOrderStatement
    contract: orders
    purpose: Full financial history of an order
    trigger: onLoad
  - operationId: holdOrder
    contract: orders
    purpose: Park a sale and free the till
    trigger: onAction
  - operationId: listOrderRefunds
    contract: orders
    purpose: List refunds against an order
    trigger: onLoad
  - operationId: modifyOrder
    contract: orders
    purpose: Add or remove lines on an existing order
    trigger: onAction
  - operationId: reprintOrder
    contract: orders
    purpose: Reprint or resend tickets
    trigger: onAction
  - operationId: rescheduleOrder
    contract: orders
    purpose: Move an order to another performance
    trigger: onAction
  - operationId: resumeOrder
    contract: orders
    purpose: Bring a parked sale back to a till
    trigger: onAction
  - operationId: voidOrder
    contract: orders
    purpose: Void an order
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves; if
      it was refunded or the performance passed, says which and offers the order list rather than an error.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-052
  name: Goods Receipt
  module: Stock & Supply
  purpose: Book in what actually arrived.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: primaryButton
        derived: true
        impliedBy: createGoodsReceipt
        label: Create goods receipt
        notes: The act the screen exists for.
      - kind: dataTable
        derived: true
        impliedBy: listPurchaseOrders
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: destructiveButton
        derived: true
        impliedBy: cancelPurchaseOrder
        label: Cancel purchase order
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createGoodsReceipt
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_APPROVE`, `ORDER_CANCEL`, `ORDER_CREATE` and 2 more to approve ledgers; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `inventory` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    - BO-049
    inferred: true
    fromFlows: true
    entryFrom:
    - BO-105
  implementation:
    app: venue-management-web
    route: /venue-operations/goods-receipt
    component: apps/venue-management-web/src/routes/venue-operations/GoodsReceiptDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 4.dc.html#ret-4e
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-4E in the client Retail pack.**'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. Pulled to Wave
    2 (CF-101). Requisitions are Wave 1 and receipt was Wave 3 — **the end of the chain arriving two waves
    after the start.** **Cross-platform navigation removed 24 August**: EMP-005. **A till does not navigate
    to a back office and a guest app does not navigate to either** — those are device handovers, and a
    flow declares them with `crossesDevice` rather than a screen pretending there is a link.'
  apis:
  - operationId: createGoodsReceipt
    contract: inventory
    purpose: Receive goods against a purchase order
    trigger: onAction
  - operationId: listPurchaseOrders
    contract: inventory
    purpose: List purchase orders
    trigger: onLoad
  - operationId: acknowledgePurchaseOrder
    contract: inventory
    purpose: Record the supplier acknowledgement
    trigger: onAction
  - operationId: cancelPurchaseOrder
    contract: inventory
    purpose: Cancel a purchase order
    trigger: onAction
  - operationId: closePurchaseOrderShort
    contract: inventory
    purpose: Close an order accepting the balance will not arrive
    trigger: onAction
  - operationId: createPurchaseOrder
    contract: inventory
    purpose: Raise a purchase order
    trigger: onAction
  - operationId: getPurchaseOrder
    contract: inventory
    purpose: Read a purchase order with receipt progress
    trigger: onLoad
  - operationId: listGoodsReceipts
    contract: inventory
    purpose: List goods receipts
    trigger: onLoad
  - operationId: rejectReceivedGoods
    contract: inventory
    purpose: Reject received goods
    trigger: onAction
  - operationId: sendPurchaseOrder
    contract: inventory
    purpose: Issue the order to the supplier
    trigger: onAction
  - operationId: closeTransferShort
    contract: inventory
    purpose: Close a transfer accepting the balance will not arrive
    trigger: onAction
  - operationId: getStockTransfer
    contract: inventory
    purpose: One transfer, its manifest and where it is
    trigger: onLoad
  density: compact
  entryState:
    params:
    - name: purchaseOrderId
      from: deepLink
    - name: receiptId
      from: deepLink
    - name: transferId
      from: deepLink
      optional: true
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `purchaseOrderId`, `receiptId`, `transferId`.'
  requiresModule: inventory
  boardFrames:
  - Retail Board 4.dc.html#ret-4e
  statesDerived: true
- id: BO-053
  name: Staff Directory
  module: People & Access Rights
  purpose: Find anyone who works at this venue.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listPrincipals
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createPrincipal
        label: Create principal
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createPrincipal
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `USER_MANAGE` to manage users; a person without it sees the reason and who to ask, because
      an empty table reads as *there is no data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/staff-directory
    component: apps/venue-management-web/src/routes/venue-operations/StaffDirectoryDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Marketing Board 1.dc.html#crm-1b
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-053
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Drawn 31 August**
    — `Marketing Board 1.dc.html` frame `crm-1b` (*Guest Directory*), matched on title at 0.80 within
    this board’s platforms.
  apis:
  - operationId: listPrincipals
    contract: identity
    purpose: List principals
    trigger: onLoad
  - operationId: getPrincipal
    contract: identity
    purpose: Read a principal
    trigger: onLoad
  - operationId: createPrincipal
    contract: identity
    purpose: Create a principal
    trigger: onAction
  - operationId: updatePrincipal
    contract: identity
    purpose: Update or deactivate a principal
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: principalId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: core
  statesDerived: true
  boardFrames:
  - Marketing Board 1.dc.html#crm-1b
- id: BO-054
  name: Role Assignment
  module: People & Access Rights
  purpose: Give somebody the permissions their job needs.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listRoles
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createRole
        label: Create role
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createRole
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ROLE_MANAGE` to manage roles; a person without it sees the reason and who to ask, because
      an empty table reads as *there is no data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/role-assignment
    component: apps/venue-management-web/src/routes/venue-operations/RoleAssignmentDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-054
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listRoles
    contract: identity
    purpose: List roles
    trigger: onLoad
  - operationId: createRole
    contract: identity
    purpose: Create a role
    trigger: onAction
  density: compact
  requiresModule: core
  statesDerived: true
- id: BO-055
  name: Rota & Scheduling
  module: People & Access Rights
  purpose: Decide who is on which gate, when.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listRotaAssignments
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createRotaAssignment
        label: Create rota assignment
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createRotaAssignment
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `WORKFORCE_MANAGE`, `WORKFORCE_VIEW` to manage workforces; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/rota-scheduling
    component: apps/venue-management-web/src/routes/venue-operations/RotaSchedulingDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-055
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listRotaAssignments
    contract: workforce
    purpose: The rota
    trigger: onLoad
  - operationId: createRotaAssignment
    contract: workforce
    purpose: Put someone on the rota
    trigger: onAction
  - operationId: updateRotaAssignment
    contract: workforce
    purpose: Move or cancel an assignment
    trigger: onAction
  - operationId: requestShiftSwap
    contract: workforce
    purpose: Ask someone to take your shift
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: assignmentId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `assignmentId`.'
  requiresModule: core
  statesDerived: true
- id: BO-056
  name: Time & Attendance
  module: People & Access Rights
  purpose: Record who actually turned up.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listAttendance
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: amendAttendance
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: amendAttendance
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ATTENDANCE_RECORD`, `WORKFORCE_MANAGE`, `WORKFORCE_VIEW` to use attendances; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/time-attendance
    component: apps/venue-management-web/src/routes/venue-operations/TimeAttendanceDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-056
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listAttendance
    contract: workforce
    purpose: Who was here
    trigger: onLoad
  - operationId: amendAttendance
    contract: workforce
    purpose: A supervisor corrects a record
    trigger: onAction
  - operationId: recordAttendance
    contract: workforce
    purpose: Clock in, clock out, or take a break
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: recordId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `recordId`.'
  requiresModule: core
  statesDerived: true
- id: BO-057
  name: Training & Certification
  module: People & Access Rights
  purpose: Know who is allowed to do what.
  capability: C00
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listPrincipals
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
  states:
    loading: Detail loads
    emptyFirstRun: Not found — it may have been deleted or moved out of scope
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `USER_MANAGE` to manage users; a person without it sees the reason and who to ask, because
      an empty table reads as *there is no data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/training-certification
    component: apps/venue-management-web/src/routes/venue-operations/TrainingCertificationDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-057
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listPrincipals
    contract: identity
    purpose: List principals
    trigger: onLoad
  density: compact
  requiresModule: core
  statesDerived: true
- id: BO-058
  name: Reporting Home
  module: Venue Operations
  purpose: Find the report rather than build it.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listReports
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: runReport
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: deleteReport
        label: Delete report
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: runReport
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `REPORT_MANAGE`, `REPORT_SCHEDULE`, `REPORT_VIEW_VENUE` to manage reports; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `analytics` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    fromFlows: true
    entryFrom:
    - BO-108
  implementation:
    app: venue-management-web
    route: /venue-operations/reporting-home
    component: apps/venue-management-web/src/routes/venue-operations/ReportingHomeDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 6.dc.html#ret-6h
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-6H in the client Retail pack.**'
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Retail board
    operations wired 24 August.** **Cross-platform navigation removed 24 August**: EMP-020. **A till does
    not navigate to a back office and a guest app does not navigate to either** — those are device handovers,
    and a flow declares them with `crossesDevice` rather than a screen pretending there is a link.'
  apis:
  - operationId: listReports
    contract: reporting
    purpose: List available report definitions
    trigger: onLoad
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  - operationId: askReportingQuestion
    contract: reporting
    purpose: Natural-language reporting query
    trigger: onAction
  - operationId: createReport
    contract: reporting
    purpose: Create a custom report definition
    trigger: onAction
  - operationId: deleteReport
    contract: reporting
    purpose: Retire a report definition
    trigger: onAction
  - operationId: getFinancialReport
    contract: finance
    purpose: P&L, balance sheet or cash flow
    trigger: onLoad
  - operationId: getReport
    contract: reporting
    purpose: Read a report definition
    trigger: onLoad
  - operationId: saveNaturalLanguageQuery
    contract: reporting
    purpose: Save a natural-language answer as a report definition
    trigger: onAction
  - operationId: updateReport
    contract: reporting
    purpose: Publish a new version of a definition
    trigger: onAction
  - operationId: createReportSchedule
    contract: reporting
    purpose: Schedule a report
    trigger: onAction
  - operationId: listReportExecutions
    contract: reporting
    purpose: List executions
    trigger: onLoad
  density: compact
  entryState:
    params:
    - name: conversationId
      from: deepLink
    - name: reportId
      from: deepLink
    coldEntry: A conversation link an agent opens from a notification. Resolves, or says it was closed
      and by whom.
  requiresModule: analytics
  boardFrames:
  - Retail Board 6.dc.html#ret-6h
  statesDerived: true
- id: BO-059
  name: Sales Reports
  module: Orders & Money
  purpose: See what sold, through which channel.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listReports
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createReport
        label: Create report
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: deleteReport
        label: Delete report
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createReport
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `REPORT_MANAGE`, `REPORT_VIEW_VENUE` to manage reports; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `analytics` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/sales-reports
    component: apps/venue-management-web/src/routes/venue-operations/SalesReportsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-059
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: getFinancialReport
    contract: finance
    purpose: P&L, balance sheet or cash flow
    trigger: onLoad
  - operationId: listReports
    contract: reporting
    purpose: List available report definitions
    trigger: onLoad
  - operationId: createReport
    contract: reporting
    purpose: Create a custom report definition
    trigger: onAction
  - operationId: askReportingQuestion
    contract: reporting
    purpose: Natural-language reporting query
    trigger: onAction
  - operationId: deleteReport
    contract: reporting
    purpose: Retire a report definition
    trigger: onAction
  - operationId: getReport
    contract: reporting
    purpose: Read a report definition
    trigger: onLoad
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  - operationId: saveNaturalLanguageQuery
    contract: reporting
    purpose: Save a natural-language answer as a report definition
    trigger: onAction
  - operationId: updateReport
    contract: reporting
    purpose: Publish a new version of a definition
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: conversationId
      from: deepLink
    - name: reportId
      from: deepLink
    coldEntry: A conversation link an agent opens from a notification. Resolves, or says it was closed
      and by whom.
  requiresModule: analytics
  statesDerived: true
- id: BO-060
  name: Attendance & Footfall
  module: Venue Operations
  purpose: See how many people actually came in.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: primaryButton
        derived: true
        impliedBy: runReport
        notes: The act the screen exists for.
      - kind: scanTarget
        derived: true
        impliedBy: listScans
        notes: '**A screen that validates a credential needs somewhere to point the camera.** `denied`
          and `hardwareError` look different because an operator facing a guest needs to know whether
          to try again or explain something.'
      - kind: destructiveButton
        derived: true
        impliedBy: deleteReport
        label: Delete report
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: dataTable
        derived: true
        impliedBy: listReports
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: searchField
        derived: true
        impliedBy: lookupTicket
        label: Search
        notes: A search that returns nothing must say so differently from a search not yet run.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: runReport
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCESS_OVERRIDE`, `ACCESS_VALIDATE`, `REPORT_MANAGE` and 2 more to use accesses; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `analytics` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-108
  implementation:
    app: venue-management-web
    route: /venue-operations/attendance-footfall
    component: apps/venue-management-web/src/routes/venue-operations/AttendanceFootfallDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-060
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  - operationId: listScans
    contract: access
    purpose: List scan events
    trigger: onLoad
  - operationId: askReportingQuestion
    contract: reporting
    purpose: Natural-language reporting query
    trigger: onAction
  - operationId: createReport
    contract: reporting
    purpose: Create a custom report definition
    trigger: onAction
  - operationId: deleteReport
    contract: reporting
    purpose: Retire a report definition
    trigger: onAction
  - operationId: getFinancialReport
    contract: finance
    purpose: P&L, balance sheet or cash flow
    trigger: onLoad
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: getReport
    contract: reporting
    purpose: Read a report definition
    trigger: onLoad
  - operationId: listReports
    contract: reporting
    purpose: List available report definitions
    trigger: onLoad
  - operationId: lookupTicket
    contract: access
    purpose: Read-only validity check without admitting
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: saveNaturalLanguageQuery
    contract: reporting
    purpose: Save a natural-language answer as a report definition
    trigger: onAction
  - operationId: syncScans
    contract: access
    purpose: Replay scans recorded offline
    trigger: onAction
  - operationId: updateReport
    contract: reporting
    purpose: Publish a new version of a definition
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  - operationId: validateGroupAccess
    contract: access
    purpose: Admit a group on one read
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: conversationId
      from: deepLink
    - name: reportId
      from: deepLink
    coldEntry: A conversation link an agent opens from a notification. Resolves, or says it was closed
      and by whom.
  requiresModule: analytics
  statesDerived: true
- id: BO-061
  name: Scheduled Reports
  module: Orders & Money
  purpose: Send a report to somebody without them asking.
  capability: C00
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listReports
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createReport
        label: Create report
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: deleteReport
        label: Delete report
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createReport
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `REPORT_MANAGE`, `REPORT_VIEW_VENUE` to manage reports; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `analytics` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/scheduled-reports
    component: apps/venue-management-web/src/routes/venue-operations/ScheduledReportsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-061
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: getFinancialReport
    contract: finance
    purpose: P&L, balance sheet or cash flow
    trigger: onLoad
  - operationId: listReports
    contract: reporting
    purpose: List available report definitions
    trigger: onLoad
  - operationId: createReport
    contract: reporting
    purpose: Create a custom report definition
    trigger: onAction
  - operationId: askReportingQuestion
    contract: reporting
    purpose: Natural-language reporting query
    trigger: onAction
  - operationId: deleteReport
    contract: reporting
    purpose: Retire a report definition
    trigger: onAction
  - operationId: getReport
    contract: reporting
    purpose: Read a report definition
    trigger: onLoad
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  - operationId: saveNaturalLanguageQuery
    contract: reporting
    purpose: Save a natural-language answer as a report definition
    trigger: onAction
  - operationId: updateReport
    contract: reporting
    purpose: Publish a new version of a definition
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: conversationId
      from: deepLink
    - name: reportId
      from: deepLink
    coldEntry: A conversation link an agent opens from a notification. Resolves, or says it was closed
      and by whom.
  requiresModule: analytics
  statesDerived: true
- id: BO-062
  name: Venue Profile
  module: Orders & Money
  purpose: The facts every other surface reads.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listDiningOutlets
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: setRefundPolicy
        label: Save refund policy
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: setRefundPolicy
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_VIEW`, `REGION_CONFIGURE` to read orders; a person without it sees the reason and who
      to ask, because an empty table reads as *there is no data* and sends somebody to support with the
      wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/venue-profile
    component: apps/venue-management-web/src/routes/venue-operations/VenueProfileDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-062
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: getRefundPolicy
    contract: orders
    purpose: Read a venue's refund policy
    trigger: onLoad
  - operationId: listDiningOutlets
    contract: fnb
    purpose: Where a guest can eat, right now
    trigger: onLoad
  - operationId: setRefundPolicy
    contract: orders
    purpose: Set a venue's refund policy
    trigger: onAction
  - operationId: listDeliveryLocations
    contract: fnb
    purpose: Where an order can be delivered
    trigger: onLoad
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: ticketing
  statesDerived: true
- id: BO-063
  name: Opening Hours & Calendar
  module: Sell
  purpose: Say when the venue is open, including the exceptions.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listPerformances
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createPerformances
        label: Create performances
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: cancelPerformance
        label: Cancel performance
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createPerformances
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `EVENT_CONFIGURE`, `PERFORMANCE_CONFIGURE`, `PRODUCT_VIEW` and 2 more to configure events;
      a person without it sees the reason and who to ask, because an empty table reads as *there is no
      data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-102
  implementation:
    app: venue-management-web
    route: /venue-operations/opening-hours-calendar
    component: apps/venue-management-web/src/routes/venue-operations/OpeningHoursCalendarDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Board 1.dc.html#seat-1c
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-063
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Improved 20
    August against the client design board**, answering 2 board screen(s): Operating Hours & Service Periods;
    Operating Hours & Sales Periods. **The id, flows and navigation are unchanged** — a board specifies
    a screen further; it does not replace it. **Drawn 26 August** — `Seat Board 1.dc.html` frame `seat-1c`.
    **The frame names this screen on its own face**, which is the first pack to do that: the earlier F&B,
    POS and Retail boards had to be hand-assigned by purpose after three derivation attempts produced
    nonsense. **A board that says what it draws removes the guess entirely.**'
  apis:
  - operationId: listPerformances
    contract: catalogue
    purpose: List performances of an event
    trigger: onLoad
  - operationId: getPerformance
    contract: catalogue
    purpose: Read a performance
    trigger: onLoad
  - operationId: createPerformances
    contract: catalogue
    purpose: Create performances, singly or by schedule
    trigger: onAction
  - operationId: cancelPerformance
    contract: catalogue
    purpose: Cancel a performance
    trigger: onAction
  - operationId: createEvent
    contract: catalogue
    purpose: Create an event
    trigger: onAction
  - operationId: getEvent
    contract: catalogue
    purpose: Read an event
    trigger: onLoad
  - operationId: getSeatAvailability
    contract: seating
    purpose: Seat status for a performance
    trigger: onLoad
  - operationId: listEvents
    contract: catalogue
    purpose: List events
    trigger: onLoad
  - operationId: recommendSeats
    contract: seating
    purpose: Recommend seats for a party
    trigger: onAction
  - operationId: updateEvent
    contract: catalogue
    purpose: Amend an event
    trigger: onAction
  - operationId: updatePerformance
    contract: catalogue
    purpose: Amend a performance
    trigger: onAction
  - operationId: getVenueSettings
    contract: tenancy
    purpose: Operational settings for this venue
    trigger: onLoad
  - operationId: updateOutlet
    contract: tenancy
    purpose: Amend an outlet
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    - name: eventId
      from: deepLink
    - name: outletId
      from: deepLink
    - name: performanceId
      from: deepLink
    coldEntry: An outlet opened from the directory.
  provenance: client board, specified
  requiresModule: ticketing
  statesDerived: true
  boardFrames:
  - Seat Board 1.dc.html#seat-1c
- id: BO-064
  name: Zones & Areas
  module: Venue Operations
  purpose: Divide the venue into the things gates control.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listOrgUnits
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createAccessPoint
        label: Create access point
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createAccessPoint
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCESS_POINT_CONFIGURE`, `SCOPE_MANAGE`, `SCOPE_VIEW` and 1 more to configure accesses; a
      person without it sees the reason and who to ask, because an empty table reads as *there is no data*
      and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `access` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-108
  implementation:
    app: venue-management-web
    route: /venue-operations/zones-areas
    component: apps/venue-management-web/src/routes/venue-operations/ZonesAreasDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-064
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listOrgUnits
    contract: tenancy
    purpose: List scope nodes visible to the session
    trigger: onLoad
  - operationId: listAccessPoints
    contract: access
    purpose: List access points
    trigger: onLoad
  - operationId: createAccessPoint
    contract: access
    purpose: Create an access point
    trigger: onAction
  - operationId: createOrgUnit
    contract: tenancy
    purpose: Create a scope node
    trigger: onAction
  - operationId: getAccessPoint
    contract: access
    purpose: Read an access point
    trigger: onLoad
  - operationId: getOrgUnit
    contract: tenancy
    purpose: Read a scope node
    trigger: onLoad
  - operationId: setAccessPointGeofence
    contract: access
    purpose: Set a geofence for handheld validation
    trigger: onAction
  - operationId: setTurnstileMode
    contract: access
    purpose: Set the operating mode of an access point
    trigger: onAction
  - operationId: updateAccessPoint
    contract: access
    purpose: Update an access point
    trigger: onAction
  - operationId: updateOrgUnit
    contract: tenancy
    purpose: Rename or deactivate a scope node
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: accessPointId
      from: deepLink
    - name: orgUnitId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `accessPointId`, `orgUnitId`.'
  requiresModule: access
  statesDerived: true
- id: BO-065
  name: Venue Configuration
  module: Orders & Money
  purpose: Set the venue-level values everything inherits from.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listDiningOutlets
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: setRefundPolicy
        label: Save refund policy
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: setRefundPolicy
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_VIEW`, `REGION_CONFIGURE` to read orders; a person without it sees the reason and who
      to ask, because an empty table reads as *there is no data* and sends somebody to support with the
      wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/venue-configuration
    component: apps/venue-management-web/src/routes/venue-operations/VenueConfigurationDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-065
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: getRefundPolicy
    contract: orders
    purpose: Read a venue's refund policy
    trigger: onLoad
  - operationId: listDiningOutlets
    contract: fnb
    purpose: Where a guest can eat, right now
    trigger: onLoad
  - operationId: setRefundPolicy
    contract: orders
    purpose: Set a venue's refund policy
    trigger: onAction
  - operationId: listDeliveryLocations
    contract: fnb
    purpose: Where an order can be delivered
    trigger: onLoad
  - operationId: setVenueSettings
    contract: tenancy
    purpose: Save the venue's configuration
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: ticketing
  statesDerived: true
- id: BO-066
  name: Notification Settings
  module: People & Access Rights
  purpose: Decide what this venue tells people, and how.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listAnnouncements
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: publishAnnouncement
        label: Publish announcement
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: publishAnnouncement
      - kind: publishGate
        impliedBy: publishAnnouncement
        notes: >
          Declares `publishAnnouncement`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  states:
    loading: Current values, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited value is shown and labelled as inherited
      (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ANNOUNCEMENT_PUBLISH`, `WORKFORCE_VIEW` to use announcements; a person without it sees the
      reason and who to ask, because an empty table reads as *there is no data* and sends somebody to
      support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**Could not load. Saving is blocked**, because a value saved over an unread one silently overwrites
      what someone else set'
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/notification-settings
    component: apps/venue-management-web/src/routes/venue-operations/NotificationSettingsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-066
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listAnnouncements
    contract: workforce
    purpose: What staff have been told
    trigger: onLoad
  - operationId: publishAnnouncement
    contract: workforce
    purpose: Tell staff something
    trigger: onAction
  - operationId: acknowledgeAnnouncement
    contract: workforce
    purpose: Confirm you have read it
    trigger: onAction
  - operationId: getAnnouncementReach
    contract: workforce
    purpose: Who has acknowledged, and who has not
    trigger: onLoad
  density: compact
  entryState:
    params:
    - name: announcementId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `announcementId`.'
  requiresModule: core
  statesDerived: true
- id: BO-067
  name: Integrations
  module: Venue Operations
  purpose: Connect the venue to the systems around it.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Detail loads
    emptyFirstRun: Not found — it may have been deleted or moved out of scope
    error: Could not load
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
    entryFrom:
    - BO-108
  implementation:
    app: venue-management-web
    route: /venue-operations/integrations
    component: apps/venue-management-web/src/routes/venue-operations/IntegrationsDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Platform Board 13.dc.html#seatp-13d
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-067
  notes: 'Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Drawn 31 August**
    — `Seat Platform Board 13.dc.html` frame `seatp-13d`. **Matched on frame title against screen name,
    constrained to this board’s platforms.** These packs label by board position (`GM-6C`) rather than
    naming the screen, so the title is the only join — *Integrations* matched at 1.0. **A cross-platform
    title match was refused**: `Outlet Management` scored 0.85 against a partner-portal screen, which
    is how a mapping goes wrong quietly.'
  density: compact
  requiresModule: core
  boardFrames:
  - Seat Platform Board 13.dc.html#seatp-13d
  apis:
  - operationId: listApiClients
    contract: public-api
    purpose: API clients this tenant has issued
    trigger: onLoad
  - operationId: listWebhookSubscriptions
    contract: public-api
    purpose: Subscriptions and their targets
    trigger: onLoad
  - operationId: listWebhookDeliveries
    contract: public-api
    purpose: Deliveries for one subscription, and which failed
    trigger: onLoad
  - operationId: createApiClient
    contract: public-api
    purpose: Issue a client
    trigger: onAction
  - operationId: createWebhookSubscription
    contract: public-api
    purpose: Subscribe an endpoint
    trigger: onAction
  entryState:
    params:
    - name: subscriptionId
      from: navigation
    coldEntry: '**Deliveries belong to a subscription**, so the list is reached from the subscription
      that owns them. Opened without one the screen shows the subscriptions and says so — never an empty
      delivery log, which reads as *nothing failed*.'
- id: BO-068
  name: Audit Log
  module: Guests & Marketing
  purpose: See who changed what at this venue.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listAuditRecords
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: resolvePermissions
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: resolvePermissions
  states:
    loading: Detail loads
    emptyFirstRun: Not found — it may have been deleted or moved out of scope
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `AI_AUDIT_VIEW`, `PERMISSION_VIEW` to read ais; a person without it sees the reason and who
      to ask, because an empty table reads as *there is no data* and sends somebody to support with the
      wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/audit-log
    component: apps/venue-management-web/src/routes/venue-operations/AuditLogDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 6.dc.html#ret-6j
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-6J in the client Retail pack.**'
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Carried `listAiInteractions`
    alone** — an audit log showing only AI prompts. The platform audit record is `identity.audit_event`.
    **Rewired 20 August.** **`listAuditRecords` wired 24 August.** This screen declared zero operations
    — an audit log with nothing behind it — and `platform.audit_record` was written by nothing and read
    by nothing. **Found by mapping the POS pack**, where four separate frames wanted an audit view. **Retail
    board operations wired 24 August.**
  apis:
  - operationId: listAuditRecords
    contract: tenancy
    purpose: Who did what, where, and when
    trigger: onLoad
  - operationId: resolvePermissions
    contract: identity
    purpose: Simulate a principal's effective permissions
    trigger: onAction
  density: compact
  requiresModule: core
  boardFrames:
  - Retail Board 6.dc.html#ret-6j
  statesDerived: true
- id: BO-069
  name: Asset Register
  module: Access & Venue
  purpose: Know what equipment exists and where.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listAssets
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createAsset
        label: Create asset
        notes: The act the screen exists for.
      - kind: searchField
        derived: true
        impliedBy: lookupAsset
        label: Search
        notes: A search that returns nothing must say so differently from a search not yet run.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createAsset
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ASSET_MANAGE`, `ASSET_VIEW`, `PRODUCT_CONFIGURE` and 1 more to manage assets; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `maintenance` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/asset-register
    component: apps/venue-management-web/src/routes/venue-operations/AssetRegisterDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-069
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listAssets
    contract: maintenance
    purpose: List assets
    trigger: onLoad
  - operationId: getAsset
    contract: maintenance
    purpose: Read an asset with history and documents
    trigger: onLoad
  - operationId: createAsset
    contract: maintenance
    purpose: Register an asset
    trigger: onAction
  - operationId: getAssetHistory
    contract: maintenance
    purpose: Service history
    trigger: onLoad
  - operationId: lookupAsset
    contract: maintenance
    purpose: Find an asset by tag or QR
    trigger: onLoad
  - operationId: setAssetStatus
    contract: maintenance
    purpose: Take an asset out of service or return it
    trigger: onAction
  - operationId: updateAsset
    contract: maintenance
    purpose: Amend an asset
    trigger: onAction
  - operationId: listGames
    contract: games
    purpose: List games
    trigger: onLoad
  - operationId: updateGame
    contract: games
    purpose: Amend a game
    trigger: onAction
  - operationId: recordGamePlay
    contract: games
    purpose: Record a play
    trigger: onAction
  - operationId: syncGamePlays
    contract: games
    purpose: Replay plays recorded offline
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: assetId
      from: deepLink
    - name: gameId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `assetId`, `gameId`.'
  requiresModule: maintenance
  statesDerived: true
- id: BO-070
  name: Work Orders
  module: Orders & Money
  purpose: Get something fixed, and know it was.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listOrders
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createOrder
        label: Create order
        notes: The act the screen exists for.
      - kind: destructiveButton
        derived: true
        impliedBy: voidOrder
        label: Void order
        notes: '**Always confirms, never the default focus.** The consequence goes in the body — *cancel
          3 orders worth AED 480* is a confirmation, *are you sure* is not.'
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createOrder
      - kind: confirmDialog
        derived: true
        label: Confirm
        notes: '**The consequence goes in the body, not the title.** *Cancel 3 orders worth AED 480* is
          a confirmation; *are you sure* is not — and a dialog that cannot name what it destroys is a
          dialog somebody dismisses.


          **Added 31 August.** `confirmDialog` and `modal` were both in the component library and used
          **zero times across 492 screens**, while `destructiveButton` was used 39 times and its own entry
          reads *always requires confirmation*.'
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_CREATE`, `ORDER_DISCOUNT`, `ORDER_EXCHANGE` and 5 more to create orders; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ticketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/work-orders
    component: apps/venue-management-web/src/routes/venue-operations/WorkOrdersDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-070
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **createRefund
    removed 18 August** — attached by module resemblance, not by what this screen does. A screen that
    does not handle money should not be able to move it (CF-87's class).
  apis:
  - operationId: listOrders
    contract: orders
    purpose: List orders
    trigger: onLoad
  - operationId: getOrder
    contract: orders
    purpose: Read an order
    trigger: onLoad
  - operationId: createOrder
    contract: orders
    purpose: Create an order
    trigger: onAction
  - operationId: applyManualDiscount
    contract: orders
    purpose: Apply a discount a cashier chose
    trigger: onAction
  - operationId: exchangeOrderLines
    contract: orders
    purpose: Exchange lines for different products or dates
    trigger: onAction
  - operationId: getOrderStatement
    contract: orders
    purpose: Full financial history of an order
    trigger: onLoad
  - operationId: holdOrder
    contract: orders
    purpose: Park a sale and free the till
    trigger: onAction
  - operationId: listOrderRefunds
    contract: orders
    purpose: List refunds against an order
    trigger: onLoad
  - operationId: modifyOrder
    contract: orders
    purpose: Add or remove lines on an existing order
    trigger: onAction
  - operationId: reprintOrder
    contract: orders
    purpose: Reprint or resend tickets
    trigger: onAction
  - operationId: rescheduleOrder
    contract: orders
    purpose: Move an order to another performance
    trigger: onAction
  - operationId: resumeOrder
    contract: orders
    purpose: Bring a parked sale back to a till
    trigger: onAction
  - operationId: voidOrder
    contract: orders
    purpose: Void an order
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: orderId
      from: deepLink
    coldEntry: '**A guest opening an order link weeks later.** Shows the order if it still resolves; if
      it was refunded or the performance passed, says which and offers the order list rather than an error.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-071
  name: Planned Maintenance
  module: Access & Venue
  purpose: Schedule the work that stops the emergencies.
  capability: C00
  wave: 3
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listMaintenancePlans
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createMaintenancePlan
        label: Create maintenance plan
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createMaintenancePlan
  states:
    loading: Detail loads
    emptyFirstRun: Not found — it may have been deleted or moved out of scope
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ASSET_MANAGE`, `ASSET_VIEW`, `ROLE_MANAGE` to manage assets; a person without it sees the
      reason and who to ask, because an empty table reads as *there is no data* and sends somebody to
      support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `maintenance` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/planned-maintenance
    component: apps/venue-management-web/src/routes/venue-operations/PlannedMaintenanceDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-071
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listMaintenancePlans
    contract: maintenance
    purpose: List planned maintenance schedules
    trigger: onLoad
  - operationId: createMaintenancePlan
    contract: maintenance
    purpose: Create a planned maintenance schedule
    trigger: onAction
  - operationId: getDueMaintenance
    contract: maintenance
    purpose: Planned tasks due or overdue
    trigger: onLoad
  - operationId: setRolePermissions
    contract: tenancy
    purpose: What this role may do
    trigger: onAction
  density: compact
  requiresModule: maintenance
  entryState:
    params:
    - name: roleId
      from: deepLink
    coldEntry: A role opened from the directory. **Permissions are set per role, not per person** — ADR-0002
      makes authorisation user-driven through roles.
  statesDerived: true
- id: BO-072
  name: Incident Log
  module: Access & Venue
  purpose: Record what happened, while it is fresh.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listIncidents
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: reportIncident
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: reportIncident
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: Nothing matches. Distinguishes an empty filter from an empty dataset
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `INCIDENT_MANAGE`, `INCIDENT_REPORT`, `INCIDENT_VIEW` to manage incidents; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `maintenance` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load . Actions on the list are disabled
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/incident-log
    component: apps/venue-management-web/src/routes/venue-operations/IncidentLogDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-072
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual.
  apis:
  - operationId: listIncidents
    contract: maintenance
    purpose: List incidents
    trigger: onLoad
  - operationId: getIncident
    contract: maintenance
    purpose: Read an incident
    trigger: onLoad
  - operationId: reportIncident
    contract: maintenance
    purpose: Report an incident
    trigger: onAction
  - operationId: recordAuthorityNotification
    contract: maintenance
    purpose: Record notification to an external authority
    trigger: onAction
  - operationId: updateIncident
    contract: maintenance
    purpose: Investigate, escalate or close an incident
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: incidentId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `incidentId`.'
  requiresModule: maintenance
  statesDerived: true
- id: BO-073
  name: Lost & Found Register
  module: Guests & Marketing
  purpose: Match what was lost to what was handed in.
  capability: C00
  wave: 2
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
      - kind: dataTable
        derived: true
        impliedBy: listLostItems
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: matchLostItem
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: matchLostItem
  states:
    loading: Detail loads
    emptyFirstRun: Not found — it may have been deleted or moved out of scope
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `CASE_MANAGE`, `CASE_VIEW` to manage cases; a person without it sees the reason and who to
      ask, because an empty table reads as *there is no data* and sends somebody to support with the wrong
      question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `marketing` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  navigation:
    exitTo:
    - BO-001
    - BO-007
    - BO-008
    - BO-009
    inferred: true
  implementation:
    app: venue-management-web
    route: /venue-operations/lost-found-register
    component: apps/venue-management-web/src/routes/venue-operations/LostFoundRegisterDetail.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-073
  notes: Definition derived from the wireframe board on 14 August. CF-53 — 67 of these 73 had no definition
    at all. States derived from the screen pattern on 17 August, not individually considered — sound for
    a list, a form or a money screen, and worth revisiting where this screen is unusual. **Carried `listCases`
    and `createCase`.** `LostItem` exists — CL-07 built it as one entity in two directions, lost and found
    — and a service case is a different thing. **Rewired 20 August.**
  apis:
  - operationId: listLostItems
    contract: marketing-crm
    purpose: Reported and found, with suggested matches
    trigger: onLoad
  - operationId: matchLostItem
    contract: marketing-crm
    purpose: Tie a report to a found item, or hand it back
    trigger: onAction
  density: compact
  entryState:
    params:
    - name: venueId
      from: session
    - name: itemId
      from: deepLink
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: marketing
  statesDerived: true
- id: BO-074
  name: Chart of Accounts
  module: Orders & Money
  purpose: Accounts, cost centres and legal entities.
  capability: C00
  wave: 1
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listAccounts
    contract: finance
    purpose: List the chart of accounts
    trigger: onLoad
  - operationId: getAccount
    contract: finance
    purpose: Read an account
    trigger: onLoad
  - operationId: createAccount
    contract: finance
    purpose: Create an account
    trigger: onAction
  - operationId: updateAccount
    contract: finance
    purpose: Rename, remap or deactivate an account
    trigger: onAction
  - operationId: listCostCenters
    contract: finance
    purpose: List cost centres
    trigger: onLoad
  - operationId: createCostCenter
    contract: finance
    purpose: Create a cost centre
    trigger: onAction
  - operationId: listLegalEntities
    contract: finance
    purpose: List legal entities
    trigger: onLoad
  - operationId: createLegalEntity
    contract: finance
    purpose: Create a legal entity
    trigger: onAction
  states:
    loading: Accounts load as a tree
    emptyFirstRun: No chart configured — a blocking gap before the first posting, shown as one
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCOUNT_CONFIGURE`, `LEDGER_VIEW` to configure accounts; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load. Editing is blocked
  navigation:
    exitTo:
    - BO-001
    - BO-075
    - BO-076
    - BO-077
    inferred: true
    fromFlows: true
  implementation:
    app: venue-management-web
    route: /finance/chart-of-accounts
    component: apps/venue-management-web/src/routes/finance/ChartOfAccounts.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-074
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing. Pulled to Wave 1 (CF-101). **F16 says it itself** — no chart of accounts
    blocks the first sale, so it cannot be Wave 2 while venue provisioning is Wave 1.
  density: compact
  entryState:
    params:
    - name: accountId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `accountId`.'
  requiresModule: core
  statesDerived: true
- id: BO-075
  name: Account Mapping
  module: Orders & Money
  purpose: Which product or movement posts to which account.
  capability: C00
  wave: 1
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listAccountMappings
    contract: finance
    purpose: Which account each transaction type posts to
    trigger: onLoad
  - operationId: setAccountMappings
    contract: finance
    purpose: Set posting mappings
    trigger: onAction
  - operationId: listTaxCodes
    contract: finance
    purpose: List tax codes
    trigger: onLoad
  - operationId: createTaxCode
    contract: finance
    purpose: Create a tax code
    trigger: onAction
  - operationId: updateTaxCode
    contract: finance
    purpose: Amend a tax code
    trigger: onAction
  - operationId: listTaxExemptions
    contract: finance
    purpose: List tax exemptions
    trigger: onLoad
  - operationId: createTaxExemption
    contract: finance
    purpose: Grant a tax exemption
    trigger: onAction
  states:
    loading: Mappings with the level each resolves from
    emptyFirstRun: Nothing mapped — **every sale would post to a suspense account**, so this is a blocking
      gap
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCOUNT_CONFIGURE`, `LEDGER_VIEW`, `TAX_CONFIGURE` to configure accounts; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load. Saving is blocked
  navigation:
    exitTo:
    - BO-001
    - BO-074
    - BO-076
    - BO-077
    inferred: true
    fromFlows: true
  implementation:
    app: venue-management-web
    route: /finance/account-mapping
    component: apps/venue-management-web/src/routes/finance/AccountMapping.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-075
  notes: 'Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing. Pulled to Wave 1 (CF-101). Without a mapping every sale posts to suspense.
    **Cross-platform navigation removed 24 August**: ADM-020. **A till does not navigate to a back office
    and a guest app does not navigate to either** — those are device handovers, and a flow declares them
    with `crossesDevice` rather than a screen pretending there is a link.'
  density: compact
  entryState:
    params:
    - name: taxCodeId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `taxCodeId`.'
  requiresModule: core
  statesDerived: true
- id: BO-076
  name: Revenue Recognition
  module: Orders & Money
  purpose: Deferred revenue, its ageing, and the schedules that release it.
  capability: C00
  wave: 2
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: getDeferredRevenue
    contract: finance
    purpose: Deferred revenue balance and ageing
    trigger: onLoad
  - operationId: runRecognition
    contract: finance
    purpose: Recognise earned revenue for a period
    trigger: onAction
  - operationId: listRecognitionSchedules
    contract: finance
    purpose: List revenue recognition schedules
    trigger: onLoad
  - operationId: createRecognitionSchedule
    contract: finance
    purpose: Define how a product class recognises revenue
    trigger: onAction
  states:
    loading: Balance and ageing
    emptyFirstRun: Nothing deferred
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ACCOUNT_CONFIGURE`, `LEDGER_POST`, `LEDGER_VIEW` to configure accounts; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load. **Running recognition is blocked** — a run against an unread balance posts
      numbers nobody checked
  navigation:
    exitTo:
    - BO-001
    - BO-074
    - BO-075
    - BO-077
    inferred: true
  implementation:
    app: venue-management-web
    route: /finance/revenue-recognition
    component: apps/venue-management-web/src/routes/finance/RevenueRecognition.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-076
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing.
  density: compact
  requiresModule: core
  statesDerived: true
- id: BO-077
  name: FX Rates & Variances
  module: Orders & Money
  purpose: Rates, and the price variances waiting for review.
  capability: C00
  wave: 2
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listFxRates
    contract: finance
    purpose: The rates in force
    trigger: onLoad
  - operationId: setFxRate
    contract: finance
    purpose: Set a rate
    trigger: onAction
  - operationId: listPriceVariances
    contract: finance
    purpose: List price variances
    trigger: onLoad
  - operationId: reviewPriceVariance
    contract: finance
    purpose: Record a review decision on an exception variance
    trigger: onAction
  states:
    loading: Rates and open variances
    emptyFirstRun: No variances — reported as a result
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_APPROVE`, `LEDGER_VIEW` to approve ledgers; a person without it sees the reason and
      who to ask, because an empty table reads as *there is no data* and sends somebody to support with
      the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load. Setting a rate is blocked
  navigation:
    exitTo:
    - BO-001
    - BO-074
    - BO-075
    - BO-076
    inferred: true
  implementation:
    app: venue-management-web
    route: /finance/fx-rates-variances
    component: apps/venue-management-web/src/routes/finance/FxRatesVariances.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-077
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing.
  density: compact
  entryState:
    params:
    - name: varianceId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `varianceId`.'
  requiresModule: core
  statesDerived: true
- id: BO-078
  name: Requisitions
  module: Stock & Supply
  purpose: Raise, track and approve a request to buy something.
  capability: C00
  wave: 1
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listRequisitions
    contract: inventory
    purpose: List requisitions
    trigger: onLoad
  - operationId: createRequisition
    contract: inventory
    purpose: Raise a requisition
    trigger: onAction
  - operationId: approveRequisition
    contract: inventory
    purpose: Approve or reject a requisition
    trigger: onAction
  - operationId: rejectRequisition
    contract: inventory
    purpose: Reject a requisition
    trigger: onAction
  - operationId: returnRequisition
    contract: inventory
    purpose: Return a requisition for more information
    trigger: onAction
  - operationId: cancelRequisition
    contract: inventory
    purpose: Cancel a requisition
    trigger: onAction
  - operationId: getSuggestedRequisitions
    contract: inventory
    purpose: Draft requisitions from reorder points
    trigger: onLoad
  - operationId: compareQuotations
    contract: inventory
    purpose: Compare quotations for a requisition
    trigger: onLoad
  - operationId: listStockLocations
    contract: inventory
    purpose: List stock locations
    trigger: onLoad
  - operationId: updateRequisitionLines
    contract: inventory
    purpose: Change what an outlet is asking for, before it is approved
    trigger: onAction
  states:
    loading: Requisitions, oldest pending first
    emptyFirstRun: Nothing outstanding
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `APPROVAL_ACT`, `LEDGER_APPROVE`, `ORDER_CREATE` and 2 more to use approvals; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `inventory` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load. Approving is blocked
  navigation:
    exitTo:
    - BO-001
    - BO-079
    - BO-080
    - BO-081
    - BO-084
    inferred: true
    fromFlows: true
    entryFrom:
    - BO-105
  implementation:
    app: venue-management-web
    route: /inventory/requisitions
    component: apps/venue-management-web/src/routes/inventory/Requisitions.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 4.dc.html#ret-4c
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-4C, RET-4K in the client Retail pack.**'
  notes: 'Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing. **Improved 20 August against the client design board**, answering 2 board
    screen(s): Requisition & Smart Replenishment; Store Inventory & Replenishment Configuration. **The
    id, flows and navigation are unchanged** — a board specifies a screen further; it does not replace
    it. **Retail board operations wired 24 August.**'
  density: compact
  entryState:
    params:
    - name: requisitionId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `requisitionId`.'
  provenance: client board, specified
  requiresModule: inventory
  boardFrames:
  - FnB Board 5.dc.html#fnb-5f
  - Retail Board 4.dc.html#ret-4c
  - Retail Board 4.dc.html#ret-4k
  statesDerived: true
- id: BO-079
  name: Stock Count
  module: Stock & Supply
  purpose: Count what is there, blind, and resolve the variance.
  capability: C00
  wave: 1
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listStockCounts
    contract: inventory
    purpose: List stock counts
    trigger: onLoad
  - operationId: startStockCount
    contract: inventory
    purpose: Start a stock count
    trigger: onAction
  - operationId: postStockCount
    contract: inventory
    purpose: Post a count and adjust stock
    trigger: onAction
  - operationId: recountStockCount
    contract: inventory
    purpose: Send a count back to be recounted
    trigger: onAction
  - operationId: cancelStockCount
    contract: inventory
    purpose: Abandon a count
    trigger: onAction
  - operationId: getCountVariance
    contract: inventory
    purpose: Variance between counted and expected
    trigger: onLoad
  - operationId: enterCountLine
    contract: fnb
    purpose: What was actually on the shelf
    trigger: onAction
  - operationId: requestRecount
    contract: fnb
    purpose: Send a line back to be counted again
    trigger: onAction
  states:
    loading: Count sheet loads
    emptyFirstRun: No count in progress
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_APPROVE`, `LEDGER_POST`, `PRODUCT_CONFIGURE` and 1 more to approve ledgers; a person
      without it sees the reason and who to ask, because an empty table reads as *there is no data* and
      sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `inventory` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Could not load. Posting is blocked.** A count posted against an unread expected figure is
      not a count'
  navigation:
    exitTo:
    - BO-001
    - BO-049
    - BO-078
    - BO-080
    - BO-081
    inferred: true
    entryFrom:
    - BO-105
  implementation:
    app: venue-management-web
    route: /inventory/stock-count
    component: apps/venue-management-web/src/routes/inventory/StockCount.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 4.dc.html#ret-4f
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-4F in the client Retail pack.**'
  notes: 'Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing. **Improved 20 August against the client design board**, answering 1 board
    screen(s): Stock Count, Reconciliation & Variance. **The id, flows and navigation are unchanged**
    — a board specifies a screen further; it does not replace it. **Operations from the 24 August F&B
    build wired here** — the contract grew and the screens had not caught up, which is how 92 operations
    reached 49% of screens.'
  density: compact
  entryState:
    params:
    - name: countId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `countId`.'
  provenance: client board, specified
  requiresModule: inventory
  boardFrames:
  - FnB Board 5.dc.html#fnb-5h
  - Retail Board 4.dc.html#ret-4f
  statesDerived: true
- id: BO-080
  name: Stock Transfers
  module: Stock & Supply
  purpose: Move stock between venues, and receive what arrives.
  capability: C00
  wave: 2
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listStockTransfers
    contract: inventory
    purpose: List transfers
    trigger: onLoad
  - operationId: createStockTransfer
    contract: inventory
    purpose: Send stock to another location
    trigger: onAction
  - operationId: receiveStockTransfer
    contract: inventory
    purpose: Receive a transfer
    trigger: onAction
  - operationId: closeTransferShort
    contract: inventory
    purpose: Close a transfer accepting the balance will not arrive
    trigger: onAction
  - operationId: createGoodsReceipt
    contract: inventory
    purpose: Receive goods against a purchase order
    trigger: onAction
  - operationId: getStockTransfer
    contract: inventory
    purpose: One transfer, its manifest and where it is
    trigger: onLoad
  states:
    loading: Transfers in flight and recently received
    emptyFirstRun: Nothing in transit
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_APPROVE`, `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to approve ledgers; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `inventory` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load. Receipt is blocked
  navigation:
    exitTo:
    - BO-001
    - BO-078
    - BO-079
    - BO-081
    inferred: true
    entryFrom:
    - BO-105
  implementation:
    app: venue-management-web
    route: /inventory/stock-transfers
    component: apps/venue-management-web/src/routes/inventory/StockTransfers.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 4.dc.html#ret-4d
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-4D in the client Retail pack.**'
  notes: 'Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing. **Improved 20 August against the client design board**, answering 1 board
    screen(s): Transfers, Distribution & Outlet Receiving. **The id, flows and navigation are unchanged**
    — a board specifies a screen further; it does not replace it. **Retail board operations wired 24 August.**'
  density: compact
  entryState:
    params:
    - name: transferId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `transferId`.'
  provenance: client board, specified
  requiresModule: inventory
  boardFrames:
  - Retail Board 4.dc.html#ret-4d
  statesDerived: true
- id: BO-081
  name: Inventory Items
  module: Stock & Supply
  purpose: What the venue stocks, and where.
  capability: C00
  wave: 2
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listInventoryItems
    contract: inventory
    purpose: List inventory items
    trigger: onLoad
  - operationId: getInventoryItem
    contract: inventory
    purpose: Read an item with stock position
    trigger: onLoad
  - operationId: createInventoryItem
    contract: inventory
    purpose: Create an inventory item
    trigger: onAction
  - operationId: updateInventoryItem
    contract: inventory
    purpose: Amend an item
    trigger: onAction
  - operationId: lookupInventoryItem
    contract: inventory
    purpose: Look up by barcode or SKU
    trigger: onLoad
  - operationId: listStockLocations
    contract: inventory
    purpose: List stock locations
    trigger: onLoad
  - operationId: createStockLocation
    contract: inventory
    purpose: Create a stock location
    trigger: onAction
  states:
    loading: Items load, paged
    emptyFirstRun: Nothing stocked at this venue
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to configure products; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `inventory` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
  navigation:
    exitTo:
    - BO-001
    - BO-078
    - BO-079
    - BO-080
    inferred: true
    entryFrom:
    - BO-105
  implementation:
    app: venue-management-web
    route: /inventory/inventory-items
    component: apps/venue-management-web/src/routes/inventory/InventoryItems.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 4.dc.html#ret-4b
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-4B in the client Retail pack.**'
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing.
  density: compact
  entryState:
    params:
    - name: itemId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `itemId`.'
  requiresModule: inventory
  boardFrames:
  - Retail Board 4.dc.html#ret-4b
  statesDerived: true
- id: BO-082
  name: Stock Movements
  module: Stock & Supply
  purpose: Every movement, and why it happened.
  capability: C00
  wave: 1
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listStockMovements
    contract: inventory
    purpose: The movement ledger
    trigger: onLoad
  - operationId: createStockMovement
    contract: inventory
    purpose: Record an issue, return or adjustment
    trigger: onAction
  - operationId: createStockTransfer
    contract: inventory
    purpose: Send stock to another location
    trigger: onAction
  - operationId: listOrders
    contract: orders
    purpose: List orders
    trigger: onLoad
  states:
    loading: Movements, newest first
    emptyFirstRun: No movements in the period
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `ORDER_VIEW`, `PRODUCT_CONFIGURE`, `PRODUCT_VIEW` to read orders; a person without it sees
      the reason and who to ask, because an empty table reads as *there is no data* and sends somebody
      to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `inventory` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load. **Recording a movement is blocked** — a movement against an unknown level produces
      a wrong level
  navigation:
    exitTo:
    - BO-001
    - BO-078
    - BO-079
    - BO-080
    inferred: true
    entryFrom:
    - BO-105
  implementation:
    app: venue-management-web
    route: /inventory/stock-movements
    component: apps/venue-management-web/src/routes/inventory/StockMovements.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 4.dc.html#ret-4g
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-4G in the client Retail pack.**'
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing. **Moved to wave 1 on 24 August.** F34 walks a retail sale and its return,
    which is a wave-1 journey — **a venue that can take a return and cannot disposition the item puts
    damaged stock back on the shelf**, and the count finds it three weeks later.
  density: compact
  requiresModule: inventory
  boardFrames:
  - Retail Board 4.dc.html#ret-4g
  statesDerived: true
- id: BO-083
  name: Suppliers
  module: Stock & Supply
  purpose: Who we buy from, and what they quoted.
  capability: C00
  wave: 2
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listSuppliers
    contract: inventory
    purpose: List suppliers
    trigger: onLoad
  - operationId: createSupplier
    contract: inventory
    purpose: Create a supplier
    trigger: onAction
  - operationId: recordQuotation
    contract: inventory
    purpose: Record a supplier quotation
    trigger: onAction
  - operationId: updateSupplier
    contract: inventory
    purpose: Change terms, or stop buying from them
    trigger: onAction
  - operationId: getSupplierPerformance
    contract: reporting
    purpose: getSupplierPerformance
    trigger: onLoad
  states:
    loading: Suppliers load
    emptyFirstRun: No suppliers — a blocking gap before the first purchase order
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `PRODUCT_CONFIGURE`, `PRODUCT_VIEW`, `REPORT_VIEW` to configure products; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `inventory` is not licensed.** The tenant has not bought this module, which
      is a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load
  navigation:
    exitTo:
    - BO-001
    - BO-078
    - BO-079
    - BO-080
    inferred: true
    entryFrom:
    - BO-105
  implementation:
    app: venue-management-web
    route: /inventory/suppliers
    component: apps/venue-management-web/src/routes/inventory/Suppliers.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 2.dc.html#ret-2g
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-2G in the client Retail pack.**'
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing. **Retail board operations wired 24 August.**
  density: compact
  entryState:
    params:
    - name: supplierId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `supplierId`.'
  requiresModule: inventory
  boardFrames:
  - Retail Board 2.dc.html#ret-2g
  statesDerived: true
- id: BO-084
  name: Approval Inbox
  module: People & Access Rights
  purpose: What is waiting on this person, sorted by what breaches soonest.
  capability: C00
  wave: 1
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listApprovalRequests
    contract: approvals
    purpose: Requests awaiting a decision, or already decided
    trigger: onLoad
  - operationId: decideApprovalRequest
    contract: approvals
    purpose: Approve or reject
    trigger: onAction
  - operationId: escalateApprovalRequest
    contract: approvals
    purpose: Move it up a level
    trigger: onAction
  states:
    loading: Requests load, ordered by SLA proximity rather than age
    emptyFirstRun: Nothing waiting — reported as a result, because an empty inbox and a broken one look
      identical
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `APPROVAL_DECIDE`, `APPROVAL_REQUEST`, `APPROVAL_VIEW` to use approvals; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**Could not load. Deciding is blocked** — an approver must not act on a request they could
      not read'
  navigation:
    exitTo:
    - BO-001
    - BO-052
    - BO-085
    - BO-086
    - BO-087
    inferred: true
    fromFlows: true
  implementation:
    app: venue-management-web
    route: /approvals/approval-inbox
    component: apps/venue-management-web/src/routes/approvals/ApprovalInbox.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-084
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing.
  density: compact
  entryState:
    params:
    - name: requestId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `requestId`.'
  requiresModule: core
  statesDerived: true
- id: BO-085
  name: Approval Request
  module: People & Access Rights
  purpose: One request, its subject, and the decision.
  capability: C00
  wave: 1
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listApprovalRequests
    contract: approvals
    purpose: Requests awaiting a decision, or already decided
    trigger: onLoad
  - operationId: decideApprovalRequest
    contract: approvals
    purpose: Approve or reject
    trigger: onAction
  - operationId: resubmitApprovalRequest
    contract: approvals
    purpose: Amend a rejected request and try again
    trigger: onAction
  - operationId: withdrawApprovalRequest
    contract: approvals
    purpose: The requester takes it back
    trigger: onAction
  - operationId: evaluateApprovalRequirement
    contract: approvals
    purpose: Does this need approval, and from whom
    trigger: onAction
  states:
    loading: The request, then the subject fetched live from the owning contract
    emptyFirstRun: —
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `APPROVAL_DECIDE`, `APPROVAL_REQUEST`, `APPROVAL_VIEW` to use approvals; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**Subject unreadable. Deciding is blocked** — the request points at an order or a shift, and
      approving one you cannot see is approving a description'
  navigation:
    exitTo:
    - BO-001
    - BO-084
    - BO-086
    - BO-087
    inferred: true
    fromFlows: true
  implementation:
    app: venue-management-web
    route: /approvals/approval-request
    component: apps/venue-management-web/src/routes/approvals/ApprovalRequest.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-085
  notes: 'Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing. **Cross-platform navigation removed 24 August**: POS-005. **A till does not
    navigate to a back office and a guest app does not navigate to either** — those are device handovers,
    and a flow declares them with `crossesDevice` rather than a screen pretending there is a link.'
  density: compact
  entryState:
    params:
    - name: requestId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `requestId`.'
  requiresModule: core
  statesDerived: true
- id: BO-086
  name: Approval Matrix
  module: People & Access Rights
  purpose: What needs approval here, and who grants it.
  capability: C00
  wave: 2
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listApprovalMatrices
    contract: approvals
    purpose: What requires approval here
    trigger: onLoad
  - operationId: setApprovalMatrix
    contract: approvals
    purpose: Configure what requires approval
    trigger: onAction
  states:
    loading: Rules in order, with the level each resolves from
    emptyFirstRun: Nothing set at this level — the inherited matrix is shown and labelled (ADR-0018)
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `APPROVAL_CONFIGURE` to configure approvals; a person without it sees the reason and who to
      ask, because an empty table reads as *there is no data* and sends somebody to support with the wrong
      question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**Could not load. Saving is blocked** — a matrix saved over an unread one silently loosens
      a control'
  navigation:
    exitTo:
    - BO-001
    - BO-084
    - BO-085
    - BO-087
    inferred: true
  implementation:
    app: venue-management-web
    route: /approvals/approval-matrix
    component: apps/venue-management-web/src/routes/approvals/ApprovalMatrix.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-086
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing.
  density: compact
  requiresModule: core
  statesDerived: true
- id: BO-087
  name: Approval Delegations
  module: People & Access Rights
  purpose: Who is standing in for whom, and until when.
  capability: C00
  wave: 2
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listApprovalDelegations
    contract: approvals
    purpose: Who is standing in for whom
    trigger: onLoad
  - operationId: createApprovalDelegation
    contract: approvals
    purpose: Delegate approval authority
    trigger: onAction
  - operationId: revokeApprovalDelegation
    contract: approvals
    purpose: End a delegation early
    trigger: onAction
  states:
    loading: Active and scheduled delegations
    emptyFirstRun: Nobody delegating
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `APPROVAL_DECIDE`, `APPROVAL_VIEW` to use approvals; a person without it sees the reason and
      who to ask, because an empty table reads as *there is no data* and sends somebody to support with
      the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load. **Creating is blocked** — a delegation created blind could exceed the delegator’s
      own authority
  navigation:
    exitTo:
    - BO-001
    - BO-084
    - BO-085
    - BO-086
    inferred: true
  implementation:
    app: venue-management-web
    route: /approvals/approval-delegations
    component: apps/venue-management-web/src/routes/approvals/ApprovalDelegations.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-087
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing.
  density: compact
  entryState:
    params:
    - name: delegationId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `delegationId`.'
  requiresModule: core
  statesDerived: true
- id: BO-088
  name: Approval Analytics
  module: People & Access Rights
  purpose: Volumes, decision times and where requests get stuck.
  capability: C00
  wave: 3
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: getApprovalAnalytics
    contract: approvals
    purpose: Volumes, times, rejections and bottlenecks
    trigger: onLoad
  - operationId: listIndexJobs
    contract: ai
    purpose: Indexing in flight and recently finished
    trigger: onLoad
  states:
    loading: Aggregated from the analytical replica
    emptyFirstRun: No requests in the period
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `AI_CONFIGURE`, `APPROVAL_VIEW` to configure ais; a person without it sees the reason and
      who to ask, because an empty table reads as *there is no data* and sends somebody to support with
      the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: Could not load
  navigation:
    exitTo:
    - BO-001
    - BO-084
    - BO-085
    - BO-086
    inferred: true
  implementation:
    app: venue-management-web
    route: /approvals/approval-analytics
    component: apps/venue-management-web/src/routes/approvals/ApprovalAnalytics.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-088
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing. **Retail board operations wired 24 August.**
  density: compact
  requiresModule: core
  statesDerived: true
- id: BO-089
  name: Journal Entries
  module: Orders & Money
  purpose: Every posting, and the ones waiting for approval.
  capability: C00
  wave: 1
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listJournalEntries
    contract: finance
    purpose: List journal entries
    trigger: onLoad
  - operationId: getJournalEntry
    contract: finance
    purpose: Read a journal entry
    trigger: onLoad
  - operationId: createJournalEntry
    contract: finance
    purpose: Post a manual journal voucher
    trigger: onAction
  - operationId: approveJournalEntry
    contract: finance
    purpose: Approve a journal entry and post it
    trigger: onAction
  - operationId: rejectJournal
    contract: finance
    purpose: Reject a journal awaiting approval
    trigger: onAction
  - operationId: reverseJournalEntry
    contract: finance
    purpose: Reverse a posted entry
    trigger: onAction
  states:
    loading: Entries load, unposted first
    emptyFirstRun: Nothing in the period
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_APPROVE`, `LEDGER_POST`, `LEDGER_VIEW` to approve ledgers; a person without it sees
      the reason and who to ask, because an empty table reads as *there is no data* and sends somebody
      to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**Could not load. Posting and reversal are both blocked** — a journal is append-only and a
      blind posting cannot be taken back, only reversed'
  navigation:
    exitTo:
    - BO-001
    - BO-074
    - BO-075
    - BO-076
    - BO-090
    inferred: true
    fromFlows: true
  implementation:
    app: venue-management-web
    route: /finance/journal-entries
    component: apps/venue-management-web/src/routes/finance/JournalEntries.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-089
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing.
  density: compact
  entryState:
    params:
    - name: entryId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `entryId`.'
  requiresModule: core
  statesDerived: true
- id: BO-090
  name: Period Close
  module: Orders & Money
  purpose: Close a fiscal period, and see what is stopping it.
  capability: C00
  wave: 1
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
      - kind: detailPanel
  apis:
  - operationId: listFiscalPeriods
    contract: finance
    purpose: List fiscal periods
    trigger: onLoad
  - operationId: beginPeriodClose
    contract: finance
    purpose: Begin closing a period
    trigger: onAction
  - operationId: closeFiscalPeriod
    contract: finance
    purpose: Close a period and lock postings
    trigger: onAction
  - operationId: abandonPeriodClose
    contract: finance
    purpose: Abandon a close in progress
    trigger: onAction
  - operationId: reopenPeriod
    contract: finance
    purpose: Reopen a closed period
    trigger: onAction
  - operationId: getTrialBalance
    contract: finance
    purpose: Trial balance for a period
    trigger: onLoad
  states:
    loading: Period state and the blocking checks, each reporting separately
    emptyFirstRun: —
    emptyNoResults: '**Nothing matched.** This screen has no filter of its own, so the scope is what narrowed
      it — the venue, the shift or the date the person arrived with. **Naming the scope is what stops
      somebody concluding the record does not exist.**'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `LEDGER_APPROVE`, `LEDGER_VIEW` to approve ledgers; a person without it sees the reason and
      who to ask, because an empty table reads as *there is no data* and sends somebody to support with
      the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.'
    error: '**A check that cannot run is a failed check, not a passed one.** Closing is blocked'
  navigation:
    exitTo:
    - BO-001
    - BO-040
    - BO-074
    - BO-075
    - BO-076
    inferred: true
    fromFlows: true
  implementation:
    app: venue-management-web
    route: /finance/period-close
    component: apps/venue-management-web/src/routes/finance/PeriodClose.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-090
  notes: Added 17 August because the contract had operations no screen declared. **Not on the wireframe
    board** — needs drawing.
  density: compact
  entryState:
    params:
    - name: periodId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `periodId`.'
  requiresModule: core
  statesDerived: true
- id: BO-091
  name: AI Policy & Spend
  module: Guests & Marketing
  purpose: What the assistant may do here, what it has cost, and what happens at the ceiling.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
      - kind: primaryButton
        derived: true
        impliedBy: setAiPolicy
        label: Save ai policy
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: setAiPolicy
  apis:
  - operationId: getAiPolicy
    contract: ai
    purpose: What the assistant may do here
    trigger: onLoad
  - operationId: setAiPolicy
    contract: ai
    purpose: Set the policy
    trigger: onAction
  - operationId: getAiUsage
    contract: ai
    purpose: Usage, cost and performance
    trigger: onLoad
  - operationId: listIndexFailures
    contract: ai
    purpose: Records an index could not embed, and at which stage
    trigger: onLoad
  states:
    loading: Policy with the level each value resolves from, then spend against the ceiling
    emptyFirstRun: No policy at this level — the inherited one applies and is labelled (ADR-0018)
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `AI_AUDIT_VIEW`, `AI_CONFIGURE` to read ais; a person without it sees the reason and who to
      ask, because an empty table reads as *there is no data* and sends somebody to support with the wrong
      question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `ai` is not licensed.** The tenant has not bought this module, which is a commercial
      answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load. **Editing is blocked** — a ceiling saved over an unread one could be lower
      than what has already been spent
  navigation:
    exitTo:
    - BO-001
    inferred: true
  implementation:
    app: venue-management-web
    route: /ai/policy
    component: apps/venue-management-web/src/routes/AiPolicySpend.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-091
  notes: Added 17 August. **Staff and guest spend shown separately** — a venue can manage the first and
    cannot stop guests asking questions. At the ceiling the manager decides whether to continue; the assistant
    does not stop on its own (CF-14). **Index failures sit beside spend on purpose** - both answer the
    same two questions, is the assistant working and what is it costing, and a failure rate is the cheaper
    half of that answer.
  density: compact
  requiresModule: ai
  statesDerived: true
- id: BO-092
  name: Venue Maps
  module: Access & Venue
  wave: 2
  purpose: Every map for this venue, and which is published.
  apis:
  - operationId: listVenueMaps
    contract: venue-map
    purpose: Maps
    trigger: onLoad
  - operationId: createVenueMap
    contract: venue-map
    purpose: Start one
    trigger: onAction
  states:
    loading: Maps, published first
    emptyFirstRun: No map yet. **The venue is navigable without one and a guest is not** — the empty state
      says what uploading a plan gets them
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `VENUE_MAP_MANAGE`, `VENUE_MAP_VIEW` to manage venues; a person without it sees the reason
      and who to ask, because an empty table reads as *there is no data* and sends somebody to support
      with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `seating` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not load. The published map is unaffected — this screen edits drafts
    emptyNoResults: 'Nothing matches the current filters. **The filters are named and clearable from here**
      — an empty list with the filter state hidden elsewhere is a person who thinks the data is gone.
      **Added 25 August with the derived list component**: a screen that lists has to say what it shows
      when the list is empty, and this screen gained the list before it gained the sentence.'
  notes: 'CF-123. **A venue may have several maps** — a park map and a floor plan per building are different
    maps, not layers of one, because a guest on the second floor should not be shown the ground floor
    toilets. **Drawn 26 August** — `Seat Board 2.dc.html` frame `seat-2e`. **The frame names this screen
    on its own face**, which is the first pack to do that: the earlier F&B, POS and Retail boards had
    to be hand-assigned by purpose after three derivation attempts produced nonsense. **A board that says
    what it draws removes the guess entirely.**'
  navigation:
    exitTo:
    - BO-093
    - BO-094
  implementation:
    app: venue-management-web
    route: /venue-mapping/venue-maps
    component: apps/venue-management-web/src/routes/venue-mapping/VenueMapList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Board 2.dc.html#seat-2e
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-092
  density: compact
  requiresModule: seating
  statesDerived: true
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: dataTable
        derived: true
        impliedBy: listVenueMaps
        notes: '**Cursor pagination, never offset** — offset drifts under concurrent writes, which on
          a venue''s busiest hour is a list that skips rows.'
      - kind: primaryButton
        derived: true
        impliedBy: createVenueMap
        label: Create venue map
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: createVenueMap
  boardFrames:
  - Seat Board 2.dc.html#seat-2e
- id: BO-093
  name: Map Import & Labelling
  module: Access & Venue
  wave: 2
  purpose: Upload a plan, check what was read, and review what the assistant suggests.
  apis:
  - operationId: importVenueGeometry
    contract: venue-map
    purpose: Read the drawing
    trigger: onAction
  - operationId: proposeVenueLabels
    contract: ai
    purpose: Suggest labels
    trigger: onAction
  - operationId: acceptVenueLabelProposals
    contract: venue-map
    purpose: Accept or reject
    trigger: onAction
  states:
    loading: Parsing the drawing
    emptyFirstRun: '**No shapes found.** Every layer name in the file is listed so a role can be mapped
      by reading rather than guessing'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `VENUE_MAP_MANAGE` to manage venues; a person without it sees the reason and who to ask, because
      an empty table reads as *there is no data* and sends somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `seating` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: '**Unreadable, or no layer matched.** The two are shown separately — a file that could not
      be opened and a file whose layers nobody claimed need different fixes'
  notes: 'CF-123. **Two failure modes on one screen, kept visually apart.** Extraction is deterministic
    and reports which layers it found; labelling is a proposal with a confidence. **A mis-parsed layer
    and a bad suggestion look identical if the screen blurs them**, and the operator is left saying only
    that the map is wrong. **Low-confidence proposals are shown, not filtered** — the shape the assistant
    is unsure about is the one most worth a human looking at. **Drawn 26 August** — `Seat Board 1.dc.html`
    frame `seat-1a`. **The frame names this screen on its own face**, which is the first pack to do that:
    the earlier F&B, POS and Retail boards had to be hand-assigned by purpose after three derivation attempts
    produced nonsense. **A board that says what it draws removes the guess entirely.**'
  navigation:
    exitTo:
    - BO-092
    - BO-094
  implementation:
    app: venue-management-web
    route: /venue-mapping/map-import
    component: apps/venue-management-web/src/routes/venue-mapping/VenueMapImportForm.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Board 1.dc.html#seat-1a
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-093
  density: compact
  entryState:
    params:
    - name: mapId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `mapId`.'
  requiresModule: seating
  statesDerived: true
  layout:
    template: form
    regions:
    - name: contentBody
      components:
      - kind: fileUpload
        notes: The geometry file being imported.
      - kind: progressIndicator
        notes: >
          Import is long enough to leave the screen. **A spinner with no proportion is a screen
          people reload**, and reloading an import is how a venue gets two of everything.
      - kind: primaryButton
        derived: true
        impliedBy: importVenueGeometry
        label: Import venue geometry
        notes: The act the screen exists for.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: importVenueGeometry
  boardFrames:
  - Seat Board 1.dc.html#seat-1a
- id: BO-094
  name: Map Editor & Publish
  module: Access & Venue
  wave: 2
  purpose: Place booths, toilets, exits, rides and restaurants, then publish.
  apis:
  - operationId: getVenueMap
    contract: venue-map
    purpose: The draft
    trigger: onLoad
  - operationId: setVenuePoint
    contract: venue-map
    purpose: Place a point
    trigger: onAction
  - operationId: publishVenueMap
    contract: venue-map
    purpose: Publish
    trigger: onAction
  - operationId: validateVenueMapGraph
    contract: venue-map
    purpose: What is unreachable, before anyone publishes it
    trigger: onAction
  - operationId: setPathClosure
    contract: venue-map
    purpose: Close a route during works or an incident
    trigger: onAction
  - operationId: getVenueMapGraph
    contract: venue-map
    purpose: The navigation graph, ready to route over
    trigger: onLoad
  states:
    loading: The draft with its points and paths
    emptyFirstRun: An imported map with no points. **Import gives geometry; this gives it meaning**
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** This screen
      needs `VENUE_MAP_MANAGE`, `VENUE_MAP_PUBLISH`, `VENUE_MAP_VIEW` to manage venues; a person without
      it sees the reason and who to ask, because an empty table reads as *there is no data* and sends
      somebody to support with the wrong question.


      **Authority is carried by the person, not the device** (ADR-0002) — so signing in again on the same
      terminal is the way through.


      **Also reached when `seating` is not licensed.** The tenant has not bought this module, which is
      a commercial answer and not a permission one — say which, because the person to ask is different.'
    error: Could not save. The published map is untouched
  notes: 'CF-123. **Publishing is a separate act from saving**, and the screen makes that visible — editing
    a live map under a guest standing in front of it is how a route ends at a wall. **Refuses to publish
    a point linked to a closed outlet**, naming which one: a restaurant point pointing nowhere is worse
    than no point, because a guest walks there. **`isStepFree` on a path is the field to get right** —
    a wheelchair user routed up a staircase was failed by the map, not the venue. **Graph validation runs
    before publish and on demand.** The screen separates two findings that read the same and are not:
    **an unreachable point is a defect, and a point reachable only by steps is a map that works until
    a wheelchair user opens it.** **Critical points — first aid, emergency exits, assembly points — are
    listed apart**, because an unreachable gift shop and an unreachable assembly point should not sit
    in one list of two hundred. **Drawn 26 August** — `Seat Board 1.dc.html` frame `seat-1b`. **The frame
    names this screen on its own face**, which is the first pack to do that: the earlier F&B, POS and
    Retail boards had to be hand-assigned by purpose after three derivation attempts produced nonsense.
    **A board that says what it draws removes the guess entirely.**'
  navigation:
    exitTo:
    - BO-092
  implementation:
    app: venue-management-web
    route: /venue-mapping/map-editor
    component: apps/venue-management-web/src/routes/venue-mapping/VenueMapEditorForm.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Seat Board 1.dc.html#seat-1b
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-094
  density: compact
  entryState:
    params:
    - name: mapId
      from: deepLink
    - name: pathId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `mapId`, `pathId`.'
  requiresModule: seating
  statesDerived: true
  layout:
    template: canvas
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        derived: true
        impliedBy: getVenueMap
        notes: One record, read-only.
      - kind: primaryButton
        derived: true
        impliedBy: setVenuePoint
        label: Save venue point
        notes: The act the screen exists for.
      - kind: publishGate
        impliedBy: publishVenueMap
        notes: >
          **Publishing a map moves people.** The gate names what changes before it happens —
          which routes, which closures, which points become unreachable.
      - kind: secondaryButton
        label: Cancel
        notes: '**A screen that can submit must be leaveable without submitting.**'
        derived: true
        impliedBy: setVenuePoint
  boardFrames:
  - Seat Board 1.dc.html#seat-1b
- id: BO-095
  name: Resources
  module: Access & Venue
  wave: 2
  purpose: Every bookable object at this venue, and whether it is free.
  apis:
  - operationId: listResources
    contract: resources
    purpose: Resources with status
    trigger: onLoad
  - operationId: createResource
    contract: resources
    purpose: Define one
    trigger: onAction
  states:
    loading: List skeleton; the count renders first
    emptyFirstRun: No bookable resources yet. **A cabana sold as a product sells a slot** — define the
      object here and it becomes something a guest can be handed and can return.
    emptyNoResults: No resource matches this kind or window. Widen the window rather than the kind — availability
      moves, definitions do not.
    emptyNoAccess: You do not have RESOURCE_VIEW. The list is not empty.
    error: Could not load resources. Existing bookings are unaffected.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search resources
      - kind: multiSelect
        label: Kind
      - kind: dataTable
        bindsTo: Resource[]
        notes: Status column carries the reason — **booked and under repair need different responses from
          somebody looking for something free**
  density: compact
  navigation:
    exitTo:
    - BO-096
    - BO-097
  implementation:
    app: venue-management-web
    route: /resources/directory
    component: apps/venue-management-web/src/routes/resources/ResourceList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-095
  notes: CF-125. **A resource is a specific object, not a quantity** — forty identical strollers are forty
    rows, because guest twelve returned stroller twelve.
  requiresModule: resources
- id: BO-096
  name: Resource Calendar
  module: Access & Venue
  wave: 2
  purpose: See what is free, and book it without double-booking.
  apis:
  - operationId: getResourceAvailability
    contract: resources
    purpose: Free windows
    trigger: onLoad
  - operationId: bookResource
    contract: resources
    purpose: Reserve
    trigger: onAction
  states:
    loading: Calendar grid; today renders first
    emptyFirstRun: Nothing booked in this window. **Free is the default state and it is worth showing
      plainly** — an empty calendar at a venue that takes bookings is either a quiet week or a resource
      nobody knows exists.
    emptyNoResults: No availability in this window. The response says why — booked, setup, teardown, maintenance
      or closed — because **wait and look elsewhere are different answers**.
    emptyNoAccess: You do not have RESOURCE_VIEW.
    error: Could not load availability. **Do not book blind** — a booking made against a stale calendar
      is the double-booking this screen exists to prevent.
  layout:
    template: calendar
    regions:
    - name: contentBody
      components:
      - kind: timeline
        bindsTo: ResourceAvailability
        notes: Setup and teardown render as their own bands, not as part of the booking — **a 30-minute
          turnaround is invisible if it is drawn inside the booking**
      - kind: primaryButton
        label: Book
      - kind: banner
        notes: Conflicts, named with times. *Not available* on something a guest can see is not an answer
  density: compact
  navigation:
    entryFrom:
    - BO-095
    exitTo:
    - BO-097
  implementation:
    app: venue-management-web
    route: /resources/calendar
    component: apps/venue-management-web/src/routes/resources/ResourceCalendar.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-096
  notes: CF-125. **Conflict detection before assignment is the whole point** — two bookings on one cabana
    is a guest arriving to find somebody in their chair, and it must be impossible rather than unlikely.
  entryState:
    params:
    - name: resourceId
      from: BO-095
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: resources
- id: BO-097
  name: Check Out & Check In
  module: Access & Venue
  wave: 2
  purpose: Hand it over with a deposit, take it back, settle.
  apis:
  - operationId: checkOutResource
    contract: resources
    purpose: Hand over
    trigger: onAction
  - operationId: checkInResource
    contract: resources
    purpose: Take back
    trigger: onAction
  - operationId: authoriseStoredValue
    contract: orders
    purpose: Hold the deposit
    trigger: onAction
  - operationId: relinquishStoredValue
    contract: orders
    purpose: Release it
    trigger: onAction
  states:
    loading: The booking and its condition history
    emptyFirstRun: Nothing is out. **The list a poolside attendant checks at close** — anything still
      here at the end of the day is a conversation.
    emptyNoResults: Nothing matches. Overdue items are shown by default because they are the ones that
      need chasing.
    emptyNoAccess: You do not have RESOURCE_BOOK.
    error: Could not load. **Check-out and check-in work offline** — the deposit hold reconciles on sync.
    offline: Queued locally. The deposit is authorised on sync, and **the condition note taken now is
      the only defence against a dispute later**.
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        bindsTo: ResourceBooking
      - kind: textField
        label: Condition note
      - kind: fileUpload
        label: Condition photos
        notes: Taken on the way out, not only on the way back
      - kind: primaryButton
        label: Check out
      - kind: secondaryButton
        label: Check in
  density: compact
  navigation:
    entryFrom:
    - BO-095
    - BO-096
    exitTo:
    - BO-095
    - BO-096
    inferred: false
    notes: '**Returns to BO-095, BO-096.** Stated on 4 September: this screen declared where it is reached
      from and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /resources/check-out
    component: apps/venue-management-web/src/routes/resources/ResourceCheckOutForm.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-097
  notes: CF-125, CF-126. **The deposit is held, not taken** — a deposit taken and refunded is two transactions
    and a fee. **Damage routes the resource to maintenance directly**, because routing through available
    leaves a window where the next guest books a broken item. **The deposit operations are declared here
    because this screen calls them** — F25 named them at steps 4 and 5 and the screen did not, which the
    flow checker refused. **A step calling an operation its screen does not declare is one of the two
    out of step**, and in this case it was the screen.
  entryState:
    params:
    - name: authorisationId
      from: deepLink
    - name: bookingId
      from: deepLink
    coldEntry: '**A staff link opened cold resolves the thing or says plainly that it is gone.** No silent
      redirect — a supervisor following a link from an alert needs to know whether the record moved, closed
      or never existed, because those are three different next actions. **The scope is resolved from the
      session, never from the link**: a link cannot move somebody to a venue they do not hold. Arrives
      with `authorisationId`, `bookingId`.'
  requiresModule: resources
- id: BO-098
  name: Qualifications
  module: Access & Venue
  wave: 2
  purpose: What a person is certified to do, and until when.
  apis:
  - operationId: setResourceQualifications
    contract: resources
    purpose: Set qualifications
    trigger: onAction
  states:
    loading: Qualifications with their expiry
    emptyFirstRun: No qualifications recorded. **A role is not a skill** — 1.2.36 requires the check before
      assignment, not after.
    emptyNoResults: Nobody matches. Expiring-soon is the default filter, because that is the list somebody
      acts on.
    emptyNoAccess: You do not have RESOURCE_MANAGE.
    error: Could not load.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: dataTable
        bindsTo: Qualification[]
        notes: Sorted by expiry, not by name — **a lifeguard certificate that lapsed last month is a safety
          failure, not a data-quality one**
      - kind: banner
        notes: Expired qualifications held by staff on today rota
  density: compact
  navigation:
    entryFrom:
    - BO-095
    exitTo:
    - BO-095
    inferred: false
    notes: '**Returns to BO-095.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /resources/qualifications
    component: apps/venue-management-web/src/routes/resources/QualificationList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-098
  notes: CF-125. **Expiry is the field that makes this worth having** — a certification with no expiry
    is one nobody renews.
  entryState:
    params:
    - name: resourceId
      from: BO-095
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: resources
- id: BO-099
  name: Session Manifest
  module: Access & Venue
  wave: 2
  purpose: Who is in a session, in what order.
  apis:
  - operationId: getSessionManifest
    contract: resources
    purpose: The manifest
    trigger: onLoad
  - operationId: reorderSessionManifest
    contract: resources
    purpose: Change the order
    trigger: onAction
  states:
    loading: Participants in running order
    emptyFirstRun: Nobody booked into this session yet.
    emptyNoResults: No participants match.
    emptyNoAccess: You do not have RESOURCE_VIEW.
    error: Could not load the manifest.
    offline: The cached manifest. **An instructor at the water edge needs this more than anyone**, and
      that is where the signal is worst.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: dataTable
        bindsTo: SessionParticipant[]
        notes: Drag to reorder. **The running order is operational** — an instructor takes beginners first,
          and booking order puts one between two advanced riders
      - kind: banner
        notes: Unsigned waivers, named. **An instructor about to start does not want to discover one at
          the water edge**
  density: compact
  navigation:
    entryFrom:
    - BO-096
    exitTo:
    - BO-096
    inferred: false
    notes: '**Returns to BO-096.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /resources/session-manifest
    component: apps/venue-management-web/src/routes/resources/SessionManifest.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-099
  notes: CF-125, CF-129. Distinct from duration, which variants already handle.
  entryState:
    params:
    - name: sessionId
      from: session
    coldEntry: Resolves from the session; a cold arrival is the ordinary case.
  requiresModule: resources
- id: BO-100
  name: Venue Home
  module: Venue Operations
  wave: 1
  purpose: The screen a venue manager opens in the morning, and the only way into everything else.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: What this venue is configured to do
    trigger: onLoad
  - operationId: listShifts
    contract: shift
    purpose: Who is on and what has been taken
    trigger: onLoad
  states:
    loading: The tiles render in place; each resolves on its own so one slow source does not hold the
      page.
    emptyFirstRun: '**A venue on its first morning.** No shifts, no orders, no stock. The state links
      to the opening checklist rather than showing eight empty tiles — **a dashboard of zeroes teaches
      a new manager nothing.**'
    emptyNoResults: Nothing happened in the window selected. Yesterday and today are the useful defaults.
    emptyNoAccess: '**Sections you cannot open are not shown.** A manager with no finance permission sees
      seven sections, not eight greyed out — a menu that lists what you may not do is a menu that invites
      a support ticket.'
    error: Could not load the summary. **Every section is still reachable** — this screen is a landing
      page, and a failed tile must not block navigation.
    partial: '**Some tiles resolved and some did not, and it says which.** A dashboard showing three numbers
      where four were promised is a manager acting on a partial picture without knowing it.'
  layout:
    template: dashboard
    regions:
    - name: sideNav
      components:
      - kind: treeNav
        bindsTo: sections
        notes: The eight sections. **Persistent — a back office is a place somebody works all day**, and
          a nav that disappears on every detail screen makes them use the browser back button as navigation.
    - name: contentBody
      components:
      - kind: metricTile
        notes: 'Today: takings, admissions, open shifts, items needing attention. **Four, not twelve**
          — a tile nobody acts on is a tile that trains people to ignore the row.'
      - kind: cardList
        bindsTo: sections
        notes: One card per section with its count of items needing attention. **The count is the point**
          — a section with nothing pending should look different from one with eleven approvals waiting.
      - kind: banner
        notes: Alerts that crossed a threshold, from VenueSettings.alerting. **On-platform and markable
          as read** (CF-134).
  navigation:
    isEntryPoint: true
    exitTo:
    - BO-101
    - BO-102
    - BO-103
    - BO-104
    - BO-105
    - BO-106
    - BO-107
    - BO-108
    entryFrom:
    - BO-108
  implementation:
    app: venue-management-web
    route: /home
    component: apps/venue-management-web/src/routes/home/VenueHomeDashboard.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-100
  notes: '**Built 20 August because P08 had none.** `BO-001 Queue Directory` was the declared entry point
    to a 99-screen back office — it exits to four queue screens, and **93 screens hung off nothing.**
    A back office entered through a queue list. **The module was also one bucket holding 72 of 99 screens**,
    so there was nothing for a home screen to point at; sections were derived from the contract each screen
    principally calls.'
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: '**Resolves from the session, so a cold arrival is the ordinary case** — a manager bookmarks
      the back office and opens it every morning. A principal with more than one venue is asked which
      before the page renders, rather than shown the first one.'
  requiresModule: core
- id: BO-101
  name: Orders & Money
  module: Orders & Money
  wave: 1
  purpose: Everything in orders & money, and what in it needs attention.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: What is enabled here
    trigger: onLoad
  - operationId: listOrders
    contract: orders
    purpose: Orders taken in this venue
    trigger: onLoad
  - operationId: listSettlements
    contract: finance
    purpose: Money settled and what is outstanding
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**Nothing configured in orders & money yet.** The action is the first thing to set
      up, not a blank list.'
    emptyNoResults: Nothing matches the filter.
    emptyNoAccess: You do not have permission for orders & money. **Said plainly** — an empty section
      reads as broken.
    error: Could not load. Venue Home is still reachable.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: screens
        notes: 28 screens, each with what needs attention.
      - kind: searchField
        label: Search orders & money
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-008
    - BO-022
    - BO-023
    - BO-024
    - BO-025
    - BO-026
    - BO-027
    - BO-028
    - BO-029
    - BO-039
    - BO-040
    - BO-041
    - BO-042
    - BO-043
    - BO-047
    - BO-048
    - BO-051
    - BO-059
    - BO-061
    - BO-062
    - BO-065
    - BO-070
    - BO-074
    - BO-075
    - BO-076
    - BO-077
    - BO-089
    - BO-090
  implementation:
    app: venue-management-web
    route: /orders-money
    component: apps/venue-management-web/src/routes/home/OrdersMoneyList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-101
  notes: Section landing. **28 screens reach the entry point through here** — before 20 August they reached
    it through nothing. **Given its section's own operations on 4 September.** It sat on `getVenueSettings`
    alone, which made it identical to every other section landing page — a hub that shows nothing of its
    section is a menu item, not a screen.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: '**Resolves from the session, so a cold arrival is the ordinary case** — a manager bookmarks
      the back office and opens it every morning. A principal with more than one venue is asked which
      before the page renders, rather than shown the first one.'
  requiresModule: core
- id: BO-102
  name: Sell
  module: Sell
  wave: 1
  purpose: Everything in sell, and what in it needs attention.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: What is enabled here
    trigger: onLoad
  - operationId: listUpsellRules
    contract: promotions
    purpose: Upsell rules in force
    trigger: onLoad
  - operationId: getUpsellSuggestions
    contract: promotions
    purpose: What to offer this basket
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**Nothing configured in sell yet.** The action is the first thing to set up, not a
      blank list.'
    emptyNoResults: Nothing matches the filter.
    emptyNoAccess: You do not have permission for sell. **Said plainly** — an empty section reads as broken.
    error: Could not load. Venue Home is still reachable.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: screens
        notes: 14 screens, each with what needs attention.
      - kind: searchField
        label: Search sell
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-007
    - BO-009
    - BO-010
    - BO-011
    - BO-012
    - BO-013
    - BO-014
    - BO-015
    - BO-016
    - BO-017
    - BO-018
    - BO-019
    - BO-037
    - BO-063
    - BO-109
    - BO-110
    - BO-111
    - BO-112
    - BO-113
    - BO-114
    - BO-115
    - BO-116
    - BO-117
    - BO-118
    - BO-119
    - BO-120
    - BO-121
    - BO-122
    - BO-123
    - BO-124
    - BO-125
    - BO-126
    - BO-142
    - BO-143
  implementation:
    app: venue-management-web
    route: /sell
    component: apps/venue-management-web/src/routes/home/SellList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-102
  notes: Section landing. **14 screens reach the entry point through here** — before 20 August they reached
    it through nothing.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: '**Resolves from the session, so a cold arrival is the ordinary case** — a manager bookmarks
      the back office and opens it every morning. A principal with more than one venue is asked which
      before the page renders, rather than shown the first one.'
  requiresModule: core
- id: BO-103
  name: Access & Venue
  module: Access & Venue
  wave: 1
  purpose: Everything in access & venue, and what in it needs attention.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: What is enabled here
    trigger: onLoad
  - operationId: listAccessPoints
    contract: access
    purpose: Gates and lanes in this venue
    trigger: onLoad
  - operationId: listScans
    contract: access
    purpose: Admissions as they happen
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**Nothing configured in access & venue yet.** The action is the first thing to set
      up, not a blank list.'
    emptyNoResults: Nothing matches the filter.
    emptyNoAccess: You do not have permission for access & venue. **Said plainly** — an empty section
      reads as broken.
    error: Could not load. Venue Home is still reachable.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: screens
        notes: 24 screens, each with what needs attention.
      - kind: searchField
        label: Search access & venue
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-001
    - BO-002
    - BO-003
    - BO-004
    - BO-005
    - BO-006
    - BO-030
    - BO-031
    - BO-032
    - BO-033
    - BO-034
    - BO-035
    - BO-038
    - BO-069
    - BO-071
    - BO-072
    - BO-092
    - BO-093
    - BO-094
    - BO-095
    - BO-096
    - BO-097
    - BO-098
    - BO-099
  implementation:
    app: venue-management-web
    route: /access-venue
    component: apps/venue-management-web/src/routes/home/AccessVenueList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-103
  notes: Section landing. **24 screens reach the entry point through here** — before 20 August they reached
    it through nothing. **Given its section's own operations on 4 September.** It sat on `getVenueSettings`
    alone, which made it identical to every other section landing page — a hub that shows nothing of its
    section is a menu item, not a screen.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: '**Resolves from the session, so a cold arrival is the ordinary case** — a manager bookmarks
      the back office and opens it every morning. A principal with more than one venue is asked which
      before the page renders, rather than shown the first one.'
  requiresModule: core
- id: BO-104
  name: Food & Beverage
  module: Food & Beverage
  wave: 1
  purpose: Everything in food & beverage, and what in it needs attention.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: What is enabled here
    trigger: onLoad
  - operationId: listMenus
    contract: fnb
    purpose: Menus and their state
    trigger: onLoad
  - operationId: createMenu
    contract: fnb
    purpose: Start a menu
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**Nothing configured in food & beverage yet.** The action is the first thing to set
      up, not a blank list.'
    emptyNoResults: Nothing matches the filter.
    emptyNoAccess: You do not have permission for food & beverage. **Said plainly** — an empty section
      reads as broken.
    error: Could not load. Venue Home is still reachable.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: screens
        notes: 4 screens, each with what needs attention.
      - kind: searchField
        label: Search food & beverage
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-020
    - BO-021
    - BO-045
    - BO-046
    - BO-134
    - BO-135
    - BO-136
  implementation:
    app: venue-management-web
    route: /food-beverage
    component: apps/venue-management-web/src/routes/home/FoodBeverageList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-104
  notes: Section landing. **4 screens reach the entry point through here** — before 20 August they reached
    it through nothing. **Navigation repointed to P15 on 20 August** — the F&B screens it linked to moved
    out of the back office when the client board was adopted.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: '**Resolves from the session, so a cold arrival is the ordinary case** — a manager bookmarks
      the back office and opens it every morning. A principal with more than one venue is asked which
      before the page renders, rather than shown the first one.'
  requiresModule: core
- id: BO-105
  name: Stock & Supply
  module: Stock & Supply
  wave: 1
  purpose: Everything in stock & supply, and what in it needs attention.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: What is enabled here
    trigger: onLoad
  - operationId: listInventoryItems
    contract: inventory
    purpose: Stock on hand
    trigger: onLoad
  - operationId: listPurchaseOrders
    contract: inventory
    purpose: Orders placed with suppliers
    trigger: onLoad
  - operationId: createInventoryItem
    contract: inventory
    purpose: Add an item
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**Nothing configured in stock & supply yet.** The action is the first thing to set
      up, not a blank list.'
    emptyNoResults: Nothing matches the filter.
    emptyNoAccess: You do not have permission for stock & supply. **Said plainly** — an empty section
      reads as broken.
    error: Could not load. Venue Home is still reachable.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: screens
        notes: 9 screens, each with what needs attention.
      - kind: searchField
        label: Search stock & supply
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-049
    - BO-050
    - BO-052
    - BO-078
    - BO-079
    - BO-080
    - BO-081
    - BO-082
    - BO-083
    - BO-137
    - BO-138
    - BO-139
    - BO-140
    - BO-141
  implementation:
    app: venue-management-web
    route: /stock-supply
    component: apps/venue-management-web/src/routes/home/StockSupplyList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-105
  notes: Section landing. **9 screens reach the entry point through here** — before 20 August they reached
    it through nothing.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: '**Resolves from the session, so a cold arrival is the ordinary case** — a manager bookmarks
      the back office and opens it every morning. A principal with more than one venue is asked which
      before the page renders, rather than shown the first one.'
  requiresModule: core
- id: BO-106
  name: People & Access Rights
  module: People & Access Rights
  wave: 1
  purpose: Everything in people & access rights, and what in it needs attention.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: What is enabled here
    trigger: onLoad
  - operationId: listRoles
    contract: identity
    purpose: Roles and who holds them
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**Nothing configured in people & access rights yet.** The action is the first thing
      to set up, not a blank list.'
    emptyNoResults: Nothing matches the filter.
    emptyNoAccess: You do not have permission for people & access rights. **Said plainly** — an empty
      section reads as broken.
    error: Could not load. Venue Home is still reachable.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: screens
        notes: 11 screens, each with what needs attention.
      - kind: searchField
        label: Search people & access rights
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-053
    - BO-054
    - BO-055
    - BO-056
    - BO-057
    - BO-066
    - BO-084
    - BO-085
    - BO-086
    - BO-087
    - BO-088
  implementation:
    app: venue-management-web
    route: /people-access-rights
    component: apps/venue-management-web/src/routes/home/PeopleAccessRightsList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-106
  notes: Section landing. **11 screens reach the entry point through here** — before 20 August they reached
    it through nothing.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: '**Resolves from the session, so a cold arrival is the ordinary case** — a manager bookmarks
      the back office and opens it every morning. A principal with more than one venue is asked which
      before the page renders, rather than shown the first one.'
  requiresModule: core
- id: BO-107
  name: Guests & Marketing
  module: Guests & Marketing
  wave: 1
  purpose: Everything in guests & marketing, and what in it needs attention.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: What is enabled here
    trigger: onLoad
  - operationId: listCampaigns
    contract: marketing-crm
    purpose: Campaigns and their reach
    trigger: onLoad
  - operationId: createCampaign
    contract: marketing-crm
    purpose: Start a campaign
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**Nothing configured in guests & marketing yet.** The action is the first thing to
      set up, not a blank list.'
    emptyNoResults: Nothing matches the filter.
    emptyNoAccess: You do not have permission for guests & marketing. **Said plainly** — an empty section
      reads as broken.
    error: Could not load. Venue Home is still reachable.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: screens
        notes: 3 screens, each with what needs attention.
      - kind: searchField
        label: Search guests & marketing
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-068
    - BO-073
    - BO-091
  implementation:
    app: venue-management-web
    route: /guests-marketing
    component: apps/venue-management-web/src/routes/home/GuestsMarketingList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-107
  notes: Section landing. **3 screens reach the entry point through here** — before 20 August they reached
    it through nothing.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: '**Resolves from the session, so a cold arrival is the ordinary case** — a manager bookmarks
      the back office and opens it every morning. A principal with more than one venue is asked which
      before the page renders, rather than shown the first one.'
  requiresModule: core
- id: BO-108
  name: Venue Operations
  module: Venue Operations
  wave: 1
  purpose: Everything in venue operations, and what in it needs attention.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: What is enabled here
    trigger: onLoad
  - operationId: listWorkOrders
    contract: maintenance
    purpose: Work raised and its state
    trigger: onLoad
  - operationId: listIncidents
    contract: maintenance
    purpose: Incidents open in the venue
    trigger: onLoad
  - operationId: listAssets
    contract: maintenance
    purpose: Assets and their condition
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**Nothing configured in venue operations yet.** The action is the first thing to set
      up, not a blank list.'
    emptyNoResults: Nothing matches the filter.
    emptyNoAccess: You do not have permission for venue operations. **Said plainly** — an empty section
      reads as broken.
    error: Could not load. Venue Home is still reachable.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: cardList
        bindsTo: screens
        notes: 6 screens, each with what needs attention.
      - kind: searchField
        label: Search venue operations
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-036
    - BO-044
    - BO-058
    - BO-060
    - BO-064
    - BO-067
    - BO-100
    - BO-127
    - BO-128
    - BO-129
    - BO-130
    - BO-131
    - BO-132
    - BO-133
  implementation:
    app: venue-management-web
    route: /venue-operations
    component: apps/venue-management-web/src/routes/home/VenueOperationsList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-108
  notes: Section landing. **6 screens reach the entry point through here** — before 20 August they reached
    it through nothing. **Given its section's own operations on 4 September.** It sat on `getVenueSettings`
    alone, which made it identical to every other section landing page — a hub that shows nothing of its
    section is a menu item, not a screen.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: '**Resolves from the session, so a cold arrival is the ordinary case** — a manager bookmarks
      the back office and opens it every morning. A principal with more than one venue is asked which
      before the page renders, rather than shown the first one.'
  requiresModule: core
- id: BO-109
  name: Menu Builder & POS Layout Designer
  module: Sell
  wave: 2
  purpose: Menu Builder & POS Layout Designer — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listMenus
    contract: fnb
    purpose: List menus
    trigger: onLoad
  - operationId: setMenuSections
    contract: fnb
    purpose: Set menu sections and their item ordering
    trigger: onAction
  - operationId: updateSaleBoard
    contract: tenancy
    purpose: Update a sale board
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Menu Builder & POS Layout Designer — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: menuId
      from: deepLink
    - name: saleBoardId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search menu builder
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/menu-builder-pos-layout-designer
    component: apps/venue-management-web/src/routes/sell/MenuBuilderPosLayoutDesignerList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/FnB Board 2.dc.html#fnb-2b
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-109
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Drawn 31 August** — `FnB Board 2.dc.html` frame `fnb-2b`. **Matched on frame title against
    screen name, constrained to this board’s platforms.** These packs label by board position (`GM-6C`)
    rather than naming the screen, so the title is the only join — *Menu Builder &amp; POS Layout Designer*
    matched at 1.0. **A cross-platform title match was refused**: `Outlet Management` scored 0.85 against
    a partner-portal screen, which is how a mapping goes wrong quietly.'
  requiresModule: fnb
  statesDerived: true
  boardFrames:
  - FnB Board 2.dc.html#fnb-2b
- id: BO-110
  name: Recipe & BOM Management
  module: Sell
  wave: 2
  purpose: Recipe & BOM Management — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listRecipes
    contract: fnb
    purpose: List recipes
    trigger: onLoad
  - operationId: setRecipe
    contract: fnb
    purpose: Define a recipe for a menu item
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Recipe & BOM Management — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search recipe
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/recipe-bom-management
    component: apps/venue-management-web/src/routes/sell/RecipeBomManagementList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-110
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: fnb
  statesDerived: true
- id: BO-111
  name: Ingredient Substitution, Allergen & Nutrition
  module: Sell
  wave: 2
  purpose: Ingredient Substitution, Allergen & Nutrition — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: setRecipe
    contract: fnb
    purpose: Define a recipe for a menu item
    trigger: onAction
  - operationId: updateMenu
    contract: fnb
    purpose: Amend a menu
    trigger: onAction
  - operationId: setSubstitutionRules
    contract: fnb
    purpose: What may replace what
    trigger: onAction
  - operationId: verifyAllergens
    contract: fnb
    purpose: Does this dish still match its claim?
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Ingredient Substitution, Allergen & Nutrition — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: menuId
      from: deepLink
    - name: menuItemId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders. An item opened from the menu. **Allergen verification is per item** — a menu-wide
      check is a job, not a screen.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search ingredient substitution, allergen
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    - BO-045
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product. **flow F31 — allergen verification returns to the
      menu it was opened from.** The flow is the authority on the journey and the navigation has to agree
      with it; marking this block non-inferred is what turned the disagreement from a warning into a failure.'
  implementation:
    app: venue-management-web
    route: /sell/ingredient-substitution-allergen-nutrition
    component: apps/venue-management-web/src/routes/sell/IngredientSubstitutionAllergenNutritioList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/FnB Board 2.dc.html#fnb-2l
    source: Claude Design F&B pack, 24 August
    note: '**Drawn as FNB-2L in the client F&B pack.**'
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: fnb
  boardFrames:
  - FnB Board 2.dc.html#fnb-2l
  statesDerived: true
- id: BO-112
  name: Production Planning & Production Sheets
  module: Sell
  wave: 2
  purpose: Production Planning & Production Sheets — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: planProductionRun
    contract: fnb
    purpose: Plan a batch, for one outlet or several
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Production Planning & Production Sheets — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search production planning
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/production-planning-production-sheets
    component: apps/venue-management-web/src/routes/sell/ProductionPlanningProductionSheetsList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 2.dc.html#ret-2e
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-2E in the client Retail pack.**'
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: fnb
  boardFrames:
  - Retail Board 2.dc.html#ret-2e
  statesDerived: true
- id: BO-113
  name: Central Kitchen & Commissary Management
  module: Sell
  wave: 2
  purpose: Central Kitchen & Commissary Management — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: planProductionRun
    contract: fnb
    purpose: Plan a batch, for one outlet or several
    trigger: onAction
  - operationId: completeProductionRun
    contract: fnb
    purpose: Record what was actually made
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Central Kitchen & Commissary Management — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: runId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search central kitchen
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/central-kitchen-commissary-management
    component: apps/venue-management-web/src/routes/sell/CentralKitchenCommissaryManagementList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 2.dc.html#ret-2d
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-2D in the client Retail pack.**'
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: fnb
  boardFrames:
  - Retail Board 2.dc.html#ret-2d
  statesDerived: true
- id: BO-114
  name: Variants, Attributes, Barcode & RFID Management
  module: Sell
  wave: 2
  purpose: Variants, Attributes, Barcode & RFID Management — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listSerialisedItems
    contract: inventory
    purpose: listSerialisedItems
    trigger: onLoad
  - operationId: lookupMerchandise
    contract: retail
    purpose: Price and stock check by barcode
    trigger: onLoad
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Variants, Attributes, Barcode & RFID Management — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search variants, attributes, barcode
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/variants-attributes-barcode-rfid-management
    component: apps/venue-management-web/src/routes/sell/VariantsAttributesBarcodeRfidManagemenList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 4.dc.html#ret-4j
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-4J in the client Retail pack.**'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not.'
  requiresModule: inventory
  boardFrames:
  - Retail Board 4.dc.html#ret-4j
  statesDerived: true
- id: BO-115
  name: Category, Brand & Merchandise Hierarchy
  module: Sell
  wave: 2
  purpose: Category, Brand & Merchandise Hierarchy — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listProductCategories
    contract: catalogue
    purpose: listProductCategories
    trigger: onLoad
  - operationId: setProductCategories
    contract: catalogue
    purpose: setProductCategories
    trigger: onAction
  - operationId: listSaleBoards
    contract: tenancy
    purpose: List sale boards
    trigger: onLoad
  - operationId: requestSuggestion
    contract: ai
    purpose: requestSuggestion
    trigger: onAction
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Category, Brand & Merchandise Hierarchy — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: reportId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search category, brand
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/category-brand-merchandise-hierarchy
    component: apps/venue-management-web/src/routes/sell/CategoryBrandMerchandiseHierarchyList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 4.dc.html#pos-4a
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-4A in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Owns POS board frame(s) POS-4A** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none.'
  requiresModule: ticketing
  boardFrames:
  - POS Board 4.dc.html#pos-4a
  statesDerived: true
- id: BO-116
  name: Merchandising & Product Presentation
  module: Sell
  wave: 2
  purpose: Merchandising & Product Presentation — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listMerchandise
    contract: retail
    purpose: List merchandise
    trigger: onLoad
  - operationId: updateMerchandise
    contract: retail
    purpose: Amend a merchandise item
    trigger: onAction
  - operationId: listSaleBoards
    contract: tenancy
    purpose: List sale boards
    trigger: onLoad
  - operationId: listWorkstations
    contract: tenancy
    purpose: List workstations
    trigger: onLoad
  - operationId: setRolePermissions
    contract: tenancy
    purpose: What this role may do
    trigger: onAction
  - operationId: setVenueSettings
    contract: tenancy
    purpose: Set support hours, quiet hours, segregated access and alerti
    trigger: onAction
  - operationId: updateSaleBoard
    contract: tenancy
    purpose: Update a sale board
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Merchandising & Product Presentation — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: merchandiseId
      from: deepLink
    - name: roleId
      from: deepLink
    - name: saleBoardId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders. A role opened from the directory.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search merchandising
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/merchandising-product-presentation
    component: apps/venue-management-web/src/routes/sell/MerchandisingProductPresentationList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 4.dc.html#pos-4b
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-4B in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Owns POS board frame(s) POS-4B** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none.'
  requiresModule: retail
  boardFrames:
  - POS Board 4.dc.html#pos-4b
  statesDerived: true
- id: BO-117
  name: Product Import, Governance & AI Configuration Assistant
  module: Sell
  wave: 2
  purpose: Product Import, Governance & AI Configuration Assistant — from the client design board, 20
    August.
  density: compact
  apis:
  - operationId: importProductCatalogue
    contract: catalogue
    purpose: Parse a catalogue file into a preview
    trigger: onAction
  - operationId: generateConfiguration
    contract: ai
    purpose: Draft a configuration from a description
    trigger: onAction
  - operationId: listSaleBoards
    contract: tenancy
    purpose: List sale boards
    trigger: onLoad
  - operationId: requestSuggestion
    contract: ai
    purpose: requestSuggestion
    trigger: onAction
  - operationId: updateSaleBoard
    contract: tenancy
    purpose: Update a sale board
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Product Import, Governance & AI Configuration Assistant — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: saleBoardId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search product import, governance
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/product-import-governance-ai-configuration-assistant
    component: apps/venue-management-web/src/routes/sell/ProductImportGovernanceAiConfigurationList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 4.dc.html#pos-4c
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-4C in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Owns POS board frame(s) POS-4C** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none.'
  requiresModule: ticketing
  boardFrames:
  - POS Board 4.dc.html#pos-4c
  statesDerived: true
- id: BO-118
  name: Campaign & Audience Management
  module: Sell
  wave: 2
  purpose: Campaign & Audience Management — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listCampaigns
    contract: marketing-crm
    purpose: List campaigns
    trigger: onLoad
  - operationId: createCampaign
    contract: marketing-crm
    purpose: Create a campaign
    trigger: onAction
  - operationId: listSegments
    contract: marketing-crm
    purpose: List segments
    trigger: onLoad
  - operationId: listSaleBoards
    contract: tenancy
    purpose: List sale boards
    trigger: onLoad
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  - operationId: updateSaleBoard
    contract: tenancy
    purpose: Update a sale board
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `createCampaign`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Campaign & Audience Management — from
      the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: reportId
      from: deepLink
    - name: saleBoardId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search campaign
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/campaign-audience-management
    component: apps/venue-management-web/src/routes/sell/CampaignAudienceManagementList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 4.dc.html#pos-4d
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-4D in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Owns POS board frame(s) POS-4D** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none.'
  requiresModule: marketing
  boardFrames:
  - POS Board 4.dc.html#pos-4d
  statesDerived: true
- id: BO-119
  name: Cross-Sell, Upsell & Recommendation Rules
  module: Sell
  wave: 2
  purpose: Cross-Sell, Upsell & Recommendation Rules — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: getRecommendations
    contract: promotions
    purpose: What else this guest might want
    trigger: onAction
  - operationId: getUpsellSuggestions
    contract: promotions
    purpose: Suggestions for a cart
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Cross-Sell, Upsell & Recommendation Rules — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search cross-sell, upsell
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/cross-sell-upsell-recommendation-rules
    component: apps/venue-management-web/src/routes/sell/CrossSellUpsellRecommendationRulesList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 5.dc.html#ret-5d
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-5D in the client Retail pack.**'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not.'
  requiresModule: ticketing
  boardFrames:
  - Retail Board 5.dc.html#ret-5d
  statesDerived: true
- id: BO-120
  name: Omnichannel Commerce & Journey Configuration
  module: Sell
  wave: 2
  purpose: Omnichannel Commerce & Journey Configuration — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listJourneys
    contract: marketing-crm
    purpose: Automated journeys
    trigger: onLoad
  - operationId: createJourney
    contract: marketing-crm
    purpose: Define an automated journey
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `createJourney`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Omnichannel Commerce & Journey Configuration
      — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search omnichannel commerce
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/omnichannel-commerce-journey-configuration
    component: apps/venue-management-web/src/routes/sell/OmnichannelCommerceJourneyConfiguratioList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 5.dc.html#ret-5g
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-120
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Drawn 31 August** — `Retail Board 5.dc.html` frame `ret-5g`. **Matched on frame title against
    screen name, constrained to this board’s platforms.** These packs label by board position (`GM-6C`)
    rather than naming the screen, so the title is the only join — *Omnichannel Commerce &amp; Journey
    Configuration* matched at 1.0. **A cross-platform title match was refused**: `Outlet Management` scored
    0.85 against a partner-portal screen, which is how a mapping goes wrong quietly.'
  requiresModule: marketing
  statesDerived: true
  boardFrames:
  - Retail Board 5.dc.html#ret-5g
- id: BO-121
  name: Personalized Offers & Guest Engagement
  module: Sell
  wave: 2
  purpose: Personalized Offers & Guest Engagement — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listSegments
    contract: marketing-crm
    purpose: List segments
    trigger: onLoad
  - operationId: createCampaign
    contract: marketing-crm
    purpose: Create a campaign
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `createCampaign`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Personalized Offers & Guest Engagement
      — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search personalized offers
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/personalized-offers-guest-engagement
    component: apps/venue-management-web/src/routes/sell/PersonalizedOffersGuestEngagementList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 5.dc.html#ret-5f
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-5F in the client Retail pack.**'
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: marketing
  boardFrames:
  - Retail Board 5.dc.html#ret-5f
  statesDerived: true
- id: BO-122
  name: POS Experience Dashboard
  module: Sell
  wave: 2
  purpose: POS Experience Dashboard — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listSaleBoards
    contract: tenancy
    purpose: List sale boards
    trigger: onLoad
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      POS Experience Dashboard — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search pos experience dashboard
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/pos-experience-dashboard
    component: apps/venue-management-web/src/routes/sell/PosExperienceDashboardList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 5.dc.html#ret-5j
    source: Claude Design Retail pack, 24 August
    note: '**Drawn as RET-5J in the client Retail pack.**'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not.'
  requiresModule: core
  boardFrames:
  - Retail Board 5.dc.html#ret-5j
  statesDerived: true
- id: BO-123
  name: POS Profile Management
  module: Sell
  wave: 2
  purpose: POS Profile Management — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: setConfigurationProfile
    contract: tenancy
    purpose: setConfigurationProfile
    trigger: onAction
  - operationId: listSaleBoards
    contract: tenancy
    purpose: List sale boards
    trigger: onLoad
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      POS Profile Management — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search pos profile management
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/pos-profile-management
    component: apps/venue-management-web/src/routes/sell/PosProfileManagementList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-123
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not.'
  requiresModule: core
  statesDerived: true
- id: BO-124
  name: Layout & Journey Builder
  module: Sell
  wave: 2
  purpose: Layout & Journey Builder — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: updateSaleBoard
    contract: tenancy
    purpose: Update a sale board
    trigger: onAction
  - operationId: createSaleBoard
    contract: tenancy
    purpose: Create a sale board
    trigger: onAction
  - operationId: deployConfigurationProfile
    contract: tenancy
    purpose: deployConfigurationProfile
    trigger: onAction
  - operationId: listAuditRecords
    contract: tenancy
    purpose: Who did what, where, and when
    trigger: onLoad
  - operationId: listDevices
    contract: tenancy
    purpose: List registered devices
    trigger: onLoad
  - operationId: recordDeviceHeartbeat
    contract: tenancy
    purpose: Device heartbeat and status
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `createSaleBoard`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Layout & Journey Builder — from the
      client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: saleBoardId
      from: deepLink
    - name: deviceId
      from: deepLink
    - name: profileId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders. A device opened from the registry. A profile opened from the list.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search layout
      - kind: dataTable
      - kind: publishGate
        impliedBy: deployConfigurationProfile
        notes: >
          Declares `deployConfigurationProfile`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/layout-journey-builder
    component: apps/venue-management-web/src/routes/sell/LayoutJourneyBuilderList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 1.dc.html#pos-1d
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-1D in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Owns POS board frame(s) POS-1D** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none.'
  requiresModule: core
  boardFrames:
  - POS Board 1.dc.html#pos-1d
  statesDerived: true
- id: BO-125
  name: Product & Category Button Configuration
  module: Sell
  wave: 2
  purpose: Product & Category Button Configuration — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: updateSaleBoard
    contract: tenancy
    purpose: Update a sale board
    trigger: onAction
  - operationId: listProducts
    contract: catalogue
    purpose: List products
    trigger: onLoad
  - operationId: getWorkstationHealth
    contract: tenancy
    purpose: getWorkstationHealth
    trigger: onLoad
  - operationId: listAlerts
    contract: reporting
    purpose: What is currently raised
    trigger: onLoad
  - operationId: recordDeviceHeartbeat
    contract: tenancy
    purpose: Device heartbeat and status
    trigger: onAction
  - operationId: reportIncident
    contract: maintenance
    purpose: Report an incident
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `recordDeviceHeartbeat`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Product & Category Button Configuration
      — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: saleBoardId
      from: deepLink
    - name: deviceId
      from: deepLink
    - name: workstationId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders. A device opened from the registry. Resolves from the session — a till is signed into.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search product
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/product-category-button-configuration
    component: apps/venue-management-web/src/routes/sell/ProductCategoryButtonConfigurationList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 1.dc.html#pos-1e
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-1E in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Owns POS board frame(s) POS-1E** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none.'
  requiresModule: ticketing
  boardFrames:
  - POS Board 1.dc.html#pos-1e
  statesDerived: true
- id: BO-126
  name: Deployment, Preview & Audit
  module: Sell
  wave: 2
  purpose: Deployment, Preview & Audit — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: deployConfigurationProfile
    contract: tenancy
    purpose: deployConfigurationProfile
    trigger: onAction
  - operationId: updateSaleBoard
    contract: tenancy
    purpose: Update a sale board
    trigger: onAction
  - operationId: listAuditRecords
    contract: tenancy
    purpose: Who did what, where, and when
    trigger: onLoad
  - operationId: listSaleBoards
    contract: tenancy
    purpose: List sale boards
    trigger: onLoad
  - operationId: listWorkstations
    contract: tenancy
    purpose: List workstations
    trigger: onLoad
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  - operationId: startRollout
    contract: platform-ops
    purpose: Start or continue a rollout
    trigger: onAction
  states:
    loading: The list, with counts; the filter bar renders first.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `startRollout`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Deployment, Preview & Audit — from
      the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Selling is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: profileId
      from: deepLink
    - name: saleBoardId
      from: deepLink
    - name: reportId
      from: deepLink
    - name: rolloutId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which before the
      page renders.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search deployment, preview
      - kind: dataTable
      - kind: publishGate
        impliedBy: deployConfigurationProfile
        notes: >
          Declares `deployConfigurationProfile`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/deployment-preview-audit
    component: apps/venue-management-web/src/routes/sell/DeploymentPreviewAuditList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 1.dc.html#pos-1f
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-1F, POS-4F in the client POS pack.** The pack''s frame is the specification
      — it carries operations, states, entry params and exits, and it is better specified than anything
      derived from the screen file.'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Owns POS board frame(s) POS-1F, POS-4F** (client pack, 24 August). **Assigned by board purpose
    rather than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none.'
  requiresModule: core
  boardFrames:
  - POS Board 1.dc.html#pos-1f
  - POS Board 4.dc.html#pos-4f
  statesDerived: true
- id: BO-127
  name: Hardware & Peripherals Management
  module: Venue Operations
  wave: 2
  purpose: Hardware & Peripherals Management — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listDevices
    contract: tenancy
    purpose: List registered devices
    trigger: onLoad
  - operationId: registerDevice
    contract: tenancy
    purpose: Register a device
    trigger: onAction
  - operationId: recordDeviceHeartbeat
    contract: tenancy
    purpose: Device heartbeat and status
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `recordDeviceHeartbeat`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Hardware & Peripherals Management —
      from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: deviceId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search hardware
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-108
    exitTo:
    - BO-108
    inferred: false
    notes: '**Returns to BO-108.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /venue-operations/hardware-peripherals-management
    component: apps/venue-management-web/src/routes/ops/HardwarePeripheralsManagementList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-127
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: core
  statesDerived: true
- id: BO-128
  name: Live Workstation Health Monitor
  module: Venue Operations
  wave: 2
  purpose: Live Workstation Health Monitor — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: getWorkstationHealth
    contract: tenancy
    purpose: getWorkstationHealth
    trigger: onLoad
  - operationId: listWorkstations
    contract: tenancy
    purpose: List workstations
    trigger: onLoad
  - operationId: setOfflinePolicy
    contract: tenancy
    purpose: setOfflinePolicy
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Live Workstation Health Monitor — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: workstationId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search live workstation health monitor
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-108
    exitTo:
    - BO-108
    inferred: false
    notes: '**Returns to BO-108.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /venue-operations/live-workstation-health-monitor
    component: apps/venue-management-web/src/routes/ops/LiveWorkstationHealthMonitorList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 5.dc.html#pos-5b
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-5B in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Owns POS board frame(s) POS-5B** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none.'
  requiresModule: core
  boardFrames:
  - POS Board 5.dc.html#pos-5b
  statesDerived: true
- id: BO-129
  name: Software, Configuration & Version Management
  module: Venue Operations
  wave: 2
  purpose: Software, Configuration & Version Management — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: setConfigurationProfile
    contract: tenancy
    purpose: setConfigurationProfile
    trigger: onAction
  - operationId: deployConfigurationProfile
    contract: tenancy
    purpose: deployConfigurationProfile
    trigger: onAction
  - operationId: configureWorkstation
    contract: tenancy
    purpose: Configure a workstation
    trigger: onAction
  - operationId: getWorkstationHealth
    contract: tenancy
    purpose: getWorkstationHealth
    trigger: onLoad
  - operationId: listWorkstations
    contract: tenancy
    purpose: List workstations
    trigger: onLoad
  - operationId: setConnectivityThresholds
    contract: tenancy
    purpose: setConnectivityThresholds
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Software, Configuration & Version Management — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: profileId
      from: deepLink
    - name: workstationId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first. Resolves
      from the session — a till is signed into.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search software, configuration
      - kind: dataTable
      - kind: publishGate
        impliedBy: deployConfigurationProfile
        notes: >
          Declares `deployConfigurationProfile`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  navigation:
    entryFrom:
    - BO-108
    exitTo:
    - BO-108
    inferred: false
    notes: '**Returns to BO-108.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /venue-operations/software-configuration-version-management
    component: apps/venue-management-web/src/routes/ops/SoftwareConfigurationVersionManagementList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 5.dc.html#pos-5d
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-5D in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Owns POS board frame(s) POS-5D** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none.'
  requiresModule: core
  boardFrames:
  - POS Board 5.dc.html#pos-5d
  statesDerived: true
- id: BO-130
  name: Offline Policy & Rules Configuration
  module: Venue Operations
  wave: 1
  purpose: Offline Policy & Rules Configuration — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: setOfflinePolicy
    contract: tenancy
    purpose: setOfflinePolicy
    trigger: onAction
  - operationId: listSyncRejections
    contract: orders
    purpose: Entries the server refused
    trigger: onLoad
  - operationId: syncOrders
    contract: orders
    purpose: Replay orders recorded offline
    trigger: onAction
  - operationId: createOrder
    contract: orders
    purpose: Post a rejected transaction at the price the guest paid
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `createOrder`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Offline Policy & Rules Configuration
      — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search offline policy
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-108
    exitTo:
    - BO-108
    inferred: false
    notes: '**Returns to BO-108.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /venue-operations/offline-policy-rules-configuration
    component: apps/venue-management-web/src/routes/ops/OfflinePolicyRulesConfigurationList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 5.dc.html#pos-5e
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-5E in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Owns POS board frame(s) POS-5E** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none. **Moved to wave 1 on 24 August.**
    F33 walks a till going offline and coming back, which is a wave-1 journey — ADR-0013 makes the POS
    local-first from the first release. **A venue that can trade offline and cannot resolve a sync rejection
    has an unbounded journal and no way to clear it**, and `check-flows` refused the flow rather than
    let that ship. **Resolved remotely by design.**  is venue-scoped and this is a web screen — **a supervisor
    at the main venue clears a mall kiosk''s rejections without going there.** An IT technician restores
    the connection; **deciding whether a sale stands is a commercial act and they have the access without
    the authority.**'
  requiresModule: core
  boardFrames:
  - POS Board 5.dc.html#pos-5e
  statesDerived: true
- id: BO-131
  name: Connectivity & Auto-Switch Settings
  module: Venue Operations
  wave: 2
  purpose: Connectivity & Auto-Switch Settings — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: setConnectivityThresholds
    contract: tenancy
    purpose: setConnectivityThresholds
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Connectivity & Auto-Switch Settings — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search connectivity
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-108
    exitTo:
    - BO-108
    inferred: false
    notes: '**Returns to BO-108.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /venue-operations/connectivity-auto-switch-settings
    component: apps/venue-management-web/src/routes/ops/ConnectivityAutoSwitchSettingsList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-131
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: core
  statesDerived: true
- id: BO-132
  name: Offline Transaction Monitor & Sync Queue
  module: Venue Operations
  wave: 2
  purpose: Offline Transaction Monitor & Sync Queue — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listSyncRejections
    contract: orders
    purpose: Entries the server refused
    trigger: onLoad
  - operationId: syncOrders
    contract: orders
    purpose: Replay orders recorded offline
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Offline Transaction Monitor & Sync Queue — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search offline transaction monitor
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-108
    exitTo:
    - BO-108
    inferred: false
    notes: '**Returns to BO-108.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /venue-operations/offline-transaction-monitor-sync-queue
    component: apps/venue-management-web/src/routes/ops/OfflineTransactionMonitorSyncQueueList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-132
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: ticketing
  statesDerived: true
- id: BO-133
  name: Offline Alerts, Limits & Audit
  module: Venue Operations
  wave: 1
  purpose: Offline Alerts, Limits & Audit — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listSyncRejections
    contract: orders
    purpose: Entries the server refused
    trigger: onLoad
  - operationId: listAlerts
    contract: reporting
    purpose: What is currently raised
    trigger: onLoad
  - operationId: listAuditRecords
    contract: tenancy
    purpose: Who did what, where, and when
    trigger: onLoad
  - operationId: decideApprovalRequest
    contract: approvals
    purpose: Approve or reject
    trigger: onAction
  - operationId: runReport
    contract: reporting
    purpose: Run a report
    trigger: onAction
  - operationId: setAlertRule
    contract: reporting
    purpose: Raise an alert when a number leaves a range
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Offline Alerts, Limits & Audit — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: reportId
      from: deepLink
    - name: requestId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first. An
      approval request opened from the queue.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search offline alerts, limits
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-108
    exitTo:
    - BO-108
    inferred: false
    notes: '**Returns to BO-108.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /venue-operations/offline-alerts-limits-audit
    component: apps/venue-management-web/src/routes/ops/OfflineAlertsLimitsAuditList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/POS Board 5.dc.html#pos-5f
    source: Claude Design POS pack, 24 August
    note: '**Drawn as POS-5F in the client POS pack.** The pack''s frame is the specification — it carries
      operations, states, entry params and exits, and it is better specified than anything derived from
      the screen file.'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Owns POS board frame(s) POS-5F** (client pack, 24 August). **Assigned by board purpose rather
    than by operation overlap** — three attempts at deriving that mapping produced plausible nonsense,
    and a reader who trusts a bad table is worse off than one who has none. **Moved to wave 1 on 24 August.**
    F33 walks a till going offline and coming back, which is a wave-1 journey — ADR-0013 makes the POS
    local-first from the first release. **A venue that can trade offline and cannot resolve a sync rejection
    has an unbounded journal and no way to clear it**, and `check-flows` refused the flow rather than
    let that ship.'
  requiresModule: ticketing
  boardFrames:
  - POS Board 5.dc.html#pos-5f
  statesDerived: true
- id: BO-134
  name: Kitchen & Preparation Stations
  module: Food & Beverage
  wave: 2
  purpose: Kitchen & Preparation Stations — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listKitchenStations
    contract: fnb
    purpose: List preparation stations and their routing
    trigger: onLoad
  - operationId: setKitchenStations
    contract: fnb
    purpose: Configure stations and item routing
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Kitchen & Preparation Stations — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search kitchen
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-104
    exitTo:
    - BO-104
    inferred: false
    notes: '**Returns to BO-104.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /food-beverage/kitchen-preparation-stations
    component: apps/venue-management-web/src/routes/ops/KitchenPreparationStationsList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/FnB Board 1.dc.html#fnb-1h
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-134
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Drawn 31 August** — `FnB Board 1.dc.html` frame `fnb-1h`. **Matched on frame title against
    screen name, constrained to this board’s platforms.** These packs label by board position (`GM-6C`)
    rather than naming the screen, so the title is the only join — *Kitchen &amp; Preparation Stations*
    matched at 1.0. **A cross-platform title match was refused**: `Outlet Management` scored 0.85 against
    a partner-portal screen, which is how a mapping goes wrong quietly.'
  requiresModule: fnb
  statesDerived: true
  boardFrames:
  - FnB Board 1.dc.html#fnb-1h
- id: BO-135
  name: Order Routing & KDS/Printer Rules
  module: Food & Beverage
  wave: 2
  purpose: Order Routing & KDS/Printer Rules — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: setKitchenStations
    contract: fnb
    purpose: Configure stations and item routing
    trigger: onAction
  - operationId: listWorkstations
    contract: tenancy
    purpose: List workstations
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Order Routing & KDS/Printer Rules — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search order routing
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-104
    exitTo:
    - BO-104
    inferred: false
    notes: '**Returns to BO-104.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /food-beverage/order-routing-kds-printer-rules
    component: apps/venue-management-web/src/routes/ops/OrderRoutingKdsPrinterRulesList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/FnB Board 1.dc.html#fnb-1j
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-135
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Drawn 31 August** — `FnB Board 1.dc.html` frame `fnb-1j`. **Matched on frame title against
    screen name, constrained to this board’s platforms.** These packs label by board position (`GM-6C`)
    rather than naming the screen, so the title is the only join — *Order Routing &amp; KDS / Printer
    Rules* matched at 1.0. **A cross-platform title match was refused**: `Outlet Management` scored 0.85
    against a partner-portal screen, which is how a mapping goes wrong quietly.'
  requiresModule: fnb
  statesDerived: true
  boardFrames:
  - FnB Board 1.dc.html#fnb-1j
- id: BO-136
  name: F&B Global Settings & Controls
  module: Food & Beverage
  wave: 2
  purpose: F&B Global Settings & Controls — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: Operational settings for this venue
    trigger: onLoad
  - operationId: setVenueSettings
    contract: tenancy
    purpose: Set support hours, quiet hours, segregated access and alerti
    trigger: onAction
  - operationId: buildProductionPlan
    contract: fnb
    purpose: Turn a forecast into a prep list
    trigger: onAction
  - operationId: releaseProductionPlan
    contract: fnb
    purpose: Make the plan real
    trigger: onAction
  - operationId: listProductionRuns
    contract: fnb
    purpose: What is being made, and what was
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `buildProductionPlan`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** F&B Global Settings & Controls — from
      the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: planId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first. A
      plan opened from the list.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search f
      - kind: dataTable
      - kind: publishGate
        impliedBy: releaseProductionPlan
        notes: >
          Declares `releaseProductionPlan`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  navigation:
    entryFrom:
    - BO-104
    exitTo:
    - BO-104
    inferred: false
    notes: '**Returns to BO-104.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /food-beverage/f-b-global-settings-controls
    component: apps/venue-management-web/src/routes/ops/FBGlobalSettingsControlsList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/FnB Board 2.dc.html#fnb-2m
    source: Claude Design F&B pack, 24 August
    note: '**Drawn as FNB-2M, FNB-2N, FNB-5D in the client F&B pack.**'
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not.'
  requiresModule: core
  boardFrames:
  - FnB Board 2.dc.html#fnb-2m
  - FnB Board 2.dc.html#fnb-2n
  - FnB Board 5.dc.html#fnb-5d
  statesDerived: true
- id: BO-137
  name: Recipe Consumption & Theoretical Inventory
  module: Stock & Supply
  wave: 2
  purpose: Recipe Consumption & Theoretical Inventory — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listRecipes
    contract: fnb
    purpose: List recipes
    trigger: onLoad
  - operationId: getCountVariance
    contract: inventory
    purpose: Variance between counted and expected
    trigger: onLoad
  - operationId: listProductionRuns
    contract: fnb
    purpose: What is being made, and what was
    trigger: onLoad
  - operationId: getProductionRun
    contract: fnb
    purpose: One run — its plan, its output, and the gap
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Recipe Consumption & Theoretical Inventory — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: countId
      from: deepLink
    - name: runId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first. A
      run opened from the list.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search recipe consumption
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-105
    exitTo:
    - BO-105
    inferred: false
    notes: '**Returns to BO-105.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /stock-supply/recipe-consumption-theoretical-inventory
    component: apps/venue-management-web/src/routes/ops/RecipeConsumptionTheoreticalInventoryList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/FnB Board 2.dc.html#fnb-2f
    source: Claude Design F&B pack, 24 August
    note: '**Drawn as FNB-2F, FNB-5C in the client F&B pack.**'
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: fnb
  boardFrames:
  - FnB Board 2.dc.html#fnb-2f
  - FnB Board 5.dc.html#fnb-5c
  statesDerived: true
- id: BO-138
  name: Production Execution & Batch Management
  module: Stock & Supply
  wave: 2
  purpose: Production Execution & Batch Management — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: planProductionRun
    contract: fnb
    purpose: Plan a batch, for one outlet or several
    trigger: onAction
  - operationId: completeProductionRun
    contract: fnb
    purpose: Record what was actually made
    trigger: onAction
  - operationId: listExpiringBatches
    contract: inventory
    purpose: What is about to go out of date
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Production Execution & Batch Management — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: runId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search production execution
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-105
    exitTo:
    - BO-105
    inferred: false
    notes: '**Returns to BO-105.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /stock-supply/production-execution-batch-management
    component: apps/venue-management-web/src/routes/ops/ProductionExecutionBatchManagementList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-138
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: fnb
  statesDerived: true
- id: BO-139
  name: Wastage, Spoilage, Returns & Write-Off
  module: Stock & Supply
  wave: 2
  purpose: Wastage, Spoilage, Returns & Write-Off — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: recordWaste
    contract: fnb
    purpose: Record waste
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `recordWaste`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Wastage, Spoilage, Returns & Write-Off
      — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: outletId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search wastage, spoilage, returns
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-105
    exitTo:
    - BO-105
    inferred: false
    notes: '**Returns to BO-105.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /stock-supply/wastage-spoilage-returns-write-off
    component: apps/venue-management-web/src/routes/ops/WastageSpoilageReturnsWriteOffList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-139
  provenance: client board, specified
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them.'
  requiresModule: fnb
  statesDerived: true
- id: BO-140
  name: Product Availability, 86 & Operational Food Safety
  module: Stock & Supply
  wave: 2
  purpose: Product Availability, 86 & Operational Food Safety — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: setItemAvailability
    contract: fnb
    purpose: Mark an item available or eighty-sixed
    trigger: onAction
  - operationId: listExpiringBatches
    contract: inventory
    purpose: What is about to go out of date
    trigger: onLoad
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Product Availability, 86 & Operational Food Safety — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: itemId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search product availability, 86
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-105
    exitTo:
    - BO-105
    inferred: false
    notes: '**Returns to BO-105.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /stock-supply/product-availability-86-operational-food-safet
    component: apps/venue-management-web/src/routes/ops/ProductAvailability86OperationalFoodSaList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-140
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not.'
  requiresModule: fnb
  statesDerived: true
- id: BO-141
  name: Operational Alerts, AI Replenishment & Action Center
  module: Stock & Supply
  wave: 2
  purpose: Operational Alerts, AI Replenishment & Action Center — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: listAlerts
    contract: reporting
    purpose: What is currently raised
    trigger: onLoad
  - operationId: acknowledgeAlert
    contract: reporting
    purpose: Take responsibility for it
    trigger: onAction
  - operationId: createRequisition
    contract: inventory
    purpose: Raise a requisition
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet — the one action is `createRequisition`,
      and it is the only thing on the screen until the first one exists.


      **Says what this screen is for, not just that it is empty.** Operational Alerts, AI Replenishment
      & Action Center — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    - name: alertId
      from: deepLink
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search operational alerts, ai replenishment
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-105
    exitTo:
    - BO-105
    inferred: false
    notes: '**Returns to BO-105.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /stock-supply/operational-alerts-ai-replenishment-action-cen
    component: apps/venue-management-web/src/routes/ops/OperationalAlertsAiReplenishmentActionList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-141
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not.'
  requiresModule: analytics
  statesDerived: true
- id: BO-142
  name: Store Rules, Controls & Permissions
  module: Sell
  wave: 2
  purpose: Store Rules, Controls & Permissions — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: Operational settings for this venue
    trigger: onLoad
  - operationId: listRoles
    contract: identity
    purpose: List roles
    trigger: onLoad
  - operationId: listStoreRules
    contract: retail
    purpose: Rules and controls in force
    trigger: onLoad
  - operationId: setStoreRules
    contract: retail
    purpose: Change a rule
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Store Rules, Controls & Permissions — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search store rules, controls
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/store-rules-controls-permissions
    component: apps/venue-management-web/src/routes/ops/StoreRulesControlsPermissionsList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 1.dc.html#ret-1j
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-142
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Drawn 31 August** — `Retail Board 1.dc.html` frame `ret-1j`. **Matched on frame title against
    screen name, constrained to this board’s platforms.** These packs label by board position (`GM-6C`)
    rather than naming the screen, so the title is the only join — *Store Rules, Controls &amp; Permissions*
    matched at 1.0. **A cross-platform title match was refused**: `Outlet Management` scored 0.85 against
    a partner-portal screen, which is how a mapping goes wrong quietly.'
  requiresModule: core
  statesDerived: true
  boardFrames:
  - Retail Board 1.dc.html#ret-1j
- id: BO-143
  name: Retail Global Settings & Controls
  module: Sell
  wave: 2
  purpose: Retail Global Settings & Controls — from the client design board, 20 August.
  density: compact
  apis:
  - operationId: getVenueSettings
    contract: tenancy
    purpose: Operational settings for this venue
    trigger: onLoad
  - operationId: setVenueSettings
    contract: tenancy
    purpose: Set support hours, quiet hours, segregated access and alerti
    trigger: onAction
  states:
    loading: The list, with counts.
    emptyFirstRun: '**A venue that opened this morning.** Nothing here yet, and nothing on this screen
      creates the first one — it is filled by another screen or by a job, and this state names which rather
      than leaving somebody looking for a button that does not exist.


      Retail Global Settings & Controls — from the client design board, 20 August.'
    emptyNoResults: Nothing matches this filter.
    emptyNoAccess: You do not have permission for this section. **Said plainly** — an empty screen reads
      as broken.
    error: Could not load. **Trading is unaffected** — this is configuration, and the till keeps working.
  entryState:
    params:
    - name: venueId
      from: session
    coldEntry: Resolves from the session. A principal with more than one venue is asked which first.
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: searchField
        label: Search retail global settings
      - kind: dataTable
  navigation:
    entryFrom:
    - BO-102
    exitTo:
    - BO-102
    inferred: false
    notes: '**Returns to BO-102.** Stated on 4 September: this screen declared where it is reached from
      and no way to leave, so whoever landed on it was stuck. The return path is the same edge travelled
      the other way, not a guess about the product.'
  implementation:
    app: venue-management-web
    route: /sell/retail-global-settings-controls
    component: apps/venue-management-web/src/routes/ops/RetailGlobalSettingsControlsList.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: designed
    board: wireframes/Retail Board 1.dc.html#ret-1k
    generatedFallback: wireframes/P08 Venue Management.dc.html#bo-143
  provenance: client board, named only
  notes: '**Added 20 August from the client design board.** The operations existed and no screen called
    them. **Named in the board contents and not written up in it** — the operations are real, the layout
    is not. **Drawn 31 August** — `Retail Board 1.dc.html` frame `ret-1k`. **Matched on frame title against
    screen name, constrained to this board’s platforms.** These packs label by board position (`GM-6C`)
    rather than naming the screen, so the title is the only join — *Retail Global Settings &amp; Controls*
    matched at 1.0. **A cross-platform title match was refused**: `Outlet Management` scored 0.85 against
    a partner-portal screen, which is how a mapping goes wrong quietly.'
  requiresModule: core
  statesDerived: true
  boardFrames:
  - Retail Board 1.dc.html#ret-1k
- id: BO-144
  name: Access Control Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Central operational/configuration landing page for the complete Access Control module.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccess
    contract: access
    purpose: Access Control Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-control-command-center-bo-144
    component: apps/venue-management-web/src/routes/access-venue/AccessControlCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-144
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-144
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '1'
    page: 6
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-145
    - BO-146
    - BO-147
    - BO-148
    - BO-149
    - BO-150
    - BO-151
    - BO-152
    - BO-153
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-145
  name: Venue & Park Access Structure
  module: Access & Venue
  requiresModule: access
  purpose: Define the highest-level physical access hierarchy.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first venue entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listVenueParkAccess
    contract: access
    purpose: Venue & Park Access Structure
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/venue-park-access-structure-bo-145
    component: apps/venue-management-web/src/routes/access-venue/VenueParkAccessStructure.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-145
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-145
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '2'
    page: 7
  navigation:
    entryFrom:
    - BO-144
    exitTo:
    - BO-144
    inferred: false
    notes: '**Reached from BO-144, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-146
  name: Access Area & Zone Builder
  module: Access & Venue
  requiresModule: access
  purpose: Divide a venue into controlled access areas.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setAccessAreaZone
    contract: access
    purpose: Access Area & Zone Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/access-area-zone-builder-bo-146
    component: apps/venue-management-web/src/routes/access-venue/AccessAreaZoneBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-146
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-146
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '3'
    page: 8
  navigation:
    entryFrom:
    - BO-144
    exitTo:
    - BO-144
    inferred: false
    notes: '**Reached from BO-144, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-147
  name: Attraction Access Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Configure attractions as access-controlled destinations.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first attraction entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setAttractionAccess
    contract: access
    purpose: Attraction Access Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/attraction-access-configuration-bo-147
    component: apps/venue-management-web/src/routes/access-venue/AttractionAccessConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-147
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-147
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '4'
    page: 9
  navigation:
    entryFrom:
    - BO-144
    exitTo:
    - BO-144
    inferred: false
    notes: '**Reached from BO-144, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-148
  name: Access Point Directory
  module: Access & Venue
  requiresModule: access
  purpose: Create the logical access points where validation occurs. An Access Point is different from
    a physical reader/device.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessPoints
    contract: access
    purpose: List access points
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-point-directory-bo-148
    component: apps/venue-management-web/src/routes/access-venue/AccessPointDirectory.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-148
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-148
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '5'
    page: 10
  navigation:
    entryFrom:
    - BO-144
    exitTo:
    - BO-144
    inferred: false
    notes: '**Reached from BO-144, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-149
  name: Gate & Lane Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Configure individual physical gates/lanes.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first gate entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setGateLane
    contract: access
    purpose: Gate & Lane Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/gate-lane-configuration-bo-149
    component: apps/venue-management-web/src/routes/access-venue/GateLaneConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-149
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-149
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '6'
    page: 11
  navigation:
    entryFrom:
    - BO-144
    exitTo:
    - BO-144
    inferred: false
    notes: '**Reached from BO-144, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-150
  name: Access Control Graphical Map Designer
  module: Access & Venue
  requiresModule: access
  purpose: Create a graphical digital twin of the access-control environment.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setAccessGraphicalMap
    contract: access
    purpose: Access Control Graphical Map Designer
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/access-control-graphical-map-designer-bo-150
    component: apps/venue-management-web/src/routes/access-venue/AccessControlGraphicalMapDesigner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-150
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-150
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '7'
    page: 12
  navigation:
    entryFrom:
    - BO-144
    exitTo:
    - BO-144
    inferred: false
    notes: '**Reached from BO-144, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-151
  name: Access Location Grouping
  module: Access & Venue
  requiresModule: access
  purpose: Group multiple access points for operational and capacity purposes. Allow access topology and
    operating behavior to change by date/time.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessLocationGrouping
    contract: access
    purpose: Access Location Grouping
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-location-grouping-bo-151
    component: apps/venue-management-web/src/routes/access-venue/AccessLocationGrouping.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-151
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-151
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '8'
    page: 13
  navigation:
    entryFrom:
    - BO-144
    exitTo:
    - BO-144
    inferred: false
    notes: '**Reached from BO-144, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-152
  name: Operating Calendar & Special Access Days
  module: Access & Venue
  requiresModule: access
  purpose: Allow access topology and operating behavior to change by date/time.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first operating entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOperatingCalendarSpecial
    contract: access
    purpose: Operating Calendar & Special Access Days
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/operating-calendar-special-access-days-bo-152
    component: apps/venue-management-web/src/routes/access-venue/OperatingCalendarSpecialAccessDays.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-152
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-152
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '9'
    page: 13
  navigation:
    entryFrom:
    - BO-144
    exitTo:
    - BO-144
    inferred: false
    notes: '**Reached from BO-144, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-153
  name: Topology Validation & Publication
  module: Access & Venue
  requiresModule: access
  purpose: 'Final validation and controlled deployment of access configuration. Before publication, TICVAI
    automatically validates: orphan gates devices without access points access points without zones incorrect
    entry/exit direction missing offline co'
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: timeline
        notes: What changed, when, and by whom
      - kind: searchField
        notes: Find a change
      - kind: publishGate
        impliedBy: publishTopologyValidation
        notes: >
          The screen's one declared operation publishes. **A validation that publishes without
          naming what it activates is a validation nobody can refuse.**
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first topology entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: publishTopologyValidation
    contract: access
    purpose: Topology Validation & Publication
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/topology-validation-publication-bo-153
    component: apps/venue-management-web/src/routes/access-venue/TopologyValidationPublication.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-153
    workshopBoard: wireframes/WS01 Access Control Board 1.dc.html#bo-153
  source:
    pack: Access Control Module_Reference.pdf
    board: '1'
    number: '10'
    page: 14
  navigation:
    entryFrom:
    - BO-144
    exitTo:
    - BO-144
    inferred: false
    notes: '**Reached from BO-144, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-154
  name: Access Rule Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Central configuration dashboard for all access and entitlement rules.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessRule
    contract: access
    purpose: Access Rule Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-rule-command-center-bo-154
    component: apps/venue-management-web/src/routes/access-venue/AccessRuleCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-154
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-154
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.1'
    page: 17
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-155
    - BO-156
    - BO-157
    - BO-158
    - BO-159
    - BO-160
    - BO-161
    - BO-162
    - BO-163
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-155
  name: Visual Access Rule Builder
  module: Access & Venue
  requiresModule: access
  purpose: Provide a no-code rule engine. This is where TICVAI should become significantly easier to configure
    than traditional access-control systems.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first visual entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setVisualAccessRule
    contract: access
    purpose: Visual Access Rule Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/visual-access-rule-builder-bo-155
    component: apps/venue-management-web/src/routes/access-venue/VisualAccessRuleBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-155
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-155
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.2'
    page: 18
  navigation:
    entryFrom:
    - BO-154
    exitTo:
    - BO-154
    inferred: false
    notes: '**Reached from BO-154, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-156
  name: Entry, Exit & Re-entry Rules
  module: Access & Venue
  requiresModule: access
  purpose: Configure admission quantity and journey sequencing. The source matrix explicitly requires
    configurable quantities for entry, exit and same-day re-entry, anti- passback intervals, required
    exit before re-entry, and required entry before exi
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first entry, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listEntryExitRule
    contract: access
    purpose: Entry, Exit & Re-entry Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/entry-exit-re-entry-rules-bo-156
    component: apps/venue-management-web/src/routes/access-venue/EntryExitReEntryRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-156
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-156
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.3'
    page: 19
  navigation:
    entryFrom:
    - BO-154
    exitTo:
    - BO-154
    inferred: false
    notes: '**Reached from BO-154, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-157
  name: Anti-Passback & Journey Sequence
  module: Access & Venue
  requiresModule: access
  purpose: Prevent credential sharing and impossible access sequences.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first anti-passback entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAntiPassbackJourney
    contract: access
    purpose: Anti-Passback & Journey Sequence
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/anti-passback-journey-sequence-bo-157
    component: apps/venue-management-web/src/routes/access-venue/AntiPassbackJourneySequence.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-157
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-157
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.4'
    page: 20
  navigation:
    entryFrom:
    - BO-154
    exitTo:
    - BO-154
    inferred: false
    notes: '**Reached from BO-154, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-158
  name: Access Validity & Time Rules
  module: Access & Venue
  requiresModule: access
  purpose: Determine when access is permitted.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessValidityTime
    contract: access
    purpose: Access Validity & Time Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-validity-time-rules-bo-158
    component: apps/venue-management-web/src/routes/access-venue/AccessValidityTimeRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-158
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-158
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.5'
    page: 21
  navigation:
    entryFrom:
    - BO-154
    exitTo:
    - BO-154
    inferred: false
    notes: '**Reached from BO-154, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-159
  name: Entitlement Consumption Engine
  module: Access & Venue
  requiresModule: access
  purpose: Determine what gets consumed when access is granted. This is critical because one credential
    may represent several different entitlements. The matrix specifically describes a single QR capable
    of carrying park admission, ride entitlement, m
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first entitlement entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listEntitlementConsumption
    contract: access
    purpose: Entitlement Consumption Engine
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/entitlement-consumption-engine-bo-159
    component: apps/venue-management-web/src/routes/access-venue/EntitlementConsumptionEngine.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-159
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-159
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.6'
    page: 22
  navigation:
    entryFrom:
    - BO-154
    exitTo:
    - BO-154
    inferred: false
    notes: '**Reached from BO-154, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-160
  name: Multi-Park & Crossover Rules
  module: Access & Venue
  requiresModule: access
  purpose: Configure complex access between multiple parks/venues. The matrix specifically requires multi-park
    access on different days, same-day crossover, park-specific entry quantities and conditional access
    based on previous park admission.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first multi-park entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMultiParkCrossover
    contract: access
    purpose: Multi-Park & Crossover Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/multi-park-crossover-rules-bo-160
    component: apps/venue-management-web/src/routes/access-venue/MultiParkCrossoverRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-160
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-160
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.7'
    page: 23
  navigation:
    entryFrom:
    - BO-154
    exitTo:
    - BO-154
    inferred: false
    notes: '**Reached from BO-154, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-161
  name: Guest, Companion & Eligibility Rules
  module: Access & Venue
  requiresModule: access
  purpose: Apply access conditions based on guest characteristics and relationships.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first guest, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGuestCompanionEligibility
    contract: access
    purpose: Guest, Companion & Eligibility Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/guest-companion-eligibility-rules-bo-161
    component: apps/venue-management-web/src/routes/access-venue/GuestCompanionEligibilityRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-161
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-161
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.8'
    page: 24
  navigation:
    entryFrom:
    - BO-154
    exitTo:
    - BO-154
    inferred: false
    notes: '**Reached from BO-154, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-162
  name: Group Admission & Quantity Validation
  module: Access & Venue
  requiresModule: access
  purpose: Handle B2B groups, school groups, tour groups and family/group tickets efficiently. The matrix
    specifically calls for faster admission for large B2B groups and the ability for one QR/group ticket
    to represent multiple admissions.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupAdmissionQuantity
    contract: access
    purpose: Group Admission & Quantity Validation
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/group-admission-quantity-validation-bo-162
    component: apps/venue-management-web/src/routes/access-venue/GroupAdmissionQuantityValidation.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-162
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-162
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.9'
    page: 25
  navigation:
    entryFrom:
    - BO-154
    exitTo:
    - BO-154
    inferred: false
    notes: '**Reached from BO-154, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-163
  name: Rule Simulation, Conflict Check & Publication
  module: Access & Venue
  requiresModule: access
  purpose: No access rule should reach a live gate without being tested.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
      - kind: publishGate
        impliedBy: publishRuleConflictCheck
        notes: >
          Declares `publishRuleConflictCheck`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first rule entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: publishRuleConflictCheck
    contract: access
    purpose: Rule Simulation, Conflict Check & Publication
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/rule-simulation-conflict-check-publication-bo-163
    component: apps/venue-management-web/src/routes/access-venue/RuleSimulationConflictCheckPublication.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-163
    workshopBoard: wireframes/WS02 Access Control Board 2.dc.html#bo-163
  source:
    pack: Access Control Module_Reference.pdf
    board: '2'
    number: '2.10'
    page: 26
  navigation:
    entryFrom:
    - BO-154
    exitTo:
    - BO-154
    inferred: false
    notes: '**Reached from BO-154, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-164
  name: Digital Credential Security Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Central configuration and monitoring page for all secure digital credentials.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first digital entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listDigitalCredentialSecurity
    contract: access
    purpose: Digital Credential Security Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/digital-credential-security-command-center-bo-164
    component: apps/venue-management-web/src/routes/access-venue/DigitalCredentialSecurityCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-164
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-164
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.1'
    page: 29
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-165
    - BO-166
    - BO-167
    - BO-168
    - BO-169
    - BO-170
    - BO-171
    - BO-172
    - BO-173
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-165
  name: Dynamic QR Security Profile Builder
  module: Access & Venue
  requiresModule: access
  purpose: Configure how a dynamic QR is generated and protected. 30 | Pag e The matrix requires a unique
    QR per issued ticket/pass and periodic QR refresh to reduce screenshot and duplication fraud.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first dynamic entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setDynamicSecurityProfile
    contract: access
    purpose: Dynamic QR Security Profile Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/dynamic-qr-security-profile-builder-bo-165
    component: apps/venue-management-web/src/routes/access-venue/DynamicQrSecurityProfileBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-165
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-165
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.2'
    page: 30
  navigation:
    entryFrom:
    - BO-164
    exitTo:
    - BO-164
    inferred: false
    notes: '**Reached from BO-164, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-166
  name: Credential Activation & Display Rules
  module: Access & Venue
  requiresModule: access
  purpose: Configure when the guest is permitted to see/use the credential. The matrix specifies that
    after registration, a ticket may appear as a blurred QR and only become clear and usable near the
    park entrance.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialActivationDisplay
    contract: access
    purpose: Credential Activation & Display Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-activation-display-rules-bo-166
    component: apps/venue-management-web/src/routes/access-venue/CredentialActivationDisplayRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-166
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-166
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.3'
    page: 32
  navigation:
    entryFrom:
    - BO-164
    exitTo:
    - BO-164
    inferred: false
    notes: '**Reached from BO-164, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-167
  name: Device Binding & Session Security
  module: Access & Venue
  requiresModule: access
  purpose: Prevent one credential from being shared across unauthorized devices. The source explicitly
    requires tickets to be linked to a specific device/user and suspicious patterns such as device sharing
    and multiple simultaneous sessions to be dete
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first device entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listDeviceBindingSession
    contract: access
    purpose: Device Binding & Session Security
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/device-binding-session-security-bo-167
    component: apps/venue-management-web/src/routes/access-venue/DeviceBindingSessionSecurity.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-167
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-167
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.4'
    page: 33
  navigation:
    entryFrom:
    - BO-164
    exitTo:
    - BO-164
    inferred: false
    notes: '**Reached from BO-164, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-168
  name: BLE Beacon & Geofence Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Configure location-aware credential activation. This is a major requirement under 3.1.9. The
    matrix requires BLE beacon proximity and geofence boundaries to activate/deactivate credentials at
    venue, attraction, zone and gate level.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first ble entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setBleBeaconGeofence
    contract: access
    purpose: BLE Beacon & Geofence Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/ble-beacon-geofence-configuration-bo-168
    component: apps/venue-management-web/src/routes/access-venue/BleBeaconGeofenceConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-168
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-168
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.5'
    page: 34
  navigation:
    entryFrom:
    - BO-164
    exitTo:
    - BO-164
    inferred: false
    notes: '**Reached from BO-164, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-169
  name: Credential Transfer & Rebinding
  module: Access & Venue
  requiresModule: access
  purpose: Securely manage digital-ticket transfers. The matrix requires tickets to be transferable through
    email/app, with the recipient required to authenticate before accessing and activating the transferred
    QR.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialTransferRebinding
    contract: access
    purpose: Credential Transfer & Rebinding
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-transfer-rebinding-bo-169
    component: apps/venue-management-web/src/routes/access-venue/CredentialTransferRebinding.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-169
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-169
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.6'
    page: 35
  navigation:
    entryFrom:
    - BO-164
    exitTo:
    - BO-164
    inferred: false
    notes: '**Reached from BO-164, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-170
  name: Credential Revocation & Lifecycle Events
  module: Access & Venue
  requiresModule: access
  purpose: Immediately invalidate credentials when the underlying ticket changes. Requirement 3.1.4 specifically
    requires dynamic QR invalidation after refunds, cancellations, transfers, exchanges, upgrades or reissues.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialRevocationLifecycle
    contract: access
    purpose: Credential Revocation & Lifecycle Events
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-revocation-lifecycle-events-bo-170
    component: apps/venue-management-web/src/routes/access-venue/CredentialRevocationLifecycleEvents.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-170
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-170
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.7'
    page: 36
  navigation:
    entryFrom:
    - BO-164
    exitTo:
    - BO-164
    inferred: false
    notes: '**Reached from BO-164, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-171
  name: Offline Cryptographic Validation Profile
  module: Access & Venue
  requiresModule: access
  purpose: Allow access devices to validate secure credentials without continuous backend connectivity.
    The matrix explicitly requires offline cryptographic validation and embedded entitlement validation
    without real-time backend connectivity.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first offline entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOfflineCryptographicValidation
    contract: access
    purpose: Offline Cryptographic Validation Profile
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/offline-cryptographic-validation-profile-bo-171
    component: apps/venue-management-web/src/routes/access-venue/OfflineCryptographicValidationProfile.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-171
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-171
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.8'
    page: 37
  navigation:
    entryFrom:
    - BO-164
    exitTo:
    - BO-164
    inferred: false
    notes: '**Reached from BO-164, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-172
  name: Embedded Entitlement Payload Designer
  module: Access & Venue
  requiresModule: access
  purpose: Determine what operational information can be securely carried by the credential for offline
    decisions. The matrix permits embedded information including ticket type, seat assignment, event ID,
    venue access rights, timeslot, reservations, l
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first embedded entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setEmbeddedEntitlementPayload
    contract: access
    purpose: Embedded Entitlement Payload Designer
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/embedded-entitlement-payload-designer-bo-172
    component: apps/venue-management-web/src/routes/access-venue/EmbeddedEntitlementPayloadDesigner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-172
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-172
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.9'
    page: 38
  navigation:
    entryFrom:
    - BO-164
    exitTo:
    - BO-164
    inferred: false
    notes: '**Reached from BO-164, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-173
  name: Credential Security Simulation, Audit & Publication
  module: Access & Venue
  requiresModule: access
  purpose: Test the complete secure credential lifecycle before production deployment.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: timeline
        notes: What changed, when, and by whom
      - kind: searchField
        notes: Find a change
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialSecurity
    contract: access
    purpose: Credential Security Simulation, Audit & Publication
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-security-simulation-audit-publication-bo-173
    component: apps/venue-management-web/src/routes/access-venue/CredentialSecuritySimulationAuditPublication.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-173
    workshopBoard: wireframes/WS03 Access Control Board 3.dc.html#bo-173
  source:
    pack: Access Control Module_Reference.pdf
    board: '3'
    number: '3.10'
    page: 39
  navigation:
    entryFrom:
    - BO-164
    exitTo:
    - BO-164
    inferred: false
    notes: '**Reached from BO-164, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-174
  name: Media & Credential Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Central management page for every media and verification technology supported by Access Control.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMediaCredential
    contract: access
    purpose: Media & Credential Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/media-credential-command-center-bo-174
    component: apps/venue-management-web/src/routes/access-venue/MediaCredentialCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-174
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-174
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.1'
    page: 44
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-175
    - BO-176
    - BO-177
    - BO-178
    - BO-179
    - BO-180
    - BO-181
    - BO-182
    - BO-183
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-175
  name: Media Type & Technology Library
  module: Access & Venue
  requiresModule: access
  purpose: Define reusable media technologies. The matrix expects support for linear barcode, two-dimensional
    codes, magnetic strips, contact/proximity RFID, NFC and biometric readers.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMediaTypeTechnology
    contract: access
    purpose: Media Type & Technology Library
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/media-type-technology-library-bo-175
    component: apps/venue-management-web/src/routes/access-venue/MediaTypeTechnologyLibrary.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-175
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-175
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.2'
    page: 45
  navigation:
    entryFrom:
    - BO-174
    exitTo:
    - BO-174
    inferred: false
    notes: '**Reached from BO-174, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-176
  name: Virtual Credential & Media Association
  module: Access & Venue
  requiresModule: access
  purpose: Associate one virtual ticket identity with its permitted media representations.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first virtual entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listVirtualCredentialMedia
    contract: access
    purpose: Virtual Credential & Media Association
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/virtual-credential-media-association-bo-176
    component: apps/venue-management-web/src/routes/access-venue/VirtualCredentialMediaAssociation.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-176
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-176
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.3'
    page: 46
  navigation:
    entryFrom:
    - BO-174
    exitTo:
    - BO-174
    inferred: false
    notes: '**Reached from BO-174, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-177
  name: Verification Method Selection & Locking
  module: Access & Venue
  requiresModule: access
  purpose: Control which verification method the guest chooses and when it becomes locked. The matrix
    specifies that although a ticket may technically support physical card, Dynamic QR, Face Pass and
    Face Tag, the customer should select one verificati
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first verification entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listVerificationMethodSelection
    contract: access
    purpose: Verification Method Selection & Locking
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/verification-method-selection-locking-bo-177
    component: apps/venue-management-web/src/routes/access-venue/VerificationMethodSelectionLocking.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-177
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-177
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.4'
    page: 47
  navigation:
    entryFrom:
    - BO-174
    exitTo:
    - BO-174
    inferred: false
    notes: '**Reached from BO-174, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-178
  name: Media Issuance & Encoding Profile
  module: Access & Venue
  requiresModule: access
  purpose: Configure how ticket identity is written or encoded onto each medium. The source requires ticket
    IDs to be generated as 2D barcode, QR or RFID and requires randomized, always- unique identifiers
    to reduce fraud.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMediaIssuanceEncoding
    contract: access
    purpose: Media Issuance & Encoding Profile
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/media-issuance-encoding-profile-bo-178
    component: apps/venue-management-web/src/routes/access-venue/MediaIssuanceEncodingProfile.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-178
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-178
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.5'
    page: 48
  navigation:
    entryFrom:
    - BO-174
    exitTo:
    - BO-174
    inferred: false
    notes: '**Reached from BO-174, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-179
  name: Media Swap & Replacement
  module: Access & Venue
  requiresModule: access
  purpose: Transfer a ticket from one medium to another without changing the underlying virtual ticket.
    This directly covers the matrix requirement to swap RFID to barcode/QR or vice versa while transferring
    the attached information.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMediaSwapReplacement
    contract: access
    purpose: Media Swap & Replacement
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/media-swap-replacement-bo-179
    component: apps/venue-management-web/src/routes/access-venue/MediaSwapReplacement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-179
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-179
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.6'
    page: 49
  navigation:
    entryFrom:
    - BO-174
    exitTo:
    - BO-174
    inferred: false
    notes: '**Reached from BO-174, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-180
  name: RFID & NFC Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Provide dedicated configuration for proximity credentials. The matrix requires RFID—including
    ISO 15693—and NFC, as well as multi-range RFID scanning at near, medium and far ranges.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first rfid entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setRfidNfc
    contract: access
    purpose: RFID & NFC Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/rfid-nfc-configuration-bo-180
    component: apps/venue-management-web/src/routes/access-venue/RfidNfcConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-180
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-180
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.7'
    page: 50
  navigation:
    entryFrom:
    - BO-174
    exitTo:
    - BO-174
    inferred: false
    notes: '**Reached from BO-174, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-181
  name: External & Partner Credential Mapping
  module: Access & Venue
  requiresModule: access
  purpose: Allow TICVAI Access Control to understand credentials generated by other systems. The matrix
    explicitly requires reading reseller/external partner ticket formats and barcodes generated by other
    systems.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first external entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listExternalPartnerCredential
    contract: access
    purpose: External & Partner Credential Mapping
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/external-partner-credential-mapping-bo-181
    component: apps/venue-management-web/src/routes/access-venue/ExternalPartnerCredentialMapping.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-181
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-181
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.8'
    page: 51
  navigation:
    entryFrom:
    - BO-174
    exitTo:
    - BO-174
    inferred: false
    notes: '**Reached from BO-174, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-182
  name: Hotel, Wallet & External Media Integration
  module: Access & Venue
  requiresModule: access
  purpose: Configure specialized external credential ecosystems. The source specifically requires hotel
    room-card integration at access points and an interface to the hotel's property-management system
    for room billing.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first hotel, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listHotelWalletExternal
    contract: access
    purpose: Hotel, Wallet & External Media Integration
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/hotel-wallet-external-media-integration-bo-182
    component: apps/venue-management-web/src/routes/access-venue/HotelWalletExternalMediaIntegration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-182
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-182
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.9'
    page: 53
  navigation:
    entryFrom:
    - BO-174
    exitTo:
    - BO-174
    inferred: false
    notes: '**Reached from BO-174, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-183
  name: Media Compatibility, Testing & Publication
  module: Access & Venue
  requiresModule: access
  purpose: Ensure that every configured medium works with the intended access-control hardware before
    deployment. This is particularly important because the matrix says the solution should operate with
    hardware selected by the venue, while hardware li
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
      - kind: publishGate
        impliedBy: publishMediaCompatibilityTesting
        notes: >
          Declares `publishMediaCompatibilityTesting`. **The gate names what the publish will affect before it
          happens** — a disabled Publish with no reason is the state operators escalate.
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: publishMediaCompatibilityTesting
    contract: access
    purpose: Media Compatibility, Testing & Publication
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/media-compatibility-testing-publication-bo-183
    component: apps/venue-management-web/src/routes/access-venue/MediaCompatibilityTestingPublication.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-183
    workshopBoard: wireframes/WS04 Access Control Board 4.dc.html#bo-183
  source:
    pack: Access Control Module_Reference.pdf
    board: '4'
    number: '4.10'
    page: 54
  navigation:
    entryFrom:
    - BO-174
    exitTo:
    - BO-174
    inferred: false
    notes: '**Reached from BO-174, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-184
  name: Biometric Access Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Central dashboard for biometric access configuration and operational health.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Active Face Pass Profiles
      - kind: metricTile
        notes: Active Face Tags
      - kind: metricTile
        notes: Enrollments Today
      - kind: metricTile
        notes: Successful Face Verifications
      - kind: metricTile
        notes: Failed Verifications
      - kind: metricTile
        notes: Manual Reviews
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first biometric entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listBiometricAccess
    contract: access
    purpose: Biometric Access Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/biometric-access-command-center-bo-184
    component: apps/venue-management-web/src/routes/access-venue/BiometricAccessCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-184
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-184
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.1'
    page: 56
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-185
    - BO-186
    - BO-187
    - BO-188
    - BO-189
    - BO-190
    - BO-191
    - BO-192
    - BO-193
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-185
  name: Biometric Verification Profile Builder
  module: Access & Venue
  requiresModule: access
  purpose: Configure which ticket/credential types can or must use biometric verification. The matrix
    specifically requires biometric checks to be configurable by ticket type, including memberships, annual
    passes, multi-day and multi-attraction produc
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first biometric entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setBiometricVerificationProfile
    contract: access
    purpose: Biometric Verification Profile Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/biometric-verification-profile-builder-bo-185
    component: apps/venue-management-web/src/routes/access-venue/BiometricVerificationProfileBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-185
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-185
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.2'
    page: 58
  navigation:
    entryFrom:
    - BO-184
    exitTo:
    - BO-184
    inferred: false
    notes: '**Reached from BO-184, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-186
  name: Face Pass Enrollment Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Configure persistent Face Pass registration. The source specifies that Face Pass may be registered
    through the App, ticket counters or Annual Pass counter.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first face entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setFacePassEnrollment
    contract: access
    purpose: Face Pass Enrollment Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/face-pass-enrollment-configuration-bo-186
    component: apps/venue-management-web/src/routes/access-venue/FacePassEnrollmentConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-186
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-186
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.3'
    page: 59
  navigation:
    entryFrom:
    - BO-184
    exitTo:
    - BO-184
    inferred: false
    notes: '**Reached from BO-184, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-187
  name: Biometric Consent & Guardian Management
  module: Access & Venue
  requiresModule: access
  purpose: Manage consent requirements associated with persistent biometric enrollment. The matrix requires
    App users to provide consent before Face Pass registration and requires guardian consent for minors.
    On-site enrollment also requires consent b
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first biometric entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listBiometricConsentGuardian
    contract: access
    purpose: Biometric Consent & Guardian Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/biometric-consent-guardian-management-bo-187
    component: apps/venue-management-web/src/routes/access-venue/BiometricConsentGuardianManagement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-187
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-187
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.4'
    page: 60
  navigation:
    entryFrom:
    - BO-184
    exitTo:
    - BO-184
    inferred: false
    notes: '**Reached from BO-184, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-188
  name: Face Tag Temporary Enrollment
  module: Access & Venue
  requiresModule: access
  purpose: Configure the temporary biometric model separately from Face Pass. The matrix describes Face
    Tag as temporarily stored facial data, enrollable at ticket counters or entry gates, with biometric
    data permanently deleted once the associated ti
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first face entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listFaceTagTemporary
    contract: access
    purpose: Face Tag Temporary Enrollment
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/face-tag-temporary-enrollment-bo-188
    component: apps/venue-management-web/src/routes/access-venue/FaceTagTemporaryEnrollment.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-188
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-188
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.5'
    page: 61
  navigation:
    entryFrom:
    - BO-184
    exitTo:
    - BO-184
    inferred: false
    notes: '**Reached from BO-184, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-189
  name: Face Matching & Verification Thresholds
  module: Access & Venue
  requiresModule: access
  purpose: Configure biometric verification behavior. The source requires a configurable Biometric Check
    Level determining the scoring of biometric comparison.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first face entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listFaceMatchingVerification
    contract: access
    purpose: Face Matching & Verification Thresholds
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/face-matching-verification-thresholds-bo-189
    component: apps/venue-management-web/src/routes/access-venue/FaceMatchingVerificationThresholds.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-189
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-189
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.6'
    page: 62
  navigation:
    entryFrom:
    - BO-184
    exitTo:
    - BO-184
    inferred: false
    notes: '**Reached from BO-184, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-190
  name: Face Change, Re-enrollment & Identity Protection
  module: Access & Venue
  requiresModule: access
  purpose: Prevent guests from replacing a registered biometric identity with another person's face. The
    source explicitly states that customers may re-register Face Pass, but the system must compare the
    new facial data with the previous profile. If t
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first face entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listFaceChangeEnrollment
    contract: access
    purpose: Face Change, Re-enrollment & Identity Protection
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/face-change-re-enrollment-identity-protection-bo-190
    component: apps/venue-management-web/src/routes/access-venue/FaceChangeReEnrollmentIdentityProtection.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-190
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-190
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.7'
    page: 63
  navigation:
    entryFrom:
    - BO-184
    exitTo:
    - BO-184
    inferred: false
    notes: '**Reached from BO-184, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-191
  name: Biometric Validation at Gate
  module: Access & Venue
  requiresModule: access
  purpose: Configure how facial verification interacts with the physical access-control journey.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first biometric entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listBiometricValidationGate
    contract: access
    purpose: Biometric Validation at Gate
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/biometric-validation-at-gate-bo-191
    component: apps/venue-management-web/src/routes/access-venue/BiometricValidationAtGate.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-191
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-191
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.8'
    page: 65
  navigation:
    entryFrom:
    - BO-184
    exitTo:
    - BO-184
    inferred: false
    notes: '**Reached from BO-184, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-192
  name: Biometric Lifecycle, Retention & Deletion
  module: Access & Venue
  requiresModule: access
  purpose: Manage biometric-data lifecycle and deletion rules.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first biometric entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listBiometricLifecycleRetention
    contract: access
    purpose: Biometric Lifecycle, Retention & Deletion
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/biometric-lifecycle-retention-deletion-bo-192
    component: apps/venue-management-web/src/routes/access-venue/BiometricLifecycleRetentionDeletion.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-192
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-192
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.9'
    page: 66
  navigation:
    entryFrom:
    - BO-184
    exitTo:
    - BO-184
    inferred: false
    notes: '**Reached from BO-184, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-193
  name: Biometric Simulation, Audit & Publication
  module: Access & Venue
  requiresModule: access
  purpose: Test biometric configurations before live deployment.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: timeline
        notes: What changed, when, and by whom
      - kind: searchField
        notes: Find a change
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first biometric entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listBiometric
    contract: access
    purpose: Biometric Simulation, Audit & Publication
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/biometric-simulation-audit-publication-bo-193
    component: apps/venue-management-web/src/routes/access-venue/BiometricSimulationAuditPublication.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-193
    workshopBoard: wireframes/WS05 Access Control Board 5.dc.html#bo-193
  source:
    pack: Access Control Module_Reference.pdf
    board: '5'
    number: '5.10'
    page: 67
  navigation:
    entryFrom:
    - BO-184
    exitTo:
    - BO-184
    inferred: false
    notes: '**Reached from BO-184, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-194
  name: Device & Gate Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Provide the central operational/configuration view of the complete access-control hardware
    estate.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Total Devices
      - kind: metricTile
        notes: Online
      - kind: metricTile
        notes: Offline
      - kind: metricTile
        notes: 70 | Pag e
      - kind: metricTile
        notes: Degraded
      - kind: metricTile
        notes: Turnstiles
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first device entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listDeviceGate
    contract: access
    purpose: Device & Gate Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/device-gate-command-center-bo-194
    component: apps/venue-management-web/src/routes/access-venue/DeviceGateCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-194
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-194
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.1'
    page: 70
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-195
    - BO-196
    - BO-197
    - BO-198
    - BO-199
    - BO-200
    - BO-201
    - BO-202
    - BO-203
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-195
  name: Device Type & Hardware Library
  module: Access & Venue
  requiresModule: access
  purpose: Create reusable hardware definitions independently from physical deployed devices.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first device entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listDeviceTypeHardware
    contract: access
    purpose: Device Type & Hardware Library
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/device-type-hardware-library-bo-195
    component: apps/venue-management-web/src/routes/access-venue/DeviceTypeHardwareLibrary.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-195
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-195
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.2'
    page: 72
  navigation:
    entryFrom:
    - BO-194
    exitTo:
    - BO-194
    inferred: false
    notes: '**Reached from BO-194, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-196
  name: Physical Device Registration & Provisioning
  module: Access & Venue
  requiresModule: access
  purpose: Register actual deployed hardware and connect it to the Board 1 topology.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first physical entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listPhysicalDeviceRegistration
    contract: access
    purpose: Physical Device Registration & Provisioning
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/physical-device-registration-provisioning-bo-196
    component: apps/venue-management-web/src/routes/access-venue/PhysicalDeviceRegistrationProvisioning.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-196
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-196
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.3'
    page: 73
  navigation:
    entryFrom:
    - BO-194
    exitTo:
    - BO-194
    inferred: false
    notes: '**Reached from BO-194, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-197
  name: Turnstile & Lane Behavior Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Configure how each turnstile or lane behaves. The matrix specifically requires software on
    turnstiles to behave differently according to card/ticket type and supports different operating modes.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first turnstile entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setTurnstileLaneBehavior
    contract: access
    purpose: Turnstile & Lane Behavior Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/turnstile-lane-behavior-configuration-bo-197
    component: apps/venue-management-web/src/routes/access-venue/TurnstileLaneBehaviorConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-197
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-197
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.4'
    page: 74
  navigation:
    entryFrom:
    - BO-194
    exitTo:
    - BO-194
    inferred: false
    notes: '**Reached from BO-194, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-198
  name: Validation Outcome & Guest Feedback Designer
  module: Access & Venue
  requiresModule: access
  purpose: Configure what the physical access device does and displays after validation. The matrix explicitly
    requires valid/non-valid messages, lights, pictograms and sounds, including green/yellow/red behavior.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first validation entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setValidationOutcomeGuest
    contract: access
    purpose: Validation Outcome & Guest Feedback Designer
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/validation-outcome-guest-feedback-designer-bo-198
    component: apps/venue-management-web/src/routes/access-venue/ValidationOutcomeGuestFeedbackDesigner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-198
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-198
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.5'
    page: 76
  navigation:
    entryFrom:
    - BO-194
    exitTo:
    - BO-194
    inferred: false
    notes: '**Reached from BO-194, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-199
  name: Reader, Scanner & Peripheral Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Configure the technologies attached to a gate/device.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first reader, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setReaderScannerPeripheral
    contract: access
    purpose: Reader, Scanner & Peripheral Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/reader-scanner-peripheral-configuration-bo-199
    component: apps/venue-management-web/src/routes/access-venue/ReaderScannerPeripheralConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-199
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-199
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.6'
    page: 77
  navigation:
    entryFrom:
    - BO-194
    exitTo:
    - BO-194
    inferred: false
    notes: '**Reached from BO-194, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-200
  name: Handheld & Mobile Access Device Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Configure mobile access-control devices used by staff. The matrix explicitly requires handheld
    devices and Android/iOS dedicated mobile applications.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first handheld entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setHandheldMobileAccess
    contract: access
    purpose: Handheld & Mobile Access Device Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/handheld-mobile-access-device-configuration-bo-200
    component: apps/venue-management-web/src/routes/access-venue/HandheldMobileAccessDeviceConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-200
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-200
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.7'
    page: 78
  navigation:
    entryFrom:
    - BO-194
    exitTo:
    - BO-194
    inferred: false
    notes: '**Reached from BO-194, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-201
  name: Gate Modes, Free Spin & Emergency Controls
  module: Access & Venue
  requiresModule: access
  purpose: Manage non-standard operational modes. The source specifically requires Free Spin and Drop
    Arm/Emergency behavior.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first gate entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGateModeFree
    contract: access
    purpose: Gate Modes, Free Spin & Emergency Controls
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/gate-modes-free-spin-emergency-controls-bo-201
    component: apps/venue-management-web/src/routes/access-venue/GateModesFreeSpinEmergencyControls.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-201
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-201
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.8'
    page: 79
  navigation:
    entryFrom:
    - BO-194
    exitTo:
    - BO-194
    inferred: false
    notes: '**Reached from BO-194, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-202
  name: Device Software, Content & Remote Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Centrally control access-control device software and guest-facing configuration. The source
    requires the ability to configure/manage software on turnstiles, add external webpages on supported
    screens, and enable payment technologies where a
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first device entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setDeviceSoftwareContent
    contract: access
    purpose: Device Software, Content & Remote Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/device-software-content-remote-configuration-bo-202
    component: apps/venue-management-web/src/routes/access-venue/DeviceSoftwareContentRemoteConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-202
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-202
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.9'
    page: 81
  navigation:
    entryFrom:
    - BO-194
    exitTo:
    - BO-194
    inferred: false
    notes: '**Reached from BO-194, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-203
  name: Hardware Compatibility, Health, Testing & Deployment
  module: Access & Venue
  requiresModule: access
  purpose: Provide the final testing and governance layer before hardware is used in production. The matrix
    says venues may select their hardware, while the provider must expose hardware limitations and recommendations.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first hardware entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listHardwareCompatibilityHealth
    contract: access
    purpose: Hardware Compatibility, Health, Testing & Deployment
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/hardware-compatibility-health-testing-deployment-bo-203
    component: apps/venue-management-web/src/routes/access-venue/HardwareCompatibilityHealthTestingDeployment.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-203
    workshopBoard: wireframes/WS06 Access Control Board 6.dc.html#bo-203
  source:
    pack: Access Control Module_Reference.pdf
    board: '6'
    number: '6.10'
    page: 82
  navigation:
    entryFrom:
    - BO-194
    exitTo:
    - BO-194
    inferred: false
    notes: '**Reached from BO-194, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-204
  name: Offline & Edge Operations Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Provide a real-time overview of offline readiness across the entire access-control estate.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Offline-Ready Devices
      - kind: metricTile
        notes: Currently Online
      - kind: metricTile
        notes: Currently Offline
      - kind: metricTile
        notes: Devices in Degraded Mode
      - kind: metricTile
        notes: Edge Nodes Online
      - kind: metricTile
        notes: Packages Current
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first offline entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOfflineEdge
    contract: access
    purpose: Offline & Edge Operations Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/offline-edge-operations-command-center-bo-204
    component: apps/venue-management-web/src/routes/access-venue/OfflineEdgeOperationsCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-204
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-204
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.1'
    page: 86
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-205
    - BO-206
    - BO-207
    - BO-208
    - BO-209
    - BO-210
    - BO-211
    - BO-212
    - BO-213
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-205
  name: Edge Node & Local Processing Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Configure where local access decisions are processed when central services cannot be reached.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first edge entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setEdgeNodeLocal
    contract: access
    purpose: Edge Node & Local Processing Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/edge-node-local-processing-configuration-bo-205
    component: apps/venue-management-web/src/routes/access-venue/EdgeNodeLocalProcessingConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-205
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-205
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.2'
    page: 87
  navigation:
    entryFrom:
    - BO-204
    exitTo:
    - BO-204
    inferred: false
    notes: '**Reached from BO-204, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-206
  name: Offline Validation Policy Builder
  module: Access & Venue
  requiresModule: access
  purpose: Define exactly which access checks are allowed to run locally. The matrix identifies key offline
    criteria including eligible media, eligible site, eligible time, ticket validity and eligible access
    mode.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first offline entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setOfflinePolicy
    contract: tenancy
    purpose: setOfflinePolicy
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/offline-validation-policy-builder-bo-206
    component: apps/venue-management-web/src/routes/access-venue/OfflineValidationPolicyBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-206
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-206
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.3'
    page: 88
  navigation:
    entryFrom:
    - BO-204
    exitTo:
    - BO-204
    inferred: false
    notes: '**Reached from BO-204, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-207
  name: Edge Package & Data Distribution
  module: Access & Venue
  requiresModule: access
  purpose: Define what configuration and operational data is securely distributed to edge devices.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first edge entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listEdgePackageData
    contract: access
    purpose: Edge Package & Data Distribution
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/edge-package-data-distribution-bo-207
    component: apps/venue-management-web/src/routes/access-venue/EdgePackageDataDistribution.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-207
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-207
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.4'
    page: 89
  navigation:
    entryFrom:
    - BO-204
    exitTo:
    - BO-204
    inferred: false
    notes: '**Reached from BO-204, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-208
  name: Offline Credential & Revocation Cache
  module: Access & Venue
  requiresModule: access
  purpose: Manage the local information required to reject credentials that should no longer be usable.
    This is especially important because Board 3 requires refunded, cancelled, transferred, exchanged,
    upgraded and reissued credentials to be invalida
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first offline entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOfflineCredentialRevocation
    contract: access
    purpose: Offline Credential & Revocation Cache
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/offline-credential-revocation-cache-bo-208
    component: apps/venue-management-web/src/routes/access-venue/OfflineCredentialRevocationCache.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-208
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-208
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.5'
    page: 90
  navigation:
    entryFrom:
    - BO-204
    exitTo:
    - BO-204
    inferred: false
    notes: '**Reached from BO-204, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-209
  name: Offline Entitlement & Usage Ledger
  module: Access & Venue
  requiresModule: access
  purpose: 'Track entitlement consumption while the central system is unavailable. This is necessary for
    tickets such as: 3 Fast Pass uses 1 park entry 1 meal 1 re-entry where usage can occur during an outage.'
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first offline entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOfflineEntitlementUsage
    contract: access
    purpose: Offline Entitlement & Usage Ledger
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/offline-entitlement-usage-ledger-bo-209
    component: apps/venue-management-web/src/routes/access-venue/OfflineEntitlementUsageLedger.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-209
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-209
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.6'
    page: 91
  navigation:
    entryFrom:
    - BO-204
    exitTo:
    - BO-204
    inferred: false
    notes: '**Reached from BO-204, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-210
  name: Connectivity Failure & Degraded Mode Policy
  module: Access & Venue
  requiresModule: access
  purpose: Configure how devices transition from normal online operation to offline/degraded operation.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first connectivity entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listConnectivityFailureDegraded
    contract: access
    purpose: Connectivity Failure & Degraded Mode Policy
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/connectivity-failure-degraded-mode-policy-bo-210
    component: apps/venue-management-web/src/routes/access-venue/ConnectivityFailureDegradedModePolicy.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-210
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-210
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.7'
    page: 93
  navigation:
    entryFrom:
    - BO-204
    exitTo:
    - BO-204
    inferred: false
    notes: '**Reached from BO-204, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-211
  name: Reconnection, Synchronization & Conflict Resolution
  module: Access & Venue
  requiresModule: access
  purpose: Synchronize everything that occurred offline when connectivity returns.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first reconnection, entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listReconnectionSynchronizationConflict
    contract: access
    purpose: Reconnection, Synchronization & Conflict Resolution
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/reconnection-synchronization-conflict-resolution-bo-211
    component: apps/venue-management-web/src/routes/access-venue/ReconnectionSynchronizationConflictResolution.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-211
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-211
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.8'
    page: 94
  navigation:
    entryFrom:
    - BO-204
    exitTo:
    - BO-204
    inferred: false
    notes: '**Reached from BO-204, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-212
  name: Offline Simulation & Resilience Testing
  module: Access & Venue
  requiresModule: access
  purpose: Allow venues to prove that their access environment will survive outages before opening to
    guests.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first offline entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: simulateOfflineResilienceTesting
    contract: access
    purpose: Offline Simulation & Resilience Testing
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/offline-simulation-resilience-testing-bo-212
    component: apps/venue-management-web/src/routes/access-venue/OfflineSimulationResilienceTesting.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-212
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-212
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.9'
    page: 95
  navigation:
    entryFrom:
    - BO-204
    exitTo:
    - BO-204
    inferred: false
    notes: '**Reached from BO-204, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-213
  name: Edge Security, Audit & Deployment
  module: Access & Venue
  requiresModule: access
  purpose: Govern the complete offline/edge environment.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: timeline
        notes: What changed, when, and by whom
      - kind: searchField
        notes: Find a change
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first edge entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listEdgeSecurityDeployment
    contract: access
    purpose: Edge Security, Audit & Deployment
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/edge-security-audit-deployment-bo-213
    component: apps/venue-management-web/src/routes/access-venue/EdgeSecurityAuditDeployment.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-213
    workshopBoard: wireframes/WS07 Access Control Board 7.dc.html#bo-213
  source:
    pack: Access Control Module_Reference.pdf
    board: '7'
    number: '7.10'
    page: 96
  navigation:
    entryFrom:
    - BO-204
    exitTo:
    - BO-204
    inferred: false
    notes: '**Reached from BO-204, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-214
  name: Guest Journey Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Central configuration and monitoring screen for all special and multi-person admission journeys.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first guest entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGuestJourney
    contract: access
    purpose: Guest Journey Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/guest-journey-command-center-bo-214
    component: apps/venue-management-web/src/routes/access-venue/GuestJourneyCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-214
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-214
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.1'
    page: 101
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-215
    - BO-216
    - BO-217
    - BO-218
    - BO-219
    - BO-220
    - BO-221
    - BO-222
    - BO-223
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-215
  name: Group & B2B Admission Profile Builder
  module: Access & Venue
  requiresModule: access
  purpose: Group & B2B Admission Profile Builder
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setGroupAdmissionProfile
    contract: access
    purpose: Group & B2B Admission Profile Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/group-b2b-admission-profile-builder-bo-215
    component: apps/venue-management-web/src/routes/access-venue/GroupB2bAdmissionProfileBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-215
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-215
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.2'
    page: 102
  navigation:
    entryFrom:
    - BO-214
    exitTo:
    - BO-214
    inferred: false
    notes: '**Reached from BO-214, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-216
  name: Group Leader & Fast B2B Validation
  module: Access & Venue
  requiresModule: access
  purpose: Solve the specific matrix requirement for faster entrance flow when large B2B groups have multiple
    tickets stored on one device.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupLeaderFast
    contract: access
    purpose: Group Leader & Fast B2B Validation
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/group-leader-fast-b2b-validation-bo-216
    component: apps/venue-management-web/src/routes/access-venue/GroupLeaderFastB2bValidation.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-216
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-216
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.3'
    page: 103
  navigation:
    entryFrom:
    - BO-214
    exitTo:
    - BO-214
    inferred: false
    notes: '**Reached from BO-214, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-217
  name: Group Attendance & Partial Entry Manager
  module: Access & Venue
  requiresModule: access
  purpose: Manage actual attendance when fewer guests arrive than the quantity purchased. The matrix explicitly
    requires the scanner to show the exact group size and allow the operator to enter actual attendants
    so daily attendance is updated correctl
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupAttendancePartial
    contract: access
    purpose: Group Attendance & Partial Entry Manager
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/group-attendance-partial-entry-manager-bo-217
    component: apps/venue-management-web/src/routes/access-venue/GroupAttendancePartialEntryManager.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-217
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-217
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.4'
    page: 104
  navigation:
    entryFrom:
    - BO-214
    exitTo:
    - BO-214
    inferred: false
    notes: '**Reached from BO-214, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-218
  name: Family, Child, POD & Companion Journey
  module: Access & Venue
  requiresModule: access
  purpose: Configure linked-person access journeys. The matrix requires child protection through adult-ticket
    pairing or biometric validation of the assigned adult. It also requires POD accompanying persons and
    nannies to be bound to a primary guest a
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first family, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listFamilyChildPod
    contract: access
    purpose: Family, Child, POD & Companion Journey
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/family-child-pod-companion-journey-bo-218
    component: apps/venue-management-web/src/routes/access-venue/FamilyChildPodCompanionJourney.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-218
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-218
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.5'
    page: 105
  navigation:
    entryFrom:
    - BO-214
    exitTo:
    - BO-214
    inferred: false
    notes: '**Reached from BO-214, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-219
  name: Re-entry & Temporary Exit Journey
  module: Access & Venue
  requiresModule: access
  purpose: Manage guests temporarily leaving and returning to the venue. 106 | Pa ge The source matrix
    requires configurable re-entry and also describes a journey using a designated re-entry gate with
    both ticket verification and a UV stamp.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first re-entry entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listEntryTemporaryExit
    contract: access
    purpose: Re-entry & Temporary Exit Journey
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/re-entry-temporary-exit-journey-bo-219
    component: apps/venue-management-web/src/routes/access-venue/ReEntryTemporaryExitJourney.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-219
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-219
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.6'
    page: 106
  navigation:
    entryFrom:
    - BO-214
    exitTo:
    - BO-214
    inferred: false
    notes: '**Reached from BO-214, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-220
  name: Multi-Park & Crossover Journey Orchestrator
  module: Access & Venue
  requiresModule: access
  purpose: Operationalize the multi-park rules configured in Board 2. The matrix specifically distinguishes
    crossover from normal entry and re-entry and requires crossover to be tracked separately.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first multi-park entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMultiParkCrossover2
    contract: access
    purpose: Multi-Park & Crossover Journey Orchestrator
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/multi-park-crossover-journey-orchestrator-bo-220
    component: apps/venue-management-web/src/routes/access-venue/MultiParkCrossoverJourneyOrchestrator.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-220
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-220
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.7'
    page: 108
  navigation:
    entryFrom:
    - BO-214
    exitTo:
    - BO-214
    inferred: false
    notes: '**Reached from BO-214, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-221
  name: Fast Pass & Attraction Access Journey
  module: Access & Venue
  requiresModule: access
  purpose: Configure the operational experience for limited and unlimited priority-access entitlements.
    The matrix requires Silver Fast Pass to support three accesses and Gold to support unlimited access
    with an optional one-access-per-ride restrictio
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first fast entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listFastPassAttraction
    contract: access
    purpose: Fast Pass & Attraction Access Journey
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/fast-pass-attraction-access-journey-bo-221
    component: apps/venue-management-web/src/routes/access-venue/FastPassAttractionAccessJourney.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-221
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-221
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.8'
    page: 109
  navigation:
    entryFrom:
    - BO-214
    exitTo:
    - BO-214
    inferred: false
    notes: '**Reached from BO-214, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-222
  name: Special Event, Free View & Alternative Admission
  module: Access & Venue
  requiresModule: access
  purpose: Configure temporary/special admission processes that differ from normal venue access. The matrix
    requires special-event products capable of capturing attendance without physical admission, N- person
    attendance entered through a turnstile/ta
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first special entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listSpecialEventFree
    contract: access
    purpose: Special Event, Free View & Alternative Admission
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/special-event-free-view-alternative-admission-bo-222
    component: apps/venue-management-web/src/routes/access-venue/SpecialEventFreeViewAlternativeAdmission.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-222
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-222
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.9'
    page: 110
  navigation:
    entryFrom:
    - BO-214
    exitTo:
    - BO-214
    inferred: false
    notes: '**Reached from BO-214, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-223
  name: Journey Simulation, Audit & Publication
  module: Access & Venue
  requiresModule: access
  purpose: Test an entire guest journey—not merely an individual scan—before deploying it.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: timeline
        notes: What changed, when, and by whom
      - kind: searchField
        notes: Find a change
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first journey entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listJourneys
    contract: marketing-crm
    purpose: Automated journeys
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/journey-simulation-audit-publication-bo-223
    component: apps/venue-management-web/src/routes/access-venue/JourneySimulationAuditPublication.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-223
    workshopBoard: wireframes/WS08 Access Control Board 8.dc.html#bo-223
  source:
    pack: Access Control Module_Reference.pdf
    board: '8'
    number: '8.10'
    page: 111
  navigation:
    entryFrom:
    - BO-214
    exitTo:
    - BO-214
    inferred: false
    notes: '**Reached from BO-214, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-224
  name: Live Access Operations Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Provide the venue control room with a real-time view of access operations across all gates,
    parks, zones, and attractions.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Guests Entered Today
      - kind: metricTile
        notes: Guests Exited
      - kind: metricTile
        notes: Guests Currently In Park
      - kind: metricTile
        notes: Valid Scans
      - kind: metricTile
        notes: Rejected Scans
      - kind: metricTile
        notes: Yellow / Intervention Scans
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first live entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listLiveAccess
    contract: access
    purpose: Live Access Operations Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/live-access-operations-command-center-bo-224
    component: apps/venue-management-web/src/routes/access-venue/LiveAccessOperationsCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-224
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-224
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.1'
    page: 116
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-225
    - BO-226
    - BO-227
    - BO-228
    - BO-229
    - BO-230
    - BO-231
    - BO-232
    - BO-233
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-225
  name: Podium Operations Console
  module: Access & Venue
  requiresModule: access
  purpose: Provide the operational interface described in the matrix for attendants controlling one or
    more turnstiles.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first podium entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listPodiumConsole
    contract: access
    purpose: Podium Operations Console
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/podium-operations-console-bo-225
    component: apps/venue-management-web/src/routes/access-venue/PodiumOperationsConsole.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-225
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-225
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.2'
    page: 117
  navigation:
    entryFrom:
    - BO-224
    exitTo:
    - BO-224
    inferred: false
    notes: '**Reached from BO-224, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-226
  name: Ticket & Credential Investigation Console
  module: Access & Venue
  requiresModule: access
  purpose: Allow operators to quickly investigate why a guest cannot enter.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first ticket entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listTicketCredentialInvestigation
    contract: access
    purpose: Ticket & Credential Investigation Console
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/ticket-credential-investigation-console-bo-226
    component: apps/venue-management-web/src/routes/access-venue/TicketCredentialInvestigationConsole.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-226
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-226
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.3'
    page: 118
  navigation:
    entryFrom:
    - BO-224
    exitTo:
    - BO-224
    inferred: false
    notes: '**Reached from BO-224, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-227
  name: Validation Exception & Reason Code Manager
  module: Access & Venue
  requiresModule: access
  purpose: Standardize what happens when access is not automatically granted. The matrix requires the
    scanner to display a reason code when a ticket is invalid.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first validation entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listValidationExceptionReason
    contract: access
    purpose: Validation Exception & Reason Code Manager
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/validation-exception-reason-code-manager-bo-227
    component: apps/venue-management-web/src/routes/access-venue/ValidationExceptionReasonCodeManager.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-227
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-227
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.4'
    page: 120
  navigation:
    entryFrom:
    - BO-224
    exitTo:
    - BO-224
    inferred: false
    notes: '**Reached from BO-224, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-228
  name: Manual Override & Supervisor Approval
  module: Access & Venue
  requiresModule: access
  purpose: Allow authorized staff to bypass selected access restrictions when operationally justified.
    The matrix explicitly requires Allow Override and operator-based ticket override.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first manual entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: approveManualOverrideSupervisor
    contract: access
    purpose: Manual Override & Supervisor Approval
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/manual-override-supervisor-approval-bo-228
    component: apps/venue-management-web/src/routes/access-venue/ManualOverrideSupervisorApproval.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-228
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-228
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.5'
    page: 121
  navigation:
    entryFrom:
    - BO-224
    exitTo:
    - BO-224
    inferred: false
    notes: '**Reached from BO-224, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-229
  name: Credential Disable, Blacklist & Whitelist Operations
  module: Access & Venue
  requiresModule: access
  purpose: Provide immediate operational security control over individual credentials.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialDisableBlacklist
    contract: access
    purpose: Credential Disable, Blacklist & Whitelist Operations
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-disable-blacklist-whitelist-operations-bo-229
    component: apps/venue-management-web/src/routes/access-venue/CredentialDisableBlacklistWhitelistOperations.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-229
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-229
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.6'
    page: 122
  navigation:
    entryFrom:
    - BO-224
    exitTo:
    - BO-224
    inferred: false
    notes: '**Reached from BO-224, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-230
  name: Live Gate Mode & Lane Control
  module: Access & Venue
  requiresModule: access
  purpose: Allow operations to change access-point modes during live operations without entering the Board
    6 engineering configuration. Board 6 defines which modes a device can support. Board 9 controls which
    permitted mode it is currently running.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first live entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listLiveGateMode
    contract: access
    purpose: Live Gate Mode & Lane Control
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/live-gate-mode-lane-control-bo-230
    component: apps/venue-management-web/src/routes/access-venue/LiveGateModeLaneControl.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-230
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-230
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.7'
    page: 124
  navigation:
    entryFrom:
    - BO-224
    exitTo:
    - BO-224
    inferred: false
    notes: '**Reached from BO-224, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-231
  name: Queue, Throughput & Lane Optimization
  module: Access & Venue
  requiresModule: access
  purpose: Manage entrance flow in real time. This addresses the matrix requirement to improve entrance
    flow and queuing, particularly for large B2B groups.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first queue, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listQueueThroughputLane
    contract: access
    purpose: Queue, Throughput & Lane Optimization
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/queue-throughput-lane-optimization-bo-231
    component: apps/venue-management-web/src/routes/access-venue/QueueThroughputLaneOptimization.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-231
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-231
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.8'
    page: 125
  navigation:
    entryFrom:
    - BO-224
    exitTo:
    - BO-224
    inferred: false
    notes: '**Reached from BO-224, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-232
  name: Operational Incident & Exception Workspace
  module: Access & Venue
  requiresModule: access
  purpose: Manage access incidents that require more than a simple override.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first operational entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setOperationalIncidentException
    contract: access
    purpose: Operational Incident & Exception Workspace
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/operational-incident-exception-workspace-bo-232
    component: apps/venue-management-web/src/routes/access-venue/OperationalIncidentExceptionWorkspace.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-232
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-232
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.9'
    page: 127
  navigation:
    entryFrom:
    - BO-224
    exitTo:
    - BO-224
    inferred: false
    notes: '**Reached from BO-224, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-233
  name: Operations Audit, Shift Handover & Control Summary
  module: Access & Venue
  requiresModule: access
  purpose: Provide full accountability for everything operators and supervisors changed during live access
    operations.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: timeline
        notes: What changed, when, and by whom
      - kind: searchField
        notes: Find a change
  states:
    loading: Skeleton while the configuration loads
    emptyNoEvents: '**The record is complete and nothing has happened yet.** No action: an
      audit trail that offers to create its first entry is offering to author evidence'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listShiftHandoverSummary
    contract: access
    purpose: Operations Audit, Shift Handover & Control Summary
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/operations-audit-shift-handover-control-summary-bo-233
    component: apps/venue-management-web/src/routes/access-venue/OperationsAuditShiftHandoverControlSummary.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-233
    workshopBoard: wireframes/WS09 Access Control Board 9.dc.html#bo-233
  source:
    pack: Access Control Module_Reference.pdf
    board: '9'
    number: '9.10'
    page: 128
  navigation:
    entryFrom:
    - BO-224
    exitTo:
    - BO-224
    inferred: false
    notes: '**Reached from BO-224, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-234
  name: Dynamic Access Policy Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Provide central governance and visibility over all dynamic access policies.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first dynamic entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listDynamicAccessPolicy
    contract: access
    purpose: Dynamic Access Policy Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/dynamic-access-policy-command-center-bo-234
    component: apps/venue-management-web/src/routes/access-venue/DynamicAccessPolicyCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-234
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-234
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.1'
    page: 133
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-235
    - BO-236
    - BO-237
    - BO-238
    - BO-239
    - BO-240
    - BO-241
    - BO-242
    - BO-243
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-235
  name: Access Attribute Catalog
  module: Access & Venue
  requiresModule: access
  purpose: Define the attributes available to the TICVAI policy engine. This becomes the reusable data
    dictionary for dynamic access decisions.
  wave: 3
  density: compact
  layout:
    template: list
    regions:
    - name: contentBody
      components:
      - kind: dataTable
        notes: Every access attribute with its scope.
      - kind: searchField
        notes: Find an attribute
      - kind: primaryButton
        notes: The act the screen exists for.
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessAttributeCatalog
    contract: access
    purpose: Access Attribute Catalog
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-attribute-catalog-bo-235
    component: apps/venue-management-web/src/routes/access-venue/AccessAttributeCatalog.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-235
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-235
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.2'
    page: 134
  navigation:
    entryFrom:
    - BO-234
    exitTo:
    - BO-234
    inferred: false
    notes: '**Reached from BO-234, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-236
  name: Visual Dynamic Policy Builder
  module: Access & Venue
  requiresModule: access
  purpose: Provide a no-code interface for constructing contextual access policies.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first visual entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setVisualDynamicPolicy
    contract: access
    purpose: Visual Dynamic Policy Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/visual-dynamic-policy-builder-bo-236
    component: apps/venue-management-web/src/routes/access-venue/VisualDynamicPolicyBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-236
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-236
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.3'
    page: 136
  navigation:
    entryFrom:
    - BO-234
    exitTo:
    - BO-234
    inferred: false
    notes: '**Reached from BO-234, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-237
  name: Context, Time, Event & Capacity Policy Builder
  module: Access & Venue
  requiresModule: access
  purpose: Configure policies driven by changing venue conditions rather than only guest attributes.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first context, entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setContextTimeEvent
    contract: access
    purpose: Context, Time, Event & Capacity Policy Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/context-time-event-capacity-policy-builder-bo-237
    component: apps/venue-management-web/src/routes/access-venue/ContextTimeEventCapacityPolicyBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-237
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-237
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.4'
    page: 137
  navigation:
    entryFrom:
    - BO-234
    exitTo:
    - BO-234
    inferred: false
    notes: '**Reached from BO-234, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-238
  name: Identity, Membership & Accreditation Policies
  module: Access & Venue
  requiresModule: access
  purpose: Configure policies based on who the requesting person is.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first identity, entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listIdentityMembershipAccreditation
    contract: access
    purpose: Identity, Membership & Accreditation Policies
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/identity-membership-accreditation-policies-bo-238
    component: apps/venue-management-web/src/routes/access-venue/IdentityMembershipAccreditationPolicies.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-238
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-238
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.5'
    page: 139
  navigation:
    entryFrom:
    - BO-234
    exitTo:
    - BO-234
    inferred: false
    notes: '**Reached from BO-234, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-239
  name: Policy Scope, Hierarchy & Inheritance
  module: Access & Venue
  requiresModule: access
  purpose: Govern how policies apply across TICVAI's multi-tenant and multi-venue architecture.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first policy entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listPolicyScopeHierarchy
    contract: access
    purpose: Policy Scope, Hierarchy & Inheritance
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/policy-scope-hierarchy-inheritance-bo-239
    component: apps/venue-management-web/src/routes/access-venue/PolicyScopeHierarchyInheritance.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-239
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-239
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.6'
    page: 140
  navigation:
    entryFrom:
    - BO-234
    exitTo:
    - BO-234
    inferred: false
    notes: '**Reached from BO-234, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-240
  name: Authorization Governance & Temporary Access
  module: Access & Venue
  requiresModule: access
  purpose: Apply enterprise governance principles to privileged and temporary access.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first authorization entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAuthorizationGovernanceTemporary
    contract: access
    purpose: Authorization Governance & Temporary Access
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/authorization-governance-temporary-access-bo-240
    component: apps/venue-management-web/src/routes/access-venue/AuthorizationGovernanceTemporaryAccess.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-240
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-240
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.7'
    page: 142
  navigation:
    entryFrom:
    - BO-234
    exitTo:
    - BO-234
    inferred: false
    notes: '**Reached from BO-234, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-241
  name: Policy Evaluation Architecture & Offline Distribution
  module: Access & Venue
  requiresModule: access
  purpose: Define where and how policies are evaluated. This screen connects Board 10 with the offline/edge
    architecture already configured in Board 7.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first policy entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listPolicyEvaluationArchitecture
    contract: access
    purpose: Policy Evaluation Architecture & Offline Distribution
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/policy-evaluation-architecture-offline-distribution-bo-241
    component: apps/venue-management-web/src/routes/access-venue/PolicyEvaluationArchitectureOfflineDistribution.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-241
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-241
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.8'
    page: 143
  navigation:
    entryFrom:
    - BO-234
    exitTo:
    - BO-234
    inferred: false
    notes: '**Reached from BO-234, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-242
  name: Policy Simulation, Conflict & Impact Analysis
  module: Access & Venue
  requiresModule: access
  purpose: Test policies before they affect live guest admission.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first policy entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: simulatePolicyConflictImpact
    contract: access
    purpose: Policy Simulation, Conflict & Impact Analysis
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/policy-simulation-conflict-impact-analysis-bo-242
    component: apps/venue-management-web/src/routes/access-venue/PolicySimulationConflictImpactAnalysis.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-242
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-242
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.9'
    page: 144
  navigation:
    entryFrom:
    - BO-234
    exitTo:
    - BO-234
    inferred: false
    notes: '**Reached from BO-234, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-243
  name: Policy Approval, Audit, Analytics & AI Optimization
  module: Access & Venue
  requiresModule: access
  purpose: Govern the complete policy lifecycle and continuously measure policy effectiveness.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first policy entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listPolicies
    contract: white-label
    purpose: List legal policies
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/policy-approval-audit-analytics-ai-optimization-bo-243
    component: apps/venue-management-web/src/routes/access-venue/PolicyApprovalAuditAnalyticsAiOptimization.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-243
    workshopBoard: wireframes/WS10 Access Control Board 10.dc.html#bo-243
  source:
    pack: Access Control Module_Reference.pdf
    board: '10'
    number: '10.10'
    page: 146
  navigation:
    entryFrom:
    - BO-234
    exitTo:
    - BO-234
    inferred: false
    notes: '**Reached from BO-234, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-244
  name: Access Security & Fraud Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Provide security teams with a real-time command center for access-related fraud and suspicious
    activity across all venues.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Active Security Alerts
      - kind: metricTile
        notes: High-Risk Credentials
      - kind: metricTile
        notes: Credentials Locked Today
      - kind: metricTile
        notes: Suspicious QR Activity
      - kind: metricTile
        notes: Device-Sharing Alerts
      - kind: metricTile
        notes: Biometric Alerts
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessSecurityFraud
    contract: access
    purpose: Access Security & Fraud Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-security-fraud-command-center-bo-244
    component: apps/venue-management-web/src/routes/access-venue/AccessSecurityFraudCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-244
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-244
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.1'
    page: 151
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-245
    - BO-246
    - BO-247
    - BO-248
    - BO-249
    - BO-250
    - BO-251
    - BO-252
    - BO-253
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-245
  name: Fraud Detection Rule & Signal Library
  module: Access & Venue
  requiresModule: access
  purpose: Configure the signals TICVAI uses to identify suspicious access behavior.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first fraud entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listFraudDetectionRule
    contract: access
    purpose: Fraud Detection Rule & Signal Library
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/fraud-detection-rule-signal-library-bo-245
    component: apps/venue-management-web/src/routes/access-venue/FraudDetectionRuleSignalLibrary.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-245
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-245
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.2'
    page: 152
  navigation:
    entryFrom:
    - BO-244
    exitTo:
    - BO-244
    inferred: false
    notes: '**Reached from BO-244, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-246
  name: Credential Sharing & Concurrent Usage Detection
  module: Access & Venue
  requiresModule: access
  purpose: 'Detect one of the most important access-control fraud scenarios: One valid ticket being shared
    by multiple people or devices.'
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialSharingConcurrent
    contract: access
    purpose: Credential Sharing & Concurrent Usage Detection
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-sharing-concurrent-usage-detection-bo-246
    component: apps/venue-management-web/src/routes/access-venue/CredentialSharingConcurrentUsageDetection.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-246
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-246
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.3'
    page: 154
  navigation:
    entryFrom:
    - BO-244
    exitTo:
    - BO-244
    inferred: false
    notes: '**Reached from BO-244, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-247
  name: Unified Identity & Credential Lock Manager
  module: Access & Venue
  requiresModule: access
  purpose: Provide the single security lock required by the matrix so suspicious activity can immediately
    stop all access associated with an identity.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first unified entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listUnifiedIdentityCredential
    contract: access
    purpose: Unified Identity & Credential Lock Manager
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/unified-identity-credential-lock-manager-bo-247
    component: apps/venue-management-web/src/routes/access-venue/UnifiedIdentityCredentialLockManager.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-247
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-247
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.4'
    page: 155
  navigation:
    entryFrom:
    - BO-244
    exitTo:
    - BO-244
    inferred: false
    notes: '**Reached from BO-244, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-248
  name: Biometric & Identity Integrity Monitoring
  module: Access & Venue
  requiresModule: access
  purpose: Detect suspicious biometric and identity-related changes without duplicating Board 5's biometric
    configuration. Board 5 configures biometrics. Board 11 monitors biometric security risk.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first biometric entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listBiometricIdentityIntegrity
    contract: access
    purpose: Biometric & Identity Integrity Monitoring
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/biometric-identity-integrity-monitoring-bo-248
    component: apps/venue-management-web/src/routes/access-venue/BiometricIdentityIntegrityMonitoring.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-248
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-248
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.5'
    page: 156
  navigation:
    entryFrom:
    - BO-244
    exitTo:
    - BO-244
    inferred: false
    notes: '**Reached from BO-244, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-249
  name: Relationship & Companion Fraud Monitoring
  module: Access & Venue
  requiresModule: access
  purpose: 'Detect abuse involving linked guests such as: Child + Adult POD + Companion Guest + Nanny
    Group Leader + Group Membership dependents.'
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first relationship entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listRelationshipCompanionFraud
    contract: access
    purpose: Relationship & Companion Fraud Monitoring
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/relationship-companion-fraud-monitoring-bo-249
    component: apps/venue-management-web/src/routes/access-venue/RelationshipCompanionFraudMonitoring.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-249
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-249
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.6'
    page: 158
  navigation:
    entryFrom:
    - BO-244
    exitTo:
    - BO-244
    inferred: false
    notes: '**Reached from BO-244, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-250
  name: Access Risk Scoring & Decision Engine
  module: Access & Venue
  requiresModule: access
  purpose: Convert multiple security signals into a unified access risk score.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessRiskScoring
    contract: access
    purpose: Access Risk Scoring & Decision Engine
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-risk-scoring-decision-engine-bo-250
    component: apps/venue-management-web/src/routes/access-venue/AccessRiskScoringDecisionEngine.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-250
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-250
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.7'
    page: 159
  navigation:
    entryFrom:
    - BO-244
    exitTo:
    - BO-244
    inferred: false
    notes: '**Reached from BO-244, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-251
  name: Real-Time Security Response & Playbook Builder
  module: Access & Venue
  requiresModule: access
  purpose: Configure what TICVAI automatically does when security conditions are detected.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first real-time entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setRealTimeSecurity
    contract: access
    purpose: Real-Time Security Response & Playbook Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/real-time-security-response-playbook-builder-bo-251
    component: apps/venue-management-web/src/routes/access-venue/RealTimeSecurityResponsePlaybookBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-251
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-251
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.8'
    page: 161
  navigation:
    entryFrom:
    - BO-244
    exitTo:
    - BO-244
    inferred: false
    notes: '**Reached from BO-244, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-252
  name: Security Investigation & Evidence Workspace
  module: Access & Venue
  requiresModule: access
  purpose: Provide security specialists with a deeper investigation environment than Board 9's operational
    incident workspace.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first security entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setSecurityInvestigationEvidence
    contract: access
    purpose: Security Investigation & Evidence Workspace
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/security-investigation-evidence-workspace-bo-252
    component: apps/venue-management-web/src/routes/access-venue/SecurityInvestigationEvidenceWorkspace.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-252
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-252
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.9'
    page: 162
  navigation:
    entryFrom:
    - BO-244
    exitTo:
    - BO-244
    inferred: false
    notes: '**Reached from BO-244, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-253
  name: Security Analytics, AI Detection & Governance
  module: Access & Venue
  requiresModule: access
  purpose: Provide long-term intelligence on fraud patterns, security controls and effectiveness.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first security entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listSecurityDetectionGovernance
    contract: access
    purpose: Security Analytics, AI Detection & Governance
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/security-analytics-ai-detection-governance-bo-253
    component: apps/venue-management-web/src/routes/access-venue/SecurityAnalyticsAiDetectionGovernance.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-253
    workshopBoard: wireframes/WS11 Access Control Board 11.dc.html#bo-253
  source:
    pack: Access Control Module_Reference.pdf
    board: '11'
    number: '11.10'
    page: 164
  navigation:
    entryFrom:
    - BO-244
    exitTo:
    - BO-244
    inferred: false
    notes: '**Reached from BO-244, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-254
  name: Access Monitoring & Analytics Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Provide a single executive and operational overview of access performance across all TICVAI-controlled
    venues.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessMonitoring
    contract: access
    purpose: Access Monitoring & Analytics Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-monitoring-analytics-command-center-bo-254
    component: apps/venue-management-web/src/routes/access-venue/AccessMonitoringAnalyticsCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-254
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-254
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.1'
    page: 168
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-255
    - BO-256
    - BO-257
    - BO-258
    - BO-259
    - BO-260
    - BO-261
    - BO-262
    - BO-263
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-255
  name: Live Venue Occupancy & People Counting
  module: Access & Venue
  requiresModule: access
  purpose: Provide real-time people counting and occupancy using entry and exit events.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first live entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listLiveVenueOccupancy
    contract: access
    purpose: Live Venue Occupancy & People Counting
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/live-venue-occupancy-people-counting-bo-255
    component: apps/venue-management-web/src/routes/access-venue/LiveVenueOccupancyPeopleCounting.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-255
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-255
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.2'
    page: 170
  navigation:
    entryFrom:
    - BO-254
    exitTo:
    - BO-254
    inferred: false
    notes: '**Reached from BO-254, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-256
  name: Graphical Access Map & Live Gate Performance
  module: Access & Venue
  requiresModule: access
  purpose: Turn the graphical access topology created in Board 1 into a live operational analytics map.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first graphical entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGraphicalAccessMap
    contract: access
    purpose: Graphical Access Map & Live Gate Performance
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/graphical-access-map-live-gate-performance-bo-256
    component: apps/venue-management-web/src/routes/access-venue/GraphicalAccessMapLiveGatePerformance.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-256
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-256
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.3'
    page: 171
  navigation:
    entryFrom:
    - BO-254
    exitTo:
    - BO-254
    inferred: false
    notes: '**Reached from BO-254, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-257
  name: Attendance & Admission Analytics
  module: Access & Venue
  requiresModule: access
  purpose: Provide detailed reporting of who actually attended compared with tickets sold/reserved. This
    is particularly important because group admission may differ from purchased quantity.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first attendance entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAttendanceAdmission
    contract: access
    purpose: Attendance & Admission Analytics
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/attendance-admission-analytics-bo-257
    component: apps/venue-management-web/src/routes/access-venue/AttendanceAdmissionAnalytics.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-257
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-257
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.4'
    page: 172
  navigation:
    entryFrom:
    - BO-254
    exitTo:
    - BO-254
    inferred: false
    notes: '**Reached from BO-254, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-258
  name: Entry, Exit, Re-entry & Crossover Analytics
  module: Access & Venue
  requiresModule: access
  purpose: Analyze complete guest movement across the access journey.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first entry, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listEntryExitCrossover
    contract: access
    purpose: Entry, Exit, Re-entry & Crossover Analytics
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/entry-exit-re-entry-crossover-analytics-bo-258
    component: apps/venue-management-web/src/routes/access-venue/EntryExitReEntryCrossoverAnalytics.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-258
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-258
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.5'
    page: 174
  navigation:
    entryFrom:
    - BO-254
    exitTo:
    - BO-254
    inferred: false
    notes: '**Reached from BO-254, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-259
  name: Throughput, Queue & Validation Performance Analytics
  module: Access & Venue
  requiresModule: access
  purpose: Measure the operational efficiency of gates and validation devices.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first throughput, entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listThroughputQueueValidation
    contract: access
    purpose: Throughput, Queue & Validation Performance Analytics
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/throughput-queue-validation-performance-analytics-bo-259
    component: apps/venue-management-web/src/routes/access-venue/ThroughputQueueValidationPerformanceAnalytics.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-259
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-259
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.6'
    page: 175
  navigation:
    entryFrom:
    - BO-254
    exitTo:
    - BO-254
    inferred: false
    notes: '**Reached from BO-254, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-260
  name: Validation Outcome & Rejection Analytics
  module: Access & Venue
  requiresModule: access
  purpose: Analyze why guests are denied or require manual intervention.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first validation entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listValidationOutcomeRejection
    contract: access
    purpose: Validation Outcome & Rejection Analytics
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/validation-outcome-rejection-analytics-bo-260
    component: apps/venue-management-web/src/routes/access-venue/ValidationOutcomeRejectionAnalytics.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-260
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-260
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.7'
    page: 176
  navigation:
    entryFrom:
    - BO-254
    exitTo:
    - BO-254
    inferred: false
    notes: '**Reached from BO-254, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-261
  name: Guest Dwell Time, Length of Stay & Attraction Flow
  module: Access & Venue
  requiresModule: access
  purpose: Use access events to understand how guests move through and use the venue.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first guest entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGuestDwellTime
    contract: access
    purpose: Guest Dwell Time, Length of Stay & Attraction Flow
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/guest-dwell-time-length-of-stay-attraction-flow-bo-261
    component: apps/venue-management-web/src/routes/access-venue/GuestDwellTimeLengthOfStayAttractionFlow.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-261
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-261
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.8'
    page: 177
  navigation:
    entryFrom:
    - BO-254
    exitTo:
    - BO-254
    inferred: false
    notes: '**Reached from BO-254, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-262
  name: Access Reports, Scheduled Reporting & Data Export
  module: Access & Venue
  requiresModule: access
  purpose: Provide configurable operational and management reports.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first access entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessReportScheduled
    contract: access
    purpose: Access Reports, Scheduled Reporting & Data Export
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/access-reports-scheduled-reporting-data-export-bo-262
    component: apps/venue-management-web/src/routes/access-venue/AccessReportsScheduledReportingDataExport.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-262
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-262
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.9'
    page: 179
  navigation:
    entryFrom:
    - BO-254
    exitTo:
    - BO-254
    inferred: false
    notes: '**Reached from BO-254, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-263
  name: AI Access Intelligence, Forecasting & Executive Insights
  module: Access & Venue
  requiresModule: access
  purpose: Turn access-control data into proactive operational intelligence. This should be the final
    intelligence screen of the entire Access Control module.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first ai entry, and this state
      carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAccessExecutiveInsight
    contract: access
    purpose: AI Access Intelligence, Forecasting & Executive Insights
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/ai-access-intelligence-forecasting-executive-insights-bo-263
    component: apps/venue-management-web/src/routes/access-venue/AiAccessIntelligenceForecastingExecutiveInsights.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-263
    workshopBoard: wireframes/WS12 Access Control Board 12.dc.html#bo-263
  source:
    pack: Access Control Module_Reference.pdf
    board: '12'
    number: '12.10'
    page: 180
  navigation:
    entryFrom:
    - BO-254
    exitTo:
    - BO-254
    inferred: false
    notes: '**Reached from BO-254, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-264
  name: Group Sales Command Center
  module: Sell
  requiresModule: ticketing
  purpose: Provide the Group Sales team with a centralized commercial workspace showing the entire group-sales
    pipeline.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: New Enquiries
      - kind: metricTile
        notes: Open Opportunities
      - kind: metricTile
        notes: Quotations Outstanding
      - kind: metricTile
        notes: Quotes Awaiting Approval
      - kind: metricTile
        notes: Confirmed Groups
      - kind: metricTile
        notes: Expected Guests
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupSale
    contract: orders
    purpose: Group Sales Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-sales-command-center-bo-264
    component: apps/venue-management-web/src/routes/sell/GroupSalesCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-264
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-264
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.1
    page: 4
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-265
    - BO-266
    - BO-267
    - BO-268
    - BO-269
    - BO-270
    - BO-271
    - BO-272
    - BO-273
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-265
  name: Group Enquiry & Opportunity Capture
  module: Sell
  requiresModule: ticketing
  purpose: Capture a new group-sales enquiry and convert it into a structured sales opportunity.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupEnquiryOpportunity
    contract: orders
    purpose: Group Enquiry & Opportunity Capture
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-enquiry-opportunity-capture-bo-265
    component: apps/venue-management-web/src/routes/sell/GroupEnquiryOpportunityCapture.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-265
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-265
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.2
    page: 5
  navigation:
    entryFrom:
    - BO-264
    exitTo:
    - BO-264
    inferred: false
    notes: '**Reached from BO-264, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-266
  name: Group Customer & Organization Profile
  module: Sell
  requiresModule: ticketing
  purpose: Maintain the customer or organization buying directly from TICVAI.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupCustomerOrganization
    contract: orders
    purpose: Group Customer & Organization Profile
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-customer-organization-profile-bo-266
    component: apps/venue-management-web/src/routes/sell/GroupCustomerOrganizationProfile.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-266
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-266
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.3
    page: 6
  navigation:
    entryFrom:
    - BO-264
    exitTo:
    - BO-264
    inferred: false
    notes: '**Reached from BO-264, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-267
  name: Group Requirements, Availability & Capacity Planner
  module: Sell
  requiresModule: ticketing
  purpose: Determine whether TICVAI can accommodate the requested group before preparing a quotation.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupRequirementAvailability
    contract: orders
    purpose: Group Requirements, Availability & Capacity Planner
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-requirements-availability-capacity-planner-bo-267
    component: apps/venue-management-web/src/routes/sell/GroupRequirementsAvailabilityCapacityPlanner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-267
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-267
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.4
    page: 8
  navigation:
    entryFrom:
    - BO-264
    exitTo:
    - BO-264
    inferred: false
    notes: '**Reached from BO-264, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-268
  name: Group Package & Experience Builder
  module: Sell
  requiresModule: ticketing
  purpose: Build a complete commercial package tailored to the group's requirements.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setGroupPackageExperience
    contract: orders
    purpose: Group Package & Experience Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/group-package-experience-builder-bo-268
    component: apps/venue-management-web/src/routes/sell/GroupPackageExperienceBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-268
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-268
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.5
    page: 9
  navigation:
    entryFrom:
    - BO-264
    exitTo:
    - BO-264
    inferred: false
    notes: '**Reached from BO-264, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-269
  name: Group Quotation Builder & Proposal Generation
  module: Sell
  requiresModule: ticketing
  purpose: Turn the configured group package into a professional customer quotation.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setGroupQuotationProposal
    contract: orders
    purpose: Group Quotation Builder & Proposal Generation
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/group-quotation-builder-proposal-generation-bo-269
    component: apps/venue-management-web/src/routes/sell/GroupQuotationBuilderProposalGeneration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-269
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-269
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.6
    page: 11
  navigation:
    entryFrom:
    - BO-264
    exitTo:
    - BO-264
    inferred: false
    notes: '**Reached from BO-264, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-270
  name: Quote Revision, Negotiation & Version Management
  module: Sell
  requiresModule: ticketing
  purpose: Manage the commercial negotiation process without losing historical versions.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first quote entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listQuoteRevisionNegotiation
    contract: orders
    purpose: Quote Revision, Negotiation & Version Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/quote-revision-negotiation-version-management-bo-270
    component: apps/venue-management-web/src/routes/sell/QuoteRevisionNegotiationVersionManagement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-270
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-270
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.7
    page: 12
  navigation:
    entryFrom:
    - BO-264
    exitTo:
    - BO-264
    inferred: false
    notes: '**Reached from BO-264, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-271
  name: Group Discount, Exception & Approval Workflow
  module: Sell
  requiresModule: ticketing
  purpose: Govern non-standard group pricing and commercial exceptions before a quote is committed.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: approveGroupDiscountException
    contract: orders
    purpose: Group Discount, Exception & Approval Workflow
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/group-discount-exception-approval-workflow-bo-271
    component: apps/venue-management-web/src/routes/sell/GroupDiscountExceptionApprovalWorkflow.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-271
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-271
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.8
    page: 13
  navigation:
    entryFrom:
    - BO-264
    exitTo:
    - BO-264
    inferred: false
    notes: '**Reached from BO-264, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-272
  name: Quote-to-Booking Conversion & Confirmation
  module: Sell
  requiresModule: ticketing
  purpose: Convert an accepted quotation into a confirmed TICVAI group booking without re-entering the
    commercial configuration.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first quote-to-booking entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listQuoteBookingConversion
    contract: orders
    purpose: Quote-to-Booking Conversion & Confirmation
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/quote-to-booking-conversion-confirmation-bo-272
    component: apps/venue-management-web/src/routes/sell/QuoteToBookingConversionConfirmation.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-272
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-272
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.9
    page: 15
  navigation:
    entryFrom:
    - BO-264
    exitTo:
    - BO-264
    inferred: false
    notes: '**Reached from BO-264, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-273
  name: Group Booking 360° & Handover Workspace
  module: Sell
  requiresModule: ticketing
  purpose: Provide a consolidated view of the completed sales journey and hand the confirmed group cleanly
    from Sales to Operations. 360° Header
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setGroupBookingHandover
    contract: orders
    purpose: Group Booking 360° & Handover Workspace
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/group-booking-360-handover-workspace-bo-273
    component: apps/venue-management-web/src/routes/sell/GroupBooking360HandoverWorkspace.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-273
    workshopBoard: wireframes/WS19 Group Sales   Corporate Booking Management Board 1.dc.html#bo-273
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '1'
    number: 9.1.10
    page: 16
  navigation:
    entryFrom:
    - BO-264
    exitTo:
    - BO-264
    inferred: false
    notes: '**Reached from BO-264, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-274
  name: Group Booking Operations Command Center
  module: Sell
  requiresModule: ticketing
  purpose: Provide Operations with a real-time command center for all confirmed and upcoming group bookings.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: 20 | Pag e
      - kind: metricTile
        notes: Upcoming Groups
      - kind: metricTile
        notes: Groups Today
      - kind: metricTile
        notes: Expected Guests Today
      - kind: metricTile
        notes: Groups Awaiting Deposit
      - kind: metricTile
        notes: Guest Lists Pending
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupBooking
    contract: orders
    purpose: Group Booking Operations Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-booking-operations-command-center-bo-274
    component: apps/venue-management-web/src/routes/sell/GroupBookingOperationsCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-274
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-274
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.1
    page: 20
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-275
    - BO-276
    - BO-277
    - BO-278
    - BO-279
    - BO-280
    - BO-281
    - BO-282
    - BO-283
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-275
  name: Group Operational Planning & Task Workspace
  module: Sell
  requiresModule: ticketing
  purpose: Convert the commercial booking into a detailed operational execution plan.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setGroupOperationalPlanning
    contract: orders
    purpose: Group Operational Planning & Task Workspace
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/group-operational-planning-task-workspace-bo-275
    component: apps/venue-management-web/src/routes/sell/GroupOperationalPlanningTaskWorkspace.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-275
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-275
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.2
    page: 22
  navigation:
    entryFrom:
    - BO-274
    exitTo:
    - BO-274
    inferred: false
    notes: '**Reached from BO-274, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-276
  name: Participants, Guest Lists & Group Structure
  module: Sell
  requiresModule: ticketing
  purpose: Manage the people participating in the group where individual information is required.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first participants, entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listParticipantGuestList
    contract: orders
    purpose: Participants, Guest Lists & Group Structure
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/participants-guest-lists-group-structure-bo-276
    component: apps/venue-management-web/src/routes/sell/ParticipantsGuestListsGroupStructure.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-276
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-276
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.3
    page: 23
  navigation:
    entryFrom:
    - BO-274
    exitTo:
    - BO-274
    inferred: false
    notes: '**Reached from BO-274, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-277
  name: Group Payment, Deposit & Balance Management
  module: Sell
  requiresModule: ticketing
  purpose: Track the complete payment lifecycle of a group booking.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupPaymentDeposit
    contract: orders
    purpose: Group Payment, Deposit & Balance Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-payment-deposit-balance-management-bo-277
    component: apps/venue-management-web/src/routes/sell/GroupPaymentDepositBalanceManagement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-277
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-277
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.4
    page: 25
  navigation:
    entryFrom:
    - BO-274
    exitTo:
    - BO-274
    inferred: false
    notes: '**Reached from BO-274, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-278
  name: Group Ticket, Seat & Entitlement Allocation
  module: Sell
  requiresModule: ticketing
  purpose: Allocate the confirmed group inventory to individual guests, subgroups or quantity blocks.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupTicketSeat
    contract: orders
    purpose: Group Ticket, Seat & Entitlement Allocation
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-ticket-seat-entitlement-allocation-bo-278
    component: apps/venue-management-web/src/routes/sell/GroupTicketSeatEntitlementAllocation.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-278
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-278
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.5
    page: 26
  navigation:
    entryFrom:
    - BO-274
    exitTo:
    - BO-274
    inferred: false
    notes: '**Reached from BO-274, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-279
  name: Group Ticket Fulfillment & Distribution
  module: Sell
  requiresModule: ticketing
  purpose: Control how tickets and other credentials are delivered to the group.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupTicketFulfillment
    contract: orders
    purpose: Group Ticket Fulfillment & Distribution
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-ticket-fulfillment-distribution-bo-279
    component: apps/venue-management-web/src/routes/sell/GroupTicketFulfillmentDistribution.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-279
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-279
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.6
    page: 27
  navigation:
    entryFrom:
    - BO-274
    exitTo:
    - BO-274
    inferred: false
    notes: '**Reached from BO-274, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-280
  name: Group Arrival, Check-In & Admission Operations
  module: Sell
  requiresModule: ticketing
  purpose: Manage the physical arrival and admission of large groups efficiently.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupArrivalCheck
    contract: orders
    purpose: Group Arrival, Check-In & Admission Operations
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-arrival-check-in-admission-operations-bo-280
    component: apps/venue-management-web/src/routes/sell/GroupArrivalCheckInAdmissionOperations.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-280
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-280
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.7
    page: 29
  navigation:
    entryFrom:
    - BO-274
    exitTo:
    - BO-274
    inferred: false
    notes: '**Reached from BO-274, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-281
  name: Group Amendments, Cancellation & Refund Operations
  module: Sell
  requiresModule: ticketing
  purpose: Manage changes occurring after group confirmation.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupAmendmentCancellation
    contract: orders
    purpose: Group Amendments, Cancellation & Refund Operations
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-amendments-cancellation-refund-operations-bo-281
    component: apps/venue-management-web/src/routes/sell/GroupAmendmentsCancellationRefundOperations.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-281
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-281
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.8
    page: 30
  navigation:
    entryFrom:
    - BO-274
    exitTo:
    - BO-274
    inferred: false
    notes: '**Reached from BO-274, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-282
  name: Group Booking Reconciliation, Closure & Performance
  module: Sell
  requiresModule: ticketing
  purpose: Close the group booking after the visit and reconcile what was sold against what actually occurred.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupBookingReconciliation
    contract: orders
    purpose: Group Booking Reconciliation, Closure & Performance
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-booking-reconciliation-closure-performance-bo-282
    component: apps/venue-management-web/src/routes/sell/GroupBookingReconciliationClosurePerformance.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-282
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-282
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.9
    page: 31
  navigation:
    entryFrom:
    - BO-274
    exitTo:
    - BO-274
    inferred: false
    notes: '**Reached from BO-274, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-283
  name: Group Sales Analytics & AI Intelligence Center
  module: Sell
  requiresModule: ticketing
  purpose: Provide management with intelligence across the complete group-sales lifecycle. This should
    combine data from Board 1 + Board 2.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first group entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listGroupSale2
    contract: orders
    purpose: Group Sales Analytics & AI Intelligence Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/group-sales-analytics-ai-intelligence-center-bo-283
    component: apps/venue-management-web/src/routes/sell/GroupSalesAnalyticsAiIntelligenceCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-283
    workshopBoard: wireframes/WS20 Group Sales   Corporate Booking Management Board 2.dc.html#bo-283
  source:
    pack: Group_Sales___Corporate_Booking_Management_Reference.pdf
    board: '2'
    number: 9.2.10
    page: 33
  navigation:
    entryFrom:
    - BO-274
    exitTo:
    - BO-274
    inferred: false
    notes: '**Reached from BO-274, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-284
  name: Membership & Annual Pass Command Center
  module: Sell
  requiresModule: membership
  purpose: Provide administrators with a centralized view of all membership, annual pass, season pass
    and subscription-style admission products.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Active Membership Products
      - kind: metricTile
        notes: Annual Pass Products
      - kind: metricTile
        notes: Draft Products
      - kind: metricTile
        notes: Active Members
      - kind: metricTile
        notes: Family Memberships
      - kind: metricTile
        notes: Memberships Expiring Soon
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMembershipAnnualPass
    contract: subscription
    purpose: Membership & Annual Pass Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/membership-annual-pass-command-center-bo-284
    component: apps/venue-management-web/src/routes/sell/MembershipAnnualPassCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-284
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-284
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.1
    page: 4
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-285
    - BO-286
    - BO-287
    - BO-288
    - BO-289
    - BO-290
    - BO-291
    - BO-292
    - BO-293
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-285
  name: Membership Product & Tier Builder
  module: Sell
  requiresModule: membership
  purpose: Configure the fundamental definition and hierarchy of a membership/pass.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setMembershipProductTier
    contract: subscription
    purpose: Membership Product & Tier Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/membership-product-tier-builder-bo-285
    component: apps/venue-management-web/src/routes/sell/MembershipProductTierBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-285
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-285
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.2
    page: 5
  navigation:
    entryFrom:
    - BO-284
    exitTo:
    - BO-284
    inferred: false
    notes: '**Reached from BO-284, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-286
  name: Membership Eligibility & Qualification Rule Builder
  module: Sell
  requiresModule: membership
  purpose: Determine who is allowed to purchase, activate, hold or renew a particular membership.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setMembershipEligibilityQualification
    contract: subscription
    purpose: Membership Eligibility & Qualification Rule Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/membership-eligibility-qualification-rule-builder-bo-286
    component: apps/venue-management-web/src/routes/sell/MembershipEligibilityQualificationRuleBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-286
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-286
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.3
    page: 7
  navigation:
    entryFrom:
    - BO-284
    exitTo:
    - BO-284
    inferred: false
    notes: '**Reached from BO-284, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-287
  name: Validity, Activation & Expiry Configuration
  module: Sell
  requiresModule: membership
  purpose: Define exactly when a membership becomes valid, how long it remains valid and how it expires.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first validity, entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setValidityActivationExpiry
    contract: subscription
    purpose: Validity, Activation & Expiry Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/validity-activation-expiry-configuration-bo-287
    component: apps/venue-management-web/src/routes/sell/ValidityActivationExpiryConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-287
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-287
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.4
    page: 8
  navigation:
    entryFrom:
    - BO-284
    exitTo:
    - BO-284
    inferred: false
    notes: '**Reached from BO-284, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-288
  name: Membership Entitlement & Admission Benefit Builder
  module: Sell
  requiresModule: membership
  purpose: Define exactly what the member receives. This is the heart of the membership product.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setMembershipEntitlementAdmission
    contract: subscription
    purpose: Membership Entitlement & Admission Benefit Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/membership-entitlement-admission-benefit-builder-bo-288
    component: apps/venue-management-web/src/routes/sell/MembershipEntitlementAdmissionBenefitBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-288
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-288
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.5
    page: 9
  navigation:
    entryFrom:
    - BO-284
    exitTo:
    - BO-284
    inferred: false
    notes: '**Reached from BO-284, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-289
  name: Membership Usage, Visit & Consumption Rules
  module: Sell
  requiresModule: membership
  purpose: Control how membership entitlements may actually be consumed. Screen 13.1.5 defines what the
    member receives. Screen 13.1.6 defines how it may be used.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMembershipUsageVisit
    contract: subscription
    purpose: Membership Usage, Visit & Consumption Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/membership-usage-visit-consumption-rules-bo-289
    component: apps/venue-management-web/src/routes/sell/MembershipUsageVisitConsumptionRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-289
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-289
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.6
    page: 11
  navigation:
    entryFrom:
    - BO-284
    exitTo:
    - BO-284
    inferred: false
    notes: '**Reached from BO-284, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-290
  name: Family, Household & Dependent Membership Configuration
  module: Sell
  requiresModule: membership
  purpose: Support memberships covering more than one person while preserving individual identities and
    entitlements.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first family, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setFamilyHouseholdDependent
    contract: subscription
    purpose: Family, Household & Dependent Membership Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/family-household-dependent-membership-configuration-bo-290
    component: apps/venue-management-web/src/routes/sell/FamilyHouseholdDependentMembershipConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-290
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-290
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.7
    page: 12
  navigation:
    entryFrom:
    - BO-284
    exitTo:
    - BO-284
    inferred: false
    notes: '**Reached from BO-284, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-291
  name: Membership Commercial, Pricing & Channel Association
  module: Sell
  requiresModule: membership
  purpose: Connect the membership contract to TICVAI's central commercial engines without duplicating
    pricing configuration.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMembershipCommercialPricing
    contract: subscription
    purpose: Membership Commercial, Pricing & Channel Association
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/membership-commercial-pricing-channel-association-bo-291
    component: apps/venue-management-web/src/routes/sell/MembershipCommercialPricingChannelAssociation.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-291
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-291
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.8
    page: 14
  navigation:
    entryFrom:
    - BO-284
    exitTo:
    - BO-284
    inferred: false
    notes: '**Reached from BO-284, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-292
  name: Renewal, Auto-Renewal & Membership Continuity Configuration
  module: Sell
  requiresModule: membership
  purpose: Define how a membership moves from one validity period into the next.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first renewal, entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setRenewalAutoMembership
    contract: subscription
    purpose: Renewal, Auto-Renewal & Membership Continuity Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/renewal-auto-renewal-membership-continuity-configuration-bo-292
    component: apps/venue-management-web/src/routes/sell/RenewalAutoRenewalMembershipContinuityConfigurat.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-292
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-292
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.9
    page: 15
  navigation:
    entryFrom:
    - BO-284
    exitTo:
    - BO-284
    inferred: false
    notes: '**Reached from BO-284, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-293
  name: Membership Product Validation, Approval, Publication & Versioning
  module: Sell
  requiresModule: membership
  purpose: Provide the final governance layer before a membership/pass configuration becomes commercially
    available.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: approveMembershipProductValidation
    contract: subscription
    purpose: Membership Product Validation, Approval, Publication & Versioning
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/membership-product-validation-approval-publication-versi-bo-293
    component: apps/venue-management-web/src/routes/sell/MembershipProductValidationApprovalPublicationVe.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-293
    workshopBoard: wireframes/WS21 Membership   Annual Pass Management Board 1.dc.html#bo-293
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '1'
    number: 13.1.10
    page: 17
  navigation:
    entryFrom:
    - BO-284
    exitTo:
    - BO-284
    inferred: false
    notes: '**Reached from BO-284, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-294
  name: Member Operations Command Center
  module: Sell
  requiresModule: membership
  purpose: Provide membership teams with a real-time operational dashboard for the complete active member
    population.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Active Members
      - kind: metricTile
        notes: New Members Today
      - kind: metricTile
        notes: Activated Today
      - kind: metricTile
        notes: Pending Activation
      - kind: metricTile
        notes: Expiring in 30 Days
      - kind: metricTile
        notes: Renewal Due
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first member entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMember
    contract: subscription
    purpose: Member Operations Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/member-operations-command-center-bo-294
    component: apps/venue-management-web/src/routes/sell/MemberOperationsCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-294
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-294
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.1
    page: 21
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-295
    - BO-296
    - BO-297
    - BO-298
    - BO-299
    - BO-300
    - BO-301
    - BO-302
    - BO-303
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-295
  name: Member 360° Membership Account Workspace
  module: Sell
  requiresModule: membership
  purpose: Provide a complete operational view of an individual member and their membership contract.
    This should be the primary screen an authorized membership-service agent opens when helping a member.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first member entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setMemberMembershipAccount
    contract: subscription
    purpose: Member 360° Membership Account Workspace
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /sell/member-360-membership-account-workspace-bo-295
    component: apps/venue-management-web/src/routes/sell/Member360MembershipAccountWorkspace.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-295
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-295
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.2
    page: 22
  navigation:
    entryFrom:
    - BO-294
    exitTo:
    - BO-294
    inferred: false
    notes: '**Reached from BO-294, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-296
  name: Membership Activation, Assignment & Credential Management
  module: Sell
  requiresModule: membership
  purpose: Manage the operational process that turns a purchased membership product into an active membership
    assigned to a specific individual.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMembershipActivationCredential
    contract: subscription
    purpose: Membership Activation, Assignment & Credential Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/membership-activation-assignment-credential-management-bo-296
    component: apps/venue-management-web/src/routes/sell/MembershipActivationAssignmentCredentialManageme.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-296
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-296
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.3
    page: 24
  navigation:
    entryFrom:
    - BO-294
    exitTo:
    - BO-294
    inferred: false
    notes: '**Reached from BO-294, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-297
  name: Visit, Admission & Entitlement Usage Monitor
  module: Sell
  requiresModule: membership
  purpose: Provide membership teams with complete visibility of how a member uses admission and other
    membership entitlements.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Total Visits
      - kind: metricTile
        notes: Visits This Month
      - kind: metricTile
        notes: Last Visit
      - kind: metricTile
        notes: Upcoming Reservation
      - kind: metricTile
        notes: Guest Tickets Used
      - kind: metricTile
        notes: Guest Tickets Remaining
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first visit, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listVisitAdmissionEntitlement
    contract: subscription
    purpose: Visit, Admission & Entitlement Usage Monitor
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/visit-admission-entitlement-usage-monitor-bo-297
    component: apps/venue-management-web/src/routes/sell/VisitAdmissionEntitlementUsageMonitor.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-297
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-297
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.4
    page: 26
  navigation:
    entryFrom:
    - BO-294
    exitTo:
    - BO-294
    inferred: false
    notes: '**Reached from BO-294, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-298
  name: Membership Freeze, Suspension & Reactivation Management
  module: Sell
  requiresModule: membership
  purpose: Manage temporary interruption of membership rights without necessarily terminating the membership
    contract.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMembershipFreezeSuspension
    contract: subscription
    purpose: Membership Freeze, Suspension & Reactivation Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/membership-freeze-suspension-reactivation-management-bo-298
    component: apps/venue-management-web/src/routes/sell/MembershipFreezeSuspensionReactivationManagement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-298
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-298
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.5
    page: 27
  navigation:
    entryFrom:
    - BO-294
    exitTo:
    - BO-294
    inferred: false
    notes: '**Reached from BO-294, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-299
  name: Membership Upgrade, Downgrade & Product Migration Operations
  module: Sell
  requiresModule: membership
  purpose: Manage operational movement of an active member between membership products or tiers.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMembershipUpgradeDowngrade
    contract: subscription
    purpose: Membership Upgrade, Downgrade & Product Migration Operations
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/membership-upgrade-downgrade-product-migration-operation-bo-299
    component: apps/venue-management-web/src/routes/sell/MembershipUpgradeDowngradeProductMigrationOperat.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-299
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-299
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.6
    page: 29
  navigation:
    entryFrom:
    - BO-294
    exitTo:
    - BO-294
    inferred: false
    notes: '**Reached from BO-294, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-300
  name: Renewal Operations & Auto-Renewal Management
  module: Sell
  requiresModule: membership
  purpose: Operationally manage memberships approaching expiry and execute the renewal policies configured
    in Board 1.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first renewal entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listRenewalAuto
    contract: subscription
    purpose: Renewal Operations & Auto-Renewal Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/renewal-operations-auto-renewal-management-bo-300
    component: apps/venue-management-web/src/routes/sell/RenewalOperationsAutoRenewalManagement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-300
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-300
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.7
    page: 30
  navigation:
    entryFrom:
    - BO-294
    exitTo:
    - BO-294
    inferred: false
    notes: '**Reached from BO-294, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-301
  name: Member Exceptions, Overrides & Service Recovery
  module: Sell
  requiresModule: membership
  purpose: Provide controlled handling of member-specific situations that fall outside normal membership
    policy.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first member entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMemberExceptionOverride
    contract: subscription
    purpose: Member Exceptions, Overrides & Service Recovery
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/member-exceptions-overrides-service-recovery-bo-301
    component: apps/venue-management-web/src/routes/sell/MemberExceptionsOverridesServiceRecovery.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-301
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-301
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.8
    page: 32
  navigation:
    entryFrom:
    - BO-294
    exitTo:
    - BO-294
    inferred: false
    notes: '**Reached from BO-294, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-302
  name: Member Lifecycle History, Audit & Case Timeline
  module: Sell
  requiresModule: membership
  purpose: Maintain a complete historical record of everything that has happened to the membership from
    purchase to final expiry.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: timeline
        notes: What changed, when, and by whom
      - kind: searchField
        notes: Find a change
  states:
    loading: Skeleton while the configuration loads
    emptyNoEvents: '**The record is complete and nothing has happened yet.** No action: an
      audit trail that offers to create its first entry is offering to author evidence'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMemberLifecycleCase
    contract: subscription
    purpose: Member Lifecycle History, Audit & Case Timeline
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/member-lifecycle-history-audit-case-timeline-bo-302
    component: apps/venue-management-web/src/routes/sell/MemberLifecycleHistoryAuditCaseTimeline.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-302
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-302
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.9
    page: 33
  navigation:
    entryFrom:
    - BO-294
    exitTo:
    - BO-294
    inferred: false
    notes: '**Reached from BO-294, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-303
  name: Membership Analytics, Renewal Intelligence & AI Retention Center
  module: Sell
  requiresModule: membership
  purpose: Turn membership operational data into actionable intelligence for retention, renewal, product
    optimization and member engagement.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Active Members
      - kind: metricTile
        notes: New Memberships
      - kind: metricTile
        notes: Renewal Rate
      - kind: metricTile
        notes: Churn Rate
      - kind: metricTile
        notes: Auto-Renew Success
      - kind: metricTile
        notes: Average Membership Tenure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first membership entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMembershipRenewalRetention
    contract: subscription
    purpose: Membership Analytics, Renewal Intelligence & AI Retention Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /sell/membership-analytics-renewal-intelligence-ai-retention-c-bo-303
    component: apps/venue-management-web/src/routes/sell/MembershipAnalyticsRenewalIntelligenceAiRetentio.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-303
    workshopBoard: wireframes/WS22 Membership   Annual Pass Management Board 2.dc.html#bo-303
  source:
    pack: Membership___Annual_Pass_Management_Reference.pdf
    board: '2'
    number: 13.2.10
    page: 35
  navigation:
    entryFrom:
    - BO-294
    exitTo:
    - BO-294
    inferred: false
    notes: '**Reached from BO-294, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-304
  name: Order & Reservation Command Center
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide the central operational workspace for searching, monitoring, opening, and managing
    every order and reservation across TICVAI.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: 3 | Pag e
      - kind: metricTile
        notes: Orders Today
      - kind: metricTile
        notes: Confirmed Orders
      - kind: metricTile
        notes: Active Reservations
      - kind: metricTile
        notes: Temporary Holds
      - kind: metricTile
        notes: Pending Payment
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOrderReservation
    contract: orders
    purpose: Order & Reservation Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/order-reservation-command-center-bo-304
    component: apps/venue-management-web/src/routes/orders-money/OrderReservationCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-304
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-304
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.1
    page: 3
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-305
    - BO-306
    - BO-307
    - BO-308
    - BO-309
    - BO-310
    - BO-311
    - BO-312
    - BO-313
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-305
  name: Order Detail & Transaction Workspace
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide the authoritative 360-degree view of a single order. This should become one of the
    most important operational screens in TICVAI.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setOrderDetailTransaction
    contract: orders
    purpose: Order Detail & Transaction Workspace
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/order-detail-transaction-workspace-bo-305
    component: apps/venue-management-web/src/routes/orders-money/OrderDetailTransactionWorkspace.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-305
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-305
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.2
    page: 5
  navigation:
    entryFrom:
    - BO-304
    exitTo:
    - BO-304
    inferred: false
    notes: '**Reached from BO-304, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-306
  name: Reservation & Hold Policy Configuration
  module: Orders & Money
  requiresModule: ticketing
  purpose: Configure how TICVAI temporarily reserves inventory before an order is fully confirmed.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first reservation entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setReservationHoldPolicy
    contract: orders
    purpose: Reservation & Hold Policy Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/reservation-hold-policy-configuration-bo-306
    component: apps/venue-management-web/src/routes/orders-money/ReservationHoldPolicyConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-306
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-306
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.3
    page: 7
  navigation:
    entryFrom:
    - BO-304
    exitTo:
    - BO-304
    inferred: false
    notes: '**Reached from BO-304, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-307
  name: Order & Reservation Status Lifecycle Configuration
  module: Orders & Money
  requiresModule: ticketing
  purpose: Define the governed state machine for orders and reservations. SoftLab should not hard-code
    status transitions independently in every channel.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setOrderReservationStatus
    contract: orders
    purpose: Order & Reservation Status Lifecycle Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/order-reservation-status-lifecycle-configuration-bo-307
    component: apps/venue-management-web/src/routes/orders-money/OrderReservationStatusLifecycleConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-307
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-307
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.4
    page: 9
  navigation:
    entryFrom:
    - BO-304
    exitTo:
    - BO-304
    inferred: false
    notes: '**Reached from BO-304, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-308
  name: Order Creation & Source/Channel Configuration
  module: Orders & Money
  requiresModule: ticketing
  purpose: Configure how orders can originate from different TICVAI sales channels while using one common
    transaction engine.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: createOrderSourceChannel
    contract: orders
    purpose: Order Creation & Source/Channel Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/order-creation-source-channel-configuration-bo-308
    component: apps/venue-management-web/src/routes/orders-money/OrderCreationSourceChannelConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-308
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-308
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.5
    page: 10
  navigation:
    entryFrom:
    - BO-304
    exitTo:
    - BO-304
    inferred: false
    notes: '**Reached from BO-304, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-309
  name: Customer, Guest & Account Assignment
  module: Orders & Money
  requiresModule: ticketing
  purpose: Define how customers are identified and associated with orders/reservations.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first customer, entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setCustomerGuestAccount
    contract: orders
    purpose: Customer, Guest & Account Assignment
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/customer-guest-account-assignment-bo-309
    component: apps/venue-management-web/src/routes/orders-money/CustomerGuestAccountAssignment.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-309
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-309
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.6
    page: 11
  navigation:
    entryFrom:
    - BO-304
    exitTo:
    - BO-304
    inferred: false
    notes: '**Reached from BO-304, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-310
  name: Order Line, Product & Entitlement Composition
  module: Orders & Money
  requiresModule: ticketing
  purpose: Manage the products and commercial components contained within an order.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOrderLineProduct
    contract: orders
    purpose: Order Line, Product & Entitlement Composition
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/order-line-product-entitlement-composition-bo-310
    component: apps/venue-management-web/src/routes/orders-money/OrderLineProductEntitlementComposition.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-310
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-310
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.7
    page: 13
  navigation:
    entryFrom:
    - BO-304
    exitTo:
    - BO-304
    inferred: false
    notes: '**Reached from BO-304, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-311
  name: Capacity Reservation & Inventory Commitment
  module: Orders & Money
  requiresModule: ticketing
  purpose: Control when and how order activity consumes real capacity/inventory. This is the transactional
    bridge between the Order Engine and the Capacity/Inventory Engines.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first capacity entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCapacityReservationInventory
    contract: orders
    purpose: Capacity Reservation & Inventory Commitment
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/capacity-reservation-inventory-commitment-bo-311
    component: apps/venue-management-web/src/routes/orders-money/CapacityReservationInventoryCommitment.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-311
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-311
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.8
    page: 14
  navigation:
    entryFrom:
    - BO-304
    exitTo:
    - BO-304
    inferred: false
    notes: '**Reached from BO-304, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-312
  name: Reservation Confirmation, Expiry & Fulfillment Readiness
  module: Orders & Money
  requiresModule: ticketing
  purpose: Determine when a reservation becomes a confirmed order and when it is ready for ticket/media
    fulfillment.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first reservation entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listReservationConfirmationExpiry
    contract: orders
    purpose: Reservation Confirmation, Expiry & Fulfillment Readiness
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/reservation-confirmation-expiry-fulfillment-readiness-bo-312
    component: apps/venue-management-web/src/routes/orders-money/ReservationConfirmationExpiryFulfillmentReadines.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-312
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-312
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.9
    page: 15
  navigation:
    entryFrom:
    - BO-304
    exitTo:
    - BO-304
    inferred: false
    notes: '**Reached from BO-304, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-313
  name: Order Lifecycle Timeline, SLA, Exceptions & AI Operations
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide complete lifecycle visibility and proactively identify orders/reservations that require
    operational intervention.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOrderLifecycleTimeline
    contract: orders
    purpose: Order Lifecycle Timeline, SLA, Exceptions & AI Operations
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/order-lifecycle-timeline-sla-exceptions-ai-operations-bo-313
    component: apps/venue-management-web/src/routes/orders-money/OrderLifecycleTimelineSlaExceptionsAiOperations.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-313
    workshopBoard: wireframes/WS23 Order   Reservation Management Board 1.dc.html#bo-313
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '1'
    number: 12.1.10
    page: 17
  navigation:
    entryFrom:
    - BO-304
    exitTo:
    - BO-304
    inferred: false
    notes: '**Reached from BO-304, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-314
  name: Amendment & After-Sales Command Center
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide one operational workspace for all post-sale activities affecting confirmed orders and
    reservations.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Amendments Today
      - kind: metricTile
        notes: Pending Amendments
      - kind: metricTile
        notes: Cancellations
      - kind: metricTile
        notes: Refund Requests
      - kind: metricTile
        notes: Refund Value
      - kind: metricTile
        notes: Voids
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first amendment entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAmendmentAfterSale
    contract: orders
    purpose: Amendment & After-Sales Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/amendment-after-sales-command-center-bo-314
    component: apps/venue-management-web/src/routes/orders-money/AmendmentAfterSalesCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-314
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-314
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.1
    page: 20
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-315
    - BO-316
    - BO-317
    - BO-318
    - BO-319
    - BO-320
    - BO-321
    - BO-322
    - BO-323
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-315
  name: Order Amendment Workspace
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide agents with a controlled workspace for modifying an existing order without directly
    editing historical transaction records. The original order must always remain reconstructable.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setOrderAmendment
    contract: orders
    purpose: Order Amendment Workspace
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/order-amendment-workspace-bo-315
    component: apps/venue-management-web/src/routes/orders-money/OrderAmendmentWorkspace.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-315
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-315
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.2
    page: 22
  navigation:
    entryFrom:
    - BO-314
    exitTo:
    - BO-314
    inferred: false
    notes: '**Reached from BO-314, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-316
  name: Amendment Eligibility & Policy Rule Builder
  module: Orders & Money
  requiresModule: ticketing
  purpose: Define when an order or reservation may be amended and which changes are permitted.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first amendment entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setAmendmentEligibilityPolicy
    contract: orders
    purpose: Amendment Eligibility & Policy Rule Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/amendment-eligibility-policy-rule-builder-bo-316
    component: apps/venue-management-web/src/routes/orders-money/AmendmentEligibilityPolicyRuleBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-316
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-316
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.3
    page: 24
  navigation:
    entryFrom:
    - BO-314
    exitTo:
    - BO-314
    inferred: false
    notes: '**Reached from BO-314, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-317
  name: Cancellation & Partial Cancellation Policy Configuration
  module: Orders & Money
  requiresModule: ticketing
  purpose: Configure whether an order, reservation, or selected order lines may be cancelled.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first cancellation entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setCancellationPartialPolicy
    contract: orders
    purpose: Cancellation & Partial Cancellation Policy Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/cancellation-partial-cancellation-policy-configuration-bo-317
    component: apps/venue-management-web/src/routes/orders-money/CancellationPartialCancellationPolicyConfigurati.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-317
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-317
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.4
    page: 26
  navigation:
    entryFrom:
    - BO-314
    exitTo:
    - BO-314
    inferred: false
    notes: '**Reached from BO-314, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-318
  name: Refund Policy & Refund Calculation Configuration
  module: Orders & Money
  requiresModule: ticketing
  purpose: Define when a cancellation/amendment creates a refundable amount and how refund entitlement
    is determined.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first refund entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setRefundPolicy
    contract: orders
    purpose: Set a venue's refund policy
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/refund-policy-refund-calculation-configuration-bo-318
    component: apps/venue-management-web/src/routes/orders-money/RefundPolicyRefundCalculationConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-318
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-318
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.5
    page: 27
  entryState:
    params:
    - name: venueId
      from: navigation
    coldEntry: '**Reached from the list that owns it**, so the identifier arrives with the navigation.
      Opened cold without one, the screen says what is missing and offers that list — never an empty form
      that looks configurable.'
  navigation:
    entryFrom:
    - BO-314
    exitTo:
    - BO-314
    inferred: false
    notes: '**Reached from BO-314, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-319
  name: Void, Reversal & Same-Day Correction Management
  module: Orders & Money
  requiresModule: ticketing
  purpose: Separate genuine void/correction operations from normal customer cancellations and refunds.
    This is important financially and operationally.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first void, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listVoidReversalSame
    contract: orders
    purpose: Void, Reversal & Same-Day Correction Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/void-reversal-same-day-correction-management-bo-319
    component: apps/venue-management-web/src/routes/orders-money/VoidReversalSameDayCorrectionManagement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-319
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-319
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.6
    page: 29
  navigation:
    entryFrom:
    - BO-314
    exitTo:
    - BO-314
    inferred: false
    notes: '**Reached from BO-314, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-320
  name: Ticket Reissue & Fulfillment Regeneration
  module: Orders & Money
  requiresModule: ticketing
  purpose: Manage ticket/media regeneration following an amendment, correction, loss, delivery failure,
    or other authorized event.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first ticket entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listTicketReissueFulfillment
    contract: orders
    purpose: Ticket Reissue & Fulfillment Regeneration
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/ticket-reissue-fulfillment-regeneration-bo-320
    component: apps/venue-management-web/src/routes/orders-money/TicketReissueFulfillmentRegeneration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-320
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-320
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.7
    page: 30
  navigation:
    entryFrom:
    - BO-314
    exitTo:
    - BO-314
    inferred: false
    notes: '**Reached from BO-314, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-321
  name: After-Sales Financial Settlement & Adjustment Workspace
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide a consolidated view of the financial consequences of amendments, cancellations, refunds,
    exchanges and corrections. This is not the payment engine; it is the after-sales financial orchestration
    layer.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first after-sales entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setAfterSaleFinancial
    contract: orders
    purpose: After-Sales Financial Settlement & Adjustment Workspace
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/after-sales-financial-settlement-adjustment-workspace-bo-321
    component: apps/venue-management-web/src/routes/orders-money/AfterSalesFinancialSettlementAdjustmentWorkspace.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-321
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-321
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.8
    page: 32
  navigation:
    entryFrom:
    - BO-314
    exitTo:
    - BO-314
    inferred: false
    notes: '**Reached from BO-314, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-322
  name: Approval, Exception & Service Recovery Management
  module: Orders & Money
  requiresModule: ticketing
  purpose: Govern after-sales actions that fall outside normal policies or exceed financial/operational
    authority.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first approval, entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: approveExceptionServiceRecovery
    contract: orders
    purpose: Approval, Exception & Service Recovery Management
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/approval-exception-service-recovery-management-bo-322
    component: apps/venue-management-web/src/routes/orders-money/ApprovalExceptionServiceRecoveryManagement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-322
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-322
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.9
    page: 33
  navigation:
    entryFrom:
    - BO-314
    exitTo:
    - BO-314
    inferred: false
    notes: '**Reached from BO-314, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-323
  name: Amendment History, Audit & After-Sales Analytics
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide complete traceability and analytical visibility across all changes made after original
    order creation.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first amendment entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listAmendmentAfterSale2
    contract: orders
    purpose: Amendment History, Audit & After-Sales Analytics
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/amendment-history-audit-after-sales-analytics-bo-323
    component: apps/venue-management-web/src/routes/orders-money/AmendmentHistoryAuditAfterSalesAnalytics.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-323
    workshopBoard: wireframes/WS24 Order   Reservation Management Board 2.dc.html#bo-323
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '2'
    number: 12.2.10
    page: 35
  navigation:
    entryFrom:
    - BO-314
    exitTo:
    - BO-314
    inferred: false
    notes: '**Reached from BO-314, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-324
  name: Payment & Order Financial Command Center
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide operations and finance teams with a centralized view of the financial status of all
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Gross Order Value
      - kind: metricTile
        notes: Fully Paid Orders
      - kind: metricTile
        notes: Partially Paid Orders
      - kind: metricTile
        notes: Unpaid Orders
      - kind: metricTile
        notes: Outstanding Balance
      - kind: metricTile
        notes: Payments Today
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first payment entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listPaymentOrderFinancial
    contract: orders
    purpose: Payment & Order Financial Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/payment-order-financial-command-center-bo-324
    component: apps/venue-management-web/src/routes/orders-money/PaymentOrderFinancialCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-324
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-324
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.1
    page: 40
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-325
    - BO-326
    - BO-327
    - BO-328
    - BO-329
    - BO-330
    - BO-331
    - BO-332
    - BO-333
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-325
  name: Order Payment Detail & Transaction Ledger
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide the complete payment history associated with an individual order.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOrderPaymentDetail
    contract: orders
    purpose: Order Payment Detail & Transaction Ledger
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/order-payment-detail-transaction-ledger-bo-325
    component: apps/venue-management-web/src/routes/orders-money/OrderPaymentDetailTransactionLedger.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-325
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-325
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.2
    page: 41
  navigation:
    entryFrom:
    - BO-324
    exitTo:
    - BO-324
    inferred: false
    notes: '**Reached from BO-324, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-326
  name: Multi-Payment, Split Tender & Payment Allocation Configuration
  module: Orders & Money
  requiresModule: ticketing
  purpose: Support orders paid using multiple payment methods and determine how each payment is allocated.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first multi-payment, entry,
      and this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setMultiPaymentSplit
    contract: orders
    purpose: Multi-Payment, Split Tender & Payment Allocation Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /orders-money/multi-payment-split-tender-payment-allocation-configurat-bo-326
    component: apps/venue-management-web/src/routes/orders-money/MultiPaymentSplitTenderPaymentAllocationConfigur.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-326
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-326
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.3
    page: 43
  navigation:
    entryFrom:
    - BO-324
    exitTo:
    - BO-324
    inferred: false
    notes: '**Reached from BO-324, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-327
  name: Deposit, Partial Payment & Outstanding Balance Management
  module: Orders & Money
  requiresModule: ticketing
  purpose: Support commercial scenarios where an order may be confirmed or reserved without full immediate
    payment.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first deposit, entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listDepositPartialPayment
    contract: orders
    purpose: Deposit, Partial Payment & Outstanding Balance Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/deposit-partial-payment-outstanding-balance-management-bo-327
    component: apps/venue-management-web/src/routes/orders-money/DepositPartialPaymentOutstandingBalanceManagemen.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-327
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-327
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.4
    page: 44
  navigation:
    entryFrom:
    - BO-324
    exitTo:
    - BO-324
    inferred: false
    notes: '**Reached from BO-324, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-328
  name: Order Split, Merge & Transaction Relationship Management
  module: Orders & Money
  requiresModule: ticketing
  purpose: Allow complex orders to be reorganized without destroying transaction history.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOrderSplitMerge
    contract: orders
    purpose: Order Split, Merge & Transaction Relationship Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/order-split-merge-transaction-relationship-management-bo-328
    component: apps/venue-management-web/src/routes/orders-money/OrderSplitMergeTransactionRelationshipManagement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-328
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-328
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.5
    page: 46
  navigation:
    entryFrom:
    - BO-324
    exitTo:
    - BO-324
    inferred: false
    notes: '**Reached from BO-324, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-329
  name: Related Order & Transaction Relationship Explorer
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide a visual relationship map for complex transaction histories. This becomes especially
    useful after amendments, upgrades, exchanges, reissues, splits and refunds.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first related entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listRelatedOrderTransaction
    contract: orders
    purpose: Related Order & Transaction Relationship Explorer
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/related-order-transaction-relationship-explorer-bo-329
    component: apps/venue-management-web/src/routes/orders-money/RelatedOrderTransactionRelationshipExplorer.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-329
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-329
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.6
    page: 47
  navigation:
    entryFrom:
    - BO-324
    exitTo:
    - BO-324
    inferred: false
    notes: '**Reached from BO-324, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-330
  name: External Payment, Partner & Settlement Reference Mapping
  module: Orders & Money
  requiresModule: ticketing
  purpose: Maintain the relationship between TICVAI transactions and external financial/channel references.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first external entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listExternalPaymentPartner
    contract: orders
    purpose: External Payment, Partner & Settlement Reference Mapping
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/external-payment-partner-settlement-reference-mapping-bo-330
    component: apps/venue-management-web/src/routes/orders-money/ExternalPaymentPartnerSettlementReferenceMapping.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-330
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-330
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.7
    page: 49
  navigation:
    entryFrom:
    - BO-324
    exitTo:
    - BO-324
    inferred: false
    notes: '**Reached from BO-324, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-331
  name: Payment Reconciliation & Exception Management
  module: Orders & Money
  requiresModule: ticketing
  purpose: Automatically compare TICVAI payment records with external payment and settlement records.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first payment entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listPaymentReconciliationException
    contract: orders
    purpose: Payment Reconciliation & Exception Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/payment-reconciliation-exception-management-bo-331
    component: apps/venue-management-web/src/routes/orders-money/PaymentReconciliationExceptionManagement.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-331
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-331
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.8
    page: 50
  navigation:
    entryFrom:
    - BO-324
    exitTo:
    - BO-324
    inferred: false
    notes: '**Reached from BO-324, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-332
  name: Financial Traceability, Control & Audit Explorer
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide a complete financial audit trail across the order lifecycle.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first financial entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listFinancialTraceability
    contract: orders
    purpose: Financial Traceability, Control & Audit Explorer
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/financial-traceability-control-audit-explorer-bo-332
    component: apps/venue-management-web/src/routes/orders-money/FinancialTraceabilityControlAuditExplorer.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-332
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-332
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.9
    page: 51
  navigation:
    entryFrom:
    - BO-324
    exitTo:
    - BO-324
    inferred: false
    notes: '**Reached from BO-324, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-333
  name: Order Financial Analytics & AI Reconciliation Intelligence
  module: Orders & Money
  requiresModule: ticketing
  purpose: Provide management-level analytics across order payment performance, balances, settlement and
    reconciliation.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first order entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listOrderFinancialReconciliation
    contract: orders
    purpose: Order Financial Analytics & AI Reconciliation Intelligence
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /orders-money/order-financial-analytics-ai-reconciliation-intelligence-bo-333
    component: apps/venue-management-web/src/routes/orders-money/OrderFinancialAnalyticsAiReconciliationIntellige.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-333
    workshopBoard: wireframes/WS25 Order   Reservation Management Board 3.dc.html#bo-333
  source:
    pack: Order___Reservation_Management_Reference.pdf
    board: '3'
    number: 12.3.10
    page: 53
  navigation:
    entryFrom:
    - BO-324
    exitTo:
    - BO-324
    inferred: false
    notes: '**Reached from BO-324, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-334
  name: Virtual Ticket Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Provide administrators and operations teams with a centralized view of all Virtual Tickets
    and their associated media across TICVAI. This is the primary administrative entry point into the
    Virtual Ticket architecture.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Total Virtual Tickets
      - kind: metricTile
        notes: Active
      - kind: metricTile
        notes: Pending Activation
      - kind: metricTile
        notes: Suspended
      - kind: metricTile
        notes: Used / Consumed
      - kind: metricTile
        notes: Partially Consumed
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first virtual entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listVirtualTicket
    contract: access
    purpose: Virtual Ticket Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/virtual-ticket-command-center-bo-334
    component: apps/venue-management-web/src/routes/access-venue/VirtualTicketCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-334
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-334
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.1
    page: 4
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-335
    - BO-336
    - BO-337
    - BO-338
    - BO-339
    - BO-340
    - BO-341
    - BO-342
    - BO-343
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-335
  name: Virtual Ticket Identity & Master Record Configuration
  module: Access & Venue
  requiresModule: access
  purpose: Define the authoritative Virtual Ticket object used throughout TICVAI. This screen is extremely
    important because the Virtual Ticket—not the QR/RFID/card— is the master ticket record.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first virtual entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setVirtualTicketIdentity
    contract: access
    purpose: Virtual Ticket Identity & Master Record Configuration
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/virtual-ticket-identity-master-record-configuration-bo-335
    component: apps/venue-management-web/src/routes/access-venue/VirtualTicketIdentityMasterRecordConfiguration.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-335
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-335
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.2
    page: 6
  navigation:
    entryFrom:
    - BO-334
    exitTo:
    - BO-334
    inferred: false
    notes: '**Reached from BO-334, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-336
  name: Virtual Ticket Status & Lifecycle Model
  module: Access & Venue
  requiresModule: access
  purpose: Configure the standardized lifecycle of a Virtual Ticket independently from the lifecycle of
    individual media.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first virtual entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listVirtualTicketStatus
    contract: access
    purpose: Virtual Ticket Status & Lifecycle Model
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/virtual-ticket-status-lifecycle-model-bo-336
    component: apps/venue-management-web/src/routes/access-venue/VirtualTicketStatusLifecycleModel.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-336
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-336
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.3
    page: 8
  navigation:
    entryFrom:
    - BO-334
    exitTo:
    - BO-334
    inferred: false
    notes: '**Reached from BO-334, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-337
  name: Media Type & Credential Technology Registry
  module: Access & Venue
  requiresModule: access
  purpose: Maintain the centralized catalogue of credential technologies supported by TICVAI. This makes
    the credential architecture extensible rather than hard-coded.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMediaTypeCredential
    contract: access
    purpose: Media Type & Credential Technology Registry
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/media-type-credential-technology-registry-bo-337
    component: apps/venue-management-web/src/routes/access-venue/MediaTypeCredentialTechnologyRegistry.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-337
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-337
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.4
    page: 9
  navigation:
    entryFrom:
    - BO-334
    exitTo:
    - BO-334
    inferred: false
    notes: '**Reached from BO-334, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-338
  name: Multi-Media Binding & Association Rules
  module: Access & Venue
  requiresModule: access
  purpose: Configure how one Virtual Ticket can be associated with multiple media simultaneously. This
    is one of the most important screens in Area 15.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first multi-media entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMultiMediaBinding
    contract: access
    purpose: Multi-Media Binding & Association Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/multi-media-binding-association-rules-bo-338
    component: apps/venue-management-web/src/routes/access-venue/MultiMediaBindingAssociationRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-338
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-338
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.5
    page: 11
  navigation:
    entryFrom:
    - BO-334
    exitTo:
    - BO-334
    inferred: false
    notes: '**Reached from BO-334, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-339
  name: Credential Identity, Token & Reference Mapping
  module: Access & Venue
  requiresModule: access
  purpose: Define how individual media identifiers resolve securely back to the authoritative Virtual
    Ticket.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialIdentityToken
    contract: access
    purpose: Credential Identity, Token & Reference Mapping
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-identity-token-reference-mapping-bo-339
    component: apps/venue-management-web/src/routes/access-venue/CredentialIdentityTokenReferenceMapping.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-339
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-339
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.6
    page: 12
  navigation:
    entryFrom:
    - BO-334
    exitTo:
    - BO-334
    inferred: false
    notes: '**Reached from BO-334, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-340
  name: Entitlement & Cross-Media Synchronization Rules
  module: Access & Venue
  requiresModule: access
  purpose: Ensure all media attached to a Virtual Ticket share the same authoritative ticket and entitlement
    state.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first entitlement entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listEntitlementCrossMedia
    contract: access
    purpose: Entitlement & Cross-Media Synchronization Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/entitlement-cross-media-synchronization-rules-bo-340
    component: apps/venue-management-web/src/routes/access-venue/EntitlementCrossMediaSynchronizationRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-340
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-340
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.7
    page: 14
  navigation:
    entryFrom:
    - BO-334
    exitTo:
    - BO-334
    inferred: false
    notes: '**Reached from BO-334, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-341
  name: Media Activation, Priority & Fallback Rules
  module: Access & Venue
  requiresModule: access
  purpose: Configure when each credential becomes active and how alternative media behave if the preferred
    credential cannot be used.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMediaActivationPriority
    contract: access
    purpose: Media Activation, Priority & Fallback Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/media-activation-priority-fallback-rules-bo-341
    component: apps/venue-management-web/src/routes/access-venue/MediaActivationPriorityFallbackRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-341
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-341
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.8
    page: 15
  navigation:
    entryFrom:
    - BO-334
    exitTo:
    - BO-334
    inferred: false
    notes: '**Reached from BO-334, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-342
  name: Media Replacement, Revocation & Rebinding Rules
  module: Access & Venue
  requiresModule: access
  purpose: Configure controlled handling of lost, stolen, damaged, compromised or replaced credential
    media.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMediaReplacementRevocation
    contract: access
    purpose: Media Replacement, Revocation & Rebinding Rules
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/media-replacement-revocation-rebinding-rules-bo-342
    component: apps/venue-management-web/src/routes/access-venue/MediaReplacementRevocationRebindingRules.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-342
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-342
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.9
    page: 16
  navigation:
    entryFrom:
    - BO-334
    exitTo:
    - BO-334
    inferred: false
    notes: '**Reached from BO-334, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-343
  name: Virtual Ticket Architecture Testing, Governance & Audit
  module: Access & Venue
  requiresModule: access
  purpose: Provide the final testing and governance environment for Virtual Ticket and multi-media configurations.
    17 | Pag e
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: timeline
        notes: What changed, when, and by whom
      - kind: searchField
        notes: Find a change
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first virtual entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listVirtualTicketArchitecture
    contract: access
    purpose: Virtual Ticket Architecture Testing, Governance & Audit
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/virtual-ticket-architecture-testing-governance-audit-bo-343
    component: apps/venue-management-web/src/routes/access-venue/VirtualTicketArchitectureTestingGovernanceAudit.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-343
    workshopBoard: wireframes/WS51 Ticket Media   Credential Management Board 1.dc.html#bo-343
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '1'
    number: 15.1.10
    page: 17
  navigation:
    entryFrom:
    - BO-334
    exitTo:
    - BO-334
    inferred: false
    notes: '**Reached from BO-334, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-344
  name: Media Design Studio Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Provide administrators with the central workspace for creating and managing all ticket and
    credential media templates. This should be the entry point for the entire no-code Media Design Studio.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Total Media Templates
      - kind: metricTile
        notes: Published
      - kind: metricTile
        notes: Draft
      - kind: metricTile
        notes: Pending Approval
      - kind: metricTile
        notes: Scheduled
      - kind: metricTile
        notes: Archived
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listMediaDesign
    contract: access
    purpose: Media Design Studio Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/media-design-studio-command-center-bo-344
    component: apps/venue-management-web/src/routes/access-venue/MediaDesignStudioCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-344
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-344
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.1
    page: 23
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-345
    - BO-346
    - BO-347
    - BO-348
    - BO-349
    - BO-350
    - BO-351
    - BO-352
    - BO-353
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-345
  name: Digital QR & Barcode Ticket Designer
  module: Access & Venue
  requiresModule: access
  purpose: Provide a visual no-code designer specifically for digital QR and barcode tickets.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first digital entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setDigitalBarcodeTicket
    contract: access
    purpose: Digital QR & Barcode Ticket Designer
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/digital-qr-barcode-ticket-designer-bo-345
    component: apps/venue-management-web/src/routes/access-venue/DigitalQrBarcodeTicketDesigner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-345
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-345
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.2
    page: 24
  navigation:
    entryFrom:
    - BO-344
    exitTo:
    - BO-344
    inferred: false
    notes: '**Reached from BO-344, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-346
  name: PDF, Printable & POS Ticket Designer
  module: Access & Venue
  requiresModule: access
  purpose: Design tickets intended for printing, PDF generation, POS, box office and other physical/document
    outputs.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first pdf, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setPdfPrintablePos
    contract: access
    purpose: PDF, Printable & POS Ticket Designer
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/pdf-printable-pos-ticket-designer-bo-346
    component: apps/venue-management-web/src/routes/access-venue/PdfPrintablePosTicketDesigner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-346
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-346
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.3
    page: 26
  navigation:
    entryFrom:
    - BO-344
    exitTo:
    - BO-344
    inferred: false
    notes: '**Reached from BO-344, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-347
  name: Apple Wallet Pass Designer
  module: Access & Venue
  requiresModule: access
  purpose: Provide an Apple Wallet-specific configuration experience for eligible TICVAI credentials.
    This should not simply be a PDF ticket rendered inside a wallet.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first apple entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setAppleWalletPass
    contract: access
    purpose: Apple Wallet Pass Designer
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/apple-wallet-pass-designer-bo-347
    component: apps/venue-management-web/src/routes/access-venue/AppleWalletPassDesigner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-347
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-347
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.4
    page: 27
  navigation:
    entryFrom:
    - BO-344
    exitTo:
    - BO-344
    inferred: false
    notes: '**Reached from BO-344, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-348
  name: Google Wallet Pass Designer
  module: Access & Venue
  requiresModule: access
  purpose: Provide a dedicated Google Wallet configuration environment.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first google entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setGoogleWalletPass
    contract: access
    purpose: Google Wallet Pass Designer
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/google-wallet-pass-designer-bo-348
    component: apps/venue-management-web/src/routes/access-venue/GoogleWalletPassDesigner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-348
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-348
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.5
    page: 29
  navigation:
    entryFrom:
    - BO-344
    exitTo:
    - BO-344
    inferred: false
    notes: '**Reached from BO-344, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-349
  name: RFID, NFC, Card & Wristband Media Designer
  module: Access & Venue
  requiresModule: access
  purpose: Configure both the visual and technical profile of physical electronic credentials. This is
    important because RFID/NFC media are not merely artwork.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first rfid, entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setRfidNfcCard
    contract: access
    purpose: RFID, NFC, Card & Wristband Media Designer
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/rfid-nfc-card-wristband-media-designer-bo-349
    component: apps/venue-management-web/src/routes/access-venue/RfidNfcCardWristbandMediaDesigner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-349
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-349
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.6
    page: 30
  navigation:
    entryFrom:
    - BO-344
    exitTo:
    - BO-344
    inferred: false
    notes: '**Reached from BO-344, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-350
  name: Digital Card, Membership & Wearable Designer
  module: Access & Venue
  requiresModule: access
  purpose: Provide specialized configuration for persistent credentials that may represent longer-lived
    relationships rather than a single event ticket.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first digital entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setDigitalCardMembership
    contract: access
    purpose: Digital Card, Membership & Wearable Designer
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/digital-card-membership-wearable-designer-bo-350
    component: apps/venue-management-web/src/routes/access-venue/DigitalCardMembershipWearableDesigner.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-350
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-350
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.7
    page: 31
  navigation:
    entryFrom:
    - BO-344
    exitTo:
    - BO-344
    inferred: false
    notes: '**Reached from BO-344, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-351
  name: Dynamic Fields, Data Mapping & Content Builder
  module: Access & Venue
  requiresModule: access
  purpose: Create a centralized reusable field library so development team does not hard-code ticket fields
    separately into every media designer. This is another important architecture screen.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first dynamic entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setDynamicFieldData
    contract: access
    purpose: Dynamic Fields, Data Mapping & Content Builder
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/dynamic-fields-data-mapping-content-builder-bo-351
    component: apps/venue-management-web/src/routes/access-venue/DynamicFieldsDataMappingContentBuilder.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-351
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-351
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.8
    page: 32
  navigation:
    entryFrom:
    - BO-344
    exitTo:
    - BO-344
    inferred: false
    notes: '**Reached from BO-344, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-352
  name: Branding, Localization & Template Inheritance
  module: Access & Venue
  requiresModule: access
  purpose: Allow TICVAI's multi-tenant clients to control branding and localization without rebuilding
    every ticket template.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first branding, entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listBrandingLocalizationTemplate
    contract: access
    purpose: Branding, Localization & Template Inheritance
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/branding-localization-template-inheritance-bo-352
    component: apps/venue-management-web/src/routes/access-venue/BrandingLocalizationTemplateInheritance.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-352
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-352
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.9
    page: 34
  navigation:
    entryFrom:
    - BO-344
    exitTo:
    - BO-344
    inferred: false
    notes: '**Reached from BO-344, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-353
  name: Multi-Media Preview, Testing, Approval & Publication
  module: Access & Venue
  requiresModule: access
  purpose: Provide the final quality and governance gate before any media template becomes operational.
    This should be a particularly visual screen.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first multi-media entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: approveMultiMediaPreview
    contract: access
    purpose: Multi-Media Preview, Testing, Approval & Publication
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/multi-media-preview-testing-approval-publication-bo-353
    component: apps/venue-management-web/src/routes/access-venue/MultiMediaPreviewTestingApprovalPublication.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-353
    workshopBoard: wireframes/WS52 Ticket Media   Credential Management Board 2.dc.html#bo-353
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '2'
    number: 15.2.10
    page: 36
  navigation:
    entryFrom:
    - BO-344
    exitTo:
    - BO-344
    inferred: false
    notes: '**Reached from BO-344, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-354
  name: Credential Operations Command Center
  module: Access & Venue
  requiresModule: access
  purpose: Provide Operations, Ticketing, Customer Service and Technical teams with a real-time command
    center covering all issued credential media. This is the operational starting point for Area 15.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Virtual Tickets Issued
      - kind: metricTile
        notes: Credentials Generated
      - kind: metricTile
        notes: Active Credentials
      - kind: metricTile
        notes: Pending Generation
      - kind: metricTile
        notes: Pending Delivery
      - kind: metricTile
        notes: Pending Binding
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredential
    contract: access
    purpose: Credential Operations Command Center
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-operations-command-center-bo-354
    component: apps/venue-management-web/src/routes/access-venue/CredentialOperationsCommandCenter.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-354
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-354
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.1
    page: 41
  navigation:
    entryFrom:
    - BO-100
    exitTo:
    - BO-100
    - BO-355
    - BO-356
    - BO-357
    - BO-358
    - BO-359
    - BO-360
    - BO-361
    - BO-362
    - BO-363
    inferred: false
    notes: '**The board''s hub.** The workshop specified this module as boards of ten and opened each
      with a command centre; the other nine screens are that board''s detail, so they are reached from
      here and return here.'
- id: BO-355
  name: Virtual Ticket & Credential 360° Workspace
  module: Access & Venue
  requiresModule: access
  purpose: Provide a complete operational view of one Virtual Ticket and every media credential currently
    or historically associated with it. This is one of the most important operational screens.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: The configuration being edited
      - kind: selectField
        notes: Scope this applies at
      - kind: toggle
        notes: Active
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first virtual entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setVirtualTicketCredential
    contract: access
    purpose: Virtual Ticket & Credential 360° Workspace
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/virtual-ticket-credential-360-workspace-bo-355
    component: apps/venue-management-web/src/routes/access-venue/VirtualTicketCredential360Workspace.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-355
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-355
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.2
    page: 43
  navigation:
    entryFrom:
    - BO-354
    exitTo:
    - BO-354
    inferred: false
    notes: '**Reached from BO-354, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-356
  name: Credential Generation & Issuance Monitor
  module: Access & Venue
  requiresModule: access
  purpose: Manage and monitor generation of credential instances from approved Board 2 media templates.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Request ID
      - kind: metricTile
        notes: Virtual Ticket
      - kind: metricTile
        notes: Media
      - kind: metricTile
        notes: Template
      - kind: metricTile
        notes: Template Version
      - kind: metricTile
        notes: Product
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialGenerationIssuance
    contract: access
    purpose: Credential Generation & Issuance Monitor
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-generation-issuance-monitor-bo-356
    component: apps/venue-management-web/src/routes/access-venue/CredentialGenerationIssuanceMonitor.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-356
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-356
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.3
    page: 45
  navigation:
    entryFrom:
    - BO-354
    exitTo:
    - BO-354
    inferred: false
    notes: '**Reached from BO-354, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-357
  name: Credential Delivery & Distribution Operations
  module: Access & Venue
  requiresModule: access
  purpose: Manage how generated ticket media are delivered or made available to customers, participants
    and operational staff.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialDeliveryDistribution
    contract: access
    purpose: Credential Delivery & Distribution Operations
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-delivery-distribution-operations-bo-357
    component: apps/venue-management-web/src/routes/access-venue/CredentialDeliveryDistributionOperations.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-357
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-357
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.4
    page: 47
  navigation:
    entryFrom:
    - BO-354
    exitTo:
    - BO-354
    inferred: false
    notes: '**Reached from BO-354, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-358
  name: Media Binding, Activation & Assignment Operations
  module: Access & Venue
  requiresModule: access
  purpose: Manage credentials that require operational assignment or activation after ticket issuance.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first media entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: setMediaBindingActivation
    contract: access
    purpose: Media Binding, Activation & Assignment Operations
    trigger: onAction
  implementation:
    app: venue-management-web
    route: /access-venue/media-binding-activation-assignment-operations-bo-358
    component: apps/venue-management-web/src/routes/access-venue/MediaBindingActivationAssignmentOperations.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-358
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-358
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.5
    page: 48
  navigation:
    entryFrom:
    - BO-354
    exitTo:
    - BO-354
    inferred: false
    notes: '**Reached from BO-354, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-359
  name: Credential Replacement, Reissue, Revocation & Recovery
  module: Access & Venue
  requiresModule: access
  purpose: Manage operational credential changes while preserving the underlying Virtual Ticket.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialReplacementReissue
    contract: access
    purpose: Credential Replacement, Reissue, Revocation & Recovery
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-replacement-reissue-revocation-recovery-bo-359
    component: apps/venue-management-web/src/routes/access-venue/CredentialReplacementReissueRevocationRecovery.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-359
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-359
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.6
    page: 50
  navigation:
    entryFrom:
    - BO-354
    exitTo:
    - BO-354
    inferred: false
    notes: '**Reached from BO-354, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-360
  name: Failed Generation, Delivery & Credential Exception Management
  module: Access & Venue
  requiresModule: access
  purpose: Provide one dedicated operational queue for credential-related failures.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first failed entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listFailedGenerationDelivery
    contract: access
    purpose: Failed Generation, Delivery & Credential Exception Management
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/failed-generation-delivery-credential-exception-manageme-bo-360
    component: apps/venue-management-web/src/routes/access-venue/FailedGenerationDeliveryCredentialExceptionManag.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-360
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-360
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.7
    page: 51
  navigation:
    entryFrom:
    - BO-354
    exitTo:
    - BO-354
    inferred: false
    notes: '**Reached from BO-354, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-361
  name: Credential Usage & Cross-Media Traceability
  module: Access & Venue
  requiresModule: access
  purpose: Provide end-to-end visibility into how the different media attached to one Virtual Ticket have
    been presented or used. This screen is for credential traceability, while Area 16 remains responsible
    for the actual access-control decision.
  wave: 3
  density: compact
  layout:
    template: split
    regions:
    - name: contentBody
      components:
      - kind: searchField
        notes: Find a record
      - kind: dataTable
        notes: Every record with its status
      - kind: detailPanel
        notes: The selected record
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first credential entry, and
      this state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialUsageCross
    contract: access
    purpose: Credential Usage & Cross-Media Traceability
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-usage-cross-media-traceability-bo-361
    component: apps/venue-management-web/src/routes/access-venue/CredentialUsageCrossMediaTraceability.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-361
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-361
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.8
    page: 53
  navigation:
    entryFrom:
    - BO-354
    exitTo:
    - BO-354
    inferred: false
    notes: '**Reached from BO-354, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-362
  name: Credential Security, Audit & Operational Evidence
  module: Access & Venue
  requiresModule: access
  purpose: Maintain complete evidence of credential creation and lifecycle activity.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: timeline
        notes: What changed, when, and by whom
      - kind: searchField
        notes: Find a change
  states:
    loading: Skeleton while the configuration loads
    emptyNoEvents: '**The record is complete and nothing has happened yet.** No action: an
      audit trail that offers to create its first entry is offering to author evidence'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listCredentialSecurityOperational
    contract: access
    purpose: Credential Security, Audit & Operational Evidence
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/credential-security-audit-operational-evidence-bo-362
    component: apps/venue-management-web/src/routes/access-venue/CredentialSecurityAuditOperationalEvidence.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-362
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-362
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.9
    page: 54
  navigation:
    entryFrom:
    - BO-354
    exitTo:
    - BO-354
    inferred: false
    notes: '**Reached from BO-354, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
- id: BO-363
  name: Ticket Media Analytics & AI Operations Intelligence
  module: Access & Venue
  requiresModule: access
  purpose: Provide management and operations with analytics and AI intelligence across Virtual Tickets
    and credential media. This should be a serious operational intelligence layer—not simply a chatbot.
  wave: 3
  density: compact
  layout:
    template: dashboard
    regions:
    - name: contentBody
      components:
      - kind: metricTile
        notes: Headline figure
      - kind: dataTable
        notes: The records behind the figures
  states:
    loading: Skeleton while the configuration loads
    emptyFirstRun: '**Nothing configured yet.** The action is to create the first ticket entry, and this
      state carries it'
    emptyNoResults: '**Nothing matched.** The filter or the scope narrowed it — naming which is what stops
      somebody concluding the record does not exist'
    emptyNoAccess: '**Says which permission is missing, and never renders as an empty list.** An empty
      table reads as *there is no data* and sends somebody to support with the wrong question'
    error: Configuration service unavailable. Says what is unaffected
  apis:
  - operationId: listTicketMedia
    contract: access
    purpose: Ticket Media Analytics & AI Operations Intelligence
    trigger: onLoad
  implementation:
    app: venue-management-web
    route: /access-venue/ticket-media-analytics-ai-operations-intelligence-bo-363
    component: apps/venue-management-web/src/routes/access-venue/TicketMediaAnalyticsAiOperationsIntelligence.tsx
    status: notStarted
  wireframe:
    status: notStarted
    provenance: generated
    board: wireframes/P08 Venue Management.dc.html#bo-363
    workshopBoard: wireframes/WS53 Ticket Media   Credential Management Board 3.dc.html#bo-363
  source:
    pack: Ticket_Media___Credential_Management_Reference.pdf
    board: '3'
    number: 15.3.10
    page: 56
  navigation:
    entryFrom:
    - BO-354
    exitTo:
    - BO-354
    inferred: false
    notes: '**Reached from BO-354, the hub of its workshop board.** Stated on 4 September: the pack groups
      its screens ten to a board behind a command centre, and that grouping is the navigation.'
