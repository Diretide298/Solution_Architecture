[Unit]
Description=TICVAI viewer — snapshot the accounts and verdicts, daily

[Timer]
OnCalendar=daily
# A server that was off at the appointed hour still takes the snapshot when it
# comes back, rather than skipping a day in silence.
Persistent=true
RandomizedDelaySec=15m

[Install]
WantedBy=timers.target
