# P07 — Staff Scanner — Access Control
#
# Derived from the wireframe board on 14 August. **Purposes and navigation are real;
# components, states and operations are not yet written.** Every screen carries
# wireframe.status drafted so the gap is visible rather than implied.

platform:
  code: P07
  audience: staff
  formFactor: handheld
  shortName: Venue Scanner
  name: Venue Scanner — Access Control
  surface: staffFacing
  runtime: reactNative
  app: venue-scanner
  appStatus: scaffolded
  offlineCapable: true
  packages:
  - design-tokens
  - ui
  - api-client
  - offline-core
  deployment:
    target: rugged handheld — Android
    distribution: MDM
    bundleUpdate: at shift start
    hosting: local, offline-first (ADR-0013)
    storeReview: false
    deviceOwnership: venue-owned, issued per shift
    networkAssumption: must function with none
    releaseCadence: monthly, never on an event day
  screenCount: 11
  wireframeBoard: wireframes/P07 Staff Scanner.dc.html
  designReferences:
  - path: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
    note: Employee app reference. The scanner shares its visual language and its offline strip.
  operator: venue
  directions:
  - ltr
  - rtl
  densityNote: '**A gate device held in one hand while the other takes a ticket.** `compact` was assigned
    by platform code on 18 August and it was wrong — a scanner is not a desktop.'
screens:
- id: SCN-001
  name: Sign in
  module: Access
  purpose: PIN or badge. Session carries the workstation.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Session check against the local store, then the server if reachable
    emptyFirstRun: Not applicable
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Sign-in failed. The reason is shown — wrong PIN reads differently from no network
    offline: '**Signs in against the cached principal list from the last bundle.** A steward locked
      out at 08:00 because the venue wifi is down is a gate that does not open'
  navigation:
    exitTo:
    - SCN-002
    - SCN-003
    inferred: true
    fromFlows: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-001
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written. **createCashMovement removed 18 August** — attached by module resemblance,
    not by what this screen does. A screen that does not handle money should not be able to move it
    (CF-87's class).
  implementation:
    app: venue-scanner
    route: /access/sign-in
    component: apps/venue-scanner/src/routes/access/SignInDetail.tsx
    status: notStarted
  apis:
  - operationId: login
    contract: identity
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: getCurrentShift
    contract: shift
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: acceptShiftVariance
    contract: shift
    purpose: Accept an over/short beyond the threshold
    trigger: onAction
  - operationId: approveShiftOpen
    contract: shift
    purpose: Approve a shift opening outside tolerance
    trigger: onAction
  - operationId: closeShift
    contract: shift
    purpose: Blind close-out
    trigger: onAction
  - operationId: forceLogout
    contract: identity
    purpose: Supervisor termination of an abandoned session
    trigger: onAction
  - operationId: getCurrentSession
    contract: identity
    purpose: Current session and effective permissions
    trigger: onLoad
  - operationId: getGuestSession
    contract: identity
    purpose: Read the current guest session
    trigger: onLoad
  - operationId: getShift
    contract: shift
    purpose: Read a shift
    trigger: onLoad
  - operationId: listActiveSessions
    contract: identity
    purpose: List active sessions
    trigger: onLoad
  - operationId: listCashMovements
    contract: shift
    purpose: Lifts, adds and the opening float
    trigger: onLoad
  - operationId: listMfaMethods
    contract: identity
    purpose: Enrolled MFA methods
    trigger: onLoad
  - operationId: listShifts
    contract: shift
    purpose: List shifts
    trigger: onLoad
  - operationId: listSsoProviders
    contract: identity
    purpose: Identity providers configured for this tenant
    trigger: onLoad
  - operationId: openShift
    contract: shift
    purpose: Open a shift
    trigger: onAction
  - operationId: recordNoSale
    contract: shift
    purpose: Open the drawer without a sale
    trigger: onAction
  - operationId: reopenShift
    contract: shift
    purpose: Reopen a shift closed in error
    trigger: onAction
  - operationId: resumeShift
    contract: shift
    purpose: Resume a suspended shift
    trigger: onAction
  - operationId: revokeAllSessions
    contract: identity
    purpose: Revoke every session in scope
    trigger: onAction
  - operationId: suspendShift
    contract: shift
    purpose: Suspend a shift so another user can log in
    trigger: onAction
  density: comfortable
- id: SCN-002
  name: Access point & direction
  module: Access
  purpose: Confirm what this device is doing before it does it.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Access points from the bundle
    emptyFirstRun: No access points configured for this venue — a configuration error, shown as one
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Cannot load. The last selection is offered
    offline: Fully offline. The access point list is in the bundle
  navigation:
    exitTo:
    - SCN-001
    - SCN-003
    - SCN-015
    inferred: true
    fromFlows: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-002
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written.
  implementation:
    app: venue-scanner
    route: /access/access-point-direction
    component: apps/venue-scanner/src/routes/access/AccessPointDirectionDetail.tsx
    status: notStarted
  apis:
  - operationId: listAccessPoints
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: setTurnstileMode
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: createAccessPoint
    contract: access
    purpose: Create an access point
    trigger: onAction
  - operationId: getAccessPoint
    contract: access
    purpose: Read an access point
    trigger: onLoad
  - operationId: setAccessPointGeofence
    contract: access
    purpose: Set a geofence for handheld validation
    trigger: onAction
  - operationId: updateAccessPoint
    contract: access
    purpose: Update an access point
    trigger: onAction
  density: comfortable
- id: SCN-003
  name: Ready to scan
  module: Access
  purpose: The screen the device sits on all day.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Instant. This screen must never wait
    emptyFirstRun: Not applicable
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Camera or scanner hardware failed — distinct from a bad scan, and it needs a different response
    offline: '**Same loop, different truth. Amber says so.** Was a separate screen until 18 August,
      and the screen already had an `offline` state — two places describing one condition is two places
      to disagree.'
    outcomeAdmitted: '**Admitted.** Readable at arm''s length and gone in 1.5 seconds. Was a screen
      at `/access/admitted` until 18 August — **a route you navigate to is not a thing that disappears
      in 1.5 seconds**, and a scanner that pushes a page per scan is a scanner that cannot do 40 guests
      a minute.'
    outcomeDenied: '**Denied, with a reason and a next action — never a boolean.** The reason is what
      the operator reads aloud, and it decides whether the next step is an override, a lookup or an
      apology.'
    outcomeBlacklisted: '**Blacklisted — denies outright regardless of entitlement.** Separated from
      `outcomeDenied` because the operator response differs: a denied guest may be helped, a blacklisted
      one is a security matter and the screen must not suggest an override.'
    overrideDialog: '**Override — a confirm dialog on `outcomeDenied`, not a screen.** The operation
      most likely to be examined after an incident, so it captures a reason and a principal. **A modal,
      because an override is a decision taken while looking at the denial**, not a page you navigate
      to afterwards.'
  navigation:
    exitTo:
    - SCN-001
    - SCN-002
    - SCN-003
    inferred: true
    fromFlows: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-003
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
    stateBoards:
    - state: outcomeAdmitted
      board: wireframes/P07 Staff Scanner.dc.html#scn-004
    - state: outcomeDenied
      board: wireframes/P07 Staff Scanner.dc.html#scn-005
    - state: overrideDialog
      board: wireframes/P07 Staff Scanner.dc.html#scn-006
    - state: outcomeBlacklisted
      board: wireframes/P07 Staff Scanner.dc.html#scn-010
    - state: offline
      board: wireframes/P07 Staff Scanner.dc.html#scn-012
    note: '**The absorbed outcomes are still drawn** and the drawings are still right — an admitted
      flash and a denial with a reason are different visuals. **What changed is that they are states
      of one screen rather than five routes**, so each board anchor is listed against the state it draws
      rather than deleted.'
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written. **Absorbed SCN-004, SCN-005, SCN-006, SCN-010, SCN-012 on 18 August.** A scanner
    is one screen the device sits on all day, and an outcome is a state of it — **routing to `/access/admitted`
    for something gone in 1.5 seconds is a page load per guest**, and at a gate doing 40 a minute that
    is the whole problem. Every absorbed screen kept its copy as a named state.
  implementation:
    app: venue-scanner
    route: /access/ready-to-scan
    component: apps/venue-scanner/src/routes/access/ReadyToScanDetail.tsx
    status: notStarted
  apis:
  - operationId: listScans
    contract: access
    purpose: List scan events
    trigger: onLoad
  - operationId: syncScans
    contract: access
    purpose: Replay scans recorded offline
    trigger: onAction
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: lookupTicket
    contract: access
    purpose: Read-only validity check without admitting
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  - operationId: validateGroupAccess
    contract: access
    purpose: Admit a group on one read
    trigger: onAction
  - operationId: consumeRedemptionRight
    contract: cross-cell
    purpose: Consume entries against a right, locally authoritative
    trigger: onAction
  - operationId: listBlacklist
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: addBlacklistEntry
    contract: access
    purpose: Blacklist a media code
    trigger: onAction
  - operationId: removeBlacklistEntry
    contract: access
    purpose: Remove a blacklist entry
    trigger: onAction
  density: comfortable
- id: SCN-007
  name: Group admission
  module: Access
  purpose: Partial admission is the normal case, not the error.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Party resolution from the bundle
    emptyFirstRun: Party of one — falls through to a normal scan
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Group could not be resolved
    offline: Fully offline. Admits what is valid and states the shortfall
  navigation:
    exitTo:
    - SCN-001
    - SCN-002
    - SCN-003
    inferred: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-007
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written.
  implementation:
    app: venue-scanner
    route: /access/group-admission
    component: apps/venue-scanner/src/routes/access/GroupAdmissionDetail.tsx
    status: notStarted
  apis:
  - operationId: validateGroupAccess
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: listScans
    contract: access
    purpose: List scan events
    trigger: onLoad
  - operationId: lookupTicket
    contract: access
    purpose: Read-only validity check without admitting
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: syncScans
    contract: access
    purpose: Replay scans recorded offline
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  density: comfortable
- id: SCN-008
  name: Manual entry
  module: Access
  purpose: Damaged media, dead phone battery, printed slip.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Reference lookup, local first
    emptyFirstRun: No match for that reference
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Not found. Offers SCN-009 by name instead
    offline: Searches the bundle only. A reference issued after the last sync will not be found, and
      the screen says so rather than denying
  navigation:
    exitTo:
    - SCN-001
    - SCN-002
    - SCN-003
    inferred: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-008
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written.
  implementation:
    app: venue-scanner
    route: /access/manual-entry
    component: apps/venue-scanner/src/routes/access/ManualEntryDetail.tsx
    status: notStarted
  apis:
  - operationId: lookupTicket
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: listScans
    contract: access
    purpose: List scan events
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: syncScans
    contract: access
    purpose: Replay scans recorded offline
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  - operationId: validateGroupAccess
    contract: access
    purpose: Admit a group on one read
    trigger: onAction
  density: comfortable
- id: SCN-009
  name: Ticket lookup
  module: Access
  purpose: Reads. Does not admit, does not decrement.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Name search across the bundle
    emptyFirstRun: No match
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Search unavailable
    offline: Bundle only, and the bundle age is shown beside the results
  navigation:
    exitTo:
    - SCN-001
    - SCN-002
    - SCN-003
    inferred: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-009
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written.
  implementation:
    app: venue-scanner
    route: /access/ticket-lookup
    component: apps/venue-scanner/src/routes/access/TicketLookupDetail.tsx
    status: notStarted
  apis:
  - operationId: lookupTicket
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: listScans
    contract: access
    purpose: List scan events
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: syncScans
    contract: access
    purpose: Replay scans recorded offline
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  - operationId: validateGroupAccess
    contract: access
    purpose: Admit a group on one read
    trigger: onAction
  density: comfortable
- id: SCN-011
  name: Delegated right
  module: Access
  purpose: A ticket issued in another cell, redeemed here.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Right resolution, which may cross cells
    emptyFirstRun: No delegated right on this media
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Cross-cell lookup failed
    offline: '**Not available offline.** A delegated right issued in another region cannot be verified
      from a local bundle, and admitting on trust is how a pass gets used twice in two countries'
  navigation:
    exitTo:
    - SCN-001
    - SCN-002
    - SCN-003
    inferred: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-011
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written.
  implementation:
    app: venue-scanner
    route: /access/delegated-right
    component: apps/venue-scanner/src/routes/access/DelegatedRightDetail.tsx
    status: notStarted
  apis:
  - operationId: getRedemptionRight
    contract: cross-cell
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: consumeRedemptionRight
    contract: cross-cell
    purpose: Consume entries against a right, locally authoritative
    trigger: onAction
  density: comfortable
- id: SCN-013
  name: Offline journal
  module: Access
  purpose: What the device decided, before anyone confirmed it.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Journal loads locally
    emptyFirstRun: Nothing journalled
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Journal unreadable — the most serious failure on the device
    offline: The journal is the offline record. It is why an offline admit is recoverable
  navigation:
    exitTo:
    - SCN-001
    - SCN-002
    - SCN-003
    inferred: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-013
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written.
  implementation:
    app: venue-scanner
    route: /access/offline-journal
    component: apps/venue-scanner/src/routes/access/OfflineJournalDetail.tsx
    status: notStarted
  apis:
  - operationId: listScans
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: lookupTicket
    contract: access
    purpose: Read-only validity check without admitting
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: syncScans
    contract: access
    purpose: Replay scans recorded offline
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  - operationId: validateGroupAccess
    contract: access
    purpose: Admit a group on one read
    trigger: onAction
  density: comfortable
- id: SCN-014
  name: Sync & reconciliation
  module: Access
  purpose: The screen nobody designs and everybody needs.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Sync in progress, with a count
    emptyFirstRun: Nothing to sync
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Sync failed. **The journal is kept** — a failed sync must never discard what it was carrying
    offline: Not applicable. This screen exists to end the offline period
  navigation:
    exitTo:
    - ADM-003
    - SCN-001
    - SCN-002
    - SCN-003
    inferred: true
    fromFlows: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-014
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written.
  implementation:
    app: venue-scanner
    route: /access/sync-reconciliation
    component: apps/venue-scanner/src/routes/access/SyncReconciliationDetail.tsx
    status: notStarted
  apis:
  - operationId: syncScans
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: listSyncRejections
    contract: orders
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: getOfflinePackage
    contract: access
    purpose: Entitlement and rule set for offline validation
    trigger: onLoad
  - operationId: listScans
    contract: access
    purpose: List scan events
    trigger: onLoad
  - operationId: lookupTicket
    contract: access
    purpose: Read-only validity check without admitting
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: syncOrders
    contract: orders
    purpose: Replay orders recorded offline
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  - operationId: validateGroupAccess
    contract: access
    purpose: Admit a group on one read
    trigger: onAction
  density: comfortable
- id: SCN-015
  name: Offline package
  module: Access
  purpose: Rule changes land here, not instantly.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Downloading. Size and progress shown
    emptyFirstRun: Not applicable
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Download failed. The previous bundle stays in place
    offline: Cannot refresh. The existing bundle continues to be used and its age is shown
  navigation:
    exitTo:
    - SCN-001
    - SCN-002
    - SCN-003
    inferred: true
    fromFlows: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-015
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written.
  implementation:
    app: venue-scanner
    route: /access/offline-package
    component: apps/venue-scanner/src/routes/access/OfflinePackageDetail.tsx
    status: notStarted
  apis:
  - operationId: getOfflinePackage
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: listScans
    contract: access
    purpose: List scan events
    trigger: onLoad
  - operationId: lookupTicket
    contract: access
    purpose: Read-only validity check without admitting
    trigger: onLoad
  - operationId: overrideAccess
    contract: access
    purpose: Admit against a failed validation
    trigger: onAction
  - operationId: syncScans
    contract: access
    purpose: Replay scans recorded offline
    trigger: onAction
  - operationId: validateAccess
    contract: access
    purpose: Validate media at an access point and admit or deny
    trigger: onAction
  - operationId: validateGroupAccess
    contract: access
    purpose: Admit a group on one read
    trigger: onAction
  density: comfortable
- id: SCN-016
  name: Gate mode
  module: Access
  purpose: Podium operation. Changes what the gate does.
  capability: C00
  wave: 1
  layout:
    template: detail
    regions:
    - name: contentBody
      components:
      - kind: detailPanel
        notes: Structure from the wireframe board. Components not yet enumerated.
  states:
    loading: Current mode from the device
    emptyFirstRun: Not applicable
    emptyNoAccess: TODO — this person may not see it. Never rendered as empty.
    error: Turnstile unreachable — the mode is a request, not a confirmed hardware state
    offline: '**Requests only.** Hardware control is out of contract and the vendor SDK is outstanding
      (CF-33)'
  navigation:
    exitTo:
    - SCN-001
    - SCN-002
    - SCN-003
    inferred: true
  wireframe:
    status: drafted
    owner: wireframe board
    board: wireframes/P07 Staff Scanner.dc.html#scn-016
    designReference: sources/designs/TICVAI_Employee_App_UI_Reference_1.pdf
  notes: Definition derived from the wireframe board on 14 August. Components, states and operations
    still to be written.
  implementation:
    app: venue-scanner
    route: /access/gate-mode
    component: apps/venue-scanner/src/routes/access/GateModeDetail.tsx
    status: notStarted
  apis:
  - operationId: setTurnstileMode
    contract: access
    purpose: From the flow it appears in
    trigger: onLoad
  - operationId: createAccessPoint
    contract: access
    purpose: Create an access point
    trigger: onAction
  - operationId: getAccessPoint
    contract: access
    purpose: Read an access point
    trigger: onLoad
  - operationId: listAccessPoints
    contract: access
    purpose: List access points
    trigger: onLoad
  - operationId: setAccessPointGeofence
    contract: access
    purpose: Set a geofence for handheld validation
    trigger: onAction
  - operationId: updateAccessPoint
    contract: access
    purpose: Update an access point
    trigger: onAction
  density: comfortable
