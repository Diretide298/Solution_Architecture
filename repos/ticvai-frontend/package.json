{
  "name": "ticvai-frontend",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint",
    "typecheck": "nx run-many -t typecheck",
    "graph": "nx graph"
  },
  "devDependencies": {
    "@nx/js": "^20.0.0",
    "@nx/react": "^20.0.0",
    "@nx/react-native": "^20.0.0",
    "@nx/eslint": "^20.0.0",
    "@nx/vite": "^20.0.0",
    "nx": "^20.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
