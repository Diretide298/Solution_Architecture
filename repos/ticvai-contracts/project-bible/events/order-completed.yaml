# order.completed v1 — published by orders
# Checked by tools/check-states.py against states/ and the contracts.

name: order.completed
version: 1
publisher: orders
description: Everything an order promised has been delivered.
aggregate: order
emittedWhen: OrderStatus paid to completed
payload:
- field: orderId
  type: string
- field: venueId
  type: uuid
- field: completedAt
  type: timestamp
- field: entitlementIds
  type: array
  notes: What was issued
consumers:
- context: marketing
  purpose: Close the purchase journey and trigger post-visit campaigns
  idempotencyKey: orderId
  onFailure: retry
  isCritical: false
- context: reporting
  purpose: Fulfilment metrics
  idempotencyKey: orderId
  onFailure: retry
  isCritical: false
retention: 30 days after publication
notes: Distinct from order.paid. Paid is money; completed is delivery, and the gap between them is where
  fulfilment failures live.
